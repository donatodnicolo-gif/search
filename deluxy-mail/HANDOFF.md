# AI Mail 2.0 (deluxy-mail) — Handoff tecnico

> Documento di ripartenza. Aggiornato: **25 agosto 2026**.
> Leggi anche `CLAUDE.md` alla radice del repo e il design system in `deluxy-design-system/`.

---

## 1. Cos'è e stato del prodotto

Client di posta aziendale **AI-first** per Deluxy (consegne di fiori di lusso a Milano). Legge la posta via IMAP, la smista, la traduce, la riassume, crea attività e prepara bozze di risposta con l'AI (OpenAI).

**Stato: in PRODUZIONE.** Multi-utente, live e usato.

> 📨 **23/08/2026 — azione nuova «Commerciale · Registra il preventivo»** (LIVE, `dpl_A5885jR25nw…`). Da una mail in cui un fornitore manda un prezzo, scrive il preventivo dentro Deluxy Scout (Edge `preventivi`, chiave **Commerciale**, la stessa di «Apri trattativa»). `scrive: true`, quindi passa **sempre** dalla conferma.
> - ⭐ **Fornitore e indirizzo li mette il codice, non il modello** (`daMail` + `ctx.controparte`): chi manda il prezzo lo dice l'header della mail, il nome in calce può essere la persona, la sede o niente. L'importo accetta «1.250,50» e lo traduce prima di partire (il campo del modulo è testo).
> - Se Scout non riconosce il lavoro risponde 404/409 **con l'elenco dei lavori aperti**, e il messaggio d'errore li mostra: un «non trovato» secco costringerebbe a uscire e andare a guardare in Scout.
> - **Novità trasversale:** `ContestoAzione` ora porta anche **`messaggioId`** (valorizzato in `actions.ts` e `azioneSezione.ts`). Serve alle azioni che scrivono un dato in un'altra app e vogliono lasciarci il rimando alla mail: in Scout il preventivo mostra «Vedi la mail». Senza, un numero comparso là non ha storia.
> - ⚠️ **Pubblicata da una copia pulita del commit, non dalla cartella**: quella sera in `src/lib/actions.ts`, `allegatiGrandi.ts`, `bozze/page.tsx`, `schema.prisma` c'era lavoro a metà di un'altra sessione che **non compilava**, e `npx vercel --prod --cwd …` carica la cartella così com'è. Si fa `git archive HEAD:deluxy-mail | tar -x -C <tmp>`, ci si copia `.vercel`, e si deploya da lì.

- **URL produzione:** https://deluxy-mail.vercel.app
- **Hosting:** Vercel (team `deluxy`, progetto `deluxy-mail`).
- **DB (dal 19/08/2026): cluster condiviso `zegbztfxisqeowngvgvh`** (eu-central-1, org **Deluxy, piano Pro**, 8 GB, backup giornalieri), **schema `mail`** — lo stesso progetto delle altre 12 app Deluxy, ognuna nel suo schema. Commutazione fatta alle **07:36 del 19/08** e verificata **dai fatti, non dalle impostazioni**: il database vecchio si è fermato (ultima scrittura 07:25) e il nuovo ha ripreso a crescere. **Collaudo: 31 tabelle su 31, 31.134 righe controllate, ZERO rimaste indietro** (i messaggi confrontati sulla chiave naturale, vedi §9). `?schema=mail` va SEMPRE nelle stringhe: `DATABASE_URL` col pooler **6543** + `&pgbouncer=true`, `DIRECT_URL` col pooler **5432**. Region `fra1` in `vercel.json`, verificata (`X-Vercel-Id: fra1::fra1`).
- **DB di prima (28/07 → 19/08):** `feleldlsreurqpdhstla` («cs@deluxy.it's», eu-west-1, piano **Free**), dove AI Mail divideva il progetto con la **piattaforma consegne** (schema `public`) ed era arrivata a **566 MB contro un tetto di 500**: se fosse scattata la sola lettura si sarebbero fermate **entrambe le app**. È la ragione del trasloco. Resta **intatto come rete di sicurezza** insieme a `sxovckndpmdbqfrfkxhl` (Free, finito in sola lettura a 1,57 GB). ⚠️ È un **secondo abbonamento Supabase**, su un account diverso: spenti i due progetti, va valutato se chiuderlo. ⚠️ Il progetto è **fragile** (Free oltre il tetto): interrogandolo chiude la connessione a metà, quindi query strette e ritentativi.
- **Porta locale:** 3070.

### 26/08 — il lotto «riassunto che lavora» + lo storico ripiegato anche in HTML

Cinque richieste dell'utente in sequenza, stessa giornata. In ordine:

1. **Lo storico citato è ripiegato anche nella vista FORMATTATA** (`src/lib/citatoHtml.ts`
   + `CorpoMessaggio`). `dividiCitato` copriva solo la vista testo, ma la predefinita è
   l'iframe: il decimo messaggio arrivava coi nove precedenti srotolati. Ora la pagina
   lavora sul DOM dentro l'iframe (stessa strada dei tasti rapidi: `allow-same-origin`,
   niente script nella mail): trova l'inizio della citazione (contenitori noti +
   marcatori «ha scritto:»/«wrote:»/«Da:»), nasconde da lì in giù, tasto «··· mostra i
   messaggi precedenti». Stesse guardie del testo: inoltro puro o citazione corta → non
   si tocca. Misurato in anteprima: 646px → **175px** da chiusa, riapribile.
   ⚠️ **`onLoad` su un iframe `srcDoc` non è affidabile** (può scattare prima che React
   agganci il gestore — qui non scattava affatto): il ripiego gira anche da un
   `useEffect` idempotente con le stesse dipendenze dell'ascolto tasti.
   ⚠️ Dopo il ripiego va chiamata `misura()`, o l'iframe resta alto quanto la mail intera.
2. **L'oggetto non fa la scala**: «Re: R: R: R: R: R: R: R: …» → «Re: …»
   (`oggettoLeggibile` in `citato.ts`, usato nell'h1 della mail aperta, originale nel
   `title`). Solo con ALMENO due prefissi: un «R: sigla» singolo può essere un oggetto
   vero e non si tocca.
3. **Le cifre esplicite nel riassunto**: campo strutturato `cifre` nello
   `SCHEMA_THREAD` (voce/valore/msgIdx), blocco «Cifre e prezzi» nella scheda con
   «→ apri» sulla mail-fonte. Valore copiato ESATTO come scritto (mai dedotto), l'ultimo
   se è cambiato (con la nota «prima 17 €»). ⚠️ Le cifre entrano anche in
   `riassuntoInTesto`: l'aggiornamento incrementale riparte da quel testo, e un prezzo
   assente lì sparirebbe dal riassunto aggiornato. Nato da: il riassunto Profondo diceva
   «ha fornito dettagli sul budget» senza UN numero.
4. **Il riassunto propone le azioni** («Si può fare da qui»): campo `azioni` nello
   schema — enum costruito dal catalogo vero (`azioniDalRiassunto()` in `appDeluxy`, la
   regola di quando proporre sta sul campo `dalRiassunto` di ogni azione: una casa
   sola). Bottone → stesso dialogo `aimail:app`, ma su **msgId della mail che PORTA i
   dati** (il prezzo si estrae dalla mail del fornitore, non da quella aperta). Max 2,
   validate contro il catalogo anche al ritorno, «vuoto è la risposta giusta» nel prompt.
5. **L'estrazione legge anche il THREAD** (`proponiPerApp` → `estraiDatiAzione` con
   `conversazione`): il valore atteso usciva «non indicato» quando il prezzo stava due
   mail prima. Ultime 8 mail del thread, 1.200 caratteri l'una, regola invariata: un
   valore si prende SOLO se scritto, il più recente se ripetuto.
6. **La catena trattativa→contatto**: campo `dopo` sull'azione
   (`commerciale.trattativa` → `anagrafiche.partner`); a invio riuscito il dialogo
   propone «→ Registra anche chi ce lo chiede in Anagrafiche». Invito, non automatismo.
7. **«Nessun negozio corrispondente» non è più un vicolo cieco** quando i candidati sono
   ZERO: non è un nome scritto male, è una **prospect nuova** («Grazia Finoli» da
   virgilio.it). La Edge `trattativa` di Scout ora accetta `crea:'si'` +
   `contattoEmail`: crea il place a 0,0 (stesso gesto dell'auto-qualifica dei lead) col
   contatto in rubrica, e apre la trattativa; il 404 senza candidati torna
   `puoiCreare:true` e AI Mail mostra «＋ Crea "…" nel CRM e apri la trattativa»
   (riusa il meccanismo dei candidati: `campoScelta:'crea'`). MAI implicito: senza il
   bottone, un nome sbagliato deve dare 404, non un doppione.
   **Deploy Edge fatto** (`supabase functions deploy trattativa`, token da
   `deluxy-scout/.env` SUPABASE_PAT). ⚠️ Verificato solo che la funzione risponda (401
   su chiave finta): la forma nuova del 404 e la creazione vera restano da provare con
   la chiave vera, dalla schermata.

Verifica del lotto: `tsc` e `npm run build` puliti (esito letto direttamente); ripiego,
cifre, azioni e dispatch dell'evento provati nel browser su una pagina d'anteprima
temporanea, poi cancellata. NON provato a schermo: la catena `dopo` (serve un invio
riuscito con la sessione vera) e il bottone «Crea» end-to-end.

---
### 25/08 sera 2 — dal riassunto si può fare una domanda

Chiesto dall'utente: «partendo da un riassunto vorrei poter fare domande: esempio *Sai per
quando?*».

- **La funzione c'era già** (`ChiediConversazione`, 4/08): cerca la risposta **solo dentro
  il thread** e la dà con la citazione esatta e la mail da cui viene. Era però montata
  **in fondo alla mail aperta** — cioè lontanissima dal riassunto, che sta nella sezione
  della conversazione più sotto. Chi leggeva il quadro e si accorgeva che gli mancava un
  dato doveva scorrere via, o rileggersi le nove mail a mano.
- Ora è **anche dentro la scheda del riassunto**, sotto «In sospeso». ⚠️ **Anche**, non
  «spostata»: in fondo alla mail ci si arriva dopo aver letto il messaggio, qui dopo aver
  letto il quadro — due momenti diversi ([[feedback-non-togliere-azioni]]).
- **Tre domande pronte** da premere: «Sai per quando?», «Che prezzo hanno fatto?», «Cosa
  aspettano da me?». Non sono decorazione: davanti a un campo vuoto la domanda giusta non
  viene in mente, e una funzione che c'è ma non si usa vale come non averla.
- 🔴 **Un guasto trovato provando**: la chiamata al modello è protetta lato server, ma
  `uid()` **lancia** quando la sessione è scaduta — e un'eccezione dentro una transizione
  React non si vede come errore, **fa morire la pagina** («Application error: a
  server-side exception has occurred»). Chi lascia la posta aperta un'ora e preme una
  domanda perdeva la schermata. Ora c'è il `try/catch` e si legge «Sessione scaduta:
  rientra.» — verificato a schermo riproducendo il caso vero, non simulandolo.
  ⚠️ **Lo stesso schema vale per gli altri chiamanti delle server action**: nessuno di
  loro cattura, e `uid()` lancia sempre allo stesso modo. Qui è chiuso solo dove ho
  lavorato.

---
### 25/08 sera — i dati da confermare sono una TABELLA, non più il JSON

Chiesto dall'utente sulla schermata di «Commerciale — Apri trattativa», che mostrava
`{ "negozio": "Lemon and Pepper", "valoreAtteso": null, … }` in un riquadro di testo.

- **Il dialogo delle app Deluxy ora mostra una tabella**: etichetta a sinistra, valore
  modificabile a destra. Tipi di campo veri — **tendina** dove i valori sono chiusi
  (`brand` dei tre negozi, `stato` commerciale: scriverli a mano era il modo più facile
  per farsi rifiutare la richiesta), **calendario** sulle date, **elenco a virgole** per
  gli array (`interessi`). Il JSON resta a un clic («Modifica come JSON»), che serve per
  i dati annidati.
- ⚠️⚠️ **La tabella mostra TUTTO quello che c'è nel JSON, non solo i campi dichiarati.**
  Una tabella costruita sui soli `campi` noti lascerebbe partire in silenzio le chiavi non
  previste, e chi conferma crederebbe di aver guardato tutto. I dichiarati vengono per
  primi, il resto in fondo con l'etichetta ricavata dalla chiave. Le voci annidate (le
  righe di una proforma) si mostrano in una **tabellina in sola lettura** — prima erano
  una riga di JSON in mezzo alle altre.
- **Mancavano i `campi` a 4 azioni su 6** (`commerciale.trattativa`, `finance.proforma`,
  `finance.verifica`, `fornitori.trova`): è per questo che si vedeva il JSON. Aggiunti.
  Ora la tabella è comunque la vista predefinita anche per un'azione che non li dichiara.
- 🔴 **Un silenzio chiuso**: `commerciale.trattativa` faceva
  `typeof dati.valoreAtteso === 'number'`, ma il campo del modulo è **testo** — un
  importo battuto a mano veniva **lasciato cadere senza dire niente**, e la trattativa si
  apriva senza valore. Ora la conversione è in un punto solo (`numeroDaTesto`, condivisa
  con `commerciale.preventivo` che la aveva in copia) e, se non è un numero, lo dice.
- **Dove prende il «negozio»** (domanda dell'utente): non c'è nessun codice che lo forzi,
  a differenza del `brand` di «Trova fornitore» e del `fornitore` del preventivo. Lo
  ricava **l'AI dalla mail**. Su questo caso il nome «Lemon and Pepper» **non compare in
  nessuna mail dell'archivio** (verificato: 0 righe su 31.575): viene dal **dominio del
  mittente**, `giorgio.ioan@lemonandpepper.com`. Supposizione ragionevole, ma non è il
  nome con cui il negozio è registrato in Commerciale — da cui il 404. Il messaggio
  d'errore ora lo spiega e dice di correggere il campo e riprovare, invece di finire in
  un vicolo cieco.

---
### Dove siamo (25 agosto 2026)

**Fotografia verificata oggi, 25 agosto 2026 alle 12:30** (giornata: la **mail aperta
rifatta**, prima su telefono e poi in gerarchia sul desktop — vedi §9):

- **Codice**: niente in sospeso in `deluxy-mail/`, tutto pushato su `origin/scout-ui`
  (verificato **sul contenuto**, non sullo SHA). Ultimo commit `158094d7`, che tocca
  **solo il manuale**; l'ultimo che cambia l'app è `9c560ae3` (le quattro azioni + «⋯ Altro»).
  ⚠️ In questi giorni **altre sessioni** hanno lavorato sulla stessa cartella (preventivi
  fornitori registrati da AI Mail, rotte per Scout): `git log -- deluxy-mail` prima di dare
  per scontato cosa c'è.
- **Produzione allineata**: deploy `dpl_B3V1954YMMAVKdvHCF2baKwMQDgK` delle **09:20**,
  `Ready`, cioè esattamente `9c560ae3` — il commit successivo è di sola documentazione,
  quindi il codice online è quello di adesso.
- **Salute**: `/api/health` → `{"ok":true,"database":true,"scrivibile":true}` in **0,61 s**,
  `X-Vercel-Id: fra1::fra1` — funzioni a Francoforte, accanto al database.
- **Database**: cluster condiviso `zegbztfxisqeowngvgvh`, schema `mail` (dal 19/08).
  `migrate-prod` è a **102 statement** (`Attivita.note/noteAutore/noteIl`,
  `Bozza.allegatiGruppo`).
- 🔎 **Da guardare a schermo, sul telefono vero**: la barra fissa in basso, il foglio
  «⋯ Altro» e che «Delega Renè» si apra **sopra** la barra e non dentro.

**Ricontrollo delle 12:55 — questa volta guardando il DATABASE di produzione, non solo
l'app.** Fino a oggi la fotografia si fermava a `/api/health` e al deploy; leggere le
tabelle ha corretto **quattro** voci dei punti aperti (dettaglio sotto, punti 1-bis, 2, 4, 6).
Quello che si vede in produzione adesso:

| | |
|---|---|
| Caselle attive | **8** (`commerciale@`, `cs@` ×2, `info@deluxyflowers.com`, `edoardo.`, `eleonora.`, `nicolo.`, `renato.`) |
| Utenti | **10** |
| Messaggi | **31.575** (l'ultimo entrato alle 12:45) |
| Ultimo giro del cron | **12:55:56–12:56:00**, tutte e 8 le caselle, **zero errori** |
| Arretrato posta in arrivo | finito ovunque (`storicoFinito = true` × 8) |
| Attività | 213, di cui **44 aperte** |
| Iscrizioni alle notifiche push | 🔴 **1 sola**, su 10 utenti |
| Chiavi delle app Deluxy | tutte e 7 presenti (anagrafiche, calendario, commerciale, finance, fornitori, scripts, tasks) |

⚠️ Due righe `Account` hanno la **stessa casella `cs@deluxy.it`** con lo stesso intervallo
di UID (16917..25821), su due utenti diversi: è il funzionamento previsto del multi-utente
(ognuno ha la sua copia della casella condivisa), ma vuol dire che quella posta viene
scaricata e analizzata **due volte** a ogni giro. Da confermare con l'utente se è voluto.

- ⚠️ **Il push non pubblica**: il deploy è manuale —
  `npx vercel --prod --cwd C:/Users/nicol/scoutwt/deluxy-mail`, da lanciare come
  comando **semplice** — quindi prima di dire «è online» si verifica che la
  produzione abbia l'ultimo lavoro (`npx vercel inspect https://deluxy-mail.vercel.app`).

**Cronologia dei lavori:** le voci di §7 sono della sessione del 23 luglio (dalla
più recente); i lavori dal **26 luglio a oggi** stanno in §9, con la data fra
parentesi. Di **oggi 25 ago**: la **mail aperta rifatta** — su telefono la barra fissa in
stile Apple Mail, sul desktop prima una barra sola e poi la **gerarchia** (Rispondi ·
Inoltra · Archivia · Cestina in vista, il resto dietro «⋯ Altro»), perché quindici comandi
tutti uguali restano rumore comunque li si impili. Il **24 ago**: i **link delle mail** si
aprono in una scheda nuova anche quando hanno `target="_self"` (Trustpilot lasciava un
rettangolo grigio), la **chiave di «Commerciale»** si può finalmente salvare (una lista di
app riscritta a mano accanto a quella vera), e il **lotto del 21-24/08** — attività
annotabili, «Da fare su questa mail», bozze che mostrano il testo e **conservano gli
allegati** (salvarle li buttava via), comandi sopra la mail, luoghi e orari che non si
perdono più nei riassunti (misurato 0/4 → 4/4) e gli **appuntamenti con data certa in agenda
da soli**. Il **18-19 ago** il **trasloco del database** sul cluster condiviso. Del
**17 ago pomeriggio**, tutti LIVE (dal più recente): **il negozio
dell'ordine lo decide la mail** (`daMail` + `brand` come enum: «Trova fornitore»
cercava l'ordine 2725 su `deluxy.it` invece che su Deluxy Flowers); **«→ App» in
testa** alla mail; **mail trascinabili sul menu** (sezioni, Posta, Archivio, Spam,
Cestino) e 🔒 **le immagini in linea tornate visibili** (la correzione XSS del 14/08
azzerava anche i `data:image` veri); **si esce dall'archivio** (`disarchiviaThread`)
e 🔴 la **dedup delle migrazioni** che cancellava per utente e non per casella;
**lo SPAM deciso da un admin vale per tutti**, i tasti Sì/No anche fuori da
`/attivita`, e **Renè crea attività ripetute**; **il destinatario è cliccabile** e
la sua scheda mostra tutto lo scambio (prima 404 per chi non era mai stato
mittente). Del mattino: **selezione multipla sulla scheda
contatto** (`/rubrica/[email]`: checkbox per riga, Cestina · Archivia · Segna
lette · **⛓ Unisci in una conversazione**, `uniciInThread`); **via i segnaposto
`<firma5.png>`** delle immagini in linea dal corpo delle mail Apple; e
**l'anteprima decodifica le entità HTML** (`p&#229;` → «på») e toglie i caratteri
invisibili con cui gli spammer spezzano le parole. Il 15-17 ago: la **revisione
multi-agente** in due lotti (XSS nell'editor, cross-tenant in `salvaMinuta`,
azioni-riga sul thread, doppio invio, collisione UID INBOX/Inviata, retention che
non girava mai, azioni app Deluxy che scrivono senza conferma — dettaglio in §9), la
**graffetta in testa** per gli allegati e il **riassunto che si rilancia**.
Prima (7 ago): la **✕ in tutti i dialoghi** (più `Esc`), e
**la risposta a un invito resta scritta sulla mail** («Hai accettato», col tasto scelto acceso). Il 5 ago: **aprire una mail
la segna letta** (prima no,
e si è visto). Prima: **anche lo SPAM segue il server** (Posta
indesiderata della casella, e «Non è spam» riporta indietro). Prima:
**cestinare lo vede anche il server** — la mail
si sposta nel Cestino della casella, e recuperarla la riporta indietro. Prima,
sempre il 5 ago: **chi si finge un marchio noto** (nome
«Shopify», indirizzo su gmail) non viene più spostato di nascosto né lasciato
passare — te lo si propone una volta, e da lì in poi quella casistica è
automatica. Il 4 ago: **«Chiedi a questa conversazione»** (una domanda, con la
mail da cui viene la risposta); **l'inoltro porta con sé gli allegati**
dell'originale (prima partiva il solo testo, e sembrava riuscito);
**↑ Precedente / ↓ Successiva** (tasti `p` e
`n`) sulla mail aperta, per scorrere la posta senza tornare in elenco; e
**rispondere e inoltrare segnano letta tutta la conversazione** — l'inoltro non segnava letto niente e la risposta una mail
sola, e in elenco una riga è un thread. Prima (3 ago): le **scorciatoie si vedono** — la lettera è
stampata sul bottone (`Rispondi R`, `Inoltra I`) e `i`/`t` funzionano come
`f`/`a`, perché le iniziali erano quelle di Gmail e non quelle italiane; e
**`Ctrl+Invio` manda la mail** (in due colpi: conferma, poi invio).
Prima, in ordine: cestinare apre la **mail successiva**;
**Spam** spostato fra le cartelle (su telefono era introvabile); **scorciatoie da
tastiera** (`?` per l'elenco); la **conversazione come pila** che si legge senza
cambiare pagina; le **anteprime ripulite** da link e CSS; e soprattutto
l'apertura di una mail resa veloce (vedi punto 1 qui sotto).

#### Punti aperti (in ordine di cosa sblocca cosa)

1. **Verificare la velocità** dopo lo spostamento delle funzioni a **dub1**
   (Dublino, accanto al database): era la causa principale della lentezza
   — le funzioni giravano a Washington. La **configurazione è confermata**
   (17/08: `X-Vercel-Id: fra1::dub1::…`, health in 0,4 s), resta il giudizio a
   mano sull'apertura di una mail. Se è ancora lenta, il prossimo indiziato sono
   le **due ondate di query** della pagina del messaggio: la seconda aspetta la
   prima e ora si possono fondere.
1-bis. ✅ **Migrazione del vincolo UID: VERIFICATA sul database** (17/08, letto da
   `pg_indexes`): esiste `Messaggio_accountId_direzione_uid_key`, il vecchio
   `Messaggio_accountId_uid_key` non c'è più.
   🔴 **Come leggere la produzione da qui — CORRETTO il 25/08.** Dal trasloco del
   18-19/08 la produzione è **`DATABASE_URL` di `.env`** (host
   `zegbztfxisqeowngvgvh`, `aws-0-eu-central-1`, schema `mail`). **`A_DATABASE_URL`
   NON è più la produzione**: punta al vecchio progetto `feleldlsreurqpdhstla`, che
   è ancora **acceso e con dentro lo schema `mail` congelato al 19/08 07:25 UTC**.
   Chi segue la vecchia riga di questo handoff ci si collega, vede l'ultimo
   `ultimoSync` di sei giorni fa e conclude che **l'app è morta** — è successo oggi.
   ⚠️ Un database dismesso ma vivo è **peggio** di uno spento: risponde, e risponde
   il falso ([[trappola-confronto-con-il-proprio-specchio]] è la parente stretta).
   Per query parametrizzate va usata la porta **5432** — sulla 6543 (pgbouncer)
   danno «prepared statement "s0" already exists»; in `.env.local` `DATABASE_URL`
   punta al Postgres **locale** e vince su `.env`, quindi l'app in locale non tocca
   la produzione.
2. **Ruotare i segreti finiti in chat il 28/07**: chiave OpenAI e password del
   database (è la stessa su vecchio e nuovo). Rigenerare, aggiornare Vercel e
   `.env`. **`APP_SECRET` no**: cambiarlo scollegherebbe le caselle.
3. **Spegnere i DUE progetti Supabase vecchi** — `feleldlsreurqpdhstla` (che ospita
   ancora la **piattaforma consegne**: si spegne solo quando anche quella se n e
   andata, o si ferma lei) e `sxovckndpmdbqfrfkxhl` — dopo qualche giorno di
   esercizio sereno del nuovo. Stanno su un **secondo account Supabase**
   (cs@deluxy.it): chiusi i progetti, valutare se chiudere anche quell abbonamento,
   visto che l org Deluxy paga gia il Pro che ora ospita AI Mail.
   ⚠️ **Finché resta acceso non è solo una spesa: è una trappola.** Verificato il
   25/08 che `feleldlsreurqpdhstla` risponde ancora e ha lo schema `mail` congelato
   al 19/08 — chi ci si collega per sbaglio legge dati veri ma vecchi di sei giorni.
   Se non si può spegnerlo subito, **rinominare lo schema `mail` in `mail_dismesso`**
   là dentro: l'errore diventa rumoroso invece che silenzioso.
4. **Timeout del cron `/api/sync` a 300s** — molto ridimensionato (misurato il
   25/08): l'arretrato della **posta in arrivo è finito su tutte e 8 le caselle**
   (`storicoFinito = true` ovunque) e nessun account ha un `ultimoErrore`; il giro
   completo di 8 caselle sta in **~5 secondi**. Resta indietro solo lo storico
   della cartella **Inviata**: `storicoInviataFinito = false` su **5 caselle su 8**
   (commerciale, le due cs@, edoardo, nicolo). Il rischio del taglio a 300s vale
   ormai solo per quel recupero, non per il giro di tutti i giorni.
5. **856 mail vecchie col testo sporco** (804 con `mailto:` incastrati, 56 col
   CSS): sono tutte **senza HTML nel database** (alleggerite, l'impaginato sta
   sul server IMAP), quindi `scripts/ripara-testi-sporchi.mjs` non le tocca. In
   elenco si vedono pulite lo stesso (la pulizia è anche in lettura), ma il
   testo che legge l'**AI** resta sporco: per sistemarle servirebbe rileggerle
   dal server una per una.
6. ✅ **Calendario condiviso: ACCESO** (verificato sul database il 25/08).
   `Impostazione.app.calendario.key` c'è (114 caratteri) e ha già lavorato:
   `calendario.cursore = 16`, `calendario.inviati` con **5 eventi** mandati. Il
   punto è chiuso; resta da guardare a schermo che gli eventi si vedano davvero
   nell'app Calendario.
7. **Scripts**: la chiave qui dev'essere di **scrittura** per salvare le
   «Risposte rapide» (`npm run chiave -- deluxy-mail --scrittura` in
   deluxy-scripts) e i testi vanno accesi per AI Mail dall'app Scripts.
8. **Da verificare a schermo** (non ho la sessione dell'utente): la pila della
   conversazione, le scorciatoie (`j`/`k` poi `r` deve rispondere al messaggio a
   fuoco), la testata compatta, «Spam» nel menu da telefono, e l'invio a
   Anagrafiche **con stato e interessi** ora che la chiave è di prima parte.
   Dal 3 ago anche: le **lettere sui bottoni** della mail aperta (`R`, `T`, `I`,
   `E`, `Canc`), che `i` inoltri e `t` risponda a tutti davvero, e che
   **`Ctrl+Invio`** mandi la mail in due colpi (conferma, poi invio) sia in
   risposta/inoltro sia in `/scrivi` — quest'ultimo con una mail vera.
   Dal 5 ago: aprire una mail non letta e vedere il pallino spegnersi (e il
   bottone diventare «Segna non letto»); poi premerlo, o `s`, e verificare che
   **resti** non letta anche cambiando sezione e tornando.
   Sempre dal 5 ago, **col telefono o la webmail aperti a fianco**: segnare una mail
   come spam e vedere che finisce nella Posta indesiderata anche lì; «Non è
   spam» deve riportarla in posta. Occhio soprattutto alla casella SecureMail:
   se non dichiara il flag `\Junk` e la cartella non si chiama in uno dei modi
   previsti, lo spostamento **non avviene** (in AI Mail è comunque a posto).
   Sempre dal 5 ago: cestinare una mail
   qui e vedere che sparisce dalla INBOX anche lì (finisce nel Cestino della
   casella); poi «Recupera» e vedere che torna. Serve una verifica anche su
   `svuota cestino` (deve cancellare per davvero, cercando ora nel Cestino) e
   sull'apertura di una mail **dal Cestino** (impaginato e allegati devono
   esserci ancora).
   Dal 5 ago: aprire la mail `Shopify <info.shopifymail.it@gmail.com>` («Verifica
   della conformità GDPR») e vedere il riquadro rosso; dire **«Sì, è spam»** e
   controllare che (a) finisca in SPAM, (b) nasca/si chiuda l'attività, (c) la
   **prossima uguale** ci vada da sola senza chiedere.
   Dal 4 ago, **col conto in mano**: inoltrare una mail con allegati e
   verificare che al destinatario arrivino **tutti** (e che il messaggio di
   esito dica «Con N allegati dell'originale»). È l'unica prova che vale: qui
   non ho né la casella né una mail vera con allegati.
   Poi: **↑ Precedente / ↓ Successiva** (e i tasti `p`/`n`) — in
   particolare che aprendo una mail **archiviata** o **nel cestino** si scorra
   lì dentro e non nella posta in arrivo. E che dopo una **risposta** e dopo un
   **inoltro** il pallino blu si
   spenga su TUTTA la conversazione, anche quando il thread aveva più mail non
   lette. (Le mail già inoltrate PRIMA di oggi restano da leggere: la correzione
   vale da qui in avanti, per quelle vecchie c'è «✓ Letto» sulla riga.)
9. **Le altre pagine non dicono le loro scorciatoie**: le lettere sono stampate
   sui bottoni della **mail aperta**, ma in posta in arrivo `c` (scrivi) e `u`
   (torna) restano invisibili, e il tasto «⌨ Scorciatoie» sta solo lì. Se
   l'utente le cerca anche altrove, il pezzo da riusare è `apriScorciatoie()`.

Chiusi in questi giorni: recupero delle mail scavalcate (29/07); trasloco del
database (28/07); **166 + 105 mail** col testo riparato (30-31/07); la chiave
`app-ai-mail` resa **driver di prima parte** in Anagrafiche (31/07), che era ciò
che impediva a stato commerciale e interessi di arrivare.

**Se riprendi da qui, sappi anche che:**

1. **Cose fatte ma MAI provate su dati reali** (io non ho accesso alla casella
   né al DB di produzione — servono la sessione dell'utente): download allegati
   a flusso, «Scarica tutti» in zip, inviti calendario Accetta/Rifiuta, svuota
   cestino sul server, **dimensione vera dal server** (`/api/dimensioni`),
   switch fra account con 2 caselle, API `?ordine=`. Se l'utente segnala che
   una non funziona, **prima chiedi cosa vede a schermo**: quasi tutte ora
   mostrano stato/errore invece di fallire mute.
2. ✅ **Notifiche push: il cron gira, l'ostacolo è un altro** (rimisurato il 25/08,
   la vecchia diagnosi «Hobby, 1 volta al giorno» era **superata**). Il cron
   `*/5` di `vercel.json` **gira davvero ogni 5 minuti**: due letture a distanza
   di cinque minuti danno `Account.ultimoSync` alle **10:50:58** e alle
   **10:55:56** UTC, cioè sulla griglia dei :50 e dei :55 — non su secondi a caso
   come sarebbe un giro lanciato da un utente; e ci sono mail entrate alle 00:xx e
   02:xx UTC, quando nessuno era davanti all'app. `sincronizzaTutti` chiama
   `notificaNuoveMail` proprio sul giro automatico (`src/lib/sync.ts:2229`).
   🔴 **Quello che manca è l'iscrizione**: in `PushIscrizione` c'è **1 sola riga**
   per **10 utenti**. Chi non le riceve, con ogni probabilità non si è mai iscritto
   da Impostazioni (o l'ha fatto da un browser diverso da quello che usa).
3. **`/api/allegati-zip` costruisce lo zip in memoria** → stesso tetto ~4,5 MB
   di Vercel: con molti allegati pesanti non parte. Il download singolo è già
   a flusso; lo zip **no**. Da convertire se serve.
4. ⚠️ **Più sessioni Claude lavorano su questa cartella** (`scoutwt`, branch
   `scout-ui`): Partner, Transactions, Messaggi, Hub. Prima di ogni push fai
   `git fetch` e verifica; **committa solo i file di deluxy-mail**, mai `-A`
   sull'intero repo.

---

## 2. Stack

- **Next.js 15** (App Router, Server Components, Server Actions), **React 19**, TypeScript.
- **Prisma 6** + **PostgreSQL**.
- **OpenAI SDK** (modello `gpt-4o-mini`, chiamate con `response_format: json_schema` strict).
- **imapflow** (lettura IMAP), **nodemailer** + MailComposer (invio SMTP, copia in "Inviata" via IMAP APPEND).
- Design system Apple-style (token in `src/app/globals.css` + `deluxy-design-system/`), oro `#B8963E` come accento.

---

## 3. Come far ripartire

### Locale
```
cd deluxy-mail
npm install
# il DB locale è il Postgres di sviluppo di Prisma:
npx prisma dev            # avvia Postgres locale (porta dinamica ~51214)
# .env.local punta a quella porta (DATABASE_URL/DIRECT_URL)
npm run dev               # Next su http://localhost:3070
```
⚠️ **Il Postgres locale di `prisma dev` è CRONICAMENTE INSTABILE**: cade sotto carico (specie se uno script e il dev server accedono insieme), cambia porta a ogni riavvio, e le DDL via proxy falliscono in modo intermittente. Per applicare schema/seed in locale conviene il driver `pg` grezzo (protocollo semplice) invece di Prisma. Aspettati di doverlo riavviare spesso.

### Variabili d'ambiente (`.env` / Vercel)
- `DATABASE_URL` (pooler pgbouncer 6543), `DIRECT_URL` (5432).
- `OPENAI_API_KEY` (chiave `sk-proj-…`), `OPENAI_MODEL` (`gpt-4o-mini`).
- `APP_SECRET` (firma cookie sessione, HMAC).
- `APP_PASSWORD` (legacy), `CRON_SECRET` (cron `/api/sync`).
- **APP DELUXY** (pannello verso le altre app): le chiavi si inseriscono ora **dall'app** in **Impostazioni App** (`/impostazioni-app`, solo admin) — vengono **cifrate** (AES-256-GCM come le password IMAP) e salvate come `Impostazione` globale (`app.anagrafiche.key`/`app.finance.key`/`app.fornitori.key`). In alternativa restano le env `ANAGRAFICHE_API_KEY` / `FINANCE_API_KEY` / `FORNITORI_PASSWORD` su Vercel: il resolver `leggiChiaviApp()` in `lib/chiaviApp.ts` legge **prima il DB cifrato, poi l'env**. Le chiavi non tornano mai al browser (la UI mostra solo *se* è impostata). Chiavi: Anagrafiche = **scrittura** (`npm run chiave -- deluxy-mail --scrittura`), Finance = `api.verificheKey`, Fornitori = password admin di search-deluxy (azione «Trova fornitore» → `GET /api/fornitori?brand&number`, header `x-app-password`/`x-app-user`). Opzionali gli URL `ANAGRAFICHE_URL` / `FINANCE_URL` / `FORNITORI_URL` (non segreti). **Senza chiave la carta dell'app è "da collegare"** e l'invio è bloccato. Le azioni ricevono la chiave via `ctx.chiave` (non leggono più `process.env` direttamente).
- ⚠️ `TZ` è **riservato** su Vercel: il fuso è forzato nel codice (vedi §9).

### Deploy
```
cd deluxy-mail
npx vercel --prod
```
Il build gira `prisma generate && node scripts/migrate-prod.mjs && next build`.
- `scripts/migrate-prod.mjs` applica **automaticamente** le migrazioni idempotenti (CREATE TABLE IF NOT EXISTS / ADD COLUMN IF NOT EXISTS) usando `DATABASE_URL`. È **non bloccante**: se il DB non risponde, il deploy non fallisce.
- **Quindi: aggiungendo tabelle/colonne allo schema, aggiungi la DDL idempotente anche in `migrate-prod.mjs`.** Non si usa `prisma migrate deploy` (l'host diretto Supabase è solo IPv6, irraggiungibile da build/CLI).

⚠️ **Non posso modificare i segreti Vercel** (guard di sicurezza): chiavi API/env sensibili le imposta l'utente dal dashboard Vercel. Il limite Hobby è **100 deploy/giorno per account**.

---

## 4. Modello dati (Prisma, `prisma/schema.prisma`)

Ogni tabella dati ha `utenteId` → **isolamento multi-utente** (ogni query è filtrata per utente). Password IMAP/SMTP cifrate (AES-256-GCM). Password utenti hashate (scrypt).

- **Utente** — email, nome, passwordHash, ruolo (`admin`/`utente`), firma, `traduzioneAuto`, `lingueLette`, `sincronizzaOgniSec`, `scaricaStoricoAuto` (scarico di tutto lo storico in background), `tokenCalendario`, attivo.
- **Account** — casella IMAP/SMTP (host/porte/utente/password cifrate), `ultimoUid`/`primoUid`/`storicoFinito` (INBOX), `cartellaInviata` + `ultimoUidInviata`/`primoUidInviata`/`storicoInviataFinito` (cartella Inviata, per lo storico degli inviati).
- **RiassuntoSezione** — il punto della situazione AI su una sezione: `taglio` (`giorni`/`thread`), `giorni`, testo, `punti` (uno per riga), conteggi. Se ne tiene uno per sezione+taglio (l'ultimo).
- **Sezione** — colonne in cui l'AI allinea la posta; `genitoreId` = sottosezione (gerarchia a **due livelli**, la figlia eredita il colore) (nome, descrizione, colore, ordine). Unica `[utenteId, nome]`. La sezione **SPAM** viene creata da sola (vedi §7).
- **Regola** — condizioni (`seMittente`/`seOggetto`/`seContiene`), `istruzioneAI`, `attivitaTesto` (attività su misura da creare), sezione, flag (`creaAttivita`, `creaBozza`, `segnaLetta`, `archivia`, `fermaQui`), `priorita`.
- **Messaggio** — uid, messageId, `thread` (radice della catena di risposte), `threadManuale` (aggancio deciso a mano: unisce mail senza legami naturali, vince su tutto), `direzione` (`entrata`/`uscita`), mittente/destinatari, oggetto, data, corpoTesto/corpoHtml, `letto`/`archiviato`/`cestinato`, `sezioneId`/`smistatoDa` (`manuale`/`regola`/`ai`/`spam`), `priorita`/`prioritaDa`, `riassunto`, `serveRisposta`, `analizzatoIl`, `lingua`/`corpoTradotto`.
- **Attivita** — titolo, dettaglio, scadenza, priorita, fatta, `creataDaAI`, link a `messaggio` o `contattoEmail`.
- **Bozza** — risposta non inviata (origine `ai`/`utente`, modo, a/cc/oggetto/corpo, `inviata`).
- **RiassuntoContatto** — quadro AI di un contatto (situazione, taskAperti). Unica `[utenteId, email]`.
- **RapportoAI** + **PropostaArchivio** — risultato dell'Assistente periodico (oggi/settimana/mese).
- **RiassuntoThread** — riassunto "per punti di vista" di una conversazione. Chiave `[utenteId, chiave]` dove `chiave` = id del messaggio capostipite del thread.
- **ContattoAI** — il "PLUS AI": presenza della riga = contatto seguito dall'AI (entra nella AI Inbox). Campo `istruzioni` = istruzioni AI per quel contatto.
- **IstruzioneThread** — istruzioni AI per una conversazione. Chiave `[utenteId, chiave]` (stessa chiave del RiassuntoThread).
- **Impostazione** — preferenze GLOBALI condivise (es. contesto aziendale dato all'AI).
- **Evento** — appuntamento del calendario (titolo, luogo, inizio/fine, giornataIntera, link opzionale al messaggio). `Utente.tokenCalendario` = token segreto del feed iCal (vuoto = feed spento).

---

## 5. Moduli `src/lib/` (le funzioni chiave)

- **db.ts** — singleton Prisma.
- **auth.ts** — sessione firmata edge-safe (Web Crypto HMAC): `creaSessione`, `verificaSessione`, `SESSION_COOKIE`. Usata dal `middleware.ts` (cancello di accesso).
- **password.ts** — `hashPassword`/`verificaPassword` (scrypt "salt.hash", solo Node).
- **sessione.ts** — `utenteCorrente()`, `richiediUtente()` (redirect a /login), `richiediAdmin()`.
- **crypto.ts** — `cifra`/`decifra` (AES-256-GCM) per le password IMAP/SMTP.
- **imap.ts** — basso livello IMAP/SMTP: `scaricaNuovi`, `scaricaVecchi`, `salvaInInviata`, `trovaCartellaInviata`, parsing; `ripulisciPerDatabase` (toglie i null byte che bloccherebbero il salvataggio).
- **sync.ts** — orchestrazione. Funzioni principali:
  - `sincronizzaAccount` / `sincronizzaUtente` / `sincronizzaTutti` — scarico IMAP + `salvaMessaggi`.
  - `salvaMessaggi` — salva i nuovi messaggi, applica le **regole** deterministiche, **traduce** se serve, **filtra lo SPAM** all'arrivo, crea le **attività definite dalle regole**.
  - `analizzaMessaggioOra` — analisi AI di un messaggio (sezione, riassunto, attività, bozza). Rispetta `smistatoDa` (`manuale`/`regola`/`spam` non vengono spostati dall'AI).
  - `analizzaContattoOra` / `leggiQuadroContatto` — quadro AI del contatto.
  - `messaggiThread` / `riassumiThreadOra` / `leggiRiassuntoThread` — conversazioni + riassunto per punti di vista.
  - `preparaEsecuzione` — l'AI scrive la mail che porta a termine un'attività.
  - `preparaRispostaDelegata` — "Delega Renè": ora passa **l'intero thread** a `scriviRisposta` e distingue **risposta vs inoltro** (vedi §7).
  - `assicuraSezioneSpam` — crea la sezione SPAM se manca.
  - `istruzioniMirate` — raccoglie le istruzioni AI del **contatto** e del **thread** (precedenza **thread > contatto > globale**).
  - `traduciMessaggioSeServe` — **NON più chiamata all'apertura** della mail: gira in background dopo il render (via action `traduciMessaggio`), così aprire una mail è istantaneo (vedi §7 e §9).
- **ai.ts** — chiamate OpenAI (tutte con json_schema strict; client con **key ripulita da spazi/a-capo**, timeout 45s, maxRetries 2):
  - `analizzaMessaggio` (analisi completa), `riassumiContatto`, `riassumiThread`, `scriviRisposta` (**ora riceve l'intero thread** come contesto e può scegliere **rispondi/inoltra**: torna `modo`+`a`; per l'inoltro sceglie il destinatario dai contatti conosciuti), `pianificaAttivita` (comando in linguaggio naturale → attività), `pianificaConProposta` (come sopra ma aggancia un contatto CONOSCIUTO alle attività e formula la proposta di azione che l'AI può intraprendere), `giudicaSpam`, `rilevaETraduci` + `traduciVerso` (traduzione), `triageLotto` + `sintetizzaPeriodo` (Assistente map-reduce). Tutte le funzioni AI accettano `istruzioni?: string[]` (le istruzioni mirate).
- **regole.ts** — `applicaRegole(regole, msg)` → `EsitoRegole` (sezione, letta, archivia, `istruzioniAI[]`, `attivitaDaCreare[]`).
- **condizioni.ts** — `condizioneSoddisfatta(testo, condizione)` / `alternative(condizione)`: dentro ogni condizione (mittente/oggetto/testo) le **alternative separate da virgola** valgono in **OR** (una basta); fra condizioni diverse resta la E. Usato da `regole.ts`, `appDeluxy.ts` (regole verso le app) e dalla retrodatazione (`retrodataRegola`, che costruisce un `AND` di gruppi `OR` in SQL). Reso in UI dal componente `ValoreCondizione`.
- **thread.ts** — raggruppamento in conversazioni (union-find su radice della catena + oggetto normalizzato): `raggruppa`, `normalizzaOggetto`, `chiaveThread`.
- **spam.ts** — `valutaSpam(msg, ctx)` → livello `basso`/`medio`/`alto` a punteggio (frasi tipiche, spoofing marchi, link IP/accorciati, maiuscolo…). Whitelist forte: contatto noto / dominio proprio / contatto AI = mai spam.
- **contattiAI.ts** — `emailContattiAI`, `eContattoAI`, `datiContattoAI` (letture difensive: se la tabella manca degradano a vuoto).
- **impostazioni.ts** — `CHIAVI`, `leggiImpostazioni`, `scriviImpostazione`.
- **registroTask.ts** — allinea le **attività** col registro centralizzato Deluxy Tasks (`https://deluxy-tasks.vercel.app`, override `TASKS_URL`) **nei due sensi**. `sincronizzaAttivitaConRegistro({limite, forza})` è chiamata in coda a `GET /api/sync` (a ogni giro del cron; `?forzaRegistro=1` rimanda tutto) e fa, in quest'ordine: (1) **RITORNO** — `GET /tasks/changes?since=<cursore>&sistema=mail` e applica qui stato, scadenza, priorità, titolo e descrizione; il cursore sta in `Impostazione[`tasks.cursore`]`, così niente si riscarica due volte e nulla si perde se un giro salta. (2) **ANDATA** — POST delle attività aperte + quelle chiuse negli ultimi 7 giorni, ma **solo se cambiate**: l'impronta di ciò che è già partito sta in `Impostazione[`tasks.inviate`]` (senza, ogni cron rifarebbe centinaia di POST). (3) le attività **cancellate** qui vengono archiviate anche là (`by-ref` + DELETE), distinguendo la cancellazione vera dall'uscita dalla finestra dei 7 giorni. Identità nel registro = `(sistema='mail', idEsterno=Attivita.id)` → upsert, mai doppioni. ⚠️ **Niente rimbalzi**: nel ritorno si saltano le task con `ultimoAttore === 'mail'` — sono l'eco delle nostre stesse scritture, e riapplicarle farebbe rimbalzare la stessa modifica a ogni giro. `asOf` = quando è cambiata qui, così se una task viene chiusa dentro il registro il nostro invio più vecchio non la riapre (`ignorata_obsoleta`). Priorità tradotte P0→urgente, P1→alta, P2→media, P3→bassa (e ritorno). `allineaAttivitaOra(id, sparita?)` fa la stessa cosa per **una sola** attività ed è chiamata da `segnaAttivita`/`eliminaAttivita`: spuntare qui deve chiudere là **subito**, non fra cinque minuti — attesa cortissima (2,5s) perché sta sul percorso di un clic. Chiave di **scrittura** risolta da `chiaviApp.ts` (`tasks`: Impostazioni App → cassaforte del Hub → env `TASKS_API_KEY`); senza chiave la sincronizzazione è spenta e la posta funziona come prima. In Impostazioni App c'è **«Sincronizza adesso»** (`sincronizzaRegistroOra`, admin) col conto di mandate/ricevute/errori: un'integrazione che si vede solo al cron successivo è indistinguibile da una che non funziona.
- **registroCalendario.ts** — il **gemello esatto** di registroTask, applicato agli APPUNTAMENTI e al calendario centralizzato Deluxy Calendario (`https://deluxy-calendario.vercel.app`, override `CALENDARIO_URL`). Stessa architettura: ritorno via `/eventi/changes` con cursore in `Impostazione[`calendario.cursore`]`, andata con impronte in `Impostazione[`calendario.inviati`]`, salto delle eco (`ultimoAttore === 'mail'`), archiviazione di ciò che è stato cancellato qui, `asOf` = `Evento.aggiornatoIl`. Finestra: gli appuntamenti da 30 giorni indietro in avanti. Gli eventi partono come `tipo: appuntamento`, con deep link alla mail da cui nascono. ⚠️ **Unico punto di tutta la sincronizzazione che cancella qualcosa**: un evento **annullato o archiviato** nel calendario condiviso viene tolto anche qui — è una scelta esplicita fatta dall'utente nell'app gemella, e l'evento resta comunque là segnato annullato. `allineaEventoOra(id, sparito?)` sta su `creaEvento`, `modificaEvento` (singolo), `eliminaEvento`, `accettaEventoProposto` e `rispondiInvito`: accettare un invito o spostare un appuntamento si vede subito anche là. **Limite noto e voluto**: le RIPETIZIONI oltre la prima occorrenza e le modifiche a tutta una serie le porta il giro del cron — allinearle a mano vorrebbe dire una chiamata per occorrenza dentro un tasto «Salva».
- **scriptPronti.ts / scriptTesto.ts** — i TESTI PRONTI dell'azienda (deluxy-scripts) dentro la schermata di scrittura. ⚠️ **Non è una sincronizzazione, ed è giusto così**: con Tasks e Calendario ci sono due copie dello stesso dato da allineare; qui i testi hanno UN padrone solo (l'app Scripts) e AI Mail li LEGGE e basta, già composti con firma e recapiti della posta (`GET /api/v1/script?app=deluxy-mail`, chiave di sola lettura). Copiarli qui creerebbe la seconda versione di un testo aziendale destinata a divergere — esattamente ciò che Scripts esiste per evitare. Il riquadro `components/TestiPronti.tsx` (in `ComposizioneNuova`) elenca i testi accesi, fa compilare **a mano** le variabili rimaste e mette oggetto e messaggio nella mail. ⚠️ Le variabili scoperte **non si indovinano**, men che meno le date: `{{DATA}}` in un invito è la data dell'evento, non oggi. Restano scritte `{{COSÌ}}` in vista. ⚠️ `componiScript` e il tipo stanno in **scriptTesto.ts** a parte: importarli da scriptPronti (che risolve le chiavi, quindi `node:crypto`) dal componente client fa **fallire il build** con «Reading from node:crypto is not handled by plugins». **Dal 27 lug si può anche SCRIVERE**: `creaScriptPronto()` fa `POST /api/v1/script` (rotta aggiunta in deluxy-scripts, chiave di **scrittura**), che crea il testo là e lo **accende per AI Mail** — un testo creato da qui e non abilitato per qui sarebbe invisibile a chi l'ha appena scritto. Non è una copia: in AI Mail non si salva nulla, il testo nasce e vive in Scripts. La sezione è `/script` («Risposte rapide» nella barra laterale) + `components/NuovaRispostaRapida.tsx`. I segnaposto `{{COSÌ}}` diventano variabili da soli lato Scripts. ⚠️ Le VARIABILI_SUGGERITE stanno in `scriptTesto.ts` (client-safe) e servono a tenere gli STESSI nomi in tutta l'azienda: è per nome che i valori impostati per l'app si agganciano.
- **format.ts** — `FUSO='Europe/Rome'`, `dataBreve`/`dataLunga` (fuso forzato), `PRIORITA`/`CODICI_PRIORITA`.
- **assistente.ts** — Assistente periodico (conta + triage a lotti + sintesi).
- **sanitizzaHtml.ts** — pulizia HTML delle mail (mostrate in iframe sandboxed).

---

## 6. "API" dell'app = Server Actions (`src/lib/actions.ts`)

Tutte scoped per utente via `uid()`. Le principali:

- **Posta/priorità:** `impostaPriorita` (setta priorità + lancia analisi AI; **non lancia mai** errori al client, torna sempre `{ok, messaggio}`), `rianalizza`.
- **Invio:** `inviaMessaggio`, `inviaNuovaMail` (mail scritta da zero dalla pagina `/scrivi`: nessun originale, apre una conversazione nuova), `inviaBozza`, `salvaMinuta` (con `modo='nuova'` e senza `messaggioId` per le mail da zero), `salvaBozza` (invio via SMTP + copia in Inviata + traduzione se la mail originale è straniera).
- **Contatti/AI:** `analizzaContatto`, `cambiaContattoAI` (PLUS AI on/off), `salvaIstruzioniContatto`, `salvaIstruzioniThread`, `riassumiConversazione`, `traduciMessaggio` (traduzione in background all'apertura, non blocca il render — usata da `TraduzioneAllApertura`).
- **Attività:** `eseguiAttivita` (AI scrive la mail), `creaAttivitaManuale` (attività tua, opzionale contatto → eseguibile dall'AI), `attivitaDaComando` (comando NL → attività via `pianificaAttivita`), `creaAttivitaConProposta` (il dialogo "Nuova attività" della posta: crea le attività, aggancia solo contatti realmente conosciuti — i mittenti recenti — e torna `proposta` + `eseguibileId` per il tasto "Procedi"), `eliminaAttivita`, toggle "fatta".
- **Regole:** `creaRegola` (con **retrodata**: applica subito le parti deterministiche allo storico via `retrodataRegola`), `attivaRegola`, `eliminaRegola`.
- **Assistente:** `contaPeriodoAI`, `avviaAssistenteAI`.
- **Sync/account/impostazioni:** `sincronizzaOra`, `scaricaStorico`, `creaAccount`, `salvaImpostazioni`.
- **Calendario:** `creaEvento` (orari in ora italiana → UTC), `eliminaEvento`, `rigeneraFeedCalendario` / `spegniFeedCalendario` (token del feed iCal). Rotta pubblica **`/api/calendario?token=…`** (esclusa dal middleware): feed iCalendar RFC 5545 generato da `lib/ics.ts`, sola lettura, da abbonare in Google/Apple/Outlook.
- **Utenti (auth-actions.ts):** `accedi`, `creaPrimoAdmin`, `esci`, `creaUtente`, `cambiaStatoUtente`, `reimpostaPassword`, `eliminaUtente`, `salvaFirma`.

**La finestra di scrittura si apre dalle ALTRE app** (29 lug) — link prefillato, la strada leggera per «scrivi a questo partner / a questo cliente» da Partner, Orders, Customer Service:

```
https://deluxy-mail.vercel.app/scrivi?a=…&cc=…&oggetto=…&corpo=…&app=…&rif=…
```

Tutti i parametri **URL-encoded** e facoltativi; tetti: `a`/`cc` 500, `oggetto` 300, `corpo` 8000 caratteri (`app`/`rif` sono solo la provenienza mostrata: «Preparata da Deluxy Orders · ordine 2529»). L'utente vede la mail già compilata, la corregge e **la manda lui** — nessuna mail parte da un link. ⚠️ Tre scelte: (1) il `corpo` è trattato come **testo**, e se contiene HTML viene convertito (`sembraHtml`→`htmlAPlain`): il contenuto lo scrive un'altra app, non l'utente, e non deve poter iniettare markup nella mail — chi vuole formattato usa `POST /api/v1/invia`; (2) via i caratteri di controllo e taglio a lunghezza, perché è testo che arriva da fuori; (3) con `?bozza=` il prefill si ignora (si riprende la bozza com'era). Perché il link regga anche a sessione scaduta, il **middleware ora conserva la destinazione** in `/login?dopo=…` e `accedi` ci torna: prima il login riportava sempre alla posta e la mail preparata dall'altra app spariva per strada. `dopo` è accettato **solo** se è un percorso interno (`/…`, non `//…`), controllato sia nel middleware sia nella server action: altrimenti sarebbe un redirect aperto.

**API pubbliche `/api/v1/*`** (per altre app Deluxy/agenti, escluse dal middleware; auth in `lib/apiAuth.ts`): chiave unica come header `x-api-key` o `Authorization: Bearer`, + `x-utente: <email>` per scegliere l'utente/casella. Il token si **genera dall'app** (Impostazioni App → «Token API di AI Mail», admin: `generaTokenApi`/`revocaTokenApi`, salvato CIFRATO in Impostazione `api.token`) oppure via env `API_TOKEN` (il DB ha precedenza). `tokenApiConfigurato()` risolve token+fonte.
  - **`POST /api/v1/invia`** — invia una mail (`inviaMailApi` in actions.ts, riusa `spedisci`/`registraInviato`, quindi **la copia finisce in «Inviata»**: è il motivo per cui un'altra app conviene che mandi da qui invece che con un SMTP suo). Body JSON `{a, cc?, oggetto, corpo, corpoHtml?}`. **Il corpo può essere formattato** (29/07/2026): o esplicito in `corpoHtml` (alias `html`), oppure HTML dentro `corpo`, riconosciuto da `sembraHtml`; il `text/plain` del multipart si ricava da solo con `htmlAPlain`. Prima passava solo testo semplice: **Deluxy Scout**, che compone le mail di Script e Sequenze in HTML, le avrebbe spedite **coi tag scritti dentro**. Retro-compatibile — chi manda testo semplice non cambia niente. ⚠️ Vale solo **dopo il deploy in produzione**, che qui non avviene col push.
  - **`GET /api/v1/contatto?email=<contatto>`** — Renè fa il punto della situazione con un contatto (`analizzaContattoOra`): `{situazione, inSospeso[], prossimeAzioni[], messaggiVisti, aggiornatoIl}` sui messaggi già scaricati.

Rotte di lettura in background (fetch, non Server Action, così non bloccano i clic):
- **`POST /api/leggi-posta`** — un giro breve di posta NUOVA. `SyncButton` la chiama all'apertura **in loop** (drena tutto l'arretrato nuovo) e poi a intervalli.
- **`POST /api/scarica-storico`** — un blocco (80) di posta VECCHIA per l'account che ne ha ancora; torna `{ scaricati, finito }`. Il componente `StoricoAuto` la chiama in loop, in background, se l'utente ha attivato `scaricaStoricoAuto` (Impostazioni), finché la casella non è completa.

Cron: **`/api/sync`** (route, autenticata con `CRON_SECRET`) — pianificato `*/5` in `vercel.json` e **misurato ogni 5 minuti in produzione** il 25/08 (non più «1 volta al giorno»: quella era la regola di Vercel Hobby, il progetto sta sull'org Deluxy). L'auto-refresh a 30s è **client-side** (SyncButton), gira solo con la app aperta e in primo piano.

**`maxDuration = 60`** è impostato sulle pagine che scatenano l'AI (`/`, `/messaggio/[id]`, `/rubrica/[email]`, `/assistente/[id]`) — necessario perché le Server Action NON ereditano il maxDuration del layout, e a 10s (default Vercel) l'analisi AI verrebbe uccisa a metà.

---

## 7. Funzionalità implementate

- **La mail si legge SUBITO: testata compatta (30 lug)** — sulla pagina del messaggio la testata era fatta di cinque blocchi impilati (oggetto 24px, mittente, destinatari+data, «AI del contatto», etichette, priorità) più la nota «l'AI non ha ancora letto» dentro un riquadro e «Questa mail fissa un appuntamento?» su una riga sua: il corpo partiva **sotto la piega**, cioè per leggere una mail bisognava scorrere. Ora oggetto a 19px, **recapiti su una riga sola** (con l'indirizzo completo nel `title`), e tutto il resto in **una striscia** che va a capo da sé: PLUS AI, «Quadro AI →», sezione, «da AI/regola/te», «📎 N», i quattro tasti di priorità (più piccoli) e la ricerca dell'appuntamento. La nota dell'AI è una riga sottile senza riquadro. ⚠️ **Nessuna azione è stata tolta** — è la lezione già pagata: per fare spazio si stringe la cornice (etichette corte, gap e padding), non si riduce il numero dei comandi. Classi: `.mail-head.compatta`, `.mail-meta.una-riga`, `.mail-strip` (che azzera i margini di ciò che ci sta dentro), `.nota-sottile`.
- **Il «testo» di certe mail conteneva gli indirizzi dei link (30 lug)** — caso vero: `Buongior mailto:renato.cassoli@deluxy.itno Luca`. Non è l'anteprima ad essere rotta: il client del mittente aveva avvolto **singole lettere** in un link (`Buongio<a href="mailto:…">r</a>no`), e la conversione in testo semplice ci ha infilato dentro l'indirizzo — quindi era sporco `corpoTesto`, cioè **anche quello che legge l'AI**. Ora `testoMigliore(testo, html)` in `lib/htmlMail.ts` (usata da `converti` in imap.ts) riconosce il testo sporco (≥3 `mailto:`, o lo stesso URL ripetuto ≥4 volte) e in quel caso ricava il testo **dall'HTML** con `htmlAPlain`, che i tag li toglie e basta: «Buongiorno Luca». Le mail nuove nascono pulite; per quelle già in archivio c'è `scripts/ripara-testi-sporchi.mjs`, che senza `--applica` **conta e non scrive**. **Eseguito il 30/07: 166 mail riparate su 21.560** (verificato sul caso di partenza: ora legge «Buongiorno Luca, Per la consegna di una torta nuziale…»). ⚠️ Ne restano **804** con tre o più `mailto:` nel testo, e sono **tutte** senza HTML nel database: sono le mail vecchie alleggerite (l'impaginato abita sul server IMAP, vedi §9), quindi da lì non si ricavano — servirebbe rileggerle dal server una per una.
- **«Letto» dalla riga della posta in arrivo (30 lug)** — il ✓ era stato messo nella pila della conversazione, ma il posto dove serviva è l'ELENCO: lì il pallino blu si toglieva solo aprendo la mail. Ora `components/SegnaLettoRiga.tsx` sta fra le azioni della riga («✓ Letto», e «Non letto» su una già letta) e chiama `segnaLettoThread(id, letto)`, che agisce su **tutte le mail del thread** via `idsThread` — una riga in posta in arrivo è una conversazione, e marcare solo la più recente lascerebbe il pallino acceso (stessa ragione per cui archivia/cestina/spam sono `perThread`). Il pallino si spegne al **clic** (stato locale in `RigaMail`, torna indietro se il salvataggio fallisce) e il clic è fermato con `preventDefault`: la riga è un link, se no aprirebbe la mail.
- **L'anteprima di una mail HTML mostrava gli indirizzi, non le parole (30 lug)** — in elenco righe come `[https://assets.revolut.com/…/logo.png]https://click.revolut.com/f/a/YTRVAK…`: il «testo» di una mail HTML non è testo, è la conversione fatta dal server di posta, e `Messaggio.anteprima` è quella (`imap.ts`: `testo.replace(/\s+/g,' ').slice(0,200)`). Ora `ripulisciAnteprima` in `lib/citato.ts` toglie immagini `[url]`/`cid:`, link nudi, link markdown (tenendo il testo) e righe di separazione; se non resta **nessuna parola** torna stringa vuota — meglio una riga vuota di una riga di indirizzi, l'oggetto la mail la racconta già. ⚠️ Applicato **in lettura** (RigaMail, ListaInviati, cestino, rubrica, pila della conversazione) e non alla scrittura: così vale anche per le ~17.000 mail già in archivio, senza migrazione e senza toccare `corpoTesto`, che resta il testo vero della mail. Verificato su cinque casi, compreso quello dello screenshot.
- **La conversazione si LEGGE, non si naviga (29 lug)** — sotto la mail c'era un elenco di link: per leggere il quinto messaggio si cambiava pagina, si perdeva il segno e due messaggi non si potevano mai vedere insieme; per giunta ogni riga mostrava l'**oggetto**, che in un thread è identico per tutti e non aiuta a trovare niente. Ora `components/ConversazioneStack.tsx` è una **pila che si apre sul posto** (l'ultima mail già aperta, come fa Gmail), e le tre differenze volute rispetto a Gmail sono: (1) sulla riga chiusa c'è la **prima riga scritta davvero** (`anteprimaPulita` in `lib/citato.ts`, cioè senza la citazione) invece dell'oggetto; (2) il **corpo si chiede solo quando apri** (`corpoDiMessaggio` in actions.ts) — la pagina non trasporta il testo di venti mail per mostrarne una, ed è coerente con l'HTML che abita sul server; (3) **tastiera**: `j`/`k`, `Invio`, `r` (mai dentro un campo di testo, se no «r» è una lettera). Più: pallino sulle non lette, riga «da qui non hai letto», graffetta allegati, «Apri/Chiudi tutte». ⚠️ **Il testo citato** (`dividiCitato`) è il pezzo delicato: taglia al primo segno riconosciuto (italiano «… ha scritto:», inglese «… wrote:», Outlook `____`/`Da:`, righe `>`) e **se non riconosce non taglia** — nascondere per sbaglio un pezzo di messaggio vero è molto peggio del disturbo che si voleva togliere. Due soglie: la citazione si nasconde solo se supera 200 caratteri, e mai se la parte «nuova» è vuota (inoltro puro). Verificato su sei casi reali (risposta italiana, inglese, Outlook, mail senza citazione, citazione corta, inoltro puro). La mail aperta resta la scheda grande in cima: nella pila è segnata «qui sopra» e ci si torna con Invio. **«Segna come letto»** (subito dopo): un **✓** sulla riga delle non lette — che toglie il pallino **senza aprire** il messaggio — più «✓ Segna come letti (N)» in testa alla conversazione (`azioneMassa(ids,'letto')`) e la voce per esteso dentro il messaggio aperto. Lo stato «letta» ha una copia **locale e ottimistica** che torna indietro se il salvataggio fallisce (la spunta si muove al clic, non a fine giro), e il tasto sta **fuori** dall'intestazione della riga: un bottone dentro un bottone non è HTML valido, e dev'essere premibile senza aprire il messaggio.
- **«Riassumi le mail di oggi» (29 lug)** — il riassunto per periodo esisteva già (i tasti *oggi / ultimi 7 / ultimi 30* in `/rene`, `eseguiRene`), ma il riquadro «Chiedi a Renè» conosceva solo cestina/archivia/appuntamento/invia: chiederglielo **a parole** finiva in «Non ho capito il comando». Ora `interpretaComandoPosta` ha l'azione **`riassunto`** più il campo `periodo` (`oggi`/`settimana`/`mese`, vuoto = non detto → settimana), e `comandoPostaAnteprima` lancia lo stesso giro di Renè rispettando **l'ambito scelto nel menu** (tutta la posta / senza sezione / una sezione), poi porta su `/rene` dove il riassunto si legge insieme agli urgenti e alle proposte. Verificato con chiamate AI vere su 8 comandi: «Riassumi le mail di oggi» → `riassunto/oggi`, «fammi il punto della settimana» → `settimana`, «cosa è arrivato questo mese?» → `mese`, «riassumimi la posta» → periodo vuoto; i quattro comandi vecchi non sono regrediti. ⚠️ Due dettagli: se sei **già** su `/rene` il componente fa `router.refresh()` invece di `push` (un push sulla stessa pagina non rileggerebbe niente), e il giro resta una **Server Action lunga** come il tasto che c'era già — accettato lì, ma è il motivo per cui non va messo in un punto dove l'utente non sta aspettando.
- **Cestinare apre la mail SUCCESSIVA (31 lug)** — `Canc`/`#` (e il tasto «Cestina» sulla mail aperta) riportavano in posta in arrivo: dopo ogni mail smaltita si ricominciava da capo, ritrovare il punto e riaprire. Ora `smaltisciEProssimo(id, 'cestina'|'archivia')` in actions.ts fa le due cose insieme e torna l'id della prossima; il client ci va. ⚠️ Tre dettagli: la successiva si cerca **prima** di toccare questa (dopo, il filtro «non cestinate» cambierebbe sotto i piedi); «successiva» è quella **sotto nell'elenco**, cioè la più recente fra le più vecchie (`data: { lt }`, ordine `desc`), non la più nuova; e si resta **nello stesso posto** — stessa sezione (SPAM compreso), stessa casella attiva, stessa direzione. Se non ce n'è più, si torna all'elenco. Vale anche per `e` (archivia): è lo stesso gesto di smaltimento.
- **Su telefono lo SPAM era introvabile (31 lug)** — non era nascosto: stava **in fondo**, dentro l'elenco delle Sezioni, dopo le quattordici voci di Posta/Strumenti/Applicazioni. Su desktop lo si vede scorrendo l'occhio; dentro il cassetto di un telefono, no. La correzione è di **classificazione**, non di CSS: lo SPAM è una **cartella** — la crea l'app da sola (`assicuraSezioneSpam`), non l'utente — quindi ora sta nel gruppo **Posta** fra Archivio e Cestino, col suo contatore, ed è tolto dall'elenco delle Sezioni per non comparire due volte. ⚠️ Resta una `Sezione` nel database (lo smistamento, le regole e `spamId` non cambiano): è solo dove la si mostra. Nota per chi cercasse una causa nel CSS: il cassetto **scorre già** (`.sidebar { overflow-y: auto }`), il problema era la posizione.
- **In cima all'anteprima compariva il CSS della mail (31 lug)** — «`P {margin-top:0;margin-bottom:0;} Gentile Nicolò…`»: le mail di Outlook portano un blocco `<style>`, e chi converte in testo semplice toglie i TAG ma lascia il **contenuto** di quel blocco. Lo faceva anche il nostro `htmlAPlain` (`.replace(/<[^>]+>/g,'')` toglie `<style>` e `</style>`, non il CSS in mezzo) — quindi la riparazione del 30 lug, in qualche caso, sostituiva la sporcizia dei link con quella del CSS. Ora `htmlAPlain` toglie **prima** `<style>`, `<script>`, `<head>` e i commenti; `ripulisciAnteprima` fa da rete in lettura (vale su tutte le mail già in archivio, senza scrivere); e lo script di riparazione riconosce anche questo tipo di sporco. ⚠️ Il taglio del CSS scatta **solo se dentro le graffe c'è una proprietà con i due punti**: `{{NOME}}` di un testo pronto non deve sparire — verificato. Ripassate **105 mail**; ne restano **56** col CSS nel testo salvato e sono tutte **senza HTML** (mail vecchie alleggerite): in elenco si vedono pulite lo stesso grazie alla pulizia in lettura, ma il `corpoTesto` che legge l'AI resta sporco finché non le si rilegge dal server.
- **Scorciatoie da tastiera + le app richiamabili da ogni mail del thread (31 lug)** — `components/Scorciatoie.tsx` sta nel **layout** (vale ovunque) e accende le scorciatoie della singola mail leggendo l'id dall'indirizzo: `c` scrivi, `u` posta, `r`/`a`/`f` rispondi/a tutti/inoltra, `e` archivia, `#` o `Canc` cestina, `s` da leggere, `?` l'elenco. ⚠️ **Lettere singole, mai `Ctrl+…`**: `Ctrl+R` ricarica e `Ctrl+F` è la ricerca del browser — rubarle rompe cose che l'utente usa da vent'anni (è anche il motivo per cui Gmail usa `r`/`e`/`j`/`k` da soli). Mai dentro un campo di testo, mai coi modificatori. ⚠️ **Il conflitto vero era su «r»**: lo ascoltavano sia le scorciatoie globali sia la pila della conversazione, e siccome la globale è montata prima riceve il tasto per prima — avrebbe risposto alla mail in cima invece che al messaggio a fuoco. Ora l'accordo sta in `components/fuocoConversazione.ts`: la pila dichiara il messaggio a fuoco **solo quando l'utente lo sceglie** (j/k o clic) e la globale risponde a quello; il ramo `r` nella pila è stato tolto, perché lo stesso tasto in due posti sono due comportamenti. In più, nella pila ogni messaggio ha ora anche **«→ App»** e **«Delega Renè»**: in un thread la mail che interessa al registro è spesso una di mezzo, e prima bisognava aprirla a tutta pagina.
- **⚠️ Aprire una mail era lentissimo: le funzioni giravano a WASHINGTON (31 lug)** — l'header di produzione diceva `X-Vercel-Id: fra1::iad1`: la richiesta entrava a Francoforte ma la funzione girava in Virginia, mentre il database Supabase sta in **eu-west-1 (Irlanda)**. Ogni query attraversava l'Atlantico (~90 ms), e la pagina di una mail ne fa **una decina**, in parte una dopo l'altra: da lì i secondi di attesa. In `vercel.json` mancava `regions` — è la **trappola già documentata nel repo** per le altre app. Ora `"regions": ["dub1"]` (Dublino, accanto al database) e l'header dice `fra1::dub1`. ⚠️ `vercel.json` **rifiuta le chiavi che non conosce**: un `"//regions"` messo lì come commento fa fallire il deploy con «should NOT have additional property», quindi la spiegazione sta qui e non nel file. Seconda causa, sommata: la pagina caricava la conversazione con `messaggiThread` (findMany **senza select**) e si portava testo, HTML e traduzione di **ogni** mail del thread — misurati **48 KB su un thread di quattro mail**, per mostrare mittente, anteprima e data. Ora c'è `righeThread` con gli undici campi che servono; `messaggiThread` resta per chi le mail deve leggerle davvero (AI, riassunti) e per la vista «ampia», dove il raggruppamento guarda i partecipanti.
- **Gli interessi finivano quasi sempre su «Gifting» (31 lug)** — su una mail che proponeva l'ingresso nel network («collaborazione», «circuito Deluxy», «quota di attivazione») l'AI sceglieva *Gifting*. Non era testardaggine del modello: nello schema **solo Gifting aveva una spiegazione accanto** («regali aziendali»), la `guida` portava proprio quell'esempio, e il contesto aziendale dice che Deluxy consegna regali — quindi tutto spingeva lì. Ora ogni linea ha la sua **definizione vera**, presa dal master (Deluxy Scout, `supabase/migrations/0003_seed.sql`): Affiliazioni = programma di affiliazione partner, Re-seller = rivendita su deluxy.it, Consegne = guanti bianchi/assicurate/multi-città, Eventi & Catering = catering e allestimenti, Gifting = regali aziendali stagionali e macarons B2B, Food Supplier = fornitura B2B di torte e pasticceria, e Clientelling/Concierge/Magazzino segnate come **ferme** (in Scout sono STANDBY). Aggiunti «al massimo due linee» e «se la mail non le nomina, null». Verificato con chiamate vere: la mail reale passa da *Gifting* ad **Affiliazioni**, e su tre casi di controllo escono Gifting (preventivo regali), Consegne (consegna assicurata) e Food Supplier (torte per un bistrot). **Lezione**: in un elenco di valori, l'unica voce spiegata diventa la risposta preferita — o si spiegano tutte o non si spiega nessuna.
- **Stato commerciale e linee di interesse (29 lug)** — l'azione *Registra contatto* mandava anagrafica e note ma **non** le due cose che dicono a che punto è la relazione. Ora `stato` e `interessi` stanno nello schema come **enum chiusi** (gli 8 stati di `deluxy-anagrafiche/src/lib/stati.ts`, le 9 linee il cui master è Scout; copia di servizio in `appDeluxy.ts` con l'avvertenza che il padrone è altrove) e `normalizzaCurati` li riconduce al catalogo sia per l'AI sia per l'utente («In trattativa» → `in_trattativa`, «gifting; eventi e catering» → `['Gifting','Eventi & Catering']`), **buttando** ciò che non riconosce: uno stato fuori catalogo fa rifiutare l'INTERA richiesta da Anagrafiche («Stato non valido»), una linea inventata sporcherebbe il registro. Verificato con una chiamata AI vera su una mail che chiede un preventivo → `in_trattativa` + `Gifting`. ⚠️ **Ma la chiave `app-ai-mail` in Anagrafiche ha `scrittura: true, scritturaPartner: false`**, e questi due campi sono «curati dal team»: il registro li **scarta** in creazione e in merge. Perciò l'esito ora **confronta ciò che si è chiesto con ciò che è tornato** e scrive «Anagrafiche non ha applicato: stato «in_trattativa» (è rimasto «prospect»)» — meglio saperlo che credere che AI Mail non li mandi. Per farli valere serve `scritturaPartner: true` su quella chiave (decisione dell'utente: allarga i permessi di un'app verso il registro).
- **⚠️ Caso reale: l'azione automatica ha registrato NOI (29 lug)** — sezione «Commerciale» in modo automatico, mail *DELUXY | Presentazione* da `martina.calia@deluxy.it` a `caterina.nicora@zimmermann.com`. Il modello ha estratto `nome: "Deluxy"`, `email: martina.calia@deluxy.it` e ha aggiornato **la nostra stessa scheda** in Anagrafiche (`cmruwinnz0000i68olaz2oyoy`, «Deluxy», prospect), aggiungendoci Martina come contatto. Due cause: (1) i **destinatari non erano nel prompt** — la controparte, `@zimmermann.com`, il modello non l'ha mai vista; (2) la `guida` diceva «l'azienda che SCRIVE» ed `email` «di norma il mittente», che su una mail **partita da noi** significa noi. Sistemato con `controparte` + `normalizza`/`verifica` (voce sotto), e verificato rigiocando **i dati esatti dell'incidente**: `Deluxy / martina.calia@deluxy.it` → `Zimmermann / caterina.nicora@zimmermann.com`; su una mail tutta interna la controparte è `null` e l'invio si blocca; una mail in arrivo dal cliente resta invariata. ⚠️ **La scheda «Deluxy» in Anagrafiche è ancora lì** con sei colleghi come contatti: da decidere se ripulirla (non è stata toccata).
- **Cosa vede l'AI prima di mandare, e cosa NON si affida a lei** (29 lug, subito dopo): `estraiDatiAzione` riceveva solo mittente/oggetto/data/corpo — quindi un'istruzione dell'utente come «l'azienda deducila dal dominio del mittente **o del destinatario**» non poteva funzionare: i destinatari nel prompt non c'erano. Ora passano `destinatari`, i **nostri domini** e la **controparte** già risolta *nel codice* (`controparteDi` in `azioneSezione.ts`: il primo indirizzo fra mittente e destinatari che non è su una nostra casella) — su una mail che abbiamo mandato noi il mittente siamo noi, e la controparte sta fra i destinatari. In più `AzioneApp` ha ora una **`verifica(dati, ctx)`** che gira PRIMA dell'invio e può dire di no: per `anagrafiche.partner` blocca gli indirizzi del **nostro dominio** e i casi senza nome azienda. ⚠️ È codice e non prompt di proposito — «i contatti @deluxy non vanno creati» scritto nelle istruzioni è un suggerimento al modello, ma qui nasce una scheda vera in un registro aziendale e in automatico non c'è nessuno a controllare (è la lezione già pagata con la lingua di Renè). L'esito bloccato si registra con `esito: 'saltato'` (badge neutro «Non mandato», non rosso: non è un errore dell'app) **con i dati preparati**, perché un invio non fatto e non raccontato è indistinguibile da uno fallito. Vale su entrambe le strade, automatica e con conferma. Infine il **log**: `InvioApp.dati` era salvato ma non si vedeva — ora `components/DatiInviati.tsx` lo apre sotto l'esito, campo per campo in italiano, e l'esito di Anagrafiche dice **quale scheda** (nome + email, `creato`/`merged`, campi aggiornati) con **link diretto** a `/partner/<id>` invece della home.
- **Una SEZIONE può chiamare un'app Deluxy** (29 lug): finora le chiamate alle app partivano solo da un tasto («→ App» sulla riga o sulla mail aperta), e le `RegolaApp` sceglievano soltanto *quale* azione usare, senza farla partire. Ora la **sezione** può avere agganciata un'azione del catalogo di `appDeluxy.ts` (campi `Sezione.azioneId` / `azioneModo` / `azioneIstruzioni`, migrazione idempotente in `migrate-prod.mjs`): spostandoci dentro una mail **a mano**, o si apre la proposta già compilata (`chiedi`) o **la chiamata parte da sola** (`automatico`) — così «questo è un contatto nuovo» diventa un gesto solo. Si imposta sulla scheda della sezione in `/sezioni` (`components/AzioneSezione.tsx` → `salvaAzioneSezione`) e nel modulo di creazione. Il lavoro vero sta in `lib/azioneSezione.ts`: `azioneDiSezione` (lettura **difensiva**: prima della migrazione le colonne non esistono e la posta deve funzionare lo stesso) e `eseguiAzioneSezioneOra` (l'AI estrae i dati → si chiama l'app → **l'esito finisce sempre in `InvioApp`**, cioè sotto la mail in «Risposte dalle app»: questo esce di casa verso un'altra app, muto non può essere). ⚠️ **Tre scelte da non disfare per sbaglio**: (1) il gancio è sullo **spostamento manuale** (`spostaInSezione` e `azioneMassa('sposta')`), non sulla sezione in sé — l'AI e le regole scrivono `sezioneId` altrove e non passano di lì, perché un errore di smistamento creerebbe schede vere dentro un registro aziendale; (2) l'esecuzione automatica sta dentro **`after()`**, se no spostare una riga vorrebbe dire aspettare l'AI più una chiamata di rete (lezione già pagata con la spunta delle attività); (3) è **idempotente** — se quella mail ha già mandato quell'azione con esito `ok` non si rifà, altrimenti bastava rimetterla nella sezione per creare un doppione. In massa parte **solo** l'automatica: «chiedi» vorrebbe dire un dialogo per ogni mail, quindi si dice soltanto quante aspettano conferma. **L'esito si vede dalla posta in arrivo** (29 lug, subito dopo): siccome la chiamata gira in `after()`, al ritorno dello spostamento non esiste ancora — `components/dopoSpostamento.ts` mostra «sto mandando…» e poi interroga `esitoAzioneMessaggio(messaggioId, azioneId)` ogni 2s (fino a 45s) finché non trova una riga `InvioApp` **più recente dello spostamento**, e mostra quella; se trova solo una riga vecchia vuol dire che l'invio è stato saltato perché già fatto, e lo dice. «È partita» non è una risposta, e dalla lista non si vedrebbe mai il seguito. Il `Flash` ha ora un **tono**: verde col ✓ per il fatto, rosso col ⚠ e 9 secondi per il non riuscito — un errore annunciato in verde con la spunta direbbe il falso. In più `InvioAppDialog` si è spostato **nel layout**: ora risponde all'evento `aimail:app` da qualunque pagina (era montato in `/` e `/messaggio/[id]`, quindi da «Posta inviata» non esisteva — stessa lezione del 27 luglio, un dialogo montato in una pagina sola è una funzione che esiste solo lì).
- **Conversazioni CHIUSE** (23 lug): una pratica finita si segna come **chiusa** — dalla pagina della conversazione («✓ Segna come chiusa» / «↻ Riapri») o dalla lista Thread (azione «Chiudi»/«Riapri»). Effetti: **esce dai «Top thread»** (`ColonnaTopThread` scarta i gruppi chiusi) e in elenco porta l'etichetta **«Chiuso»** con la riga in tono più smorzato. **Non è un archivio**: le mail restano dove sono e una risposta nuova si vede lo stesso. Nuovo modello `ThreadChiuso` + migrazione idempotente e FK; `lib/threadChiusi.ts` (`idsThreadChiusi`, `threadEChiuso`, letture difensive); action `cambiaThreadChiuso`. Come per nome e PLUS AI, il segno si scrive su **ogni mail** della conversazione, così non si perde agganciando mail vecchie.
- **Posta inviata: si vede la conversazione VERA e si cerca per nome** (23 lug): due difetti. (1) Il raggruppamento girava **solo sugli inviati**, quindi una mia risposta dentro uno scambio di venti messaggi risultava «1» e non si capiva che facesse parte di un thread. Ora si raggruppa su una **finestra di tutta la posta** (2000 righe, solo i campi per raggruppare) e si tengono i gruppi con almeno una mia mail: il numero sulla riga è ora **la conversazione intera** (nel titolo: «N messaggi nella conversazione · M tuoi»). ⚠️ Le **azioni** (cestina/sposta) restano sulle **sole mie mail** (`ids` = solo le uscite): da «Posta inviata» non si deve cestinare anche la posta ricevuta. (2) La ricerca non trovava i **nomi delle conversazioni**: ora include le chiavi di `chiaviPerNome`.
- **API: la posta di un ORDINE** (23 lug): **`GET /api/v1/messaggi?ordine=2529`** (stessa auth delle altre v1: `x-api-key` + `x-utente`). Serve alle altre app — **deluxy-orders** in primis, e Partner — per mostrare «la posta di questo ordine» avendo solo il numero, senza sapere chi ha scritto. È il gemello di `?cliente=` (che Finance già usa). Parametri: `limite` (default 30, max 100), `giorni` (default 365), `direzione=entrata` (di default TUTTE: di un ordine interessa anche cosa abbiamo risposto). ⚠️ Il problema vero è il **rumore** (cercare «2529» prende importi, telefoni, altri codici), quindi il filtro non è un semplice `contains`: (1) match come **token isolato** — niente cifre attaccate, così «112529» e «25290» vengono scartati, ma «106654/26» resta valido; (2) ogni risultato dichiara **`dove`** ('oggetto' | 'corpo') e **`sicuro`** (true se è nell'oggetto o se nel corpo ha accanto «ordine/order/rif/#»), e i sicuri vengono per primi — chi chiama decide se mostrare gli incerti. La risposta include `quanti` e `sicuri`. **Verificato 8/8** sui casi limite.
- **Dimensione VERA dal server (allegati compresi)** (23 lug): il peso era calcolato dai **soli corpi salvati**, quindi **senza allegati** — una mail con un catalogo da 20 MB risultava di poche KB e l'ordinamento per dimensione era inutile proprio dove serve. Ora si chiede la dimensione **al server IMAP** (`RFC822.SIZE`): `dimensioniDalServer` in imap.ts fa una FETCH di **soli numeri** (nessun contenuto scaricato), quindi è veloce anche su archivi grandi — verificato che imapflow supporti `size: true` e restituisca `msg.size`. `ripassaDimensioni` (sync.ts) riempie a blocchi (400) le mail con `dimensione: null`, raggruppando per account+cartella (INBOX / Inviata). Gira in due punti: nel lavoro di background dello storico e su **`POST /api/dimensioni`**, che la lista chiama **quando ordini per dimensione** (una volta, solo se manca qualche peso, con avviso «Chiedo al server la dimensione vera…»). ⚠️ Le stime scritte dal vecchio backfill vengono **azzerate** in `migrate-prod.mjs` (si riconoscono perché coincidono ESATTAMENTE con la somma dei corpi: una dimensione vera è sempre maggiore, ci sono almeno gli header), così il ripasso le rifà con il valore reale.
- **Ordine per DIMENSIONE: ora funziona davvero (+ peso visibile)** (23 lug): il campo `Messaggio.dimensione` è popolato solo dalle mail scaricate dopo la sua introduzione; il backfill SQL in `migrate-prod.mjs` non bastava (restava tutto a 0 → ordinare per dimensione non produceva alcun effetto). Ora **non si dipende più dal backfill**: in posta in arrivo e inviata, per le sole righe con dimensione mancante si ricava dai byte dei corpi con **una query di soli interi** (`octet_length(corpoTesto)+COALESCE(octet_length(corpoHtml),0)`, `Prisma.sql` + `Prisma.join` sugli id mostrati). **Costo zero quando il dato c'è già** (la query parte solo se `senzaPeso.length > 0`). In più, ordinando per dimensione ogni riga **mostra il peso** (`pesoLeggibile`, «312 KB» / «—»): il criterio diventa verificabile a colpo d'occhio invece di dover indovinare se funziona.
- **Switch fra account (multi-casella)** (23 lug): con più caselle collegate, in cima alla sidebar c'è il **selettore casella** («Tutte le caselle» / casella A / B) — `SelettoreAccount` → `impostaAccountAttivo` (cookie `account_attivo` per device, 1 anno). La scelta **filtra la posta**: posta in arrivo e inviata mostrano solo quella casella, e i **contatori** della sidebar (in arrivo, cestino, badge sezioni) si riferiscono a quella. Lettura server in `lib/accountAttivo.ts` (`accountAttivoId` VALIDA che l'id sia dell'utente; `caselleUtente`). **Mittente scegliibile**: in mail nuova (`/scrivi` → `ComposizioneNuova`) il campo **«Da»** è un menu delle caselle (predefinita l'attiva); `inviaNuovaMail` usa `mittenteAccountId` invece del sempre-primo-account. Con UNA sola casella nulla cambia (selettore e menu Da non compaiono). Le risposte continuano a partire dalla casella che ha ricevuto l'originale.
- **«+» azione rapida nella sidebar (popup, senza cambiare pagina)** (23 lug): accanto alle applicazioni compare al passaggio del mouse un **«+»** (`PiuAzione`) che apre un **popup** per fare l'azione senza lasciare la posta: **Calendario → nuovo appuntamento**, **Attività → nuova attività**, **Renè AI → Chiedi a Renè** (riusa `ComandoRene`). Dialogo unico `AzioneRapida` montato nella `Shell` (quindi su ogni pagina), aperto via evento `aimail:azione-rapida`. I form sono compatti e riusano `creaEvento`/`creaAttivitaManuale`; per invitati/ricorrenze il popup rimanda al Calendario. Solo **desktop** (`.nav-piu` nascosto sotto 900px: su mobile la sidebar è un cassetto). Voce sidebar estesa con `azione?: TipoAzione`.
- **Alias dei contatti (capito anche dall'AI)** (23 lug): in Rubrica ogni contatto ha una colonna **Alias** editabile (`EditaAlias` → `salvaAlias`). Nuovo modello **`AliasContatto`** (`[utenteId, email]` unica) + migrazione idempotente e FK. L'alias entra nella rubrica (`elencoContatti` lo fonde e ci si può cercare) e — punto chiave — nella lista che va all'**AI**: `contattiPerAI(contatti)` mette l'alias come «nome» (`Alias (Nome vero)`), usato da tutti i punti dove Renè scrive (delega, «Chiedi a Renè», invio). Così «invia a Nicolò Deluxy» funziona: nel comando `invia`, se il destinatario non è un'email si **risolve** su alias/nome della rubrica → indirizzo (se non trovato, lo dice e invita ad assegnare un alias). `lib/contatti.ts`: `aliasContatti`, `contattiPerAI`, letture difensive.
- **«Chiedi a Renè» capisce anche «invia mail a…»** (23 lug): il box `/rene` conosceva cestina/archivia/appuntamento ma non l'invio. Ora `interpretaComandoPosta` ha l'azione **`invia`** con `destinatario` + `istruzione`; `comandoPostaAnteprima` per 'invia' fa scrivere la mail a `scriviMailNuova` (stile, firma, contesto), forza il **destinatario detto dall'utente** (vince su quello che sceglierebbe l'AI), crea una **bozza** (`origine: ai`, `modo: nuova`) e torna `{fatto:true, vaiA:/scrivi?bozza=…}` — il componente ci porta. ⚠️ **Non invia da sola**: prepara la bozza e la apri per controllarla e mandarla (regola di prodotto: l'AI propone, l'utente invia). Testo d'aiuto e messaggio d'errore aggiornati.
- **«Svuota cestino» ora cancella davvero sul server** (23 lug): si cancellava per **UID memorizzato**; se il server aveva cambiato UIDVALIDITY quell'UID non esisteva più (niente cancellato) o puntava a un'ALTRA mail (rischio di cancellare la sbagliata). Ora `eliminaDalServer` prende `{uid, messageId}[]` e **trova la mail per Message-ID** (`client.search({header:{'message-id':…}})`) ricavando l'UID ATTUALE in quella cartella; se non la trova lì, la salta (niente cancellazioni a caso). L'UID memorizzato resta solo come ripiego quando manca il Message-ID. Torna il numero **realmente** cancellato (`messageDelete` fa flag \Deleted + EXPUNGE = definitivo), non quello tentato — così il messaggio all'utente è veritiero. `svuotaCestino` seleziona ora anche `messageId`.
- **Ordinamento in posta in arrivo e inviata** (23 lug): barra «Ordina per» con **Data / Mittente (Destinatario negli inviati) / Oggetto / Dimensione**, ognuno asc/disc (ripremendo si inverte; verso naturale = data e dimensione decrescenti). L'ordinamento è **lato client** sulle righe già caricate (istantaneo, nessun giro al server): `Ordinamento.tsx` (`BarraOrdinamento` + `confrontaRighe`), usato da `ListaMail` e `ListaInviati`. Per la **dimensione** nuovo campo `Messaggio.dimensione` (byte del grezzo): popolato in `converti` (`source.length`) e in `registraInviato` (`raw.length`); migrazione idempotente `ADD COLUMN`. A parità di campo, criterio secondario stabile = la più recente. ⚠️ Le mail dello STORICO (dimensione null) sono riempite una-tantum in `migrate-prod.mjs` con `octet_length(corpoTesto)+octet_length(corpoHtml)` (idempotente, solo righe NULL): senza questo l'ordine per dimensione era inerte perché quasi tutto era 0. Coerenza imperfetta: le nuove mail hanno il byte reale del grezzo (allegati inclusi), lo storico la sola lunghezza del corpo — per i testi è confrontabile, per vecchie mail con allegati pesanti la stima è più bassa (recuperabile solo riscaricando dal server).
- **Riassunto thread INCREMENTALE (meno lavoro AI) + ricerca fra i riassunti** (23 lug): (1) `riassumiThreadOra` non rimacina più tutto il thread: se esiste già un riassunto fatto su meno messaggi e ne mancano **≤8**, passa all'AI il **riassunto precedente + solo le mail nuove** (indici GLOBALI preservati per i link «apri»), con l'istruzione di aggiornare (`riassumiThread` accetta `precedente` e messaggi con `idx`). Se ne mancano tante o il conteggio è incoerente, si rifà completo (più sicuro). `riassuntoInTesto` rende il vecchio in testo. (2) `/riassunti` ha la **ricerca**: contenuto del riassunto (il JSON salvato contiene sintesi/punti/sospesi → `contains`), nome dato al thread (`chiaviPerNome`) e oggetto del capostipite.
- **«Ne restano 189» su un thread di 65 — RISOLTO** (23 lug): nella pagina della conversazione era montato il lettore AI **globale** (`AnalisiAIInbox`), il cui `restano` è il totale delle mail AI+ di TUTTA la casella — messo lì sembrava riferirsi ai messaggi di quel thread. Ora la pagina non usa più il conteggio globale: `RiassuntoConversazione` ha `autoAggiorna` (thread AI+ e riassunto vecchio → `messaggiVisti < conversazione.length` o assente) e rigenera **solo QUESTA conversazione** all'apertura, mostrando «L'AI sta aggiornando il riassunto di questa conversazione…». Il lettore globale resta solo nella AI Inbox, dove il conteggio ha senso.
- **La lettura AI+ non crea più task-tappabuchi** (23 lug): `analizzaMessaggioOra` creava SEMPRE almeno un'attività (fallback «Gestire: <oggetto>» quando l'AI non trovava azioni). Con la lettura automatica dei contatti/thread AI+ questo generava **un task per ogni mail** — il diluvio in `/attivita`. Ora la funzione ha un parametro `origine`: **'priorita'** (dai tu una priorità → garantisce almeno un promemoria, invariato) vs **'auto'** (lettura AI+ in sottofondo → legge e riassume, ma crea un'attività SOLO se l'AI ne trova una vera; niente fallback). Solo `/api/analizza-ai-inbox` passa 'auto'; tutti gli altri chiamanti (priorità, rianalizza, inviata con priorità) restano deliberati. NB: le bozze auto restano come prima (solo se `serveRisposta`).
- **Attività doppie — RISOLTO (idempotenza + pulizia)** (23 lug): l'analisi cancella-e-ricrea le attività di una mail in **due passi non atomici**; da quando il lettore AI gira in sottofondo (3 mail/giro) può sovrapporsi a un'analisi scatenata dall'utente → entrambe cancellano e ricreano → doppioni. Ora `creaAttivitaUnica` (sync.ts) **salta la creazione se esiste già** un'attività identica e non fatta per lo stesso messaggio (stesso titolo) — usata sia dall'analisi AI sia dalle attività da regola. **Pulizia una-tantum** in `migrate-prod.mjs`: DELETE idempotente dei doppioni non-fatti (stessa `utenteId`+`titolo`+`messaggioId`/`contattoEmail`, tiene la più vecchia; non tocca le fatte). ⚠️ NON è un vero fix di concorrenza (due `findFirst` simultanei possono ancora passare entrambi): riduce drasticamente i casi e la pulizia periodica sana i rari residui; un lock DB vero servirebbe solo se ricomparissero.
- **Calendario: doppio clic su un giorno → nuovo appuntamento** (23 lug): le celle da OGGI in poi (`chiave >= oggiIt`, confronto fra stringhe "YYYY-MM-DD") si aprono con **doppio clic** e precompilano il form «Nuovo appuntamento» su quella data. `CellaGiorno` (client) avvolge il contenuto reso lato server e, su doppio clic fuori da un evento/attività, lancia l'evento `aimail:nuovo-evento` con `{giorno}`; `NuovoEvento` lo ascolta, si apre, precompila (`key` sull'input date per rimontarlo) e scrolla in cima. I giorni passati non si aprono (cursore/hover solo sulle celle apribili). **Sidebar**: «Attività» spostata da Applicazioni a **Strumenti** (è materiale di lavoro sulla posta, non un'app a sé).
- **Renè rispondeva in italiano a mail inglesi — RISOLTO in codice** (23 lug): la lingua veniva presa da `Messaggio.lingua`, che però lo riempie **solo la traduzione automatica** (`traduzioneAuto`): chi legge l'inglese la tiene spenta → campo **null** proprio sulle mail straniere → il prompt «usa la lingua della conversazione» e il modello, vedendo le NOSTRE risposte italiane nel thread, sceglieva italiano. Ora c'è **`lib/rilevaLingua.ts`**: riconoscimento deterministico (punteggio su parole comuni di it/en/fr/es/de/pt/nl, **nessuna chiamata AI, costo zero**) che scarta righe citate e intestazioni d'inoltro, così decide sul testo NUOVO. `linguaPerRisposta()` in sync.ts: usa `Messaggio.lingua` se c'è, altrimenti riconosce dall'**ultima mail RICEVUTA** del thread (mai dalle nostre uscite); usata da tutti e tre i punti in cui Renè scrive (delega, «Chiedi a Renè», esecuzione attività). Il prompt ora dichiara la lingua come **regola assoluta** che vince sul resto della conversazione. **Verificato 7/7** su casi reali, compreso «inglese con citazione italiana sotto» (la trappola esatta di questo caso). Lezione ribadita: quel che deve essere vero si applica nel codice, non si affida al prompt.
- **Notifiche push: una per mail (non più una sola) + anti-doppione** (23 lug): `notificaNuoveMail` mandava **una sola notifica riepilogativa** anche per dieci mail («ne arriva solo una»). Ora manda **una notifica per ogni mail** (fino a 5, poi un «e altre N»), ciascuna con un **`tag` diverso** (il messageId) — il service worker usa `tag`+`renotify` così sul telefono si impilano invece di sostituirsi. Nuovo campo **`Messaggio.notificatoIl`** (+ migrazione idempotente): le mail già notificate non tornano, così cron e giro interattivo non le rimandano; la selezione non usa più `creatoIl >= da` ma `notificatoIl: null`. Ogni notifica apre direttamente `/messaggio/<id>`. ⚠️ **Causa di fondo del «non arrivano», NON di codice**: l'unico trigger è il cron `/api/sync`, e su **Vercel Hobby il cron gira ~1 volta al giorno** (la pianificazione `*/5` in vercel.json è ignorata sotto il giorno). Per notifiche tempestive serve un **cron esterno** (es. cron-job.org) che chiami `GET /api/sync?token=<CRON_SECRET>` ogni pochi minuti, oppure Vercel Pro. Il codice è pronto: appena il cron gira spesso, le notifiche arrivano puntuali e distinte.
- **L'AI non parte più a ogni apertura della posta** (23 lug): il lettore automatico `AnalisiAIInbox` era montato su OGNI vista della posta in arrivo, quindi ogni apertura di «In arrivo» faceva comunque una chiamata a `/api/analizza-ai-inbox` (query DB sempre; OpenAI solo se c'era davvero da leggere). Ora gira **solo nella AI Inbox** e nelle **conversazioni AI+** (dove era già). Nessuna rilettura in loop: `analizzaMessaggioOra` marca `analizzatoIl` e `riassumiThreadOra` aggiorna `messaggiVisti`, quindi il costo AI scatta solo su contenuto nuovo — il problema era solo il numero di chiamate a vuoto, non un ciclo.
- **«Scarica» di un allegato grande non faceva nulla — RISOLTO (streaming)** (23 lug): il download restituiva l'allegato **bufferizzato in memoria** (`new Response(new Uint8Array(buf))`), ma su Vercel una risposta bufferizzata non può superare **~4,5 MB**: un catalogo lo supera e il clic non produceva nulla. Ora, quando c'è la `parte` (il caso normale), `/api/allegato` **trasmette a flusso**: `scaricaParteStream` (imap.ts) tiene la connessione IMAP aperta e restituisce il Readable; la rotta lo avvolge in un `ReadableStream` web e chiude la connessione su `end`/`error`/`cancel`. A flusso il limite dei 4,5 MB non si applica. Resta la strada bufferizzata (per indice) come riserva quando la parte non è nota. ⚠️ **Nota aperta**: `/api/allegati-zip` («Scarica tutti») costruisce lo zip in memoria e lo restituisce bufferizzato → stesso tetto ~4,5 MB per archivi grandi; da convertire a flusso se serve.
- **«Chiedi a Renè» dentro la scrittura + graffetta nella barra** (23 lug): mentre scrivi una risposta non c'era modo di farti aiutare (il «Delega Renè» sta fuori, prepara una bozza e ti sposta di pagina). Ora sopra l'editor c'è **«AI Chiedi a Renè di scrivere»**: digli cosa dire e il testo compare **nell'editor aperto** — nuova `testoRispostaRene` in sync.ts (riusa `scriviRisposta` con thread, stile, firma, istruzioni mirate e **lingua**) esposta dall'action `chiediARene`; nessuna bozza creata, nessuna navigazione. Se c'è già del testo scritto glielo si passa, così «rendila più formale» lavora su quello. ⚠️ L'editor prende il contenuto **una sola volta** (se no il cursore salterebbe a ogni tasto): per sostituirlo si rimonta con una `key` che cambia. Aggiunta anche la **graffetta 📎 accanto al link** nella barra: è una `<label>` legata al campo file degli allegati (`idAllegati`/`idInput`), quindi apre il selettore senza ref né stato in più.
- **Allegati fermi su «Carico dal server…» — RISOLTO alla radice** (23 lug): due cause, la seconda introdotta poche ore prima. (1) **`leggiAllegati` scaricava l'INTERO messaggio** solo per elencare nomi e dimensioni: con un catalogo da decine di MB la funzione scadeva. Ora chiede al server la sola **BODYSTRUCTURE** (pochi byte, istantanea) e torna anche **`parte`**, l'indirizzo IMAP del pezzo; il download singolo usa `scaricaParte` (`client.download`) e **scarica solo quell'allegato**, non tutta la mail (la vecchia strada per indice resta come riserva). (2) Il riquadro invito, montato su ogni mail **con allegati**, faceva un SECONDO scarico completo in parallelo: ora il filtro non guarda più `allegati > 0` ma solo le parole spia nel testo (gli inviti le hanno sempre). **Lezione**: elencare non è scaricare — se per mostrare una lista si tira giù il contenuto, prima o poi arriva la mail che fa scadere tutto.
- **Il riquadro dell'invito non compariva** (23 lug): era mostrato solo con `messaggio.allegati > 0`, ma la parte **`text/calendar`** di un invito Exchange **spesso non viene contata come allegato** — quindi proprio sugli inviti veri non compariva. Ora il componente è montato su tutte le mail in ARRIVO e il filtro sta in `leggiInvito`: prima un controllo **a costo zero** sul testo già salvato (parole spia: vcalendar/invito/meeting/quando/organizer…), e solo se promette bene si legge dal server — così le mail normali non pagano una connessione IMAP e gli inviti si vedono comunque.
- **Inviti di calendario RICEVUTI: Accetta / Forse / Rifiuta** (23 lug): un invito Outlook/Google arriva come parte **`text/calendar`** (METHOD:REQUEST) e non c'era modo di rispondere. Ora sulla mail compare il riquadro **Invito a un appuntamento** con i tre pulsanti: l'appuntamento entra in **Calendario** (con «Rifiuta» no; con «Forse» il titolo lo dice) e all'organizzatore parte la **risposta iCal METHOD:REPLY** con `PARTSTAT`, quella che aggiorna lo stato del partecipante nel suo calendario (riusa `spedisci`/`registraInviato`, `DaInviare.ics` c'era già). Parser **deterministico** in `lib/invitoRicevuto.ts` (`leggiIcs`, `rispostaIcs`): niente AI, una data non si indovina. **Verificato** su un iCal Exchange reale: righe spezzate (folding) ricomposte, virgole protette, `TZID` Windows («W. Europe Standard Time») e forma `…Z` → entrambe 11:00 italiane, giornata intera, METHOD:CANCEL mostrato come «annullato» senza pulsanti. Si legge dal server DOPO il render (come gli allegati), solo se la mail ha allegati: aprire resta istantaneo e, senza invito, non compare nulla.
- **Lo stato della lettura AI sta dentro «Punti di vista della conversazione»** (23 lug): la riga di stato era in cima al pannello della conversazione, lontana dal risultato. Ora `RiassuntoConversazione` accetta una prop `stato` (ReactNode) e la mostra **dentro il riquadro**, sopra il testo: si legge lì cosa sta facendo l'AI e lì compare il riassunto quando è pronto.
- **Lettura AI: si vede cosa fa, e si può forzare** (23 lug): «la lettura AI non parte» era **indistinguibile** da «ha già letto tutto»: il componente non mostrava nulla in entrambi i casi, e ingoiava anche gli errori (500/401/rete) tornando in silenzio. Ora `AnalisiAIInbox` ha `sempreVisibile` (usato nella pagina della conversazione): dice sempre se sta leggendo, se ha finito, se non c'era niente da leggere o **qual è l'errore**; e ha il pulsante **«Fai leggere ora»** per forzare un giro anche a scheda in secondo piano. La rotta torna in più `dettaglio` (contattiAI, mailThreadAI, mailDaLeggere, riassuntiDaRifare) per capire *perché* non fa niente. **Lezione**: un lavoro in sottofondo che non dice mai nulla non è diagnosticabile — né dall'utente né da chi lo sviluppa.
- **L'AI legge davvero i thread AI+ (ovunque) e niente più form istruzioni** (23 lug): un thread con PLUS AI restava non letto perché `AnalisiAIInbox` era montato **solo nella vista AI Inbox** — «sempre» deve voler dire con l'app aperta, non su una scheda sola. Ora gira in **qualunque vista** della posta e anche nella **pagina della conversazione** quando quel thread ha il PLUS AI (di norma è un giro a vuoto: una chiamata che torna `restano: 0`). Inoltre `/api/analizza-ai-inbox` tiene aggiornato il **riassunto della conversazione**: `threadAIdaRiassumere` raggruppa le mail dei thread AI+ e confronta `RiassuntoThread.messaggiVisti` col numero di messaggi — se il riassunto manca o è vecchio ne rigenera **uno per giro** (`riassumiThreadOra`), ed è questa la parte «riassunto aggiornato con l'ultima mail». Tolto dalla pagina della conversazione il form **«Istruzioni AI per questa conversazione»** (c'è «Delega Renè», che si scrive a parole): le istruzioni già salvate **restano attive** — le rilegge `istruzioniMirate` in sync.ts — e quelle per CONTATTO restano modificabili dalla Rubrica.
- **AI+ visibile e commutabile dalla lista Thread; «Cestina tutta la conversazione» dentro il thread** (23 lug): in `/thread` ogni riga mostra il **marchio AI** se la conversazione ha il PLUS AI e ha l'azione **«+ AI» / «✓ AI+»** per accenderlo o spegnerlo lì (variante `riga` di `ThreadAIToggle` dentro `AzioniThread`; la pagina calcola l'appartenenza con `idsThreadAI` + `Set`, una query in parallelo ai nomi). Nella pagina della conversazione, accanto al PLUS AI, c'è ora **«Cestina tutta la conversazione»** (`CestinaThread` → `cestinaThread`, già esistente per la lista): chiede conferma dicendo **quante** mail tocca, poi torna alla posta. Le mail restano recuperabili dal Cestino.
- **PLUS AI anche sulle CONVERSAZIONI** (23 lug): il PLUS AI non è più solo del contatto. Dalla pagina di una conversazione, pulsante **«+ AI su questa conversazione»** (`ThreadAIToggle` → `cambiaThreadAI`): l'AI legge sempre quelle mail e il thread entra nella **AI Inbox** (la vista fa ora `OR` fra mittenti-contatto-AI e id delle mail dei thread AI). Nuovo modello **`ThreadAI`** + migrazione idempotente e FK. ⚠️ Come per `NomeThread`, il segno si scrive su **ogni mail** del thread (`createMany` su tutti i membri): la chiave del thread cambia agganciando mail vecchie e le liste vedono solo la posta recente — segnando i membri, la conversazione si riconosce da qualunque sua mail (`lib/threadAI.ts`: `idsThreadAI`, `threadHaAI`, letture difensive). **La regola** «l'AI legge sempre il contenuto salvo che esista già un riassunto aggiornato all'ultima mail» vale ora per contatto **e** per conversazione: `/api/analizza-ai-inbox` unisce le due sorgenti e salta le mail con `analizzatoIl` valorizzato (quando ne arriva una nuova, è quella a risultare da leggere).
- **«Top thread · 30 giorni» in posta in arrivo** (23 lug): in cima alla colonna destra (sopra l'agenda) le **5 conversazioni più corpose** degli ultimi 30 giorni — più messaggi in cima, a parità la più recente. Mostra il **nome dato a mano** se c'è (altrimenti l'oggetto), numero di messaggi, quante parti, data dell'ultimo scambio e il pallino se ci sono non lette; link «Tutti →» alla pagina Thread. Componente `ColonnaTopThread` (server): una query leggera (solo i campi per raggruppare, finestra 1500, SPAM e cestino esclusi) + `raggruppa` + `nomiPerGruppi`. Lettura difensiva: in caso di errore non mostra nulla invece di rompere la posta. Solo desktop, come il resto della colonna.
- **RISOLTO: il nome di una conversazione «spariva» dopo un aggancio** (23 lug): dato un nome (es. «HBI») a un thread in cui erano state agganciate mail vecchie dalla ricerca, il nome non si trovava più. ⚠️ **Causa (difetto di progetto del giorno prima)**: `chiaveThread` = id del messaggio **più VECCHIO del gruppo**, e il gruppo dipende da quali mail sono caricate — la pagina di un messaggio raccoglie anche le agganciate a mano di qualunque età, le liste solo la posta recente. Agganciando una mail vecchia la chiave cambiava e la chiave calcolata dalle liste non combaciava più con quella salvata. **Correzioni**: (1) il nome si SCRIVE su tutte le mail della conversazione (`createMany` su ogni membro, previa `deleteMany`) così qualunque mail presente nella finestra lo porta; (2) si LEGGE con **`nomiPerGruppi`** che cerca su tutti i membri del gruppo (sostituisce `nomiPerChiavi` in posta in arrivo, inviata, clienti, thread e nella ricerca «Aggancia»); `nomeDiThread` accetta i membri; (3) in `/thread` la ricerca per nome carica **esplicitamente** le mail che portano il nome (anche vecchie) e le loro compagne agganciate a mano, se no una conversazione battezzata e non più toccata restava fuori dalla finestra. **I nomi già dati vanno ridati una volta** per essere riscritti su tutti i membri (quelli vecchi restano leggibili se la mail-chiave è nella finestra).
- **Anche il CORPO viaggia a pezzi (inoltri pesanti) + bozze auto che sparivano** (23 lug): dopo il fix sugli allegati restava l'errore «An unexpected response was received from the server» su mail **senza allegati**: a sfondare il tetto dei 4,5 MB era il **corpo** (inoltri che si portano dietro le immagini incorporate). ⚠️ Stessa causa, **anche per il salvataggio bozza**: `salvaMinuta` è una Server Action con lo stesso limite → il salvataggio automatico falliva **in silenzio** (l'hook ingoiava l'errore) ed ecco perché «le bozze automatiche non erano in Bozze». Ora `corpoTroppoGrande()` (>1,5 MB) fa caricare il corpo a pezzi con lo stesso meccanismo degli allegati (`caricaCorpoGrande` → campo `corpoGruppo`), e il server lo ricompone con **`leggiCorpo(form, utenteId)`**, usato da `inviaMessaggio`, `inviaNuovaMail` **e `salvaMinuta`**. Entrambe le composizioni passano ora da `formPronto()` (invio, «Salva bozza» e salvataggio automatico: un solo punto). L'hook `useBozzaAuto` ha lo stato **`errore`** e la UI mostra «⚠ Bozza NON salvata»: un salvataggio fallito non deve mai essere muto.
- **AI Inbox: l'AI legge da sola le mail dei contatti col PLUS AI** (23 lug): prima l'analisi partiva solo dando una priorità, quindi in AI Inbox restavano mail mai lette dall'AI. Ora, con la AI Inbox aperta, `AnalisiAIInbox` chiama in loop **`POST /api/analizza-ai-inbox`** (rotta, non Server Action: non blocca i clic) che analizza **3 mail per giro** (`analizzaMessaggioOra`) e torna `{fatti, restano}`; a fine giro un solo `router.refresh()`. **Il «già aggiornato» è `analizzatoIl`**: una mail già letta non si rilegge, e quando ne arriva una più recente è QUELLA a risultare da analizzare — così il quadro segue sempre l'ultima mail senza rifare il thread. Si ferma se la scheda va in secondo piano o se un giro non conclude nulla (chiave/quota OpenAI): si riprende alla prossima apertura.
- **Mail pesanti: ora partono (allegati caricati a pezzi)** (23 lug): con allegati oltre pochi MB l'invio moriva con «Application error: a client-side exception…» e la mail NON partiva. ⚠️ **Causa**: su Vercel il corpo di UNA richiesta non può superare **4,5 MB** — limite di piattaforma, prima del nostro codice; `serverActions.bodySizeLimit: 20mb` in `next.config.ts` era quindi una promessa che la piattaforma non poteva mantenere, e la Server Action dell'invio veniva rifiutata al trasporto (da cui la schermata bianca). **Soluzione**: se gli allegati superano 3 MB, il browser li carica PRIMA a pezzi da 3 MB su **`POST /api/allegato-carica`** (`components/caricaAllegati.ts`), che li deposita in **`AllegatoCaricato`** (bytea, indicizzato per `[utenteId, gruppo]`); l'invio porta con sé solo `allegatiGruppo` e `leggiAllegati` li **ricompone** in ordine (`lib/allegatiGrandi.ts`), poi si cancellano (più pulizia dei residui oltre 24h). Barra «Carico gli allegati… %» accanto ai pulsanti. Inoltre l'invio è **avvolto in try/catch** in entrambe le composizioni: un errore ora si legge («La mail è troppo pesante…»), non diventa una pagina bianca. Migrazione idempotente + FK.
- **APP DELUXY: modulo al posto del JSON + aggancio all'azienda** (23 lug): il dialogo di conferma mostrava i dati estratti dall'AI come **JSON grezzo**. Ora, per le azioni che dichiarano `campi` (`CampoAzione[]` nel catalogo `appDeluxy.ts`), il dialogo rende un **modulo** con un campo per voce (etichetta, tipo testo/email/telefono/lungo, aiuto); resta il tasto «Modifica come JSON» e le azioni con dati annidati (proforma) continuano a usare il JSON. Il modulo lavora **sempre sul JSON** (lo legge e lo riscrive a ogni battuta), così il resto del flusso non cambia; campo svuotato = `null`, non stringa vuota. **Aggancio azienda** (`cercaAzienda: true`, oggi su «Anagrafiche — Registra contatto»): si cerca un'azienda già nel registro (`cercaPartnerAnagrafiche`) e la si sceglie → nei dati compare `partnerId` e `esegui` fa **PATCH** su quella scheda invece del POST che creerebbe un doppione (`partnerId` NON sta nello schema dell'AI: lo aggiunge solo l'utente dal dialogo). Senza scelta, comportamento di prima: si crea una scheda nuova.
- **Sidebar riorganizzata in 5 gruppi** (23 lug): **Posta** (dove sta la posta: in arrivo, bozze, inviata, archivio, cestino) · **Strumenti** (altri modi di guardare la STESSA posta: thread, clienti, riassunti, rubrica) · **Applicazioni** (cose che lavorano per te: Renè AI, attività, calendario, sequenze) · **Sezioni** (dinamiche, invariate) · **Gestione** (regole, sezioni, statistiche, impostazioni, utenti). Dentro ogni gruppo l'ordine è per frequenza d'uso, non alfabetico. Il markup delle voci era ricopiato tre volte: ora c'è un solo componente `Gruppo` (cambiare una riga si faceva in tre punti).
- **Bozze salvate da sole** (23 lug): quello che inizi a scrivere si salva da solo (hook `useBozzaAuto`, usato da `Composizione` e `ComposizioneNuova`): niente più mail perse chiudendo la finestra o cambiando pagina senza premere «Salva bozza». Scelte per non riempire le Bozze di spazzatura: si salva **solo dopo una modifica vera** rispetto al testo precompilato (aprire una risposta e chiuderla non crea nulla), si aspetta che si smetta di scrivere (2,5 s), e dal primo salvataggio in poi si **aggiorna** quella bozza (l'id torna dal server e si riusa). ⚠️ Due trappole evitate: (1) l'attesa si basa sul **contenuto** e non su un booleano — con un booleano il timer sarebbe partito una volta sola; (2) dopo l'invio il salvataggio è **spento per sempre** (`inviato`), altrimenti ricreava la bozza di una mail già partita (il server l'aveva appena cancellata).
- **Finestra di scrittura in basso a destra** (23 lug): dalla posta in arrivo, **Rispondi / A tutti / Inoltra** aprono una finestra in basso a destra invece di cambiare pagina (stile Gmail), con riduci a icona e chiudi. Dentro c'è la **STESSA `Composizione`** della pagina intera — non una copia semplificata: allegati, priorità, sequenze, aggancio, avviso di traduzione e conferma d'invio identici (`Composizione` ha ora `onFatto?`, che sostituisce il ritorno a `tornaA`). I dati li prepara la nuova action **`preparaComposizione(messaggioId, modo)`** (stesso lavoro di `/messaggio/[id]/scrivi`, restituito al client). ⚠️ Le azioni di riga restano **link veri**: Ctrl/Cmd-click, tasto centrale e «apri in nuova scheda» continuano ad andare alla pagina intera; solo il click normale apre la finestra, e solo se è montata (`finestraDisponibile()`, flag su `window`) e lo schermo è ≥900px — su mobile si va alla pagina come prima.
- **Iconcina «inoltrato» ↪** (23 lug): in lista una conversazione inoltrata mostra ↪ accanto al ↩ di «risposto». Prima non era tracciabile (l'inoltro apriva una catena nuova); ora **l'inoltro si aggancia all'originale** (vedi sotto) e la mail in uscita porta il nuovo campo **`Messaggio.modoInvio`** (`rispondi`|`inoltra`|`nuova`, migrazione idempotente). La riga è «inoltrata» se nel thread c'è un'uscita con `modoInvio='inoltra'`, trovata per radice **o per codice di aggancio manuale** (l'inoltro non ha la radice del thread). ⚠️ Vale per gli inoltri **da qui in avanti**: quelli vecchi non hanno il dato.
- **Anche l'inoltro entra nella conversazione** (23 lug): `agganciaA` in `inviaMessaggio` non è più `inoltro ? null : messaggioId` ma **sempre** `messaggioId` — inoltrando, la mail parte agganciata all'originale invece di aprire uno scambio a sé. Se in composizione hai scelto un'altra conversazione, vince quella.
- **Pulsanti d'invio anche in cima** (23 lug): in risposta/inoltro (`Composizione`) e mail nuova (`ComposizioneNuova`) la barra Annulla / Salva bozza / Invia è ora **anche in alto** (`.form-azioni-alto`), identica a quella in fondo: con una mail lunga non si deve più scorrere tutto per mandarla. Gli stessi pulsanti sono definiti una volta sola e resi due volte.
- **Nuovo appuntamento: un'ora di durata** (23 lug): il form nasce 09:00–**10:00** (prima «Alle» era vuoto → evento senza fine). Spostando l'inizio la fine lo segue **mantenendo la durata** impostata.
- **Ripetizione modificabile dalla scheda** (23 lug): nella modifica di un appuntamento c'è la spunta «Cambia la ripetizione» / «Fai ripetere questo appuntamento» che rivela i controlli di `Ricorrenza`. Si tocca la serie **solo se la spunti**, così salvare un cambio di titolo non rifà mai la serie per sbaglio. Vale **da questo appuntamento in avanti**: le occorrenze future si rifanno, quelle passate restano (cancellare la storia sarebbe un danno). «Non si ripete» stacca la ripetizione da qui in poi.
- **Nome della conversazione anche dalle liste** (23 lug): il nome si dava solo dalla pagina del messaggio; ora c'è il pulsante **«Nome»/«Rinomina»** nelle azioni di riga di **posta in arrivo**, **Clienti**, **Posta inviata** e **Thread** (`NomeThreadBottone` + `NomeThreadDialog`, schema leggero pulsante-evento + UN dialogo per pagina, come «Aggancia»: con centinaia di righe un modale per riga sarebbe pesantissimo). ⚠️ Sistemato anche un difetto preesistente: **`/clienti` non montava nessun dialogo**, quindi «Aggancia» e «Delega Renè» in quelle righe non aprivano niente.
- **Rispondere aggancia la risposta all'originale** (23 lug): rispondendo, la mail inviata viene agganciata **sempre** alla mail d'origine (`agganciaA = messaggioId` in `inviaMessaggio` quando non è un inoltro, e `bozza.messaggio.id` in `inviaBozza`). Prima il legame era affidato solo a `thread` (header) + oggetto: se gli header non bastavano, la risposta restava una conversazione a sé e la mail d'origine risultava «da sola». Ora il thread si forma comunque (`threadManuale`). L'inoltro resta libero: si aggancia solo se scegli tu la conversazione.
- **Scheda dell'appuntamento + ricorrenze** (23 lug): nel calendario gli appuntamenti sono **cliccabili** (griglia e «Prossimi») e aprono la scheda `EventoDettaglio` — dettaglio, **Modifica**, **Elimina**, link alla mail collegata. Nuova action `modificaEvento`; `eliminaEvento(id, ambito)` ora accetta `'questo' | 'serie'`. **Ricorrenze** (componente `Ricorrenza` nel form di creazione): ogni tot **giorni**, **giorni della settimana** (ogni N settimane, giorni scelti), **giorno del mese** (numero preciso, ultimo, **ultimo feriale**, primo feriale), con data di fine (default 1 anno, tetto 400 occorrenze). ⚠️ **Scelta di fondo**: le occorrenze sono **righe vere** (una per data) legate da `Evento.serieId`, non regole espanse a runtime — così griglia, «Prossimi», feed iCal, colonna in posta e notifiche funzionano senza sapere nulla delle ripetizioni. `Evento.regola` tiene la descrizione leggibile. Logica pura in `lib/ricorrenze.ts` (lavora su giorni «YYYY-MM-DD», mai su istanti: l'ora legale non sposta niente) — **verificata**: es. ultimo feriale 2026 → 31 lug, 31 ago, 30 set, **30 ott** (il 31 è sabato), 30 nov, 31 dic; giorno 31 salta i mesi corti. Su una serie, modifica/elimina chiedono **«solo questo» o «tutta la serie»**; sulla serie ogni occorrenza resta nel suo giorno e prende il nuovo orario (spostare le date = rifare la serie). Gli inviti per mail partono solo dalla prima occorrenza. Migrazione idempotente: `serieId`, `regola`, indice.
- **Nome dato a mano alle conversazioni + ricerca per nome** (23 lug): l'oggetto spesso non aiuta a ritrovare uno scambio («Re: IMPORTANTE: 106654/26 …»). Ora dalla pagina del messaggio, sul pannello della conversazione, c'è **«✎ Dai un nome a questa conversazione»** (`NomeThreadForm` → `salvaNomeThread`). Nuovo modello **`NomeThread`** (`[utenteId, chiave]` unica, `chiave` = id del capostipite, come RiassuntoThread/IstruzioneThread) + DDL idempotente e FK in `migrate-prod.mjs`. Letture in `lib/nomiThread.ts` (`nomiPerChiavi`, `nomeDiThread`, `chiaviPerNome`), **difensive**: senza tabella migrata degradano a "nessun nome". Il nome compare come **badge oro** nelle righe di posta in arrivo, posta inviata e Thread, e nei risultati di «Aggancia». **Ricerca**: in `/thread` si cerca anche per nome — ⚠️ il filtro NON è più solo SQL sul singolo messaggio: si raggruppa e si filtrano i GRUPPI (un thread trovato per nome non ha nessuna mail che contiene la parola, e un thread trovato per testo va comunque mostrato INTERO col conteggio giusto); le mail che combaciano ma stanno fuori dalla finestra recente si caricano a parte, così le ricerche vecchie continuano a funzionare. Anche `cercaDaAgganciare` cerca fra i nomi (la chiave È un id di messaggio → basta includerlo nell'OR).
- **«Scarica tutti» gli allegati in uno zip** (23 lug): con più di un allegato, in testa al box compare **⤓ Scarica tutti (N)** → `GET /api/allegati-zip?messaggio=<id>` costruisce lo zip al momento (jszip, DEFLATE) e lo serve. Novità in `imap.ts`: **`leggiTuttiAllegati`** — UNA connessione e UNO scarico del sorgente per tutti i file; `leggiAllegato` ne prende uno per volta, usarla in ciclo avrebbe aperto una connessione IMAP e riscaricato l'intera mail per ogni allegato. Nomi ripuliti dai caratteri vietati e **de-duplicati** (i vari `image001.png` diventano `image001 (2).png`…); nome dell'archivio = oggetto della mail. Tetto complessivo **45 MB**: oltre, lo zip è parziale e contiene `ATTENZIONE.txt` che dice quanti file mancano (si prendono singolarmente). Autenticazione col cookie come `/api/allegato` (la rotta è dentro il matcher del middleware). Dipendenza nuova: `jszip`.
- **Aggancio a una conversazione mentre scrivi** (23 lug): inoltro e mail nuova aprono sempre uno scambio NUOVO (nessun legame con l'originale). Ora in composizione c'è **«⚭ Aggancia a una conversazione»** (componente `AgganciaCompose`): si cerca il thread e si sceglie — è solo un'intenzione, niente viene toccato finché non si invia. Il campo `agganciaA` viaggia nel form; `registraInviato` (nuovo parametro finale, opzionale) applica `agganciaAlThread` sulla copia appena registrata in Posta inviata, best-effort (se fallisce la mail è comunque partita e lo dice). `cercaDaAgganciare` accetta ora `messaggioId` VUOTO = «sto scrivendo, non c'è una conversazione di partenza». Mostrato solo per l'inoltro in `Composizione` (una risposta sta già nel thread) e sempre in `ComposizioneNuova`.
- **Sequenze spostata in «Applicazioni»** (23 lug): la voce `/sequenze` (modelli di follow-up: si aggancia una sequenza all'invio di una mail e i passi partono da soli se il destinatario non risponde) stava nel gruppo **Posta** della sidebar, fra le caselle; è uno strumento, non una casella → ora è in **Applicazioni**, sotto Renè AI. Solo `Sidebar.tsx` (la stessa sidebar serve anche il drawer mobile).
- **Posta inviata a conversazioni** (23 lug): `/inviata` mostrava una riga per mail; ora usa `raggruppa()` come la posta in arrivo — più mie mail dello stesso scambio fanno UNA riga (volto = la più recente) col contatore accanto al destinatario (`thread-count`, tooltip «N tue mail in questa conversazione»). ⚠️ Le azioni di riga agiscono sull'INTERO gruppo quando `nel > 1` (`RigaInviata.ids[]` → `azioneMassa`): cestinando solo il volto la riga sarebbe ricomparsa con la mail precedente. Anche la selezione multipla espande le righe scelte in tutti i loro id.
- **Navigazione molto più veloce fra le pagine** (23 lug): cambiare pagina (posta in arrivo ⇄ inviata) era lentissimo. Quattro cause, tutte rimosse:
  1. **Corpi delle mail portati inutilmente in lista.** `/inviata` chiedeva 500 messaggi **completi** (`corpoTesto`+`corpoHtml`, cioè l'HTML intero di ogni mail) per mostrarne l'anteprima: decine di MB dal DB a ogni apertura. Aggiunto `omit` (e take 500→300). Stesso difetto in `/cestino` (200) e `/statistiche` (2000): corretti. **Regola: in una LISTA non si chiedono mai i corpi.**
  2. **Due chiamate HTTP dentro il render della posta in arrivo.** `leggiChiaviApp()` faceva una fetch al hub (`no-store`) a OGNI apertura → ora cache in memoria 5 min + timeout 4s + riuso dell'ultima risposta buona (`svuotaCacheChiavi()` per invalidare). `indiceClienti()` (badge cliente) poteva fare 20 fetch **in sequenza** ad Anagrafiche → ora cache 30 min, max 10 pagine, **non blocca mai**: se in memoria c'è una copia (anche scaduta) la usa e aggiorna in sottofondo; al primo giro assoluto aspetta al massimo 1,2s, poi va avanti senza badge. Una sola costruzione alla volta anche con richieste in parallelo.
  3. **~9 query al database in fila** in `page.tsx` (Vercel USA ↔ Supabase Europa: ogni query è un viaggio). Ora un solo `Promise.all` per tutto ciò che non dipende da altro (contatti AI, sezioni, account, storico, chiavi app, sezione attiva + sottosezioni); restano solo le due query dipendenti (posta, poi le risposte). Da ~10 ondate a ~4.
  4. **Payload verso il browser.** La posta prendeva 2000 messaggi e ne spediva 1200 righe, **col corpo tradotto INTERO** in ogni riga (in lista se ne mostrano 200 caratteri). Ora take 800, cap 300 conversazioni, `corpoTradotto` troncato a 200 caratteri, e la relazione `sezione` con `select` invece che intera.
- **«Chiedi a Renè» crea anche appuntamenti** (23 lug): il box in `/rene` capiva solo cestina/archivia — «crea appuntamento per domani ore 12 …» (anche coi dati di un invito Teams/Zoom incollati) rispondeva "Non ho capito". Ora `interpretaComandoPosta` ha l'azione `appuntamento`; `comandoPostaAnteprima` chiama `estraiAppuntamentoDaTesto` (ai.ts, come `estraiAppuntamento` ma senza mail d'origine) e crea SUBITO l'evento (non distruttivo, come nella Delega) — ritorno `{fatto: true}`, il componente mostra il banner Flash e svuota il campo. Senza data certa non inventa e lo dice.
- **Cc sempre presente in composizione** (23 lug): in `Composizione` il campo Cc compariva solo per "rispondi a tutti"/inoltro (o se già valorizzato) — nella risposta singola non si poteva mettere nessuno in copia. Ora c'è sempre.
- **Renè risponde nella lingua della mail** (23 lug): il prompt di `scriviRisposta` imponeva "in italiano" sempre: le bozze delegate (e l'esecuzione attività) uscivano in italiano anche su mail in inglese — e per le lingue "lette" la traduzione in uscita non scatta (giustamente), quindi partivano in italiano. Ora `scriviRisposta` accetta `lingua` (= `Messaggio.lingua` rilevata all'arrivo, passata da `preparaRispostaDelegata` e `preparaEsecuzione`) e il prompt dice di scrivere NELLA lingua della conversazione (il compito resta in italiano perché è un'istruzione per Renè). Se la lingua non è rilevata, usa quella dell'ultima mail ricevuta del thread. Per le lingue non lette la traduzione in uscita resta e diventa un passaggio quasi-identità.
- **Prossimi appuntamenti in posta in arrivo** (23 lug): in cima alla colonna destra (sopra APP DELUXY e "Da fare") c'è l'agenda: i prossimi 6 eventi del calendario entro 30 giorni, da inizio giornata di oggi (ora italiana, così si vedono anche i "giornata intera" di oggi). Badge blu "oggi"/"domani", ora/luogo, titolo che apre la mail collegata se c'è, link "Calendario →". Componente `ColonnaCalendario.tsx` (server, lettura difensiva: senza eventi o senza tabella non si mostra nulla). Solo desktop, come il resto della colonna (`.col-attivita` sparisce sotto i 1100px).
- **Badge cliente in posta in arrivo** (23 lug): se il mittente di una riga combacia con un CLIENTE di Anagrafiche (stato attivo, match per email esatta o dominio aziendale — stessa regola della pagina `/clienti`), la riga mostra il badge verde col nome dell'azienda. In `page.tsx`: `indiceClienti()` (lib/anagrafiche.ts, cache 10 min) parte in parallelo alle query e si aspetta solo alla costruzione delle righe; `RigaData.clienteNome` esisteva già in `RigaMail`. Se Anagrafiche non risponde o manca la chiave, nessun badge (nessun errore).
- **Lettura IMAP** + scarico storico progressivo; invio SMTP con copia in "Inviata".
- **Scarico posta nuova all'apertura** (21 lug): aprendo l'app, `SyncButton` drena in background TUTTA la posta nuova arretrata (ripete il giro breve `/api/leggi-posta` finché non arriva più niente), senza bloccare l'uso; si ferma se lasci la scheda. Rispetta la spunta "Automatico". Il timer periodico resta un giro singolo.
- **Scarico di tutto lo storico in background** (21 lug): impostazione `Utente.scaricaStoricoAuto` (Impostazioni → "Scarica tutta la posta di sempre"). Se attiva, con l'app aperta il componente `StoricoAuto` chiama `/api/scarica-storico` in loop (un blocco alla volta, con 8s di ritardo iniziale per dare precedenza alla posta nuova e una pausa fra i blocchi) finché la casella non è completa (`storicoFinito`). Non blocca l'app; riprende alla prossima apertura. Riusa `scaricaStorico()` (cursore `primoUid`).
- **Risposta/Inoltro su mobile con iconcine** (21 lug): nella lista, `RispostaAzioni` (Rispondi/A tutti/Inoltra) prima era `display:none` su schermo stretto → su mobile mancavano. Ora ogni azione ha icona + etichetta: desktop mostra il testo, mobile (≤700px) solo l'iconcina (SVG). Vedi `.risposta-icona`/`.risposta-testo` in globals.css.
- **Ricerca: snippet con la parola evidenziata** (21 lug): i risultati mostravano l'anteprima generica, per cui non si capiva perché una mail (es. "Re: ORDINE TIFFANY") uscisse cercando "zegna" (la parola era nel corpo, anche nella parte citata di risposte/inoltri). Ora ogni risultato mostra uno **snippet attorno alla parola trovata, evidenziata** (`snippetRicerca` in page.tsx cerca in corpo/traduzione/oggetto/destinatari/nome/mittente; `evidenzia()` in RigaMail avvolge il match in `<mark class="ricerca-hit">`). La garanzia resta: una mail esce SOLO se contiene davvero la parola (il filtro SQL `contains` + `raggruppa` che raggruppa solo i match).
- **Perf posta in arrivo** (20 lug, sera): (1) «Delega Renè» e «Aggancia» nelle righe erano componenti client con stato + modale ciascuno → ~200 istanze in una lista da 100 righe. Ora pulsanti-evento (`DelegaReneBottone`/`AgganciaBottone`, zero hook) + UN dialogo condiviso per pagina (`DelegaReneDialog`/`AgganciaDialog`), come `BottoneApp`/`InvioAppDialog`. (2) **Caricamento progressivo**: il server calcola tutte le righe raggruppate come dati leggeri (`RigaData` in `RigaMail.tsx`) e le passa a `ListaMail` (client) che ne monta **25 alla volta** con IntersectionObserver (+ pulsante «Carica altre»). Così si idratano solo le righe viste. Il vecchio JSX di riga inline in page.tsx è ora in `RigaMail`. Cap alzato a 300 gruppi.
- **Aggancio thread e vista completa dalla riga** (20 lug, sera): nella riga della posta ci sono ora «Aggancia» (modale `AgganciaRiga`, riusa `cercaDaAgganciare`/`agganciaAlThread` — prima solo dalla pagina messaggio) e, sui thread (nel>1), «Apri completo» → `/messaggio/[id]?ampia=1`. Nella pagina messaggio un toggle **«Questo thread ⇄ Con le correlate»**: `messaggiThread(utenteId, id, ampia)` — con `ampia` aggiunge al thread stretto TUTTE le mail scambiate con le stesse persone (controparti del thread, deterministico, cap 60), etichettate «(N correlate)».
- **Delega Renè** (20 lug, sera): su ogni mail (azione «Delega Renè» nella riga della posta e bottone nella pagina messaggio) apri un dialogo con due rami. **«Prepara la risposta»**: scrivi a parole cosa rispondere e Renè fa la bozza (stile + istruzioni mirate). **«＋ Metti in agenda»**: Renè ricava data/ora/luogo dalla mail (o dall'indicazione, es. «la call è giovedì alle 15») e crea l'evento in Calendario legato alla mail — `estraiAppuntamento` (ai.ts) + `preparaEventoDelegato` (sync.ts, conversione Europe/Rome→UTC); se non c'è una data certa non inventa e lo dice; conferma con banner Flash. `preparaRispostaDelegata` in sync.ts (riusa `scriviRisposta`, sostituisce l'eventuale bozza AI precedente) → apre `/messaggio/[id]/scrivi?bozza=…`. Non invia: la controlli e la mandi tu. Componente `DelegaRene.tsx` (varianti riga/bottone).
- **Delega Renè legge l'istruzione (un solo pulsante)** (21 lug): il dialogo non ha più i due bottoni "Prepara risposta" / "Metti in agenda". Un solo campo + "Procedi": `classificaDelega` (ai.ts) legge il testo e decide mail vs evento; `delegaReneAuto` (actions.ts) instrada a `preparaRispostaDelegata` o `preparaEventoDelegato`. "riassumi", "recap a X", "declina" → mail; "metti in agenda", "call giovedì 15" → evento.
- **Recap/mail ai destinatari indicati** (21 lug): se l'istruzione nomina i destinatari (persone/rubrica), Renè li mette in "a" invece del mittente originale. `scriviRisposta` risolve i nomi con la RUBRICA COMPLETA (`elencoContatti`, ~200) passata da `preparaRispostaDelegata`/`preparaEsecuzione`. In risposta: `a = testo.a || messaggio.mittente`.
- **Firma Deluxy da form dedicato** (21 lug): in Impostazioni «La tua firma» — campi (nome, ruolo, reparto, email, telefono, sito) → `costruisciFirma` (`lib/firma.ts`) genera l'HTML col template Deluxy (logo + disclaimer) salvato in `Utente.firma`; i campi restano in `Utente.firmaDati` (JSON) per riaprire il form. Email/nome precompilati dall'utente al primo accesso. La firma NON si tocca più da `salvaImpostazioni`. `preparaRisposta`/scrittura: se la firma è HTML (`sembraHtml`) si usa così com'è e la risposta diventa HTML (l'editor la rende). Migrazione idempotente `firmaDati`.
- **Renè AI: storico priorità + guida di gestione; Statistiche** (21 lug): in `/rene` — «Storico priorità» (`StoricoPriorita`): elenca le mail a cui hai dato una priorità con l'esito DEDOTTO dallo stato (risposto/attività/archiviata/cestinata/in sospeso), niente tabella di log. «Come gestire le richieste»: impostazione globale `guida_gestione` (CHIAVI, admin, `salvaGuidaGestione`) iniettata nell'analisi (`analizzaMessaggioOra` → `istruzioniAI`, via `contestoAI`), così le prossime mail simili si smistano/prioritizzano come indicato. Nuova pagina `/statistiche` (gruppo Gestione in sidebar): conteggi per priorità e per esito (derivati, cap 2000).
- **Notifiche push sul telefono** (21 lug): Web Push. Modello `PushIscrizione` (endpoint/p256dh/auth per utente) + migrazione idempotente. `lib/push.ts` (web-push, VAPID) con `inviaPush`/`notificaNuoveMail`; service worker `public/sw.js` (push + notificationclick); componente client `NotifichePush` in Impostazioni (registra SW, chiede permesso, si iscrive, salva via `salvaIscrizionePush`/`rimuoviIscrizionePush`). Trigger: in `sincronizzaTutti` (il giro del CRON, cioè quando l'utente NON è sull'app) — se arriva posta nuova (entrata, non letta, non spam), una notifica riepilogativa. **Config Vercel richiesta**: `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` + `NEXT_PUBLIC_VAPID_PUBLIC_KEY` (stesso valore pubblico). **Frequenza**: le notifiche partono quando gira il cron `/api/sync` — su Hobby 1/giorno; per il quasi-real-time serve un cron ESTERNO (es. cron-job.org) che pinga `/api/sync?token=CRON_SECRET` ogni ~5 min. Senza VAPID, l'invio è no-op.
- **Chiavi APP DELUXY dalla cassaforte di deluxy-hub** (21 lug): `leggiChiaviApp` (`lib/chiaviApp.ts`) ora ha TRE sorgenti in ordine: override locale (Impostazioni App, cifrato) → **hub** (`GET /api/chiavi?progetto=deluxy-mail`, header `X-Hub-Token`) → env. Si attiva mettendo su Vercel di AI Mail `HUB_KEYS_TOKEN` (lo stesso token del hub) ed eventualmente `HUB_URL` (default https://deluxy-hub.vercel.app); le chiavi vanno inserite nel hub (pagina `/chiavi`) sotto progetto **deluxy-mail** coi nomi env (`ANAGRAFICHE_API_KEY`/`FINANCE_API_KEY`/`FORNITORI_PASSWORD`). Senza token il hub è ignorato (comportamento invariato). `statoChiaviApp` espone anche `daHub` e la UI mostra "Presa dalla cassaforte di deluxy-hub".
- **Target al tocco più grandi su mobile** (21 lug): sulle righe di posta le azioni (`.azione-riga`/`.archivia-def`) diventano chip da ≥40px ben distanziate e le iconcine risposta 40×40 (prima erano link/icone minuscole e attaccate → su mobile si toccava quella sbagliata). Solo `@media (max-width:700px)` in globals.css.
- **Svuota cestino = cancella ANCHE dal server** (21 lug): `svuotaCestino` ora, oltre a togliere le righe dal DB, cancella i messaggi DAL SERVER IMAP (`eliminaDalServer` in imap.ts: `messageDelete` + expunge). Incoming dalla INBOX, outgoing dalla cartella Inviata. Solo UID reali (>0): le copie locali degli inviati hanno uid negativo e si saltano. **IRREVERSIBILE** — cambia il vecchio principio "non si cancella mai dal server". Errori di rete: le righe si tolgono comunque dall'app e si avvisa.
- **Storico degli INVIATI in background** (21 lug): oltre alla INBOX, si scarica anche la cartella "Inviata". `sincronizzaInviata` (sync.ts): inviati nuovi + (a esaurimento) storico più vecchio, salvati come direzione 'uscita' via `salvaInviati` (dedup per messageId; alle copie fatte dall'app — uid negativo — aggiorna l'uid reale così diventano cancellabili dal server). Cursori dedicati su Account (`ultimoUidInviata`/`primoUidInviata`/`storicoInviataFinito`). `scaricaNuovi`/`scaricaVecchi` in imap.ts ora accettano `opts { cartella, ultimoUid/primoUid }` per leggere una cartella diversa dalla INBOX. Il drain gira nel background esistente: `/api/scarica-storico` fa prima lo storico INBOX poi quello Inviata (attivo con l'impostazione `scaricaStoricoAuto` / componente `StoricoAuto`).
- **Iconcina "risposto" sulle righe** (21 lug): una conversazione con una nostra mail in USCITA nel thread mostra ↩ verde in lista (`risposto` in RigaData, calcolato in page.tsx: `threadRisposti` = thread delle uscite). Gli INOLTRI aprono una conversazione nuova (thread=null) → non tracciabili così: servirebbe un riferimento esplicito all'originale (non fatto).
- **Ricerca in Posta inviata** (21 lug): `/inviata?q=` filtra per destinatario/oggetto/testo; `RicercaMail` ora parametrizzata con `base`/`placeholder` (riusata anche qui).
- **Chiedi a Renè: comandi di massa** (21 lug): box "Chiedi a Renè" in `/rene` (`ComandoRene`) per comandi su gruppi di mail — es. «cancella tutte le mail di mario@…», «archivia le mail con oggetto sollecito». `interpretaComandoPosta` (ai.ts) → {azione cestina/archivia, criterio mittente/oggetto, valore}; `comandoPostaAnteprima` CONTA quante mail e chiede conferma (mai distruttivo di colpo); `comandoPostaEsegui` fa `updateMany` (cestina = recuperabile). WHERE sempre scoped per utente, cestino escluso.
- **Cc anche nell'inoltro** (21 lug): `Composizione` mostra il campo Cc anche per modo 'inoltra' (prima solo 'tutti'/quando cc valorizzato).
- **Ricerca rubrica più evidente** (21 lug): `CercaContatti` ora è una barra piena sotto il titolo (prima piccola in alto a destra). La ricerca c'era già (`elencoContatti(u.id, q)`).
- **Autocompletamento destinatari dalla rubrica** (21 lug): nei campi A/Cc di scrittura (rispondi/inoltra `Composizione`, mail nuova `ComposizioneNuova`) compare un menu coi contatti che combaciano mentre scrivi (nome o email); si sceglie con clic, frecce+Invio o Tab. Componente `CampoDestinatari`; la rubrica (`elencoContatti`) è passata dalle pagine `scrivi`. Non vincola: puoi digitare anche un indirizzo non in rubrica.
- **Rubrica = anche chi hai scritto tu** (21 lug): `elencoContatti` ora include i destinatari delle mail INVIATE (estratti da `destinatari` delle ultime 500 uscite), non solo i mittenti. Così un contatto a cui mandi una mail resta in rubrica «se non esiste già» (dedup per email, esclusi i propri indirizzi). Nessun salvataggio esplicito: la mail inviata registra già i destinatari.
- **Risposta mantiene l'HTML dell'originale** (21 lug): `preparaRisposta` (rispondi.ts), se l'originale è HTML, costruisce la risposta in HTML con la citazione in `<blockquote>` (HTML ripulito con `sanitizzaHtml`) invece del testo con "> ". L'editor (`EditorRicco`) rende l'HTML (`sembraHtml`). Inoltro e mail di solo testo restano invariati.
- **Delega Renè: riprende il thread + capisce inoltro vs risposta** (21 lug): due difetti risolti in `preparaRispostaDelegata` (sync.ts). (1) Prima passava a `scriviRisposta` **solo il singolo messaggio**: Renè rispondeva ignorando la conversazione. Ora carica **tutto il thread** (`messaggiThread`) e lo mette nel prompt, così risponde a ciò che è rimasto in sospeso. (2) Prima creava **sempre** una bozza `modo='rispondi'` verso il mittente: un'istruzione tipo «inoltra a Mario» diventava lo stesso una risposta. Ora `scriviRisposta` sceglie `modo` (`rispondi`/`inoltra`) e, per l'inoltro, il destinatario dai contatti recenti; la bozza d'inoltro ha oggetto `Fwd:`, il testo di Renè + originale citato sotto (riuso di `inoltrato`/`prefissa` da `lib/rispondi.ts`, ora esportate), destinatario preso dai contatti o vuoto se non trovato. Rete di sicurezza: se l'inoltro non è consentito (risposte automatiche) resta sempre `rispondi`.
- **Conferma d'invio ben visibile** (20 lug, sera): dopo un invio riuscito compare un banner verde «✓ Messaggio inviato a X» in alto al centro (`components/Flash.tsx`, montato nel layout). Sopravvive al cambio pagina via `sessionStorage` (`mettiFlash` prima di navigare) o compare al volo senza navigare via evento `window` (`mostraFlash`, usato in `BozzaEditor`). Sparisce da solo dopo 4s o al clic.
- **Composizione ricca** (20 lug, sera): editor formattato (`EditorRicco`, contenteditable + execCommand: grassetto/corsivo/sottolineato/elenchi/link) e **allegati** (`Allegati`) su risposta, mail nuova e bozza AI. Il corpo viaggia come **HTML** (multipart html + text/plain derivato via `lib/htmlMail.ts`); le bozze salvano l'HTML; la copia in "Inviata" tiene `corpoHtml`. Allegati via FormData (`leggiAllegati`), tetto 20 MB (`next.config.ts` → `serverActions.bodySizeLimit`), NON salvati nelle bozze (solo all'invio). **Traduzione in uscita**: se la lingua va tradotta si invia solo testo (la formattazione non sopravvive alla traduzione) — caso raro, per le lingue lette resta l'HTML. `sembraHtml` cerca solo i tag noti per non scambiare un testo con `<parola>` per HTML.
- **Sottosezioni + riassunto AI per sezione** (20 lug): in `/sezioni` ogni sezione può avere sottosezioni (menu "Dentro a…"); l'albero si vede anche in sidebar (figlie rientrate) e aprendo la madre si vede **anche** la posta delle figlie. Su ogni sezione e sottosezione c'è il riassunto AI (`riassumiSezioneOra`) in due tagli: **per periodo** (1/3/7/14/30 giorni) o **per conversazione** (fino a 12 thread, ultimi 6 messaggi ciascuno). Il riassunto della madre comprende la posta delle figlie.
- **Sezioni** configurabili + **Regole** (deterministiche e/o con istruzione AI) con **retrodatazione** allo storico e **attività su misura**.
- **Priorità P0–P3** su ogni mail → scatena l'analisi AI (riassunto + attività + bozza + smistamento in sezione). **L'AI riceve TUTTA la conversazione** (20 lug): l'ultima mail è quella analizzata, i messaggi precedenti (max 8, accorciati, tradotti se serve) vanno nel prompt come contesto non fidato — così non crea attività per cose già fatte o già risposte.
- **Aggancio manuale al thread** (20 lug): dal dettaglio di una mail, "⚭ Aggancia una mail" cerca per oggetto/mittente e unisce due conversazioni che non hanno né catena di risposte né oggetto in comune (`threadManuale`, `agganciaAlThread`/`staccaDalThread`). L'aggancio vale per l'intero thread di entrambe le mail e le mail agganciate entrano nel thread anche se più vecchie della finestra dei 400 candidati.
- **Traduzione automatica**: mail in lingua straniera tradotte in italiano (all'arrivo e — se sfuggite al giro di sync — al primo accesso, ma **in background**, vedi sotto); risposte scritte in italiano e inviate nella lingua originale. Configurabile (`lingueLette`).
- **Apertura mail istantanea** (21 lug): aprire una mail non aspetta più l'AI. Prima `/messaggio/[id]/page.tsx` faceva `await traduciMessaggioSeServe(...)` **nel render** (chiamata OpenAI sincrona alla prima apertura di ogni mail in arrivo: rilevare la lingua/tradurre = 1–4 s a schermo bianco) e ~8 query in serie. Ora: (1) la traduzione è **fuori dal percorso critico** — la mail compare subito con l'originale (usando `Messaggio.lingua`/`corpoTradotto` già salvati) e, se manca ed è attiva la traduzione auto, il componente client `TraduzioneAllApertura` chiama l'action `traduciMessaggio` **dopo il render** e aggiorna la pagina solo quando c'è una lingua nuova (nessun ciclo se fallisce); (2) tutte le letture indipendenti (sezioni, thread, thread stretto, PLUS AI, risposte-app) girano in **un solo `Promise.all`**, e così pure riassunto+istruzioni del thread. Tempo = la query più lenta, non la somma.
- **Conversazioni (thread)**: raggruppamento per catena di risposte e per oggetto (anche con destinatari/oggetti diversi); **riassunto "per punti di vista"** che spiega la posizione di ogni parte.
- **Posta in arrivo = solo posta da smistare** (20 lug): una mail smistata in una sezione (da regola o AI) sta NELLA SUA SEZIONE, come le cartelle di un client classico — non resta anche in posta in arrivo. Senza questo, una sezione ad alto volume (es. le notifiche ordini) replicava sé stessa in home e seppelliva il resto. La vista si chiama "In arrivo" (ex "Tutte"); la AI Inbox invece mostra il contatto in qualunque sezione (tranne SPAM).
- **Rubrica** con quadro AI del contatto (mail ricevute+inviate → prossime attività).
- **PLUS AI** sui contatti + **AI Inbox** (vista dedicata) vs **Tutte** (predefinita). SPAM escluso dalla posta in arrivo.
- **Istruzioni AI mirate** per **contatto** e per **conversazione** (precedenza thread > contatto > globale), applicate a tutte le chiamate AI. Fidate, separate dal corpo mail.
- **Attività**: create dall'AI, **a mano**, o da **comando in linguaggio naturale**; tutte eseguibili dall'AI (scrive la mail).
- **Risposte dalle app sulla mail** (20 lug): quando si richiama un'app (APP DELUXY), l'esito viene salvato in `InvioApp` e mostrato **in fondo alla pagina del messaggio** («Risposte dalle app»: badge app + esito + testo su più righe + link «Apri nell'app»). Nella lista posta la mail ha il badge viola «Risposta app» (`_count.inviiApp`). La risposta di «Trova fornitore» è formattata su più righe (nome, distanza+minuti, indirizzo, telefono, WhatsApp, valutazione, aperto ora).
- **APP DELUXY** (20 lug): pannello a destra della posta con una carta per funzione delle altre app — oggi **Anagrafiche → Registra contatto** (POST /api/v1/partners), **Finance → Crea proforma** (POST /api/proforma), **Finance → Verifica partner** (GET /api/verifiche). **Drag & drop** di una mail su una carta (o sulla carta "Automatico", dove decidono le **Regole APP DELUXY** in /regole: condizioni esatte + nota per l'AI + priorità); su **mobile** il bottone **"→ App"** su ogni riga. Flusso: l'AI estrae i dati dalla mail (json_schema strict, `estraiDatiAzione`; corpo non fidato, mai inventare) → l'utente li vede in un JSON modificabile e **conferma** → chiamata HTTP vera → esito + storico in `InvioApp`. Catalogo e client HTTP in `src/lib/appDeluxy.ts` (per aggiungere un'azione si aggiunge lì: schema, guida, esegui).
- **Tasti "Nuova mail" e "Nuova attività"** in testa alla posta (20 lug): "Nuova mail" apre `/scrivi` (mail da zero, conversazione nuova, bozze riprendibili con badge "nuova mail"); "Nuova attività" apre il dialogo con l'AI che **chiede quale attività bisogna seguire**, la crea (agganciando solo contatti conosciuti) e **propone l'azione che può intraprendere** — col tasto "Procedi" prepara subito la bozza di mail. **Su mobile** (≤900px) i due tasti spariscono: c'è il **pulsante "+" fisso in basso a destra** che apre il foglio "Cosa vuoi creare?" (mail o attività) — tutto in `NuoveAzioni.tsx` (classi `.nuove-inline`/`.fab`/`.scelta-*`).
- **Stile di scrittura di Renè** (20 lug, sera): impostazione globale `stile_scrittura` (in `/rene`, «Come scrivo le mail», admin) letta da OGNI mail generata — risposte (`scriviRisposta`), mail nuove (`scriviMailNuova`) ed esecuzione attività, più le bozze dell'analisi (`analizzaMessaggio`). Serve via `contestoAI()` in sync.ts. Default `STILE_DEFAULT` (in impostazioni.ts): saluto d'apertura + corpo cortese + chiusura + firma — così le mail non escono più mozze/telegrafiche come prima (i vecchi prompt imponevano «asciutto, dritto al punto» e saltavano il «Buongiorno»/commiato).
- **Renè AI** (20 lug, sera — pagina `/rene`, sidebar → Applicazioni): l'agente che tiene la casella in ordine. Giro su periodo a scelta (oggi/7/30 gg): legge posta+SPAM+cestino (digest compatto, 1 sola chiamata AI — `reneAnalizza` in ai.ts), consulta e RISCRIVE il suo **taccuino** (`ReneMemoria`, tetto 1500 caratteri, modificabile a mano dalla pagina), e genera **proposte** (`ReneProposta`: sezione/regola/smista/attivita/evento) che si applicano SOLO dopo conferma — bottoni Approva / **«Approva, e fai sempre così»** (crea una `ReneConseguenza` per quel tipo: dai giri dopo si applica da sola, revocabile) / No. **Anti-tormentone**: ogni proposta ha una firma; una proposta uguale a una già fatta (anche rifiutata) non viene riproposta. **Urgenti senza risposta** calcolate DETERMINISTICAMENTE (P0/P1 o serveRisposta, nessuna nostra mail dopo nel thread — `urgentiSenzaRisposta` in `lib/rene.ts`), non dall'AI. Orchestrazione in `lib/rene.ts` (`eseguiRene`, `applicaPropostaRene`); le sezioni citate dalle proposte si creano al volo se mancano; smistamenti mai sopra `smistatoDa='manuale'`. L'**Assistente AI sparisce dalla posta in arrivo** (il pannello fisso); le vecchie pagine `/assistente/[id]` restano leggibili.
- **Assistente AI** (storico, oggi/settimana/mese): riassunto del periodo + attività + proposte di archiviazione (map-reduce) — superato da Renè, pannello rimosso dalla home.
- **Anti-SPAM automatico all'arrivo** (euristiche gratuite + giudizio AI sui casi dubbi), prudente per non nascondere mail di lavoro; sezione SPAM **recuperabile** (mai cancellati).
- **"Non è spam"** (21 lug): pulsante sulle mail in sezione SPAM (riga e pagina messaggio) → `segnalaNonSpam` riporta la mail in Posta in arrivo, `smistatoDa='manuale'` (l'AI non la rispedisce in spam). Da lì il mittente è "noto" (ha un messaggio in archivio, vedi whitelist in `salvaMessaggi`), quindi le sue prossime mail non finiscono più spam. NB: dare una **priorità** a una mail lancia l'analisi AI che la **smista in una sezione** → sparisce da "In arrivo" (che mostra solo la posta non smistata): la si ritrova nella sua sezione.
- **Multi-utente** con login (email+password), ruoli, admin che crea gli utenti.
- **Ricerca posta** (20 lug, sera): barra in cima alla posta in arrivo (`RicercaMail`) → `/?q=…`; il ramo di ricerca in `page.tsx` cerca fra **ricevute E inviate** (oggetto, mittente, mittenteNome, destinatari, corpoTesto, corpoTradotto; `mode: insensitive`), in tutte le sezioni tranne il cestino, ignorando i filtri sezione/vista/priorità. I risultati riusano `ListaMail`/`RigaMail` (caricamento progressivo); le mail inviate mostrano «a …» + badge «inviata» (`RigaData.inviata`/`destinatari`). Min 2 caratteri.
- **Mobile**: sidebar a scomparsa (drawer con hamburger); Assistente AI nascosto su mobile per far vedere subito la posta.
- **Appuntamento proposto dall'AI** (20 lug): quando dai una priorità a una mail che è un invito a riunione/appuntamento (anche link Teams/Zoom/Meet), l'analisi estrae l'evento (`analizzaMessaggio` → campo `evento`) e lo salva in `Messaggio.eventoProposto`. La pagina del messaggio mostra la card «Appuntamento trovato dall'AI» con **Aggiungi al calendario** (`accettaEventoProposto` → crea `Evento` legato alla mail, ora in Europe/Rome→UTC) o **Ignora** (`ignoraEventoProposto`). In lista posta la mail ha il badge blu «Appuntamento». Niente inserimenti automatici: si conferma sempre. Parser/lettore in `lib/eventoProposto.ts` (fuori dalle server action perché sincrono).
- **Esegui anche senza mail d'origine** (20 lug, sera): `preparaEsecuzione` ora gestisce le attività NON nate da una mail (es. create dal dialogo "Nuova attività"): l'AI scrive una **mail nuova** (`scriviMailNuova` in ai.ts — destinatari solo fra i mittenti recenti, dati del compito usati tali e quali) come bozza `modo='nuova'` che si apre in `/scrivi?bozza=…`. Nel dialogo "Nuova attività" il tasto **«Procedi: prepara la mail»** ora c'è sempre (prima senza contatto agganciato restava solo «Va bene così», che chiudeva e basta — da lì il «faccio va bene così e non succede nulla»).
- **Esegui nella lista DA FARE** (20 lug): la colonna DA FARE della posta in arrivo (`ColonnaAttivita`) ora ha il tasto **Esegui** (come la pagina Attività) su ogni task con mail/contatto d'origine — prima c'era solo la spunta (che segna «fatta», non esegue: da qui l'equivoco «eseguita ma non succede nulla»).
- **Calendario** (20 lug): pagina `/calendario` — vista mensile + prossimi 30 giorni, appuntamenti a mano (anche giornata intera), eliminazione con conferma. **Le attività con scadenza** compaiono nella griglia sul loro giorno (chip col pallino colore-priorità → apre la mail/attività) e in una sezione **«Attività in scadenza»** ordinata per urgenza (scadenza poi priorità). **Sincronizzazione con le altre agende** via feed iCal segreto (pannello in fondo alla pagina: accendi → copia il link → "Da URL" in Google Calendar / calendario iPhone / Outlook; rigenera o spegni quando vuoi). Su mobile la griglia mostra i giorni a pallini.

---

## 8. Sicurezza (difese anti prompt-injection)

- Il **corpo delle email è DATO non fidato**: nei prompt è sempre delimitato e marcato "contenuto non fidato"; le istruzioni scritte dentro NON vanno eseguite.
- Le **istruzioni dell'utente** (regole, istruzioni contatto/thread) sono fidate e separate dal corpo.
- HTML delle mail in **iframe sandboxed** (no script). `sanitizzaHtml` toglie script/on*/javascript:.
- Sessioni firmate HMAC; password hashate; credenziali IMAP cifrate.

---

## 9. Problemi noti / gotchas

- **«Veramente troppo incasinato» → gerarchia, non compressione (25 ago)** — dopo la barra
  unica il rumore restava: **quindici comandi tutti uguali sono quindici pillole**, comunque
  le impili. Rifatta `AzioniMessaggio` con una gerarchia: in vista **Rispondi · Inoltra ·
  Archivia · Cestina** (le quattro d'uso continuo), tutto il resto dietro **«⋯ Altro»** —
  Rispondi a tutti, Delega Renè, Aggancia, Segna letto, Spam, Scorciatoie, Sposta in sezione.
  Nessuna azione tolta ([[feedback-non-togliere-azioni]]): cambia quante gridano insieme.
  ⚠️ Sul desktop il menù è un pannellino `absolute` sotto il bottone (la barra lì non ha
  overflow); **su telefono la barra fissa scorre in orizzontale e un absolute verrebbe
  TAGLIATO dall'overflow**: lì il menù è un foglio `position:fixed` sopra la barra, stile
  foglio azioni iOS — il fixed non viene tosato dall'overflow dell'antenato.
  ⚠️ I dialoghi (Delega Renè, Aggancia) stanno FUORI dal menù: devono restare montati anche
  a menù chiuso, o l'evento che li apre non troverebbe nessuno.

- **La mail aperta su telefono era un impilamento (24 ago)** — «da mobile l'app è un
  disastro»: tre file di bottoni che a schermo stretto andavano a capo su sei-sette righe,
  e la mail cominciava sotto la piega. Rifatta in stile Apple Mail (blocco CSS in coda a
  `globals.css`, commentato): **azioni in una barra fissa in basso** che scorre di lato —
  tutte, nessuna tolta ([[feedback-non-togliere-azioni]]) — righe di comandi che scorrono
  in orizzontale, testa di navigazione sticky col vetro, bersagli 40px, campi a 16px (sotto,
  iOS zooma da solo), suggerimenti di tastiera nascosti su `hover:none` (non sulla
  larghezza: un iPad largo resta touch).
  ⚠️ **Niente `backdrop-filter` sulla barra fissa**: dentro ci sono dialoghi
  `position:fixed` (Delega Renè, Aggancia) e un filtro sull'antenato diventa il loro
  containing block — il dialogo si aprirebbe DENTRO la barra. E lo sfondo sta
  sull'ELEMENTO, non su un `::before` assoluto: il background di un contenitore che
  scorre non scorre via, lo pseudo-elemento sì.
  ⚠️ Verifica a schermo dall'utente richiesta ([[feedback-verifica-utente]]): il telefono
  vero non ce l'ho.

- **«Leggi recensione» lasciava un rettangolo grigio (24 ago)** — nella mail di Trustpilot
  il bottone ha **`target="_self"`** (misurato sull'HTML nel database), che **vince** sul
  `<base target="_blank">` dell'involucro: il clic navigava l'**iframe** verso
  trustpilot.com, che rifiuta di essere incorniciato (X-Frame-Options) → al posto della
  mail restava un riquadro grigio vuoto, e la recensione non si apriva da nessuna parte.
  Ora i clic sui link li intercetta la PAGINA (stessa strada dei tasti rapidi:
  `allow-same-origin`, niente script nel contenuto) e apre una scheda nuova con
  `noopener,noreferrer`. Solo http/https: `mailto:` resta al browser.
  ⚠️ **Regola**: dentro un iframe con contenuto altrui, `<base target>` non basta — un
  `target` scritto sul singolo link lo scavalca, e il guasto si vede solo sulle mail che
  lo usano (quelle «di piattaforma»: Trustpilot, corrieri, marketplace).

- **«App sconosciuta» salvando la chiave di Commerciale (24 ago)** — la card mostrava il
  campo, si incollava la chiave, e sotto compariva in rosso «App sconosciuta.». L'app c'era
  eccome: `commerciale` sta in `NomeChiaveApp` e in `MAPPA` (`chiaviApp.ts`) dal 23/08.
  ⚠️ Ma `salvaChiaveAppAction` **riscriveva la lista a mano**:
  `['anagrafiche','finance','fornitori','tasks','calendario','scripts']` — sei nomi su sette,
  fermi a prima. Aggiungere un'app a `MAPPA` non la rendeva salvabile.
  Ora `NOMI` è **esportata da `chiaviApp`** e l'azione usa quella: una sola fonte, e
  aggiungere un'app basta e avanza. Il messaggio d'errore dice anche **quale** nome è arrivato
  e quali sono quelli noti — «App sconosciuta» da solo non fa capire da che parte guardare.
  ⚠️ **Regola**: un elenco di cose ammesse non si riscrive accanto a quello vero. Due liste
  divergono sempre, e la seconda non lo dice — qui il sintomo era un campo che accettava il
  testo e poi lo rifiutava.

- **Lotto del 21-24/08/2026 — sette cose, tutte nate da una segnalazione a schermo.**
  1. **Le attività si annotano** (`Attivita.note`, `noteAutore`, `noteIl`). ⚠️ Il primo
     tentativo riusava `dettaglio` per non aggiungere colonne: sbagliato, perché quello è la
     descrizione di COSA FARE, quasi sempre scritta dall'AI, e «modifica» invitava a
     cancellarla per annotare («non modifica ma aggiungi nota»). Sono due cose diverse e
     restano due campi; al registro condiviso vanno insieme (`descrizione` = dettaglio +
     «Note (autore, data): …», con `noteIl` nell'impronta perché una nota modificata riparta).
  2. **Le cose da fare si vedono aprendo la mail** (blocco «Da fare su questa mail»).
     ⚠️ Difetto più vecchio trovato strada facendo: l'elenco stava DENTRO il riquadro
     «Cosa ha capito l'AI», quindi compariva **solo se l'AI aveva già letto la mail** — una
     attività creata a mano su una mail mai analizzata era invisibile dalla mail da cui nasce.
  3. **Le bozze mostrano il testo, non i tag**: il corpo è HTML (lo produce l'editor) e
     l'elenco lo stampava grezzo (`<p><br></p><table style=…`). Ora passa da `htmlAPlain`,
     e solo se `sembraHtml`: una bozza di solo testo non va convertita.
  4. 🔴 **Le bozze conservano gli allegati** (`Bozza.allegatiGruppo`). ⚠️ Non era «non li
     salva»: `salvaMinuta` chiamava `ripulisciAllegatiGrandi`, quindi **salvare una bozza
     BUTTAVA VIA i file già caricati**. Un allegato in partenza non vive da nessun'altra
     parte. Il salvataggio RIMPIAZZA il gruppo (togliere un allegato deve toglierlo davvero),
     l'invio ci aggiunge i conservati. ⚠️⚠️ La pulizia dopo l'invio sta **dopo
     `registraInviato`**, non dopo il flag `inviata`: quel flag è il LUCCHETTO preso PRIMA di
     spedire, e cancellare lì avrebbe perso gli allegati a ogni invio fallito. Ce l'avevo
     messa lì e l'ho spostata: è la classe di errore che si vede solo quando è tardi.
  5. **Nella conversazione i comandi stanno SOPRA la mail.** ⚠️ **QUARTA volta** che rientra
     la stessa segnalazione con la stessa forma (allegati → graffetta in testa, scorciatoie →
     lettere sui bottoni, app → «→ App» in testa, ora Rispondi/Inoltra/Stacca). **Regola
     scritta nel codice**: in questa pagina un comando si mette DOPO l'intestazione, mai in
     fondo — ciò che sta sotto il testo, per chi guarda, non esiste.
  6. ⭐ **Nei riassunti luoghi e orari non si perdono** (`REGOLA_DOVE_QUANDO`, un posto solo,
     usata da tutti e tre i prompt: mail singola, conversazione, sezione). Nato da un riassunto
     che diceva «un evento aziendale a Milano l'8 settembre 2026» mentre la mail diceva «at the
     **Plein hotel** … **starting 20:00**». ⚠️ **MISURATO prima di tenerlo** (regola
     dell'utente): 4 chiamate col prompt vecchio e 4 col nuovo, stesso testo — «Plein» 0/4 →
     4/4, «20:00» 0/4 → 4/4, «2026» 0/4 → 4/4; rimisurato dopo l'estrazione della costante,
     ancora 4/4.
  7. **Gli appuntamenti con data certa entrano in agenda da soli** (`sync.ts`, dove prima si
     salvava solo `eventoProposto`). ⚠️ **Due cancelli**: il prompt accetta come evento solo
     data+ora precise, e qui la data dev'essere valida una volta convertita in UTC; se uno dei
     due non passa si torna alla proposta col tasto, che non scrive niente. L'evento nasce
     `creatoDaAI` e legato al messaggio, e la mail mostra il riquadro **«In agenda»** con
     quando, dove e il tasto per toglierlo — una scrittura che non si vede è una scrittura di
     cui non ci si fida.
  ⚠️ **Da finire**: riaprendo una bozza con «Riprendi», la schermata di scrittura non mostra
  ancora l'elenco degli allegati conservati (partono, ma non si vedono finché non invii).

- **«I tasti rapidi a volte non funzionano» (20 ago)** — e il «a volte» era la chiave, come
  già per il pallino blu e per le mail inoltrate. Non è casuale: **dipende da dove hai
  cliccato l'ultima volta**. Il corpo della mail è un `<iframe>` (`CorpoMessaggio`); appena
  ci clicchi dentro — per scorrere, o per selezionare una frase — il fuoco passa al documento
  dell'iframe, e da quel momento `r`, `e`, `n`, `p`, `Canc` non arrivano più a nessuno,
  perché sia `Scorciatoie` sia `NavigaMessaggi` ascoltano sulla **finestra principale**.
  Riprendevano a funzionare cliccando fuori dalla mail, il che rende il difetto
  irriproducibile a comando e facile da liquidare come «a volte».
  Ora l'iframe inoltra i suoi `keydown` alla pagina. ⚠️ Si può ascoltare lì dentro perché la
  sandbox ha `allow-same-origin`; **non ha `allow-scripts` e non deve averlo** — dentro la
  mail non gira niente, l'ascolto lo installa la pagina. La difesa XSS resta intatta.
  ⚠️ Si dispaccia un evento **nuovo**: un evento appartiene al suo documento e non si
  ridispaccia altrove. ⚠️ Coi modificatori premuti non si inoltra, o `Ctrl+F` dentro la mail
  smetterebbe di aprire la ricerca del browser.
  ⚠️ Resta un secondo caso, **voluto**: col fuoco su un campo o sul menù a tendina delle
  sezioni le lettere non scattano (`INPUT`/`TEXTAREA`/`SELECT`/`contentEditable`). È giusto
  così — scrivere «e» in un campo non deve archiviare la mail — ma è l'altra metà del
  «a volte» e va detto a chi lo segnala.
  **Regola generale**: se una funzione è governata dalla tastiera e c'è un iframe in pagina,
  chiedersi sempre dove va a finire il fuoco. Un ascoltatore su `window` non sente i tasti
  premuti dentro un documento diverso.

- 🔴 **L'archivio finiva nel cestino, e sul server (20 ago) — regola SPENTA** — l'utente,
  guardando la webmail: «perché mi risultano tutte queste mail in cestino su server?».
  Catena, contata sul database: **54 regole con `archivia: true`** (49 di `nicolo.donato`,
  4 di `renato.cassoli`, 1 di `eleonora.mannini`) archiviano all'arrivo → `manutenzioneRetention`
  spostava nel cestino tutto ciò che stava in archivio da **30 giorni** → e dal 14/08 quello
  spostamento **seguiva sulla casella** (`allineaCartellaOra`). Risultato: **1.088 mail nel
  cestino, di cui 1.073 finite lì da sole** e **15** buttate da una persona; 1.071 su
  `renato.cassoli@deluxy.it`.
  🔴 **Perché era grave**: `svuotaCestinoDi` cancella **dal server, irreversibilmente**, e
  cancella **tutto** il cestino. Nel cestino le automatiche e le volontarie sono
  indistinguibili, quindi un solo clic su «Svuota cestino» — gesto che chi lo fa legge come
  «butto ciò che ho buttato» — avrebbe distrutto mille mail archiviate. Verificato che
  l'ultimo passo **non è automatico**: nessun cron chiama `svuotaCestinoDi`, solo la rotta
  attivata dall'utente.
  ⚠️ **`archivioInCestinoGiorni: 0`** = spenta, con **guardia esplicita prima della soglia**:
  `soglia(0)` è «adesso», quindi senza il controllo `<= 0` la regola «spenta» avrebbe preso
  `data < adesso` e cestinato **l'intero archivio al primo giro**. È la trappola che stavo
  introducendo io spegnendola.
  ⚠️ **Le 1.073 già spostate sono rimaste nel cestino**, per scelta dell'utente («ok così come
  ora»). Restano a un clic dalla cancellazione definitiva: se si riprende in mano la cosa, il
  recupero va fatto distinguendo `archiviato: true` (automatiche) da chi è stato cestinato a mano.
  ⚠️ **Lezione: archiviare non è buttare.** Chi archivia mette da parte per ritrovare — nella
  webmail dell'utente ci sono cartelle `Archivio 2023/2024/2025`. Una scadenza che trasforma
  «da parte» in «nel cestino», in silenzio e pure sul server, non è pulizia: è perdita di dati
  differita. Se si riaccende, **non** deve rispecchiarsi sulla casella, e il cestino deve
  dire quali mail ci sono finite da sole.
  ⚠️ Nota a margine: la retention dello SPAM (90 giorni) fa `deleteMany` **solo sul database**
  e non tocca il server — asimmetria opposta, lasciata com'è.

- 🔴 **L'immagine incollata non arrivava a destinazione (19 ago)** — segnalato in due
  battute: prima «non riesco a ridurre la dimensione della foto», poi «l'immagine poi non
  è neanche partita». Erano **due difetti diversi**, e il secondo era il grave.
  1. **Non si poteva ridimensionare**: `EditorRicco` è un `contenteditable` con
     `execCommand` e non aveva **nessuna** gestione delle immagini. ⚠️ Chrome, a differenza
     di Firefox, **non disegna le maniglie** di ridimensionamento: senza comandi espliciti
     un'immagine incollata resta grande com'è. Ora un clic la seleziona e compare una
     riga: Piccola/Media/Grande/Piena. ⚠️ Si scrive **sia l'attributo `width` sia lo stile
     inline**: i client di posta sono divisi e un foglio di stile esterno non arriva mai.
  2. 🔴 **Non partiva**: `spedisci` passava il corpo a `MailComposer` così com'era, e le
     immagini incollate sono `<img src="data:image/…;base64,…">`. **Gmail e Outlook
     bloccano le immagini `data:`**: la mail arrivava senza. ⚠️ **È il difetto peggiore
     della famiglia — sembra riuscito**: nella propria copia l'immagine si vede benissimo,
     perché lì il `data:` c'è ancora. Ora `immaginiInLineaComeAllegati` (in `htmlMail.ts`)
     le trasforma in parti MIME vere con `cid:`, `inline`, dentro un `multipart/related`.
  ⚠️ **Non si usa l'opzione `attachDataUrls` di nodemailer**: l'ho provata per prima ed è
  **inefficace qui**, perché la applica il *transporter* mentre noi costruiamo il MIME con
  `MailComposer` e spediamo il `raw` già fatto. Misurato prima di tenerla: col flag acceso
  il `data:` restava identico. **Lezione: un'opzione che «secondo la documentazione» risolve
  va vista funzionare, o si committa una correzione che non corregge.**
  ⚠️ Tocca **solo ciò che parte**: `registraInviato` salva `m.corpoHtml` originale coi
  `data:`, e deve restare così — il sanificatore toglie gli `<img src="cid:">` quando mostra
  una mail (non potrebbero caricarsi), quindi la nostra copia resta visibile.
  ⚠️ `image/svg+xml` **non** viene incorporato: un SVG è un documento e può portare script
  (stessa regola di `sanitizzaHtml`). Verificato con 10 controlli sul MIME prodotto, comprese
  le controprove «nessuna immagine → corpo intatto» e «svg → zero allegati».
  ⚠️ Resta il tetto Vercel di **4,5 MB per richiesta**: una mail con foto grandi può non
  partire affatto. Per questo il tasto **«Alleggerisci il file»** ridisegna davvero
  l'immagine (canvas → JPEG 0.82) invece di limitarsi a mostrarla piccola: ridurre la
  larghezza a schermo **non toglie un byte**, ed è così che nascono le mail da 20 MB.

- **TRASLOCO del database sul cluster condiviso (18 ago)** — AI Mail lascia
  `feleldlsreurqpdhstla` (account **cs@deluxy.it**, eu-west-1, piano **Free**, dove
  divide il progetto con la **piattaforma consegne**: misurato 566 MB, già oltre i
  500 MB del Free, quindi le due app rischiavano di andare in sola lettura insieme) e
  passa a **`zegbztfxisqeowngvgvh`** (eu-central-1, schema `mail`), il cluster delle
  altre 12 app, che sta sull'org **Deluxy in piano Pro** — 8 GB, backup giornalieri.
  ⚠️ **Il piano NON si legge dal database**: le due istanze sono identiche (Micro,
  `max_connections=60`). Si legge dalla Management API col PAT in `deluxy-scout/.env`
  (`GET /v1/organizations/<id>` → `"plan":"pro"`) o dai backup, che sul Free non esistono.
  Cose imparate, tutte pagate sul campo:
  1. **La copia a lotti da 100 ha ucciso la connessione della sorgente** («Server has
     closed the connection» a ~300/2521). In quelle 100 righe c'erano mail con **20 MB
     di HTML l'una**: la risposta era di centinaia di MB. Ora `LOTTO_MESSAGGI` e
     `GIORNI_HTML_COPIA` sono variabili d'ambiente di `scripts/sposta-database.mjs`.
  2. **Un trasloco non deve trascinarsi una cache.** L'HTML è ricaricabile dall'IMAP
     (`htmlServer.ts`): copiato con `GIORNI_HTML_COPIA=7` invece dei 30 di norma, il
     trasferimento è passato da ~1 GB a ~90 MB. Restano copiate **intere** le mail con
     `uid ≤ 0` (4 righe): per quelle il database è l'unica copia al mondo.
  3. ⚠️ **`creatoIl` è in UTC** (Vercel gira a UTC): due messaggi «salvati alle 15:45»
     sembravano persi da ore, erano invece arrivati alle **17:45 locali**, due minuti
     dopo l'avvio della copia. Prima di dire «mancano dei dati», convertire il fuso.
  4. ⚠️ **Una copia da un database VIVO è vecchia appena finisce**: la posta continua ad
     arrivare. Quindi l'ordine del passaggio è **prima si sposta la produzione sul
     database nuovo, poi si ripassa lo script** per raccogliere la coda (è ripetibile,
     `skipDuplicates`) — non il contrario, o la coda cresce all'infinito.
  5. ⚠️ **La region va spostata con il database**: `vercel.json` da `["dub1"]` (Dublino,
     accanto a eu-west-1) a **`["fra1"]`** (Francoforte, accanto a eu-central-1). Senza,
     ogni query riattraversa mezz'Europa — è la trappola del 31/07 al contrario.
  6. ⚠️ **I segreti su Vercel non li può cambiare Claude** (guard): `DATABASE_URL` e
     `DIRECT_URL` di produzione le incolla l'utente dal dashboard, prendendole da
     `deluxy-mail/.env.nuovo` (ignorato da git). Il vecchio progetto **resta intatto**
     come rete di sicurezza finché il nuovo non ha qualche giorno di esercizio sereno.
  7. ✅ **Esito (19/08 ore 08:5x)**: commutato e collaudato. **31 tabelle su 31, 31.134 righe
     del vecchio controllate, ZERO rimaste indietro.** Ma non è filato liscio, e le due code
     valgono più del risultato: (a) dopo la commutazione la risincronizzazione IMAP ha
     riportato 82 messaggi **da sola**, e ne restavano **13 fuori** — non rumore ma posta
     operativa («Saldo», «ORDER TO DELIVER BY 12pm», «ORDINE 2744»): il cursore `ultimoUid`
     riparte da dov'è, **non ricostruisce il buco**, quindi il ripasso NON è opzionale;
     (b) **6 attività** create sul vecchio dopo la copia erano rimaste indietro (le ha prese
     un recupero mirato, e nessuna è finita orfana perché la sua mail c'era già).
  8. ⚠️⚠️ **Confrontare due database per `id` DÀ IL NUMERO SBAGLIATO.** L'IMAP reimporta la
     stessa mail con un id nuovo: per id lo scarto risultava **2**, sulla chiave naturale
     `(accountId, direzione, uid)` erano **13**. Su una tabella che una risincronizzazione può
     riempire da sola, l'identità è la chiave naturale, non la chiave primaria.
  9. ⚠️ **Il progetto vecchio è fragile** (Free oltre il tetto): chiude la connessione a metà
     lettura, anche solo contando le righe. Per interrogarlo servono query strette, una
     connessione nuova a ogni tentativo e ritentativi con attesa crescente — il ripasso in
     blocco è morto due volte, il recupero mirato **una mail per volta** è passato al primo colpo.

- **Revisione completa del 14/08/2026 (workflow multi-agente) — corretti**. Sei revisori + verifica avversaria su ogni bug. Corretti in un lotto (commit del 14/08):
  1. ⚠️🔒 **XSS nell'editor di risposta**: il corpo HTML di una mail (non fidato) finiva in `innerHTML` di `EditorRicco` (documento PRINCIPALE, non l'iframe sandbox della vista) cliccando **Rispondi/Inoltra**. `sanitizzaHtml` toglieva gli `on*` solo se preceduti da uno **spazio**, ma `<img src=x /onerror=…>` ha una `/` prima — il browser lo esegue lo stesso. Ora il filtro cattura il separatore (`[\s"'`/]`), toglie anche `svg`/`math`/`base` e gli URL `data:`/`javascript:`, e `EditorRicco` risanifica al montaggio (protegge le bozze già salvate col payload). Verificato con 8 payload.
  2. 🔒 **Cross-tenant in `salvaMinuta`**: il `messaggioId` arrivava dalla form senza verificarne il proprietario; una bozza agganciata alla mail di un altro utente avrebbe spedito, all'invio, **dalla SUA casella** (`inviaBozza` legge `bozza.messaggio.account`). Ora si valida: se il messaggio non è tuo, la bozza si salva slegata.
  3. **Azioni di riga che toccavano UNA mail invece del thread** (la classe già rientrata 3 volte, trovata in altri 3 punti): la **selezione multipla** (`ListaMail.esegui` ora espande a `r.ids`), `smaltisciEProssimo` (dettaglio + tasti `e`/`Canc`, ora espande a `idsThread` e esclude il thread dal «prossimo»), e Archivia/Spam dal dettaglio (`AzioniMessaggio` usa ora `archiviaThreadSenzaAggiornare`/`segnalaSpamThread`).
  4. **Doppio invio (`inviaBozza`)**: check-then-act sul flag `inviata` (letto prima, scritto dopo l'SMTP). Ora lock atomico `updateMany(where inviata:false)` prima di spedire, rilasciato se l'SMTP fallisce.
  5. 🔒 **HTML injection nella pagina pubblica `/api/invito`**: il titolo dell'evento (da mail) interpolato senza escape. Ora `esc()`.
  6. **Prestazioni**: `/rubrica/[email]` trasportava i 3 corpi TEXT di 200 mail per mostrare solo l'anteprima (ora `omit`); `/statistiche` lasciava dentro `corpoTradotto` per 2000 righe (ora nell'`omit`); `/clienti` mandava `corpoTradotto` INTERO al browser per 2000 righe (ora troncato a 400 char, quanti ne mostra `RigaMail`).
  7. **UI**: hamburger invisibile su mobile (`var(--text-primary)` non esiste in tokens.css → sostituito con `var(--text)` in 9 punti); `FinestraScrivi` a **900px esatti** apriva una finestra `display:none` (`>=` → `>`); `PrioritaButtons` teneva `useState(priorita)` mai riallineato (aggiunto `useEffect`, come pallino/letto).

  **Il secondo lotto della stessa revisione (14 ago, «tutti») — corretti anche questi:**
  8. ⚠️ **Collisione UID INBOX/Inviata** (era alta): gli UID IMAP sono per-cartella, ma il vincolo era `@@unique([accountId, uid])` — un inviato con lo stesso uid di una mail in arrivo la faceva risultare «già presente», scavalcata per sempre dal cursore. Ora il vincolo è `@@unique([accountId, direzione, uid])` (migrazione: `DROP` vecchio index/constraint + `CREATE` nuovo, in `migrate-prod.mjs`) e **tutti** i controlli di presenza (`giaPresenti` INBOX/Inviata, `cercaEImporta`, il dedup in `salvaMessaggi`) sono ora scoped per `direzione`. ⚠️ Allargare un vincolo unico non viola dati esistenti — migrazione sicura.
  9. **Retention archivio→cestino solo in locale**: `manutenzioneRetention` marcava `cestinato` senza spostare sul server → HTML/allegati cercati nel Trash dove la mail non era. Ora prende gli **id** (a blocchi di 100, idempotente) e chiama `allineaCartellaOra(..., 'auto', 'cestino')` per far seguire la casella.
  10. **Retention e sequenze non giravano mai** (in coda dopo il giro che satura i 300s): spostate **in testa** a `sincronizzaTutti`, come già i registri. ⚠️ Costo accettato: le sequenze girano su uno stato posta di un giro fa.
  11. **`sincronizzaInviata` avanzava il cursore sui non-salvati**: `salvaInviati` ora torna `{ salvati, primoFallito }` e il cursore si ferma prima del primo fallimento **transitorio** (permanente = si salta, o ci si incaglia).
  12. **`registraInviato` uid negativo in race**: due invii simultanei calcolavano lo stesso uid → il secondo violava l'unico e l'invio, già partito via SMTP, risultava «non riuscito». Ora **retry su P2002** ricalcolando l'uid.
  13. **cestino parte sempre da «normale»**: `allineaCartellaDopo` ha ora la sorgente **`'auto'`** che deduce da OGNI mail (dalla sua sezione) se parte dal Junk o da INBOX — un thread misto non lascia più mail vive in SPAM sul server. Usato da tutti i punti che cestinano.
  14. **`SpostaSezione` spezzava la conversazione**: `spostaInSezione` ora sposta TUTTO il thread (`idsThread`), come archivia/cestina/spam.
  15. **Selezione che sopravvive al cambio filtro**: `ListaMail` ora pota la selezione alle sole righe correnti quando `righe` cambia (un `useEffect`).
  16. 🔒 **Azioni app Deluxy «automatiche» su dati AI da mail non fidata**: le azioni che **scrivono** (`anagrafiche.partner`, `finance.proforma`, `commerciale.trattativa`, marcate `scrive: true` in `appDeluxy.ts`) NON partono più da sole — anche con la sezione in «automatico» si chiede conferma mostrando i dati (in `spostaInSezione` e nel ramo `sposta` di `azioneMassa`). Le azioni di sola lettura (verifica/trova) restano automatiche.
- **«Trova fornitore»: «Ordine non trovato in Fornitori» (17 ago)** — la proposta a schermo diceva `{"brand": "deluxy.it", "number": "2725"}`, ma quell'ordine è di **Deluxy Flowers**: i numeri d'ordine sono **per negozio**, quindi su `deluxy.it` non esiste. Contato sul database: nella mail (`cmsx2psfc00hzl404biun3axw`) compare **solo** `deluxyflowers.com`, `deluxy.it` **mai**. ⚠️ **Misurato prima di dare la colpa al modello** (lezione già in memoria): riprodotto il prompt di produzione — stesso system, stessa guida, stesso `CONTESTO AZIENDALE` («non impostato»), stessi `NOSTRI DOMINI`, la stessa istruzione «fidata» della `RegolaApp`, corpo a 6000 caratteri — il modello risponde `deluxyflowers.com` **4/4** con gpt-4o-mini e **3/3** con gpt-4o. Quindi il valore sbagliato non si spiega col prompt e **non si corregge nel prompt**. Tre correzioni: (1) `brand` è ora un **enum** dei tre negozi (`NEGOZI_FORNITORI`, copia di servizio di `BRANDS` in `deluxy-search-supplier/api/order.js`) — un negozio inesistente non è più rappresentabile; (2) nuovo gancio **`daMail`** sull'azione: se la mail nomina **UN SOLO** negozio dei tre, quello vince sul modello (con zero o due non si tocca niente: non si indovina). ⚠️ Sta **dopo l'estrazione** e non in `normalizza`, che gira all'invio quando nei dati c'è anche ciò che l'utente ha corretto a mano: lì sovrascriverebbe la sua correzione. ⚠️ Confronto sul **dominio intero**, o `deluxy.it` «combacerebbe» dentro `deluxyflowers.com` — la stessa trappola del `includes` di `casoMarchio`; (3) il 404 ora dice **cosa ha provato**, gli altri negozi possibili e — se l'app li manda — gli **ultimi ordini veri** di quel negozio: un «non trovato» che non dice dove ha cercato non è diagnosticabile. Verificato con **8 prove**, compresa la mail vera.
- **«Come richiamo una app da qui?» (17 ago)** — la scheda «Manda a un'app Deluxy» (`AppSuMessaggio`) c'è dal 27 luglio, ma sta **in fondo** alla pagina del messaggio: sotto il corpo e sotto la conversazione. Su una catena di risposte — la norma qui — vuol dire non trovarla. Aggiunto **«→ App» nella striscia in testa** (`BottoneApp`, lo stesso della riga in elenco: manda l'evento `aimail:app`, e il dialogo è già montato nel layout). La scheda in fondo resta dov'era: chi la usa non deve reimparare niente. ⚠️ **Terza volta** con la stessa forma (allegati → graffetta in testa, scorciatoie → lettere sui bottoni, ora le app): in questa pagina tutto ciò che sta sotto il corpo, per chi guarda, non esiste.
- **Trascinare le mail nel menu (17 ago)** — chiesto: «permetti drag and drop per spostare le mail in altre caselle o metterle nel cestino». Il trascinamento **esisteva già** ma solo verso le carte APP DELUXY (`MailDrag`, formato `text/aimail-id`, un id solo). Ora `MailDrag` mette anche `text/aimail-ids` (JSON) e le voci del menu sono bersagli: **sezioni**, **Posta in arrivo**, **Archivio**, **Spam**, **Cestino** (`components/VoceMenu.tsx`: `VoceMenu` + `DropMail`, riusato dalle sezioni che hanno un contenuto proprio). ⚠️ Si trascina la **conversazione** (`RigaData.ids`), non la mail di testa — la trappola «una riga È un thread», già rientrata tre volte; e trascinando una riga **selezionata** si trascina l'intera selezione, o con dieci mail spuntate il gesto ne sposterebbe una. ⚠️ Bozze e Posta inviata **non** sono bersagli: non sono stati in cui si possa mettere una mail. ⚠️ Lo SPAM passa da `segnalaSpamThread` (è l'unico che deve far seguire la Posta indesiderata della casella); il ritorno in posta da `rimettiInPostaMassa`, che allinea il server **con la sorgente giusta per ogni mail** (dal Cestino o dal Junk: con un solo «da normale» il server resterebbe indietro). ⚠️ `onDragOver` **deve** chiamare `preventDefault()`, o il drop non avviene e non c'è alcun errore: semplicemente non succede nulla. ⚠️ Niente su telefono: il drag HTML5 non esiste sul touch — lì restano il menu «Sposta in…» e i tasti, che non sono stati toccati.
- 🔒 **Le immagini in linea si vedevano come icone rotte (17 ago)** — «perché si vede così?», con `firma5.png` e tre `Screenshot …png` in fila. Non era la mail: era la **correzione XSS del 14/08** troppo larga. `sanitizzaHtml` sostituiva con `#` **qualsiasi** `src="data:…"`, comprese le immagini incorporate in base64 — che sono il modo normale in cui i client mettono firme e screenshot. Misurato sul database: quella mail ha 7 `<img>`, di cui **4 `data:`** (le vere), 2 http e **1 `x-msg://`** (riferimento interno di Apple Mail). Ora `immagineInLineaSicura()` lascia passare solo `src="data:image/<raster>;base64,…"`; e i tag `<img>` con `cid:`/`x-msg:` — che non potranno **mai** caricarsi — vengono **rimossi**, così non resta l'icona rotta col nome del file. ⚠️ `image/svg+xml` resta bloccato: un SVG è un documento e può contenere script. ⚠️ `data:` resta vietato in `href`/`action` (lì è una navigazione, non un'immagine). Verificato con **16 prove**: le 4 forme lecite passano, e restano bloccati `data:text/html`, `data:image/svg+xml`, `data:` senza base64, `javascript:` in href, `data:` in href/action, `onerror` col separatore `/`, `<svg onload>`. **Lezione**: una difesa scritta «via tutto ciò che assomiglia al pericolo» rompe il caso legittimo più comune — e lo rompe **in silenzio**, perché nessun test guardava come si vede una mail con la firma.
- **Dall'archivio non si tornava indietro (17 ago)** — l'utente: «questa mail dice che è in archivio ma non trovo il modo di togliere da archivio». Vero: `archiviato` si poteva solo mettere. Sulla mail aperta il tasto «Archivia» **spariva** e non compariva nulla al suo posto; in elenco, `AzioniRiga` mostrava «Archivia» solo con `!archiviato` — quindi negli Archiviati non c'era **nessun** modo di rimettere una mail in posta (l'unico ritorno esistente era «Ripristina» dal cestino). Ora `disarchiviaThread()` + «Togli dall'archivio» sul dettaglio e «Disarchivia» sulla riga. ⚠️ Agisce su **tutto il thread**, come `archiviaThreadSenzaAggiornare`: se archiviare prende la conversazione, disarchiviare deve prendere la stessa cosa. ⚠️ Non tocca la casella: archiviare qui è uno stato di AI Mail e non sposta niente sul server, quindi non c'è nulla da riportare indietro. **Lezione**: ogni stato che si può METTERE da una schermata deve poter essere TOLTO dalla stessa schermata — un tasto che scompare non è un'azione reversibile.
- 🔴 **La dedup nelle migrazioni cancellava per UTENTE, non per casella (17 ago, trovata leggendo)** — in `scripts/migrate-prod.mjs` la pulizia una-tantum dei doppioni confrontava `utenteId` + `messageId`: chi ha **due caselle** riceve legittimamente la stessa mail in entrambe (alias, mail in copia) e sono **due mail vere**, ognuna col suo letto/archiviato e la sua posizione IMAP — una delle due sarebbe stata cancellata **a ogni deploy**. Contato in produzione prima di toccare niente: righe colpite **0** (le due copie della mail di Linn sono di utenti diversi, Nicolò ed Eleonora, quindi fuori tiro), ma **due utenti hanno già due caselle** — era una bomba a orologeria. Aggiunto `m."accountId" = k."accountId"`. ⚠️ Non ho eseguito la migrazione a mano per «vedere l'errore»: quella DELETE, lanciata da qui, avrebbe cancellato posta vera (vedi la regola in memoria «mai cancellare dati reali»). Si è contato con una SELECT.
- **«87/98 statement applicati»: era pgbouncer, non le migrazioni (17 ago)** — un deploy ha riportato 11 statement «saltati» sulla stessa DDL che il deploy precedente applicava tutta (98/98). Riprodotto in locale contro la produzione: `ERROR: prepared statement "s0" already exists` — la **6543** è il transaction pooler, dove le prepared statement collidono; sulla **5432** (session) le stesse query passano. Verificato che in produzione **non manca nulla** (31 tabelle, e tutte le colonne recenti presenti: `spamCaso`, `invitoRisposta`, `cartellaSpam`, `azioneId`, …). `migrate-prod.mjs` ora usa **`DIRECT_URL`** se c'è (con ripiego sulla 6543: meglio 87 statement che zero). ⚠️ Vale per **qualunque** script che spari molte query: sulla 6543 falliscono a caso, e l'errore sembra un problema di schema.
- ✅ **CHIUSO il punto «migrazione UID verificata solo nella logica»**: in produzione esiste `Messaggio_accountId_direzione_uid_key` e il vecchio `Messaggio_accountId_uid_key` **non c'è più** (letto da `pg_indexes` il 17/08).
- **Lo SPAM deciso da un admin vale per tutta l'azienda (17 ago)** — l'utente: «lo spam impostato da amministratore è poi centralizzato per tutte le mail». **Non era così**: le casistiche stavano solo in `Impostazione['spam.casi:<utenteId>']`, quindi la decisione di Nicolò valeva per Nicolò e i colleghi se la ritrovavano da decidere (e `Regola`, `Sezione`, `ContattoAI`, `Messaggio` hanno tutte `utenteId`: l'unica cosa condivisa era il contesto aziendale per l'AI). Chiesto e fatto: ora c'è la riga **globale** `spam.casi`, che scrivono **solo gli admin**, e in lettura `decisioniSpam` somma personale + azienda con la **globale che vince** (se no «vale per tutti» sarebbe falso proprio per chi aveva già detto no). `decidiSpamCaso` guarda il ruolo di chi decide e, se è admin, applica la decisione anche alla posta in attesa **degli altri utenti**: ⚠️ una sezione SPAM per utente, quindi si cerca per ognuno e si allinea la SUA casella (`allineaCartellaDopo` per utente) — con una sola query si sposterebbero mail in una sezione che non è loro. ⚠️ Un utente normale continua a decidere solo per sé.
- **«Esegui» sulle approvazioni: i tasti giusti c'erano solo in /attivita (17 ago)** — l'utente mostra la colonna «Da fare» con «Approva: è spam? Vision edge Solution…»: premendo **Esegui** compare il messaggio rosso «questa è una richiesta di approvazione… rispondi con Sì/No», **ma quei due tasti lì non ci sono**. Il guard nella server action (7 ago) funziona, la UI no: `DecidiSpamRiga` era stata messa solo nella pagina Attività, mentre «Esegui» compare in **quattro** posti. Ora anche `ColonnaAttivita` (che perciò seleziona `messaggio.spamCaso`) e la pagina `/assistente/[id]` mostrano «Sì, è spam» / «No, è buona» al posto di «Esegui». ⚠️ `PannelloAI` non è toccato: le sue azioni nascono da `analizzaContattoOra` (legate a un contatto, non a un messaggio) e non sono mai approvazioni. **Lezione, di nuovo**: un tasto che sul 5% dei casi fa la cosa sbagliata non va corretto, va **tolto** — e va tolto in TUTTI i posti in cui compare, non dove si è visto il problema.
- **«Delega Renè» non capiva «crea una task per il 15 di ogni mese» (17 ago)** — segnalato con lo schermo: rispondeva «Manca la data specifica per l'appuntamento». Causa: `classificaDelega` aveva **due soli esiti** (`risposta` | `agenda`), quindi una task ripetuta finiva in «appuntamento», che pretende una data e un'ora. Ora l'esito è **tre** (`attivita`) e `preparaAttivitaDelegate` crea le attività **collegate alla mail** (`messaggioId`, che era la seconda metà della richiesta: «collegando a questa mail ogni task»). ⚠️ **La ripetizione la espande il CODICE** (`lib/ricorrenzaAttivita.ts`), al modello si chiede solo `ripeti` («ogni quanto») e la **prima** scadenza: una lista di date scritta dal modello sbaglia i mesi corti e i giorni che non esistono. Sul mensile si tiene il giorno della prima scadenza e, se il mese è troppo corto, si prende l'**ultimo giorno di quel mese** («il 31 di ogni mese» a febbraio = 28, non il 3 marzo). Tetti: 12 volte di default sul mensile (12 settimane, 3 anni), **massimo 24** — una coda infinita di attività non è un promemoria. ⚠️ Le attività non sono `creataDaAI`: quel flag fa cancellare e riscrivere le proposte a ogni giro di `analizzaContattoOra`, e queste sono state chieste. ⚠️ Il corpo della mail entra come **contesto dichiarato non fidato** (§8), non come istruzione. La stessa espansione vale ora anche per il dialogo «Nuova attività» (`attivitaDaComando`), che con «ogni mese» ne creava una sola. **Misurato con chiamate vere**: classificazione **8/8** (la frase dell'utente → `attivita`, «metti in agenda la call di giovedì alle 15» → `agenda`, recap/inoltra → `risposta`), e **11/11** le prove sulle date.
- **Il destinatario non era cliccabile, e la sua scheda non esisteva (17 ago)** — segnalato sulla mail `cms7mlqrd000njp046z5cgeud` (proposta floreale a `linn_mp@hotmail.com`): «non posso cliccare su mail "a" linn_ per andare nel dettaglio di tutte le sue mail». Erano **due** difetti, e il secondo era il grave. (1) Nella testata il **mittente** era un link dal primo giorno, i **destinatari** testo morto — eppure su una mail INVIATA la controparte sta proprio lì. Ora `components/IndirizziCliccabili.tsx` spezza il campo con una regex **a gruppo di cattura** e trasforma in link solo ciò che è un indirizzo, lasciando nomi, virgole e angolari come sono (ricostruire il campo da un elenco di indirizzi perderebbe i nomi). (2) `/rubrica/[email]` cercava `mittente: email` **e basta**: per un contatto a cui avevamo solo SCRITTO la pagina rispondeva **404** — pur essendo linkata dalla rubrica, che i destinatari li conta da sempre (`elencoContatti`, ramo `daInvii`) — e per tutti gli altri mancavano le nostre risposte, cioè metà dello scambio. Ora la query è un OR (`mittente` **oppure** `direzione:'uscita'` + `destinatari contains`), l'elenco dice il verso con `↗`/`↙` e il KPI conta «N ricevute · M inviate». ⚠️ `contains` su un campo di testo libero è largo per forza: `altrolinn_mp@hotmail.com` **contiene** `linn_mp@hotmail.com`, quindi la precisione la mette un filtro in JS che confronta gli indirizzi **estratti** (`indirizziIn`), non la sottostringa. ⚠️ Il nome di un contatto mai-mittente si prende da come l'abbiamo indirizzato noi (`nomeNeiDestinatari`: `Linn Persson <…>`), o la scheda si intitolerebbe col pezzo davanti alla @. ⚠️ **Non** si è toccato il `a <destinatari>` dentro la pila della conversazione (`ConversazioneStack`): sta dentro un `<button>`, e un link dentro un bottone non è HTML valido — è la stessa ragione per cui il tasto «letto» della riga vive fuori dall'intestazione. Verificate con 20 prove le due funzioni e lo split (indirizzo che contiene il nostro, nome del destinatario vicino, punti nella parte locale, testo ricomposto identico).
  **Coda (stessa giornata): il link portava ancora a un 404, e la causa era mia.** Avevo scritto `direzione: 'uscita'` nel ramo dei destinatari, ma ⚠️⚠️ **`direzione` dice in quale CARTELLA è stata trovata la mail (INBOX / Inviata), non chi l'ha scritta**: misurato sul database di produzione, le due mail per `linn_mp@hotmail.com` sono `entrata` da `martina.calia@deluxy.it` (partite da un indirizzo Deluxy, con la casella in copia), quindi il filtro le scartava entrambe e la scheda restava un 404. Tolto il vincolo di direzione, e il **verso** in elenco (`↗`/`↙`, flag `alContatto`) più le etichette del KPI («N da lui/lei · M a lui/lei», non «inviate da te», che non sarebbe vero) si decidono ora dal **mittente**. ⚠️ Lo stesso errore era in `analizzaContattoOra` (`lib/sync.ts`, il quadro AI del contatto e l'API `/api/v1/contatto` che leggono Scout e FINANCE) in **due** modi: scartava le stesse mail («Nessun messaggio con questo contatto» a chi ne aveva), e prendeva il nome del contatto dalla prima mail `entrata` — cioè dal **collega** che gli aveva scritto: Linn sarebbe diventata «Martina Calia» per il modello, che poi le si rivolge così. Anche `daMe` ora guarda il mittente: dedurlo dalla cartella attribuisce al contatto parole nostre. **Lezione**: in questa app `direzione` è una proprietà della **cartella**, non dell'autore; per sapere chi ha scritto si guarda `mittente`, e per sapere con chi si parla si guarda mittente **e** destinatari.
- **Selezione multipla + azioni sulla scheda contatto (17 ago)** — «permettimi di selezionare più mail e scegliere diverse azioni tra cui: cestino, creare thread». La scheda `/rubrica/[email]` elencava le mail senza modo di agire in blocco. Nuovo `components/MessaggiContatto.tsx` (client): checkbox per riga, «Seleziona tutti», e barra con **Cestina · Archivia · Segna lette · ⛓ Unisci in una conversazione**. ⚠️ Qui una riga è UNA mail (la scheda le mostra una per una), quindi le azioni agiscono sulle scelte; il cestino usa `azioneMassa` (che ora fa seguire il server con sorgente `'auto'`). **«Unisci»** è nuovo: `uniciInThread(ids)` in `actions.ts` dà a tutte lo stesso `threadManuale` (ognuna trascina la sua conversazione; riusa un codice esistente se c'è) — è la stessa base di `agganciaAlThread`, ma per N mail. Compare solo con 2+ selezionate. ⚠️ La pagina passa dati **serializzabili** al client (date come ISO): `PrioritaButtons`/`RispostaAzioni` vivono ora dentro il componente client.
- **Immagini in linea mostrate come `<firma5.png>` nel corpo (17 ago)** — «perché si legge così?»: una mail Apple con firma e screenshot incorporati mostrava, nel testo, `<firma5.png><Screenshot 2026-07-18 alle 23.53.46.png>…`. I client Apple, nel testo semplice, mettono ogni immagine in linea come `<nomefile>`. Nuova `senzaSegnapostoFile()` in `lib/citato.ts`: toglie i `<nome.ext>` con un'estensione nota (immagini + pdf/office/zip), applicata all'anteprima (`ripulisciAnteprima`) e al corpo mostrato (`CorpoMessaggio`/`TestoConCitazione`) — quindi sistema **anche le mail vecchie**. ⚠️ **Non tocca gli indirizzi** fra parentesi angolari (`<martina.calia@deluxy.it>`): il pattern esclude «@» e «/», così mail e URL restano. Le immagini vere si vedono nella «versione formattata» (iframe) e fra gli allegati.
- **Anteprima con entità HTML grezze e caratteri-esca (17 ago)** — «perché si legge così?»: una promozionale mostrava `Op til 40% p&#229; udvalgte buketter &#8199;&#847; &#8199;&#847;…` invece di «Op til 40% på udvalgte buketter». Due cose insieme: **entità numeriche** non decodificate (`&#229;`=å) e, fra le parole, `&#8199;&#847;` = spazio-cifra (invisibile) + giuntore di grafemi (invisibile) ripetuti, **messi apposta per spezzare le parole e ingannare i filtri antispam**. Corretto in `ripulisciAnteprima` (`lib/citato.ts`, applicata al display → sistema **anche le mail vecchie**): decodifica le entità (numeriche dec/hex + `&nbsp;&amp;&quot;&apos;`, NON `&lt;/&gt;` per non far rivivere residui di tag) e toglie i caratteri invisibili (`­͏​-‏  ⁠﻿`). ⚠️ **` `/` ` vanno scritti come ESCAPE nella regex, mai come carattere grezzo**: dentro un letterale `/.../` sono terminatori di riga e danno «Invalid regular expression» — la build non se n'era accorta finché non è passata coi giusti escape. ⚠️ La stessa sporcizia sta anche nel `corpoTesto` che legge l'**AI** (vedi il punto aperto «856 mail col testo sporco»): la decodifica in lettura pulisce ciò che si mostra, non ciò che l'AI riceve.
- **Dalle attività non si tornava alla mail (7 ago)** — «mi manca la possibilità di andare a riprendere la mail o le mail da cui parte questa attività». Il link **c'era già**: era il titolo del blocco, ma con `textDecoration: 'none'` e nient'altro — cioè, a vederlo, testo. Ed è la stessa lezione delle scorciatoie: una funzione che non si annuncia, per chi guarda non esiste. Ora in `/attivita`: un **«Apri la conversazione (N) →»** in fondo all'intestazione del blocco, la riga **«da <oggetto>»** su OGNI attività (prima compariva solo se il gruppo aveva mail diverse) e un tasto **«✉ Mail»** accanto a «Esegui» — perché prima di eseguire, di solito, si rilegge.
- **La priorità non prepara più la risposta: c'è «R+» (7 ago)** — chiesto dall'utente. Dare un P0 voleva dire «questa è urgente» e faceva partire **anche** una bozza di risposta: un gesto, due effetti, e chi stava solo classificando la posta si ritrovava bozze mai chieste. Ora `analizzaMessaggioOra` ha il parametro `conBozza` e `impostaPriorita` lo passa a **false** (riassunto e attività restano); la bozza si chiede col tasto **R+** accanto alle priorità (`components/BottoneRispostaAI.tsx` → `preparaRispostaAI`). ⚠️ Sotto R+ c'è la strada di **«Delega Renè»** (`preparaRispostaDelegata`), non quella dell'analisi: legge **tutta la conversazione** e la rubrica, quindi scrive meglio della vecchia bozza, che vedeva la sola mail. ⚠️ La lettura in sottofondo (AI+) continua a proporre la bozza — lì la proposta È la funzione — quindi `conBozza` resta `true` di default.
- **Gli allegati si scaricano dalla graffetta in testa (14 ago)** — «dammi possibilità di scaricare allegati subito da qui invece di scorrere in basso». L'elenco c'era ma sta **sotto il corpo della mail**, e su una catena di risposte citate — la norma qui — vuol dire scorrere fino in fondo per un file che si sa già di volere. La graffetta `📎 5` in testa diceva quanti erano ed era l'unica cosa in alto: ora è il comando (`components/AllegatiInTesta.tsx`). ⚠️ Chiusa non costa niente: `AllegatiMessaggio` legge dal server al **montaggio**, quindi finché non la apri non parte nessuna richiesta. L'elenco in fondo resta dov'era: chi lo usa non deve reimparare niente. ⚠️ Terzo posto in cui compaiono gli stessi allegati (in fondo, nella pila, in testa) e sempre con la stessa funzione: se cambia, cambia per tutti e tre.
- **Riassunto: come si rilancia, e gli allegati dentro la pila (9 ago)** — due domande dell'utente in fila, stessa radice: **una cosa che si può fare ma non si vede, per chi guarda non esiste**. (1) «Come posso rilanciare il riassunto?» — bastava ripremere il livello, ma il tasto acceso sembra uno **stato**, non un comando: ora quello attivo diventa `↻ Profondo` e il suo title dice «rifai da capo». In più, se la conversazione è cresciuta dopo l'ultima lettura, compare **«Da aggiornare»** col conto («fatta su 10 messaggi, adesso sono 17»): il dato c'era già (`messaggiVisti`) ma andava confrontato con quelli di ORA — il componente riceve `messaggiOra` dalla pagina. (2) «Dove vedo gli allegati di questa mail del thread?» — la graffetta sulla riga li annunciava, ma `AllegatiMessaggio` stava **solo** sulla mail a tutta pagina: dentro una conversazione il file che serve (planimetria, preventivo) sta quasi sempre in un messaggio di mezzo. Ora l'elenco compare aprendo quel messaggio nella pila, e si chiede al server solo allora.
- **Mancavano le mail inviate da un ALTRO programma (9 ago)** — segnalato con lo screenshot della webmail: mail presenti là e assenti in «Posta inviata». Causa: `sincronizzaUtente` (il giro frequente, quello del cron e del tasto «Aggiorna posta») leggeva **solo la INBOX**; la cartella «Inviata» la guardava soltanto `sincronizzaInviata`, chiamata unicamente da `/api/scarica-storico` — cioè lo scarico di background, su richiesta. Quindi tutto ciò che mandavi da webmail o telefono entrava in AI Mail solo se e quando qualcuno lanciava lo storico. ⚠️ **Perché non si era mai notato**: le mail scritte DA QUI si salvano da sole al momento dell'invio (`registraInviato`), quindi la posta inviata sembrava completa — mancava solo quella scritta altrove. Ora il giro frequente fa anche gli inviati **nuovi** (`sincronizzaInviata(id, false)`: solo oltre il cursore, max 25, una connessione), dentro un `try` perché un inciampo lì non deve far sembrare fallito tutto il giro. ⚠️ Lo **storico vecchio** resta al drain di background: il cursore `ultimoUidInviata` fa entrare solo il nuovo, e le mail più vecchie del cursore si prendono da «Scarica tutta la posta di sempre». ⚠️ Niente doppioni: `salvaInviati` riconosce per Message-ID la copia già salvata all'invio.
- **«Non è spam» toglieva UNA mail, non la conversazione (9 ago)** — segnalato: «ho tolto questo thread da spam ma alcune mail continuano ad esserci». I due gesti opposti lavoravano su quantità diverse: dalla riga, «Spam» chiama `segnalaSpamThread` (tutto il thread, `perThread` in `AzioniRiga`), ma «Non è spam» chiamava `segnalaNonSpam` su **un id solo**. Ora anche il ritorno è del thread, e sposta indietro anche sulla casella. ⚠️ Si toccano **solo le mail davvero nello SPAM** (`sezione: { nome: 'SPAM' }` nel where): se la conversazione ne ha altre già in posta, azzerarne la sezione sarebbe un danno gratuito. ⚠️ È la **terza volta in tre giorni** che salta fuori la stessa cosa (pallino blu, «✓ Letto», ora lo spam): in questa app **una riga È un thread**, e ogni azione di riga va scritta sul thread — aggiungendone una, la domanda da farsi è «su quante mail agisce?».
- **Le condizioni anche in Inviata e Bozze (9 ago)** — richiesto subito dopo. `CondizioniRicerca` ha ora `base` (dove rimandare la ricerca) e `campi` (quali condizioni mostrare): negli **inviati** niente «da» (il mittente sei sempre tu), nelle **bozze** niente «da», niente allegati e niente sezione — una bozza non ne ha. ⚠️ Mostrare un campo che non filtra niente è peggio che non averlo: sembra rotto. ⚠️ In **Bozze** non c'era **nessuna ricerca**: aggiunta anche quella, e il periodo lì guarda `aggiornataIl` (l'ultima modifica), che è la data mostrata sulla riga — non la creazione. ⚠️ Lo stato vuoto delle bozze ora distingue «Nessuna bozza» da «Nessuna bozza **trovata**»: cercando, il primo sarebbe una bugia.
- **Condizioni di ricerca (9 ago)** — richiesto. La ricerca era una sola casella di testo su sei campi in OR: nessun modo di dire «da Martina», «a settembre», «con allegati», «solo nell'oggetto». Ora `components/CondizioniRicerca.tsx` più il ramo `whereRicerca` in `ListaPosta`: `da`, `a`, `dal`, `al`, `allegati`, `dove` (ovunque / oggetto / corpo / persone) e `sezione`, **in AND** fra loro e con le parole. ⚠️ Tutto **nell'indirizzo** (`?q=…&da=…&dal=…`), non in uno stato del componente: una ricerca costruita in sei mosse si ricarica, si tiene fra i preferiti, si manda a un collega. ⚠️ La pagina entra in modalità ricerca se c'è `q` **oppure** una condizione qualsiasi: «tutto quello che mi ha mandato Martina a settembre» è una domanda completa anche senza parole. ⚠️ `al` include il giorno indicato (`T23:59:59`), o «al 30 settembre» si fermerebbe al 29. ⚠️ `snippetRicerca` ora si tira indietro sotto i 2 caratteri: cercando la stringa vuota tornava l'inizio della prima mail spacciato per risultato.
- **Riassunto della conversazione a tre livelli (9 ago)** — richiesto: «veloce, medio, profondo». Non è solo un prompt diverso: cambia **quanto testo** si manda (600 / 1500 / 5000 caratteri per mail) e, per «profondo», anche il **modello** — `MODELLO_GRANDE` (`OPENAI_MODEL_GRANDE`, default `gpt-4o`), acceso SOLO lì. ⚠️ È l'unico punto dell'app che usa il modello grande, ed è deliberato: su un riassunto lungo la differenza si vede (misurato in questo progetto: piccolo 0/6, grande 6/6 sul seguire istruzioni), su uno veloce sarebbe denaro buttato. Si torna a un modello solo mettendo `OPENAI_MODEL_GRANDE` uguale a `OPENAI_MODEL`. ⚠️ **L'aggiornamento incrementale vale solo per «medio»**: chiedendo «profondo» si rilegge tutto da capo — aggiornare un riassunto veloce con due mail nuove darebbe un finto approfondimento, con la parte vecchia rimasta corta. ⚠️ Il livello si SALVA dentro la vista (`AnalisiThreadVista.livello`) e si mostra in fondo: se no, riaprendo, due righe sembrano un riassunto povero invece che una lettura veloce.
- **«Bozza pronta» su una mail già inviata (9 ago)** — segnalato. Rispondendo si cancellava **solo la bozza da cui eri partito** (`bozzaId` nel form), ma su quella mail ce n'è spesso una seconda: quella scritta dall'**AI**, che è una riga diversa di `Bozza`. Restava `inviata: false`, e il badge in elenco (`bozze: { where: { inviata: false } }`) continuava a dire «Bozza pronta» su una mail a cui avevi già risposto. Ora rispondere cancella **tutte** le bozze non inviate di quella mail, e `inviaBozza` fa lo stesso con le altre rimaste. ⚠️ **L'inoltro no**: girare la mail a un collega non risponde a chi ha scritto, e quella bozza serve ancora — stessa distinzione di `serveRisposta`. ⚠️ Le bozze **già rimaste indietro** (prima di oggi) restano lì: la correzione vale da qui in avanti, per le vecchie serve una pulizia una tantum — da fare guardando prima l'elenco, perché fra quelle ci sono anche testi scritti a mano e mai finiti.
- **Le mail di Renè uscivano «tutte attaccate» (9 ago)** — segnalato: «ha scritto il testo ma non è formale e non va a capo nei punti giusti». ⚠️ **Non era il prompt**, ed è il motivo per cui è stato misurato prima di toccarlo (lezione già in memoria: misurare su N chiamate, non indovinare): con 6 chiamate vere, il modello impagina **bene** — saluto su una riga, 6 paragrafi separati. Il guasto sta DOPO: il modello scrive il corpo a testo semplice e in fondo ci incolla la **firma**, che è una `<table>` HTML. Ne esce una stringa **mista**, e `EditorRicco` sceglieva fra «tutto testo» e «tutto HTML»: trovata la tabella la dichiarava HTML e la inseriva tale e quale — dove gli a-capo del testo non valgono niente. A schermo: muro di testo e sotto una firma impaginata benissimo (il dettaglio che avrebbe dovuto far sospettare subito). Ora c'è `mistoAHtml()` in `lib/htmlMail.ts`: taglia al primo blocco HTML, converte quello che sta prima, lascia stare il resto. Verificato: 0 → 9 `<br>`, firma intatta, HTML puro invariato, testo puro invariato. ⚠️ Vale per **tutte** le bozze (Renè, analisi, testi pronti), non solo per quella segnalata. Sul prompt sono comunque aggiunte le regole di **impaginazione** e di **registro formale** (in italiano si dà del Lei), che mancavano del tutto.
- **«Esegui» su una richiesta di approvazione scriveva una risposta al phishing (7 ago)** — segnalato dall'utente, ed è il difetto più brutto di questa serie: premendo «Esegui» sull'attività «Approva: è spam? …» l'app ha aperto una **bozza di risposta alla mail falsa**. Causa: «Esegui» ha UN significato solo — «l'AI scrive la mail che porta a termine il compito» — e le attività di approvazione nate il 5 ago sono compiti di un altro tipo, che si chiudono **decidendo**. Ora in `/attivita` quelle righe mostrano «Sì, è spam» / «No, è buona» (`components/DecidiSpamRiga.tsx` → `decidiSpamCaso`, che applica la decisione a **tutta la casistica** in attesa), e `eseguiAttivita` **rifiuta** in partenza se la mail ha `spamCaso` valorizzato, spiegando cosa fare. ⚠️ Il guard sta nella **server action**, non solo nella pagina: «Esegui» compare in quattro posti (attività, assistente, colonna della posta, pannello AI) e mettere il controllo solo dove si è visto il problema vuol dire ritrovarlo negli altri tre. ⚠️ **Lezione**: un tasto che fa la cosa giusta nel 95% dei casi, sul restante 5% non va corretto — va **tolto**. Aggiungendo un tipo nuovo di attività, chiedersi sempre se «Esegui» ha senso per lui.
- **Pallino blu e tasto «Non letto» sulla stessa riga (7 ago)** — segnalato subito dopo la correzione del pallino, ed **è una coda di quella correzione**: `SegnaLettoRiga` teneva un suo `useState(nonLetto)` inizializzato al primo render e mai riallineato. Riallineando la RIGA (il pallino) e non il TASTO dentro di lei, i due si sono messi a dire cose diverse: pallino blu, tasto «Non letto». Ora quel tasto **non ha più stato**: legge il `nonLetto` della riga e la cambia con `onCambio` (che era già lì, ma facoltativo — ora è obbligatorio). ⚠️ **La lezione, più forte di quella di ieri**: non basta riallineare lo stato locale al server, bisogna avere **una sola copia** di quello stato. Due componenti che tengono lo stesso dato sono due verità che prima o poi divergono; se il dato serve al genitore, il figlio lo riceve e basta. ⚠️ Restano nell'app altri `useState(prop)` che rispecchiano uno stato del server (`CheckAttivita`, `ChiudiThread`, `BottoneContattoAI`): lì non c'è un secondo elemento che mostri lo stesso dato, quindi non si contraddicono a schermo, ma la trappola è la stessa.
- **I dialoghi non avevano la ✕ (7 ago)** — segnalato sul dialogo «Aggancia una mail»: il tasto «Chiudi» c'era, ma **in fondo**, e quel dialogo mostra decine di risultati che scorrono — quindi restava sotto la piega e la finestra sembrava senza uscita. (Il clic fuori chiudeva già, ma è una cosa che si sa solo se la si prova, e su una finestra che sembra bloccata non si prova.) Ora `components/ChiudiModale.tsx`: la **✕ in alto a destra** più `useChiudiConEsc` (il tasto `Esc`, che è quello che si prova per primo), messi in **tutti e otto** i dialoghi dell'app — aggancia, delega Renè, azione rapida, nome conversazione, app Deluxy, nuova attività, appuntamento, scorciatoie. ⚠️ `.modal-title` è diventata una riga **sticky**: in un dialogo che scorre, un tasto di chiusura che scorre via non risolve niente. I margini negativi (`margin: -20px -22px 12px`) e `top: -20px` servono a farla aderire al bordo del dialogo nonostante il padding: senza, sopra l'intestazione appiccicata si vedrebbe passare il contenuto.
- **Invito accettato, ma la mail restava uguale (7 ago)** — segnalato dall'utente: «dopo aver fatto accetta si è aggiunta al calendario ma la mail è rimasta uguale». Funzionava tutto (evento creato, risposta iCal spedita all'organizzatore): **non restava traccia**. Due cause distinte. (1) **La risposta non era salvata da nessuna parte**: viveva nello `useState` del componente, quindi bastava riaprire la mail e i tre tasti tornavano vergini, come se non avessi mai risposto. Ora ci sono `Messaggio.invitoRisposta` e `invitoRispostoIl` (DDL in migrate-prod), scritti **per primi** in `rispondiInvito`, e il riquadro mostra «Hai accettato · 7 ago 16:52» col tasto scelto acceso. Gli altri due restano premibili: cambiare idea è normale e l'organizzatore riceve la risposta nuova, come in Outlook. (2) **Il riquadro non si rileggeva**: `router.refresh()` rifà i Server Component, ma questo è un componente **client** e i suoi dati arrivano da `leggiInvito`, che gira una volta sola al montaggio — quindi nemmeno «Già nel tuo calendario» compariva. Ora dopo la risposta si richiama `leggiInvito`. ⚠️ Vale in generale in quest'app: **`router.refresh()` non aggiorna i dati che un componente client si è preso da solo con una server action**; quelli vanno richiesti di nuovo a mano.
- **«Diminuisce il conteggio ma il colore rimane blu» (5 ago, subito dopo)** — la segnalazione successiva, e sotto c'erano **due** difetti. (1) **Stato locale mai riallineato**: `RigaMail` faceva `useState(r.nonLetti)`, che tiene il valore del PRIMO render per sempre; tornando in elenco il server diceva ormai «letta» ma la riga restava blu — mentre il contatore in alto, che stato locale non ha, calava. Due pezzi della stessa schermata che si contraddicono: il classico. Ora c'è `useEffect(() => setNonLetti(r.nonLetti), [r.nonLetti])` — locale per reagire al clic, ma il server vince. (2) **Si segnava una mail sola**: in elenco una riga È un thread, e la conversazione della segnalazione ne aveva due. Ora l'apertura chiama `segnaLettoThread` (tutta la conversazione, come «✓ Letto», rispondi e inoltra), e la pagina passa `daSegnare` guardando **tutte** le mail del thread, non solo quella aperta — se no una conversazione con la mail in cima già letta non sarebbe stata toccata. ⚠️ Vale anche per il contrario: quando si tiene uno stato in locale «per reattività», va sempre previsto il riallineamento al server, o si guardano due verità diverse.
- **Aprire una mail non la segnava letta (5 ago)** — segnalato dall'utente («questa mail mi risulta da leggere nonostante l'abbia aperta»): era così da sempre, il pallino si spegneva solo col tasto «✓ Letto», rispondendo, archiviando o cestinando. Ora `components/SegnaLettaAllApertura.tsx`, montato dalla pagina del messaggio. ⚠️ **Perché un componente client e non una scrittura nel render**: la pagina è un Server Component rieseguito a ogni `router.refresh()`, e il refresh lo fa anche **«Segna non letto»** — marcando dentro il render, quel tasto si sarebbe disfatto da solo un istante dopo. Marcando **al montaggio**, invece, è una volta per mail aperta e il «non letto» resta. ⚠️ `id` nelle dipendenze e non `[]`: passando da una mail all'altra il componente può essere riusato invece che rimontato, e la seconda resterebbe da leggere. ⚠️ **Niente `router.refresh()` dopo la marcatura**: rifarebbe l'intero render (conversazione, riassunto, chiavi) per cambiare una parola su un bottone — è il «lavoro enorme per un risultato minuscolo» già pagato in §9. Si manda un evento (`aimail:letta`) e `AzioniMessaggio` tiene lo stato in locale, come fa già `ConversazioneStack`.
- **Anche lo SPAM segue il server (5 ago, subito dopo il cestino)** — stesso meccanismo, stessa richiesta dell'utente: segnare spam sposta la mail nella **Posta indesiderata** della casella (flag IMAP `\Junk`, `Account.cartellaSpam`, DDL in migrate-prod) e «Non è spam» la riporta in posta. Aggancio su **tutti e quattro** i modi in cui una mail finisce nello SPAM: tasto «Spam», SPAM di tutto il thread, **casistica approvata** («e fallo sempre») e **antispam automatico all'arrivo**. ⚠️ Il modulo è diventato `lib/cartelleServer.ts` (era `cestinoServer.ts`: un file chiamato «cestino» che sposta anche lo spam è un nome che mente) e la funzione prende **`da` e `a` espliciti**: quando il codice gira il database è già aggiornato, quindi da lì non si può più sapere da dove veniva la mail — una appena tolta dallo SPAM risulta «in posta normale». Chi chiama lo sa, il modulo no. È per questo che `smaltisciEProssimo` ora legge anche il **nome della sezione**: cestinare una mail che sta nello SPAM parte dalla Posta indesiderata, non da INBOX. ⚠️ Nel **cron** l'allineamento NON gira in `after()` una mail per volta: gli id si accumulano e si spostano **in blocco a fine giro** (una connessione invece di N), best-effort e in fondo — lì il fiato è già corto. ⚠️ `cartellaDiMessaggio` ora guarda anche la sezione, e le tre letture dal server (allegati, zip, HTML) si portano dietro `sezione: { nome }`: senza, gli allegati di una mail nello SPAM si sarebbero cercati in INBOX.
- **Cestinare ora lo vede anche il server (5 ago)** — domanda dell'utente: «il server recepisce le mail che mandiamo nel cestino?». **No**: cestinare scriveva solo `cestinato: true` nel database, la mail restava intatta in INBOX — dal telefono te la ritrovavi, e la casella non si sgonfiava mai (solo «svuota cestino» toccava il server, cancellando). Ora `lib/cestinoServer.ts`: cestinare **sposta** la mail nella cartella Cestino della casella (IMAP MOVE, `spostaSulServer` in imap.ts), recuperarla la riporta al suo posto — INBOX, o «Inviata» se è una mail partita. La cartella si scopre da sola (flag `\Trash`, poi i nomi comuni) e si salva in `Account.cartellaCestino` (DDL in migrate-prod): è una LIST, non va rifatta a ogni mail. ⚠️ **Si SPOSTA, non si cancella**: è reversibile, ed è la ragione per cui può girare da solo senza chiedere niente — svuotare il cestino, che cancella per sempre, resta un'azione dichiarata. ⚠️ Gira in **`after()`**: è una connessione IMAP per giro e cestinare dieci mail di fila non deve far aspettare nessuno; se fallisce, **il cestino di AI Mail resta quello buono** (la mail è comunque fuori dai piedi qui: al massimo il server non è allineato, mai il contrario). ⚠️ Le mail si cercano **per Message-ID** come in `eliminaDalServer`: un UID vecchio può puntare a un'altra mail, e qui si sposterebbe quella. ⚠️ **Due code da non dimenticare**, ed erano regressioni che stavo introducendo io: (1) `svuotaCestinoDi` ora guarda **prima** nella cartella Cestino — se no, dopo questa modifica non avrebbe più trovato niente da cancellare sul server — e poi comunque nelle cartelle normali, per le mail cestinate PRIMA di oggi; (2) `cartellaDiMessaggio()` dice dove sta ORA una mail, e la usano HTML dal server, allegati e zip: senza, aprire una mail dal Cestino avrebbe perso impaginato e allegati. ⚠️ **Eccezione voluta**: la retention automatica (archiviati → cestino dopo N giorni, in `sync.ts`) NON tocca il server — sposterebbe migliaia di mail da un cron che già va in timeout.
- **Chi si finge un marchio: si approva una volta, poi è automatico (5 ago)** — regola dettata dall'utente su un caso vero (`Shopify <info.shopifymail.it@gmail.com>`, oggetto «Verifica della conformità GDPR»): *se ci si presenta come Shopify ma l'indirizzo è un altro o è gmail, è spam*. Il controllo c'era già ma **non prendeva questo caso**: `shopify` non era fra i marchi, e soprattutto il confronto era `dominio.includes(marca)` — con cui `shopifymail.it` «è» Shopify. Ora `casoMarchio()` in `lib/spam.ts` confronta il dominio **per intero o come sottodominio** contro l'elenco dei domini VERI del marchio, cerca il marchio nel nome mostrato **e nella parte prima della @** (è lì che lo nascondono), e tratta a parte i **provider gratuiti** (nessuna azienda scrive ai clienti da gmail). ⚠️ Aggiungendo un marchio a `MARCHI`, elencare TUTTI i suoi domini: uno dimenticato manda in SPAM la posta vera. ⚠️ **La casistica non dà punti al punteggio**: se li desse, la mail verrebbe spostata comunque e l'approvazione sarebbe una domanda a cose fatte. Il flusso chiesto è **la prima volta decidi tu, poi si applica da sola**: `lib/spamCasi.ts` tiene approvate/rifiutate per utente in `Impostazione['spam.casi:<utenteId>']`, la mail in attesa porta `Messaggio.spamCaso/spamMotivo` (DDL in `migrate-prod.mjs`), e la proposta viaggia su **due binari** — il riquadro rosso sopra la mail (`components/PropostaSpam.tsx`) e un'**attività** «Approva: è spam? …», cioè la «richiesta di approvazione come task». ⚠️ Anche il **no** si ricorda, come le proposte rifiutate di Renè: una proposta che ritorna a ogni mail è peggio di non averla mai fatta. ⚠️ `casoMarchio` è **pura e senza database** apposta: la pagina del messaggio la rifà al volo, così la regola vale anche per la posta arrivata PRIMA che esistesse (se no proprio la mail che hai sotto gli occhi resterebbe senza avviso), e `decidiSpamCaso` ricalcola l'id per lo stesso motivo. Decidendo, la scelta si applica a **tutte** le mail della stessa casistica ancora in attesa e chiude le attività relative.
- **«Chiedi a questa conversazione»: una DOMANDA, non una bozza (4 ago)** — chiesto dall'utente («posso chiedere a Renè se in un thread c'è o no un'informazione?»). Non si poteva: il riassunto PLUS AI racconta lo scambio ma non risponde, e **«Delega Renè» ha due soli esiti** — `classificaDelega` torna `'risposta' | 'agenda'` — quindi a «c'è l'IBAN?» preparava *una mail che chiede l'IBAN*. Domandare e far scrivere sono due gesti diversi, e mancava il primo. Ora: `rispondiSuThread()` in `lib/ai.ts` (schema strict `trovato/risposta/msgIdx/citazione`, `temperature: 0`), `rispondiSuThreadOra()` in `lib/sync.ts`, azione `chiediAllaConversazione()`, UI `components/ChiediConversazione.tsx` in fondo alla mail (dove uno arriva dopo averla letta e si accorge che gli manca un dato). ⚠️ **La risposta esce solo con la fonte**: mail che si apre + parole esatte copiate; se l'indice del messaggio non è valido, `trovato` viene forzato a **false** lato server — una citazione che non si può aprire vale quanto un'affermazione secca, e qui si decidono soldi e consegne. ⚠️ «Non l'ho trovato» è una risposta **buona**: il prompt dice esplicitamente che *non sono sicuro* e *non c'è* sono la stessa cosa. ⚠️ I corpi restano **dato non fidato** (§8) e non si salva niente: è una domanda, non un documento — quindi nessun `revalidatePath`, o si pagherebbe un rerender per una cosa che deve sembrare una risposta.
- **L'inoltro non inoltrava gli allegati (4 ago)** — segnalato dall'utente. `inviaMessaggio` metteva nella mail solo `leggiAllegati(form)`, cioè i file **aggiunti a mano in quella schermata**: quelli dell'originale non li andava a prendere nessuno, in nessun modo. È il difetto peggiore della famiglia, perché **sembra riuscito**: la mail parte, dice «inviato», e se ne accorge solo chi la riceve. Ora c'è `allegatiInoltrati()` in `lib/actions.ts`: riprende i file dalla casella **IMAP** con `leggiTuttiAllegati` (una connessione, un solo scarico del sorgente — la stessa di «Scarica tutti»). ⚠️ **Dal server, non dal browser**: i file sono già in casella, e farli passare per la richiesta li fermerebbe al tetto Vercel di 4,5 MB. ⚠️ Tetto `MAX_INOLTRO` 20 MB — non è Vercel, sono i server di posta (sopra ~25 MB rifiutano) e una funzione che impacchetta 200 MB in memoria muore; ciò che non entra **si dice** nell'esito, non si perde in silenzio. ⚠️ Se l'IMAP non risponde l'inoltro parte **col solo testo** e l'esito lo dichiara: una mail monca senza avviso è peggio di una mail non partita. ⚠️ Niente doppioni: un file riallegato a mano (stesso nome e stessa dimensione) non parte due volte. In più, l'esito dice **quanti** allegati sono partiti, e la schermata di inoltro (pagina **e** finestra) avvisa PRIMA: «📎 I 3 allegati dell'originale partono con l'inoltro» — se no li si riallega a mano, in doppio. `maxDuration = 60` aggiunto alla pagina `/messaggio/[id]/scrivi`: le Server Action non ereditano quello del layout, e scaricare allegati pesanti non sta in dieci secondi.
- **Precedente / Successiva sulla mail aperta (4 ago)** — chiesto dall'utente: scorrere la posta senza tornare in elenco a ogni mail. `lib/vicine.ts` (`mailVicine`) trova le due confinanti con due `findFirst` (`data gt` / `data lt`, ordine invertito) dentro l'**ondata parallela** della pagina — non aggiunge un'attesa in fila — e l'indice `Messaggio_posta_idx` le copre. `components/NavigaMessaggi.tsx` disegna i tasti e tiene le scorciatoie **`p`** e **`n`**: stanno lì e non nelle scorciatoie generali perché solo questa pagina sa quali sono le mail confinanti. ⚠️ **Si resta nella stessa lista**: stessa direzione, stessa sezione, stessa casella attiva, e **stesso stato di archiviato/cestinato della mail aperta** — scorrere gli Archiviati e ritrovarsi nella posta in arrivo è un salto senza ritorno. ⚠️ Si cammina fra i **messaggi**, non fra le conversazioni: è la stessa regola di `smaltisciEProssimo` dopo `Canc`, e due definizioni diverse di «la prossima» sarebbero peggio di una imperfetta. ⚠️ In cima o in fondo il tasto resta **visibile e spento** (`.btn.disabilitato`), non sparisce: un comando che compare e scompare si smette di cercare.
- **«Le mail inoltrate o risposte a volte rimangono da leggere» (4 ago)** — segnalazione dell'utente, ed erano **due** difetti diversi che davano lo stesso sintomo. (1) **L'inoltro non segnava letto niente**: in `inviaMessaggio` la riga era `if (!inoltro) { … letto: true … }`, quindi inoltrare lasciava la mail fra le da leggere — sempre, non «a volte». (2) **La risposta segnava letta una mail sola**, ma in posta in arrivo **una riga È un thread** (`nonLetti: g.some((x) => !x.letto)` in `ListaPosta`): bastava un'altra mail non letta nella stessa conversazione e il pallino restava acceso — ecco «a volte». È la stessa regola già scritta per il tasto «✓ Letto» della riga (`segnaLettoThread`), solo non applicata qui. Ora c'è `segnaConversazioneGestita(utenteId, messaggioId, rispostaVera)` in `lib/actions.ts`, chiamata da `inviaMessaggio` (risposta **e** inoltro), da `inviaBozza` (la risposta scritta da Renè) e da `rispondiInvito` (Accetta/Rifiuta di un invito **è** rispondere). ⚠️ `serveRisposta` lo spegne **solo la risposta vera**: inoltrare a un collega non risponde a chi ha scritto, e quella mail una risposta la aspetta ancora. ⚠️ `idsThread` lavora su una finestra di 400 candidati e per una mail molto vecchia può tornare **vuoto**: c'è il ripiego sul solo `messaggioId`, altrimenti il caso raro sarebbe «non segna letto proprio niente», cioè il difetto di partenza. **Lezione**: quando l'elenco mostra i **gruppi**, ogni azione che cambia uno stato mostrato deve agire sul gruppo — se no la si vede «non funzionare» pur avendo funzionato.
- **Mandare la mail da tastiera: `Ctrl+Invio` (3 ago)** — mancava, ed era la domanda successiva dell'utente («tasto rapido per invio mail qual è?»). Sta in `components/useInvioRapido.ts`, usato sia da `Composizione` (rispondi/inoltra) sia da `ComposizioneNuova` (`/scrivi`); la scritta `Ctrl+Invio` è sul bottone. ⚠️ **È l'unica scorciatoia col modificatore, e non contraddice la regola «lettere singole, mai Ctrl»**: quella nasce dal fatto che `Ctrl+R`/`Ctrl+F`/`Ctrl+S` sono del browser — `Ctrl+Invio` non è di nessuno (è quello di Gmail e Outlook), e qui una lettera singola sarebbe impossibile perché si sta scrivendo. ⚠️ **Non salta la conferma**: primo `Ctrl+Invio` arma «Confermi l'invio a…?», secondo spedisce — esattamente i due clic. Su un'azione irreversibile la tastiera non può essere una scorciatoia *anche* per i controlli. ⚠️ Il riquadro «Chiedi a Renè» è marcato `data-senza-invio-rapido`: lì `Invio` chiede già a Renè di scrivere, e `Ctrl+Invio` avrebbe spedito proprio ciò che si stava per far riscrivere.
- **Le scorciatoie c'erano ma nessuno le vedeva — ora sono STAMPATE sui bottoni (3 ago)** — segnalazione dell'utente sulla pagina di un messaggio: «non vedo opzioni per pulsanti rapidi… R per rispondere, I per inoltrare». Due cose sbagliate, non una. (1) **Scoperta**: le scorciatoie esistevano dal 31 luglio ma stavano SOLO nell'elenco che si apre col `?`, e un elenco dietro un tasto lo trova soltanto chi già sa che c'è — cioè chi non ne ha bisogno. Ora la lettera è scritta sul bottone stesso (`Rispondi R`, `Rispondi a tutti T`, `Inoltra I`, `Archivia E`, `Cestina Canc`, classe `.btn kbd.tasto`) più un tasto **«⌨ Scorciatoie»** che apre l'elenco senza indovinare il `?` (`apriScorciatoie()` in `components/Scorciatoie.tsx`, evento `aimail:scorciatoie`). Su schermo ≤900px lettere e tasto **spariscono**: senza tastiera sono rumore. (2) **La lettera giusta**: `inoltra` era solo `f` e `rispondi a tutti` solo `a` — le iniziali **di Gmail**, non quelle italiane. Ora `i` e `t` fanno la stessa cosa, `f` e `a` restano per chi arriva da Gmail. ⚠️ Attenzione ad aggiungerne altre: `i` e `t` erano libere, ma le lettere singole si esauriscono in fretta e un doppione muto (due handler sullo stesso tasto) è già costato caro con `r` — vedi §7, `fuocoConversazione.ts`. **Lezione**: una funzione che si scopre solo leggendo la documentazione, per l'utente non esiste; e le scorciatoie si scelgono nella lingua di chi le preme, non in quella del client da cui abbiamo copiato.
- **Il database era a 1,5 GB: i corpi HTML ora abitano sul server (28 lug)** — a disco pieno Supabase mette il progetto in SOLA LETTURA e l'app «si inchioda» (vedi sotto): la causa era la tabella `Messaggio` (1471 MB su 1485 totali, righe morte trascurabili → non era gonfiore, erano dati veri, e il grosso erano i **corpi HTML**: 5-10 volte il testo, usati solo per rimostrare la mail impaginata). Soluzione in `lib/htmlServer.ts`, stessa filosofia di allegati e inviti: **la casella IMAP è la fonte di verità, l'HTML si riprende da lì quando serve**. In pratica: (1) l'HTML resta nel DB solo per la posta degli ultimi `GIORNI_HTML_CALDO` (30) giorni — quella si apre di continuo e resta istantanea; (2) `pulisciHtmlVecchio()` nel cron toglie l'HTML alle mail più vecchie con `uid > 0`, mille a giro (graduale APPOSTA: un UPDATE unico su decine di migliaia di TEXT terrebbe il DB occupato per minuti); (3) all'apertura, `CorpoMessaggio` mostra subito il testo e `leggiHtmlMessaggio` riprende l'impaginato dal server (`htmlDalServer`: BODYSTRUCTURE → sola parte `text/html` senza nome file → decodifica col charset dichiarato) — **senza risalvarlo**; (4) risposta/inoltro citano formattato lo stesso: `htmlDiMessaggio` riempie il buco prima di `preparaRisposta`; (5) lo storico scaricato non salva più HTML per mail fuori finestra (`htmlCaldo` in `salvaMessaggi`/`salvaInviati`). Copie solo-locali (`uid ≤ 0`) tengono l'HTML: per loro il DB è l'unico posto dove esiste. ⚠️ Lo spazio liberato diventa riusabile con l'autovacuum (il DB smette di crescere); il **numero** riportato da Supabase scende solo con un `VACUUM FULL` una tantum (libera-spazio.sql). ⚠️ Mail cancellata dal server = impaginato perso per sempre, resta il testo.
- **Lo svuota-cestino si fermava cambiando schermata (RISOLTO 29 lug)** — il 27 luglio il lavoro era uscito dalla Server Action ed era finito in una **fetch** verso `POST /api/svuota-cestino`: l'app non si bloccava più, ma il lavoro restava appeso al browser. Cambiando pagina il componente spariva, la richiesta se ne andava con lui e lo svuotamento si fermava a metà — senza che nulla lo dicesse. Ora il POST **prenota** il lavoro (scrive «in corso» e risponde subito) e il lavoro vero gira in **`after()`**: stessa invocazione, ma dopo che la risposta è partita, quindi continua anche a scheda chiusa. L'avanzamento sta sul SERVER, in una riga di `Impostazione` per utente (`cestino.lavoro:<utenteId>`: fase, fatte/totali, esito), e `GET /api/svuota-cestino` lo restituisce: la pagina lo chiede all'apertura e ogni 3 secondi, quindi il lavoro **si ritrova** tornando sul cestino o da un altro dispositivo. `svuotaCestinoDi` accetta ora un `Avanzamento` e `eliminaDalServer` un `onAvanzamento` per mail (la scrittura è limitata a una ogni 2s: non deve rallentare per farsi vedere). ⚠️ Tre paletti: la funzione **muore comunque a 300s**, quindi uno stato «in corso» fermo da 90s si dichiara **interrotto** e il tasto diventa «Riprendi» (riparte da ciò che è rimasto — la query rilegge i `cestinato: true`, non c'è niente da riavvolgere); un esito «finito» **scade** dopo 15 minuti, se no al posto del tasto resterebbe per sempre il messaggio dell'ultima volta; e la vecchia Server Action `svuotaCestino()`, che non chiamava più nessuno, è stata **tolta** — una seconda porta che scavalca lo stato vorrebbe dire due svuotamenti in parallelo sulle stesse mail, e qui si cancella per sempre. **Lezione**: «gira in una fetch invece che in una Server Action» libera l'interfaccia, non rende il lavoro indipendente; per quello serve che a tenerlo in piedi sia il server, non la pagina.
- **Svuotare il cestino bloccava tutta l'app (RISOLTO 27 lug)** — `svuotaCestino` era una **Server Action**, e le Server Action di Next si accodano con le navigazioni: finché girava, ogni clic nell'app restava fermo. Ed è lunga davvero — apre una connessione IMAP per casella e, per **ogni** mail, cerca il suo Message-ID sul server prima di cancellarla. È lo **stesso identico inciampo** già pagato con «Aggiorna posta» (vedi più sotto): la lezione era scritta nell'handoff e l'ho ripetuta lo stesso. Ora il lavoro sta in `lib/cestino.ts` (`svuotaCestinoDi`) e si chiama con una **fetch** a `POST /api/svuota-cestino` (`maxDuration=300`); il componente lo dice esplicitamente all'utente: «puoi continuare a usare l'app». ⚠️ **La lentezza in sé NON è stata toccata, ed è voluto**: la ricerca per Message-ID esiste perché un UID vecchio può puntare a un'ALTRA mail, e qui si cancella per sempre. Su un'operazione irreversibile la prudenza vale più della velocità — il problema era far aspettare l'utente, non la durata.
- **Attività raggruppate per provenienza (27 lug)** — `/attivita` era un elenco piatto, e un elenco piatto nasconde la cosa più utile che si sappia su una lista di compiti: quali riguardano la stessa pratica. Ora si raggruppano per **conversazione** (non per singola mail: due attività nate da due messaggi dello stesso scambio sono la stessa pratica), usando lo stesso `raggruppa()` del resto dell'app — così un thread battezzato si chiama qui come si chiama nella pagina Thread — più due blocchi a parte per le attività nate dal punto della situazione con un contatto e per quelle scritte a mano. Il raggruppamento gira sulle sole mail citate dalle attività aperte: sono poche, non costa niente.
- **Spuntare un'attività ci metteva un'eternità (RISOLTO 27 lug)** — ed era un danno fatto il giorno prima, aggiungendo l'allineamento col registro Attività. Tre cose sommate: (1) `segnaAttivita` faceva `await allineaAttivitaOra(id)` **prima** di rispondere — una chiamata di rete a Deluxy Tasks in Europa (fino a 2,5s di timeout) più due letture/scritture dello stato delle impronte; (2) la casella aveva `checked` dal server ed era `disabled` durante la chiamata, quindi **non si muoveva** finché il giro non era finito; (3) `router.refresh()` ricostruiva l'intera pagina per aggiornare un contatore laterale. Ora: l'allineamento gira dentro **`after()`** di `next/server` (stessa invocazione, ma dopo che la risposta è partita — il registro si allinea lo stesso e nessuno lo aspetta), la spunta ha stato **locale e ottimistico** che torna indietro se il salvataggio fallisce, e il refresh è sparito. Stesso trattamento per `allineaEventoOra` su tutte le azioni del calendario. **Lezione**: «best-effort con timeout corto» non vuol dire «non si sente» — se sta prima della risposta, si sente. Il posto degli allineamenti verso altre app è **dopo**, in `after()`.
- **Le APP DELUXY si richiamavano solo dalla RIGA, non dalla mail aperta (27 lug)** — il meccanismo c'era ed è pulito (`BottoneApp`/`CarteApp` lanciano l'evento `aimail:app`, `InvioAppDialog` lo ascolta), ma il dialogo era montato **solo in `page.tsx`**: sulla pagina del messaggio non esisteva, quindi da lì non si poteva mandare niente a nessuna app — bisognava tornare in lista e ritrovare la riga, cioè scegliere l'app PRIMA di aver letto la mail. Ora la pagina del messaggio monta `InvioAppDialog` e mostra `components/AppSuMessaggio.tsx`: «Automatico» (decidono le regole) più un tasto per ogni funzione, con le app non collegate visibili ma disabilitate e il motivo nel title. Serve `leggiChiaviApp()` anche lì — è in cache 5 minuti, quindi non è una fetch a ogni apertura di mail. **Lezione**: un dialogo montato in una sola pagina è una funzione che esiste solo lì, anche quando il pulsante per chiamarla sembra generico.
- **La pagina della posta ora esce PRIMA dell'elenco (27 lug)** — l'elenco stava dentro `page.tsx`, e Next non manda una riga di HTML finché il componente non ha finito: cambiare cartella voleva dire restare sul bianco per tutto il tempo delle query (finestra di 800 mail, raggruppamento in conversazioni, nomi, risposte, indice clienti). Ora la parte pesante vive in `components/ListaPosta.tsx`, dentro un `<Suspense fallback={<ListaPostaInCorso />}>`: titolo, filtri, schede e colonna di destra escono subito, l'elenco arriva dopo. ⚠️ **Il lavoro è identico**: non è diventato più veloce, è diventato **non bloccante** — ed è quello che si percepisce. Chi tocca `page.tsx` non rimetta query della posta lì dentro: tutto ciò che è pesante va in `ListaPosta`, altrimenti si torna al bianco. Nella pagina resta solo l'ondata leggera (sezioni, conteggio caselle, chiavi in cache), che serve a disegnare la cornice.
- **«Archiviati» lentissimo: mancava l'indice giusto (RISOLTO 27 lug)** — la vista archiviati è la stessa query della posta con `archiviato: true`, ma con solo `[utenteId, data]` il database partiva dalla mail **più recente** e scendeva scartando tutto ciò che archiviato non è, finché non ne trovava 800. Le mail archiviate sono **vecchie per definizione**: si finiva a scorrere l'intera casella a ogni apertura. Ora c'è `Messaggio_posta_idx` = `(utenteId, direzione, cestinato, archiviato, data)` — le colonne di uguaglianza prima, la data (che ordina) per ultima. Serve anche alla posta in arrivo, che ha esattamente la stessa forma. Seconda causa, minore ma reale: lo SPAM si escludeva con un filtro sulla **relazione** (`NOT: { sezione: { nome: 'SPAM' } }`), che Prisma traduce in una sottoquery valutata **per ogni riga candidata** — in posta in arrivo si nota poco, sugli archiviati le candidate erano migliaia. Ora si usa l'id dello SPAM (già letto nell'ondata parallela) nella forma `sezioneId IS NULL OR sezioneId <> spamId`: ⚠️ scritta **per esteso**, perché il `NOT sezioneId = spamId` secco su un campo nullabile dà NULL per la posta non smistata e la ESCLUDE — la trappola era vera, solo mal risolta. **Lezione**: un `orderBy` + `take` su un filtro non indicizzato non è una query veloce che ogni tanto rallenta, è una scansione mascherata da paginazione.
- **Il cron `/api/sync` va in timeout a 300s, e questo teneva SPENTI i registri condivisi (RISOLTO 26 lug)** — nei log di produzione **ogni** giro del cron finiva con `Vercel Runtime Timeout Error: Task timed out after 300 seconds`: `sincronizzaTutti()` esaurisce da sola il budget della funzione. La sincronizzazione con Deluxy Tasks e Deluxy Calendario stava **in fondo alla rotta**, dopo la lettura della posta, quindi non è mai stata eseguita nemmeno una volta — nel registro la chiave di AI Mail risultava «mai usata», e da fuori sembrava soltanto che l'integrazione non funzionasse. Ora i due allineamenti stanno **in testa** alla rotta (costano pochi secondi e girano sempre, anche quando la posta poi sfora) e c'è `?soloRegistro=1` per lanciare solo quelli, in pochi secondi. ⚠️ **Il timeout della lettura posta resta un problema APERTO**: il cron continua a essere ucciso a 300s, quindi la posta arretrata si legge solo fin dove arriva a ogni giro. **Lezione**: quello che deve girare comunque non si mette dopo la cosa che può non finire — e un lavoro periodico che fallisce *sempre*, se nessuno guarda i log, è indistinguibile da uno che non è mai stato scritto.
- **«Cestina tutto» lentissimo e cartelle lente ad aprirsi (RISOLTO 26 lug)** — erano quattro cause sommate, tutte sullo stesso schema «lavoro enorme per un risultato minuscolo»:
  1. **Le azioni sul thread caricavano le mail INTERE per ricavarne gli id.** `cestinaThread`, `archiviaThreadSenzaAggiornare`, `segnalaSpamThread`, il nome e il PLUS AI chiamavano `messaggiThread()`, che fa `findMany` **senza select**: corpo testo, corpo HTML e traduzione di ogni mail della conversazione trasportati dall'Europa agli Stati Uniti — megabyte — solo per leggerne l'`id`. Ora ci sono `idsThread()` (solo gli id) e `membriThread()` (id + data) in `lib/sync.ts`: `messaggiThread` resta per chi le mail deve LEGGERLE davvero (pagina della conversazione, AI).
  2. **Ogni azione su una riga rifaceva l'intera pagina.** `AzioniRiga` e `AzioniThread` chiamavano `router.refresh()` dopo aver già fatto sparire la riga: la posta in arrivo (800 mail da rileggere e raggruppare) o la pagina Thread (2000) venivano ricostruite da zero a ogni singola mail cestinata. Cestinandone dieci di fila si pagavano dieci ricostruzioni complete. Il refresh è stato tolto dove la riga sparisce già da sé — la `revalidatePath` della server action basta, e alla navigazione successiva la lista è aggiornata. Resta dove serve davvero (azioni di massa, «Sì, sempre» che crea una regola).
  3. **La posta in arrivo si portava dietro i campi TEXT.** La query aveva `omit: { corpoTesto, corpoHtml }` — «tutto tranne due campi» — e quindi trasportava `corpoTradotto` **intero** per 800 righe, per mostrarne 200 caratteri. Lo SPAM, pieno di mail straniere tradotte, era il caso peggiore. Ora c'è un `select` esplicito dei soli campi usati, e la traduzione si taglia **nel database** (`left(...)`) con una query mirata sulle sole righe mostrate, fusa con quella che calcola le dimensioni mancanti.
  4. **La colonna di destra bloccava la pagina.** «Top thread» rastrella 1500 mail degli ultimi 30 giorni: senza `<Suspense>` la posta non compariva finché quella query non aveva finito — anche solo per aprire lo SPAM. Ora le tre colonne sono dentro `<Suspense>` e la lista si vede subito.
  In più: `nomiPerGruppi` non manda più al database un `IN (…)` con gli id di tutte le mail mostrate (fino a 2000) — legge le poche righe `NomeThread` dell'utente e incrocia in memoria; le radici dei thread si deduplicano prima delle due query «ha risposto / ha inoltrato»; e sono nati due indici (`utenteId,direzione,thread` e `utenteId,thread`) in `migrate-prod.mjs`.
- **Inviti di calendario: i tasti Accetta/Forse/Rifiuta non comparivano (26 lug)** — due errori in fila, e vale la pena tenerli distinti perché il secondo è stato commesso *correggendo* il primo.
  1. **Parole spia.** `leggiInvito` decideva se andare a cercare l'invito guardando oggetto e corpo (`/invit|riunione|meeting|quando:|…/`). Un invito con un oggetto che non le contiene — la maggioranza — veniva scartato **prima ancora di essere letto**. Un formato si riconosce dalla STRUTTURA, mai da parole nel testo (parente stretta della lezione «non dedurre dal testo libero»).
  2. **Il filtro «allegati».** Sostituendo le parole spia si è usato `leggiAllegati`, che però risponde a un'altra domanda — «quali file mostro nell'elenco allegati?» — e tiene solo i nodi con `disposition: attachment` o con un nome di file. La parte `text/calendar` di un invito Outlook sta dentro un `multipart/alternative` e **non ha né disposition né nome**: è un'alternativa al corpo, non un allegato. Con quel filtro spariva di nuovo. Ora c'è `strutturaMessaggio` in `lib/imap.ts` che restituisce le foglie della BODYSTRUCTURE **senza scartare niente**, e la ricerca vive in `testoIcalDiMail` (una sola, usata sia dal riquadro sia dalla risposta). Ripiego su `leggiTuttiAllegati` (mail intera + mailparser, più tollerante coi MIME malfatti) **solo** se la mail promette (allegati contati o `BEGIN:VCALENDAR` nel corpo salvato).
  In più: ogni via di fallimento tornava `null` e il riquadro spariva in silenzio. Ora `leggiInvito` torna un `EsitoInvito` a tre stati (`nessuno` / `trovato` / `errore`) e se un invito c'è ma non si legge, **lo dice**. E c'è `?diagnosi=1` sull'indirizzo del messaggio (`components/DiagnosiInvito.tsx` + `diagnosiInvito`): mostra uid, cartella, tutte le parti MIME dichiarate dal server e le prime righe dell'iCal. **Lezione**: quando una funzione può fallire in cinque modi e li riassume tutti in `null` il bug è invisibile, e si finisce a tirare a indovinare un deploy per ipotesi — meglio una diagnosi a richiesta.
- **«Non riesco ad accettare l'invito» ≠ invito rotto (26 lug)** — la diagnosi su una mail reale (`?diagnosi=1`) ha chiuso la questione: quella mail ha **solo** `text/plain` + `text/html`, nessuna parte `text/calendar`. Non era un invito iCalendar ma un **biglietto HTML**, e per il protocollo non c'è niente da accettare — nessun organizzatore a cui mandare il REPLY. Accetta/Forse/Rifiuta non possono comparire, ed è giusto così. Il bisogno però era vero, e ora lo copre `cercaAppuntamento` + `components/CercaAppuntamento.tsx`: il tasto «Questa mail fissa un appuntamento?» fa cercare la data all'AI e, se la trova, riappare la scelta a due vie di sempre («＋ Aggiungi al calendario» / «Ignora»). Prima quella proposta nasceva **solo di rimbalzo**, dando una priorità alla mail: se non gliel'avevi data, non c'era modo di chiederla. **Lezione**: prima di continuare a irrobustire un meccanismo, verificare che il caso reale sia davvero quel meccanismo — le due correzioni precedenti erano bug veri, ma non *questo* bug.
- **Azioni rapide nei «Top thread» (26 lug)** — la colonna è un posto da cui si *smaltisce*: le conversazioni che ci stanno sono le più grosse, e quasi sempre o le si apre o si dichiara finita la pratica o si butta via tutto. Sotto ogni riga ci sono ora **Apri / Chiudi / Cestina tutto** (`components/AzioniTopThread.tsx`, conferma inline sul cestinamento con il numero di mail). Entrambe le azioni tolgono la conversazione dai Top thread, quindi la riga sparisce in ottimistico e — come tutto il resto dopo il fix di prestazioni — **senza `router.refresh()`**.
- **Una mail vecchia risultava «da sola» (RISOLTO 26 lug)** — `messaggiThread` cercava la conversazione dentro le **400 mail più recenti**: aprendo una mail più vecchia di quella finestra la si restituiva da sola, quindi la scheda diceva «questa mail è da sola» e sparivano il nome della conversazione e «cestina tutta la conversazione» — anche quando la conversazione esisteva eccome. Ora, fuori finestra, `idsThread` va a pescare i compagni per **radice della catena**, **aggancio manuale** e **oggetto specifico**, e li raggruppa con le stesse regole della lista. Nome, PLUS AI, «chiusa» e cestinamento sono inoltre disponibili **anche su una mail sola**: se domani le agganci una compagna, il nome è già lì e vale per entrambe.
- **Postgres locale (`prisma dev`) instabile** — vedi §3. Non è un problema del codice: cade da solo. Per test locali con dati, usa il driver `pg` grezzo per seed/schema.
- **Chiave OpenAI su Vercel**: se incollata con uno spazio/a-capo finale, l'header Authorization diventa invalido e l'SDK riporta "Connection error." (sembra rete ma è la chiave). Il codice ora **ripulisce** la chiave, ma se dà **401** la chiave è proprio sbagliata → va reinserita nel dashboard Vercel (io non posso toccare i segreti).
- **Fuso orario**: Vercel gira in UTC; le date sono formattate esplicitamente in `Europe/Rome` (costante `FUSO` in `format.ts`). `TZ` è riservato su Vercel e non si può impostare.
- **Migrazioni**: solo idempotenti in `scripts/migrate-prod.mjs` (no `prisma migrate deploy`, host diretto Supabase IPv6-only). Le letture delle tabelle nuove sono **difensive** (try/catch → vuoto) così un deploy prima della migrazione non rompe la app.
- **Deploy Hobby**: max 100/giorno per account; le Server Action AI hanno bisogno di `maxDuration=60` sulle pagine.
- **Traduzione in RISPOSTA non rispettava le lingue lette (RISOLTO 20 lug, sera)**: rispondendo a una mail in una lingua che l'utente LEGGE (es. inglese), compariva comunque il banner «scrivi in italiano, traduco io» e all'invio la risposta veniva tradotta. Ora `traduciSeStraniera` (in uscita) e il banner della pagina `scrivi` usano **la stessa regola** della traduzione in arrivo (`leggiSenzaTraduzione` in `lib/lingue.ts`, modulo condiviso): se la lingua è italiano o fra le `lingueLette`, la risposta non si tocca. Prima l'helper viveva solo in sync.ts.
- **Traduceva le lingue "già lette" (RISOLTO 20 lug)**: le mail in inglese venivano tradotte anche con l'inglese spuntato in Impostazioni. La regola viveva **solo nel prompt**, che per giunta si contraddiceva ("DEVI riempire la traduzione" vs "lasciala vuota se è una lingua letta"): il modello seguiva l'imperativo più forte. Ora c'è il guardiano deterministico `leggiSenzaTraduzione()` in `sync.ts` che **scarta la traduzione nel codice** se la lingua rilevata è l'italiano o è fra le `lingueLette` (confronto tollerante a maiuscole/spazi/"inglese (britannico)"), e il prompt è stato riscritto in due passi. Le traduzioni già salvate a torto sono state ripulite da una UPDATE idempotente in `migrate-prod.mjs`. **Lezione**: una scelta dell'utente non va mai affidata al prompt, si applica nel codice.
- **Badge "Tradotto" su testo NON tradotto (RISOLTO 20 lug)**: caso diverso dal precedente — lingua da tradurre (es. spagnolo, non fra le lette), ma l'AI ha rimesso l'ORIGINALE nel campo `traduzione` invece di tradurre. `corpoTradotto` finiva valorizzato con testo straniero e il badge si accendeva. Ora `traduzioneFinta()` in `sync.ts` scarta la traduzione quando è (quasi) identica all'originale (uguaglianza normalizzata o stesso incipit di 300 caratteri); pulizia delle esistenti con una UPDATE in `migrate-prod.mjs`.
- **Aprire una mail non blocca più sull'AI** (21 lug): la pagina del messaggio aspettava una traduzione OpenAI sincrona nel render (secondi alla prima apertura). Ora la traduzione gira in background dopo il render e le query indipendenti sono parallele — vedi §7 "Apertura mail istantanea". **Lezione (ricorrente in questa app):** non mettere MAI una chiamata AI sul percorso critico del render; mostra subito il dato salvato e arricchisci dopo.
- **La lettura posta non blocca più la UI** (20 lug, sera): prima «Aggiorna posta» era una Server Action lunga (fino a ~35s + storico) e le Server Action condividono la coda con le navigazioni → l'app si bloccava durante la lettura. Ora (1) il giro interattivo è BREVE — `sincronizzaAccount(..., esaurisci=false)`: solo posta nuova, budget ~7s, niente storico (via `sincronizzaUtente`); (2) `SyncButton` legge via **fetch** a `POST /api/leggi-posta` (rotta autenticata dal cookie), non più Server Action, così non entra nella coda e i clic restano liberi; refresh solo se arriva qualcosa. Lo storico si scarica dal cron giornaliero (`sincronizzaTutti` = `esaurisci=true`) e dal pulsante manuale in Impostazioni (`ScaricaStorico`).
- **Auto-refresh**: client-side, solo con app aperta; **intervallo scelto dall'utente** in Impostazioni → "Controlla la posta ogni" (30s/1/2/5/10 min, default **1 minuto**, `Utente.sincronizzaOgniSec`, whitelist lato server). La spunta on/off resta per dispositivo (localStorage). Un refresh a app chiusa richiederebbe un cron più frequente (Pro).
- **Sync inchiodato (RISOLTO 20 lug)**: `ultimoUid` avanzava solo a fine giro; su Vercel un giro con traduzioni + giudizi spam AI superava i 60s, la funzione veniva uccisa e il sync ripartiva sempre dallo stesso blocco (in posta comparivano quasi solo le mail "veloci da salvare", es. le notifiche dal proprio dominio). Ora il cursore avanza **messaggio per messaggio** (`avanzaCursore` in `salvaMessaggi`) e le chiamate AI per giro sono contingentate (5 giudizi spam + 5 traduzioni; il resto si recupera all'apertura). Inoltre la home prende 400 messaggi **senza i corpi** (`omit`) e taglia a 100 righe DOPO il raggruppamento, così le notifiche ad alto volume non spingono fuori il resto della posta.
- **Cursore regredito dallo storico (RISOLTO 20 lug, sera)**: il primo `avanzaCursore` girava anche per lo **storico** (uid bassi) e faceva un update secco: scaricando lo storico, `ultimoUid` REGREDIVA e il sync rimacinava all'infinito mail già viste — la posta nuova non arrivava più. Ora: (1) il cursore avanza solo per lo scarico dei nuovi (`avanzaUltimoUid: true`) e ogni update è **monotono** (`updateMany` con `ultimoUid: { lt }`); (2) `scaricaNuovi` fa prima una **search leggera** e scavalca gli uid già in DB senza rifetcharne il corpo → un cursore rimasto indietro si ripara da solo in un giro; (3) `sincronizzaAccount` gira **a esaurimento** (più blocchi da 25 fino a ~35s di budget); (4) quando non c'è niente di nuovo scarica da solo un blocco di **storico** (niente più bottone in Impostazioni per la posta vecchia); (5) le mail arrivate in **più copie** (stesso Message-ID, uid diversi: alias/inoltri) si salvano una volta sola — prima gonfiavano i thread e doppiavano le attività delle regole. Le copie già salvate prima del fix restano nel DB (pulizia su richiesta).

---

## 10. Sviluppi possibili (non ancora fatti)

- Feedback anti-spam "Segnala / Non è spam" con liste bianche/nere personali (oggi si sposta a mano dalla sezione).
- Retrodatazione delle regole anche per le parti AI (oggi solo deterministiche).
- Autenticazione mittente vera (SPF/DKIM/DMARC) leggendo gli header IMAP, per uno spam più affidabile.
- Invio massivo controllato (es. "scrivi a tutti i partner"): oggi l'AI crea solo l'attività, non invia.
- App Android (era previsto un wrapper Tauri; `src-tauri/` esiste).
- Migrazione a un DB locale stabile per lo sviluppo.

---

## 11. Struttura file (orientamento rapido)

```
deluxy-mail/
  prisma/schema.prisma        # modello dati
  scripts/migrate-prod.mjs    # migrazioni idempotenti (girano nel build)
  src/middleware.ts           # cancello di accesso (sessione)
  src/app/
    layout.tsx                # Shell (drawer mobile), maxDuration
    page.tsx                  # posta in arrivo (Tutte / AI Inbox, thread, spam escluso)
    scrivi/page.tsx           # nuova mail da zero (anche ?bozza= per riprenderla)
    messaggio/[id]/page.tsx   # dettaglio + conversazione + istruzioni thread
    rubrica/[email]/page.tsx  # contatto + PLUS AI + istruzioni + quadro AI
    attivita/page.tsx         # attività + NuovaAttivita (manuale + comando AI)
    regole/page.tsx           # regole (retrodata, attività su misura)
    impostazioni-app/page.tsx # APP DELUXY: stato chiavi (Anagrafiche/Finance/Fornitori) + regole di smistamento
    impostazioni, sezioni, utenti, bozze, inviata, cestino, assistente/[id]
    api/sync/route.ts         # cron
    api/leggi-posta/route.ts  # posta NUOVA in background (SyncButton)
    api/scarica-storico/route.ts # storico in background (StoricoAuto)
  src/lib/                    # vedi §5
  src/components/             # UI (Shell, Sidebar, PrioritaButtons, EditorIstruzioni,
                              #     NuovaAttivita, NuoveAzioni [tasti + dialogo AI],
                              #     ComposizioneNuova, RiassuntoConversazione,
                              #     TraduzioneAllApertura [traduzione in background],
                              #     SyncButton/StoricoAuto [scarico posta/storico in background], …)
```

---

**Regola operativa Deluxy:** aggiornare QUESTO handoff e la memoria a ogni commit; committare spesso; niente segreti nel repo; confermare le azioni esterne; riportare l'esito reale.
