# Deluxy Scout — Stato del progetto & Handoff

Ultimo aggiornamento: **21 agosto 2026** (verifica dello stato di produzione + controllo dal vivo delle Edge Function, con un buco di sicurezza trovato in `hubspot-match`; il lavoro più recente è del 01/08 — vedi «Sera del 01/08/2026» in fondo al §2). Questo documento permette a un altro agente di riprendere il progetto senza contesto pregresso.

> 📍 **Dove si lavora**: worktree **`C:\Users\nicol\scoutwt\deluxy-scout`**, branch **`scout-ui`** (repo `donatodnicolo-gif/search`). `C:\Users\nicol\app\deluxy-scout` (su `main`) è una **copia ferma alla migrazione 0028**: non lavorarci e non copiarne file. Il worktree `scoutwt` è condiviso con altre app che vivono su `scout-ui` (AI Mail, Customer Service, Merchandising): committare spesso.

> 📞 **LA CODA RICHIAMI ERA LA PIPELINE TRAVESTITA (21/08/2026)** — segnalazione utente: «in home ho la visita qui come task aperta», su **Moncler Milano Montenapoleone**.
> - **Non era un task**: il task di quel negozio («Punto situazione») risultava già completato. Era la sezione **«Telefono — chi chiamo oggi»**, alimentata da `daRicontattare()` in `lib/metrics.ts`: unica visita del **16/07** con esito *interessato*, soglia 3 giorni → «36g fa · ritardo».
> - **Perché non se ne andava**: quella coda guardava **solo l'ultima visita**, e l'unica cosa che la svuotava era **registrarne un'altra**. Non contavano la trattativa aperta, il task chiuso, la mail partita, i contatti in rubrica. Una telefonata conterebbe solo se registrata in `chiamate`, che in tutto il database ha **zero righe**.
> - **Misurato sui dati veri**: **35 negozi in coda, tutti e 35 con una trattativa aperta**; 6 fermi da oltre 30 giorni. E lo specchio: **tutte e 37 le trattative aperte hanno `scadenza` NULL**, mentre «Trattative da muovere» richiede `scadenza <= oggi` → **quella sezione è sempre vuota**. La pipeline non si vedeva dove doveva, e tornava dove non doveva.
> - **Correzione (commit `68de17b3`)**: `daRicontattare(places, visits, oggi, opzioni)` ora accetta `conTrattativaAperta` (il negozio esce dalla coda: lo muove la pipeline) e `ultimoContatto` (chiamate + `contatti_avviati`: i giorni ripartono dall'ultimo contatto, non dalla visita). Nuovo `placeIdConTrattativaAperta()` in `lib/metrics.ts` — il criterio di «aperta» sta in **un punto solo** — e `fetchUltimoContattoPerPlace()` in `lib/db.ts` (paginata su `id`, best-effort per fonte). Applicata alle **tre** schermate che rispondono alla stessa domanda: Home, Da fare, Dashboard. `tsc` pulito, **40/40 jest** (5 test nuovi).
> - **✖️ E la coda ha la sua «×» (21/08, commit `fc8bf9a6`)** — richiesta utente: poter chiudere una riga dalla Home senza aprire il negozio e inventarsi una visita.
>   - **Migrazione 0060**: `places.richiamo_chiuso_il` + `richiamo_chiuso_da`. ⚠️ **Sta su `places` e non su `visits`**, che sarebbe il posto naturale: la RLS di `visits` (`visits_update_own`) lascia aggiornare **solo al proprietario della visita**, e in Home la coda mostra le visite di tutti — chi non l'ha fatta non potrebbe chiudere la riga, cioè proprio il caso per cui la × serve. Su `places` l'UPDATE è aperto a ogni autenticato.
>   - **È una data, non un flag**: la coda salta il negozio finché l'ultima visita è **più vecchia** della chiusura, quindi una visita nuova con esito «interessato»/«da richiamare» lo **rimette in lista da sola**, senza che nessuno azzeri niente.
>   - **In Home e in «Da fare», con lo stesso testo di conferma.** Rimozione ottimistica ma **con rollback**: se il server rifiuta, la riga torna e si dice perché — una riga sparita in silenzio farebbe credere chiuso un richiamo che domani ricompare. `chiudiRichiamo()` in `lib/db.ts` guarda le righe tornate dall'UPDATE (una scrittura fermata dalla RLS **non è un errore**: torna zero righe) e traduce la colonna mancante in «applica la migrazione 0060».
>   - Verificato: `tsc` pulito, **42/42 jest** (4 test nuovi), `expo export --platform web` ok. **Non provato a schermo**: sta dietro il login.
> - **🔢 Le quattro tessere della Home si aprono (21/08, commit `d53992fe`)** — richiesta utente: «fammi vedere il dettaglio cliccando sui tab». Ogni tessera (Visite 7g · Chiamate 7g · Trattative · Pipeline) è premibile e apre un foglio con **le righe che compongono quel numero**; da ogni riga si va alla scheda del negozio.
>   - **I numeri ora NASCONO dalle righe.** Prima «Chiamate 7g» era un `count(*)` per conto suo (`contaChiamateDal`) e l'elenco che avrebbe dovuto spiegarlo non esisteva: due conti separati sulla stessa cosa prima o poi non tornano — la stessa trappola trovata fra coda richiami e trattative. Ora c'è `fetchChiamateDal()` che restituisce le righe, e il numero è `.length`.
>   - **L'elenco vuoto dice PERCHÉ è vuoto.** Su «Chiamate 7g» conta doppio: il bottone che registra una chiamata **sta solo in Affiliazioni**, quindi quel numero può essere 0 anche dopo una giornata al telefono, e senza spiegazione sembra che l'app abbia perso i dati.
> - **⚠️ «803 azioni per vendere oggi» non sono azioni di oggi** (misurato il 21/08): il titolo somma `giro + richiami + lead + daMuovere + daRiprendere + task`, e **`giro` da solo fa 803** — sono tutti i negozi con la ⭐ e stato `da_visitare` (995 stellati in totale su 1765), cioè l'intero magazzino di selezionati, che l'import dal registro stella per costruzione (`starred: true`, se no non compaiono in nessuna lista). Un giro di un giorno non è 803 negozi. ✅ **CORRETTO il 21/08** (commit `54eb41d4`): il giro tiene, in ordine, ① chi ha una **visita pianificata** per oggi o in ritardo (impegno preso: non si taglia mai, nemmeno oltre il tetto), ② chi è stato **scelto da una persona** (`creato_da` — erano 28 su 803), ③ il resto per priorità, fino a **`TETTO_GIRO` = 10**. Il taglio è **dichiarato** sotto al titolo («10 di 803 selezionati — gli altri restano sulla Mappa») e il titolone conta `giroOggi`, non il magazzino. ⚠️ Dati che spiegano le scelte: `visita_pianificata` **non l'ha mai usata nessuno** (0 righe) e la priorità non discrimina (**395 P1** su 803), quindi l'unico segnale vero era «l'ha scelto una persona».
> - **👥 «Questo risulta già un cliente» — e la Riconciliazione non poteva vederlo (21/08, migr. 0061 + commit `9f061a12`)**. Segnalazione su un negozio che la Home proponeva **da visitare**: `attivita/c3e1a003…` = «**Amir**», via del Pellegrino 15 Roma, dal registro, `prospect`, 0 contatti e 0 trattative. A **zero metri** c'era «**Amir Roma.\n Cioccolato e Pasticceria**», stato **cliente** (`attivo`) con un contatto: la stessa pasticceria, due schede.
>   - **Perché la coppia non usciva mai in `/riconciliazione`** — tre punti ciechi della RPC `coppie_duplicate`: ① confrontava **solo registro ↔ Google** (`a.anagrafiche_id is not null and b.anagrafiche_id is null`) e qui **entrambe vengono dal registro** (dove esistono davvero due anagrafiche per lo stesso negozio: `cmrovu83x…` e `cmrovrunk…`); ② scartava le schede **nascoste**, e quella del cliente lo era — quindi si vedeva solo il doppione vuoto; ③ «amir» contro «amir roma. cioccolato e pasticceria» ha somiglianza trigram **0,14**, sotto la soglia 0,6.
>   - **Migrazione 0061**: si confrontano tutte le coppie vicine (non solo registro↔Google), vale anche il **prefisso** (un nome contenuto nell'altro, minimo 4 caratteri — se no un negozio chiamato «Bar» pesca mezza città, e solo entro 150 m quando la somiglianza è sotto soglia), **una** scheda nascosta entra in coppia (due no), e **chi resta lo decide il lavoro fatto** — contatti + visite + trattative, con +100 a chi è cliente e +10 a chi viene dal registro — non più la provenienza. Tenere la scheda del registro e buttare quella con la storia dentro era il modo elegante di perdere i dati.
>   - **Esito misurato**: **218 coppie** (erano ~294 col criterio vecchio) di cui 57 «stesso negozio» e 150 «probabile»; **27** sono coppie registro↔registro che prima non esistevano per l'app. La coppia Amir esce col verdetto *probabile* (gli indirizzi sono scritti in due modi, e la regola di sicurezza non promuove mai in quel caso) e **tiene la scheda del cliente**. Trovato anche «AMIRI» / «AMIRI - Milan» a 12 m, che il trigram si perdeva (0,50).
>   - ⚠️ **E unendo, il superstite torna visibile** (`unisciCoppia` in `lib/riconciliazione.ts`): `unisci_places` sposta i figli e completa i campi vuoti ma **non tocca `nascosto`**. Qui la scheda buona era nascosta: unendo nel modo giusto il negozio sarebbe **sparito da tutte le liste** finendo in Profilo → Nascosti. Nascondere è una scelta fatta su un'altra scheda in un altro momento; unire vuol dire «questa la tengo».
>   - 📌 **Da fare a mano**: la coppia Amir (e le altre 217) va **unita dall'utente** in `/riconciliazione` → scheda Doppioni. E il doppione nasce a monte: nel **registro Anagrafiche** ci sono due anagrafiche per lo stesso negozio — finché la sorgente ha doppioni, l'import li riporta.
> - **🔗 Il doppione si vede (e si chiude) dalla scheda del negozio (21/08, commit `c40d750d`)** — richiesta utente: «segnala riconciliazioni possibili anche nella scheda cliente e nel caso vengono fatte chiudile».
>   - ⚠️ **C'erano DUE regole diverse per la stessa domanda**: `/riconciliazione` usava la RPC `coppie_duplicate` (coordinate + nome), la scheda usava `trovaDuplicati()` in `lib/db.ts` (indirizzo identico, oppure stessa prima parola + stessa città). Due regole rispondono diverso, ed è esattamente ciò che è successo col caso Amir: **nessuna delle due lo segnalava**, perché entrambe scartavano le schede nascoste.
>   - **Ora la scheda legge le stesse coppie della Riconciliazione** e ci somma la ricerca locale, che serve ancora: la RPC richiede le coordinate, la locale no (copre i negozi a 0,0). Le coppie già coperte non si ripetono.
>   - **La coppia si chiude in tutti e due i posti**, perché sono le stesse tabelle: «Unisci» elimina il duplicato, «Non è un doppione» scrive in `duplicati_ignorati` (prima il testo diceva «Ignora suggerimento», che non spiegava che valeva per sempre e ovunque).
>   - ⚠️ **La direzione non è più «vince la scheda che stai guardando»**: chi resta lo dice la Riconciliazione (peso: contatti+visite+trattative, +100 al cliente). Il bottone lo scrive — «Unisci qui» o «Unisci là» — e la riga dice «Resta questa scheda» / «Resta «X»». Se a sparire è la scheda aperta, dopo l'unione si va su quella che resta: restare mostrerebbe un negozio che non esiste più. Prima il bottone era sempre «Unisci qui», quindi **aprendo il doppione vuoto si cancellava la scheda buona**.
>   - Rimosse le due funzioni vecchie (`unisci`/`ignora` nella scheda): erano una seconda strada per unire, con la direzione sbagliata e senza il passaggio del `google_place_id` che fa `unisciCoppia`.
> - **⏱️ «Doppioni mi dice 0»: la funzione scadeva, e l'errore veniva ingoiato (21/08, migr. 0062 + 0063, commit `f80bc723`)**. Terza puntata della stessa storia.
>   - La 0061 ha allargato il criterio e `coppie_duplicate` è passata a **11 secondi**; il tetto di PostgREST per l'utente loggato è **8**. La chiamata moriva, `fetchCoppieDuplicate().catch(() => [])` restituiva una lista vuota e la scheda diceva «Doppioni 0» — **un guasto travestito da buona notizia**. ⚠️ Misurato dopo: il solo ramo trigram a 2 km costava già ~5 s **prima** della 0061; si era sul filo senza saperlo.
>   - **0062**: nuova colonna generata e indicizzata **`places.nome_chiave`** (la prima parola del nome, minuscola). Il grosso delle coppie si trova con un **join di uguaglianza** (`a.nome_chiave = b.nome_chiave`) invece di confrontare i nomi su ogni coppia dentro il raggio. Il trigram resta per i casi in cui la prima parola cambia («L'Angolo dei Fiori» / «Angolo dei Fiori»), ma solo entro 150 m. Da 11 s a ~2,5 s.
>   - **0063**: la sola prima parola però era troppo larga — **1520 coppie**, perché le prime parole più frequenti sono generiche (`studio` 61 negozi, `pasticceria` 24, `hotel` 15, `fioreria` 15, `milano` 13). Aggiunti **300 m** e un nome che regge il confronto (somiglianza ≥ 0,3 **oppure** un nome contenuto nell'altro). Esito finale: **329 coppie in ~1,8 s di SQL**, col caso Amir dentro.
>   - **La schermata non ingoia più l'errore**: se la ricerca non riesce lo scrive, e traduce il timeout in italiano invece di mostrare zero.
>   - 🔎 **E c'è la ricerca** (richiesta utente): una barra sola che filtra **tutte e due le schede** — nome, indirizzo, città — con il conteggio «N di M» quando è attiva, perché un elenco filtrato senza quel numero sembra un elenco che ha perso i dati.
> - **🚫 In Affiliazioni non compare più chi è già cliente (21/08, commit `2f9093fe`)** — richiesta utente: «quelli già clienti non servono qui». Giusto: in quella schermata si telefona per **affiliare**, e un cliente è già dall'altra parte; in mezzo alle chiamate da fare costa tempo due volte, una a leggerlo e una a capire se va chiamato.
>   - Sui dati veri: **16 su 223**. Si guardano **tutte e tre** le colonne (`stato = 'cliente'`, `stato_affiliazione = 'attivo'`, `anagrafiche_stato = 'attivo'`) perché lo «attivo» può arrivare dal registro, da Scout, o dall'esito di una visita — e non si può sapere quale delle tre sia valorizzata. (Oggi concordano tutte e tre su quei 16, ma è una coincidenza dei dati, non una garanzia.)
>   - Il taglio è **dichiarato nel sottotitolo**: «N da reclutare · … · 16 già clienti, tenuti fuori». Un elenco che si accorcia senza dirlo sembra un elenco che ha perso dei dati.
> - **📊 Affiliazioni in TABELLA sopra i 900px (21/08, commit `f5ec7ca4`)** — richiesta utente. Sette colonne (negozio · città · referente · telefono · stato · ultima chiamata · azioni), intestazione dentro l area che scorre (in questa app ce n è **una sola**, vedi la trappola delle testate fisse). **Sotto i 900px restano le schede**: sette colonne su un telefono sono illeggibili e qui il nome non si tronca mai; è la stessa soglia oltre cui i filtri stanno già aperti. ⚠️ La tabella **non toglie nulla**: stella, telefono cliccabile, referente, stato coi sette passi e registro Anagrafiche ci sono tutti — su una tabella la tentazione è togliere le azioni per far entrare le colonne ([[feedback-non-togliere-azioni]]).
>   - **Stile rifatto sul design system + clic e agenda (commit `b992bc48`)**. Il DS ha un paragrafo apposta sulle tabelle e la prima versione ne rispettava poche righe. Ora: tabella **dentro una card** (l'intestazione la chiude in alto, `Chiusura` in basso), intestazioni **non urlate** (12px peso 500 terziario — via il maiuscolo spaziato), **hover** appena percettibile, divisori hairline, stato come **pillola con dot e tinta al 10%**, celle vuote «—», date **a destra con `tabular-nums`** (incolonnate si confrontano con l'occhio), e il selezionato come **filo oro a sinistra** invece di un blocco di colore.
>   - **👤 Colonna «Segnalato da» + data (commit `3fce8cf5`)**: il nome di chi ha messo dentro il negozio (`creato_da`), e se non c'è nessuno la provenienza — «Registro», «Ricerca Google», «Inserito a mano» — con la data sotto, nella stessa colonna («Registro · 17 lug» si legge in un colpo, su due colonne no). ⚠️ Misurato: **su 223 affiliazioni ZERO hanno `creato_da`** (199 dall'import del registro, 23 dalla scoperta Google), quindi la colonna dice quasi sempre «Registro». Non è un buco: è l'informazione che nessuno ci ha ancora messo mano. La ricerca cerca anche lì dentro. 📌 Il registro Anagrafiche ha un campo **`fonte`** più preciso (su un partner vero vale `"excel"`): leggerlo però è **una chiamata per negozio**, quindi per averlo in tabella andrebbe importato in una colonna di `places`.
>   - **🔗 AFFILIAZIONI E PROVINCE SONO UNA VISTA SOLA (commit `f304644a`)** — richiesta utente. Erano due voci di menu per due domande che sono la stessa in due tempi: «dove non abbiamo nessuno» e «chi chiamo». Ora la copertura è la **terza scheda in cima**, accanto a «Elenco» e «Ricerca» (commit `62b67062`): era nata come pannello richiudibile dentro la testata dell’elenco, ma lì rubava una riga a tutti — anche a chi era venuto solo per telefonare — e stava in mezzo ai filtri invece che fra le viste. I suoi dati (registro + venduto da Orders) si caricano **solo quando la scheda si apre**. Toccando una provincia il nome **finisce nella ricerca e si torna all’Elenco già filtrato**, invece di restare sulla tabella a chiedersi cosa sia successo.
>     - ⚠️ **Bug chiuso per strada**: il clic sulla provincia portava a `/affiliazioni?zona=…`, ma quella schermata legge solo `lat/lng/indirizzo` — **il parametro non lo leggeva nessuno** e si atterrava sull'elenco intero come se il clic non avesse fatto niente. Ora passa `?cerca=`, che viene applicato.
>     - La tabella è un **componente condiviso** (`components/CoperturaProvince.tsx`), non una copia: `app/(app)/province.tsx` è ridotta a un involucro. Due copie della stessa tabella divergono al primo ritocco.
>     - Menu: una voce sola, **«Affiliazioni · Copertura»**. La rotta `/province` **resta viva** (fuori dal menu) per i link già in giro e per quelli di altre app.
>   - ⚠️ **Cap del contenuto**: le tabelle usano `contenutoLargo` (**1180**, nuovo in `lib/theme.ts`) invece dei **760** di `contenutoCentrato`. Quei 760 sono tarati sulle righe «nome … valore», dove una colonna larga allontana i due estremi; su sette colonne lasciavano ~100px a colonna e il telefono diventava illeggibile.
>   - **La riga si apre al clic** (scheda del negozio). ⚠️ Le azioni dentro la riga (stella, telefono, stato, calendario, registro) **fermano l'evento** con `stopPropagation`: sul web risalirebbe e aprirebbe anche la scheda.
>   - **📅 «Quando lo chiamo» si fissa in AGENDA** (richiesta utente). Il Calendario di Scout legge **i task con scadenza** (più i follow-up delle trattative): quindi la data di una chiamata è un **task datato sul negozio**, col titolo già scritto («Chiamare X») via la nuova prop `titoloIniziale` di `TaskFormModal`. Compare in Calendario, in «Da fare» e nella Home **senza aggiungere niente al database** — un campo nuovo tipo `chiamata_pianificata` sarebbe rimasto una data che non guarda nessuno (è già successo con `visita_pianificata`: 0 righe su 803).
> - ⏳ **COSA RESTA (serve l'utente):**
>   1. ~~La correzione non è in produzione~~ ✅ **PUBBLICATA il 21/08** (`dpl_5xzcz8t6…`). ⚠️ **E il `VERCEL_TOKEN` non serviva**: la CLI di Vercel su questa macchina è **già autenticata** (`npx vercel whoami` → `donatodnicolo-gif`) e `scripts/deploy-web.sh` ha il ramo senza token. Da qui il deploy si fa con `bash scripts/deploy-web.sh` e basta — chiedere un token all'utente era una perdita di tempo. Verificato dall'esterno: il dominio serve Scout, il bundle contiene «Chiudere il richiamo?» e `richiamo_chiuso_il`, il font Ionicons pesa **442.604 byte** (non i 1,6 kB della trappola) e `/api/health` risponde `{"ok":true,"database":true}`.
>   2. **Le scadenze delle 37 trattative**: la query è pronta e commentata in **`scripts/scadenze-trattative-mancanti.sql`** (le distribuisce sui prossimi 7 giorni, le più trascurate per prime; tocca solo le righe con `scadenza is null`; restituisce gli id per poter tornare indietro). **Non l'ho eseguita**: la scrittura in produzione è stata fermata dal guardrail delle azioni. Si lancia con `SUPABASE_PAT=… node scripts/mgmt-query.mjs scripts/scadenze-trattative-mancanti.sql`. Senza, «Trattative da muovere» resta vuota anche dopo il deploy.
> - ⚠️ **Da decidere**: `chiamate` è vuota, quindi *nessuno registra le telefonate nell'app*. Il bottone «Chiama» c'è (Affiliazioni), ma il resto delle schermate apre il telefono senza lasciare traccia. Finché è così, «ti ho già richiamato» non lo sa nessuno e la coda si regge solo sulle visite e sulle mail.

> 🔓 **CONTROLLO DELLE EDGE FUNCTION DAL VIVO (21/08/2026) — una è aperta a chiunque**
> - **`hubspot-match` accetta `sync_crm` senza nessuna autenticazione.** Provato dall'esterno: una POST **senza header di autorizzazione** riceve una risposta *dal codice* (`{"error":"ANTHROPIC_API_KEY non configurato"}`), quindi la funzione è pubblicata `--no-verify-jwt` e il gateway non filtra niente. Nel codice il ramo dell'azione sta **prima** del controllo dell'identità — `supabase/functions/hubspot-match/index.ts:124` fa `if (body.action === 'sync_crm') return await syncCrm(admin, token);` mentre `getUser(jwt)` e il 401 arrivano solo alle righe 131-133. Un anonimo che conosce l'URL può quindi far riversare **tutto il CRM HubSpot** (aziende, contatti, trattative) nelle tabelle mirror con la *service role*, in loop: quota HubSpot bruciata e scritture sul database. La risposta restituisce solo i conteggi, quindi non è esfiltrazione diretta di dati. ⚠️ **`sync_crm` NON è stato lanciato per provarlo**: avrebbe scritto in produzione — la prova è il codice più il fatto che il gateway lascia passare.
>   - **Correzione**: spostare il controllo (sessione utente **oppure** `_shared/chiaveIn.ts`) **sopra** la riga 124, prima dello smistamento delle azioni.
>   - **La regola che ne esce**: in una funzione pubblicata `--no-verify-jwt` il primo `return` di un ramo d'azione **è già la porta d'ingresso**. L'autenticazione va prima dello smistamento, non dentro il ramo che «se la ricorda»: basta aggiungere un'azione nuova sopra il controllo e la funzione diventa pubblica senza che nessuno se ne accorga.
> - **Il workflow ripubblica 8 funzioni su 20.** In `scripts/allinea-supabase.mjs`: `FUNZIONI = ['anagrafiche','ordini','health','finance','invio-email','partner','lead','trattativa']`. Le altre **12** — `discover`, `geocode`, `mail`, `linee`, `hubspot-sync`, `hubspot-match`, `smtp-config`, `notifica-task`, `promemoria`, `calendario-ics`, `proforma`, `assistente-trattative` — **non le aggiorna nessuno**: se ne modifichi il codice, il push non le tocca e in produzione resta la versione vecchia **in silenzio**. È anche il motivo per cui la correzione di `hubspot-match` qui sopra va deployata a mano (o aggiunta alla lista).
>   - ⚠️ Aggiungendone una a `FUNZIONI` bisogna metterla **anche in `SENZA_JWT`** se oggi è pubblica al gateway e fa l'auth nel codice (`mail`, `linee`, `hubspot-sync`, `hubspot-match`): senza la flag il gateway la chiuderebbe e l'`x-api-key` non servirebbe più a niente — è la trappola già pagata con `lead` e `trattativa`.
> - **Mappa dell'autenticazione, misurata dall'esterno** (POST vuota, nessun header). Rispondono **dal codice**, cioè sono pubbliche al gateway: `health` (200), `partner`/`lead`/`trattativa`/`linee` («Chiave API mancante»), `mail` e `hubspot-sync` («Non autenticato»), `calendario-ics` («token mancante»), `hubspot-match` (il buco qui sopra). Rispondono col **401 del gateway** (`UNAUTHORIZED_NO_AUTH_HEADER`), quindi pretendono una sessione: `ordini`, `finance`, `anagrafiche`, `discover`, `geocode`, `invio-email`, `smtp-config`, `assistente-trattative`, `notifica-task`, `promemoria`, `proforma`. Con una `x-api-key` finta `partner`/`lead`/`trattativa` rispondono «Chiave API non valida»: il confronto avviene davvero, sono vive.
> - **`ANTHROPIC_API_KEY` non è configurato** — lo dice il server, non l'handoff: **Assistente AI su Trattative** e **match AI dei contatti HubSpot** sono spenti.

> 🐛 **TRAPPOLA: `select('*')` si ferma a 1000 righe — i negozi oltre sparivano (26 lug 2026)** — segnalazione «se metto la stella in un negozio da mappa non mi va in selezionati». **Non era la stella.**
> - **Causa:** `fetchPlaces()` faceva una `select('*')` secca. PostgREST tronca a **1000 righe** e i negozi sono **1313**: 313 non venivano proprio caricati e mancavano da Mappa, Selezionati e Potenziali. Senza `order()` il troncamento non è nemmeno stabile → lo stesso negozio compariva o no a seconda della query. Misurato: dei negozi stellati, nell'elenco caricato ce n'erano **2 su 5**.
> - **Correzione:** `fetchPlaces` ora **pagina a blocchi di 1000 con `order('id')`**. L'ordine stabile è obbligatorio: senza, le pagine possono ripetere o saltare righe.
> - **⚠️ Vale per tutte le tabelle:** qualunque `select()` senza `range()` in questa app ha lo stesso tetto. Da controllare quando una lista «perde» record senza motivo.
> - **Due rinforzi:** la lista accetta un negozio se ha `creato_da` **oppure** la ⭐ (la stella è già la scelta di una persona: quelli stellati prima del 23/07 restavano fuori) — stesso criterio nel conteggio dei chip, se no i numeri non tornano con le righe; e `aggiornaStarred` ora **fa emergere l'errore** sulla scrittura di `creato_da`, che prima falliva in silenzio.
>
> 🏷️ **«Visite» si chiama POTENZIALI, e ha tutte le azioni di contatto (26 lug 2026)** — richiesta utente: «visite ora viene rinominato in Potenziali» e «in potenziali le azioni sono comunque tutte quelle per instaurare un contatto».
> - **Rinominata solo l'etichetta**: voce di menu e titolo dicono «Potenziali», ma la **rotta resta `/visite`** e il file `app/(app)/visite.tsx` — così i link già in giro continuano a funzionare. Chi cerca il codice deve saperlo.
> - **Azioni complete su ogni scheda** (5): Chiama · WhatsApp · Email · Completa/Registra la visita · Nuova trattativa. Quelle senza recapito restano **visibili ma spente**, così si vede a colpo d'occhio cosa manca.
> - **Da dove arrivano i recapiti:** `fetchRecapitiPlace()` in `lib/db.ts` — telefoni ed email non stanno su `places` ma sui `contacts`, e si caricano in **una query sola** per tutta la lista. Fra più referenti vince chi **decide**, poi il primo con un recapito; gli archiviati si saltano.
>
> 🗑️ **Si può eliminare una trattativa (26 lug 2026)** — nato dal caso «Akris non dovrebbe essere una trattativa»: dall'app **non c'era modo di cancellarne una**, quindi una trattativa nata per sbaglio restava per sempre e teneva il negozio fuori dalle Visite.
> - **`eliminaDeal(id)`** in `lib/db.ts` + bottone **«Elimina trattativa»** in fondo al form, con conferma (`lib/dialoghi.ts`, che sul web usa `window.confirm` perché `Alert.alert` non mostra i pulsanti).
> - **Solo sulle trattative di Scout**: il bottone non compare su quelle che arrivano da **HubSpot** o dal **registro Anagrafiche**, che non nascono qui e tornerebbero al primo sync — vanno chiuse nell'app che le possiede.
> - **Effetto voluto:** eliminata la trattativa, il negozio torna indietro nel funnel e, se ha una visita senza risposta, **ricompare fra le Visite** (è il caso di Akris).
> - **Caso Akris, per la cronaca:** la trattativa esiste davvero in `deals` (fase *Appuntamento fissato*) ma ha **valore, canale e `created_at` tutti NULL** → è una delle 14 preesistenti alla migr. 0039, non creata dall'app. Sul negozio c'è una visita del 23/07 con esito *da richiamare*. In più **«Akris» è duplicato**: due schede, una `Akris` (visitato, con visita e trattativa) e una `AKRIS` (da visitare, dal registro come *prospect*) — il lavoro fatto su una non si vede sull'altra. Il duplicato **non è stato toccato**.
>
> 🚶 **Sezione POTENZIALI (rotta /visite), in Vendita (26 lug 2026)** — richiesta utente: «prima di vendita metti visite dove salveremo i risultati delle visite e anche quelle in bozza che devono essere completate. Cancella le visite che son diventate trattative. Da visite ci sarà il pulsante per creare trattative».
> - **`app/(app)/visite.tsx`** con due sezioni: **Da completare** (le bozze, cioè `places.da_completare` — negozi segnati «sono stato qui» ma con la visita mai compilata) e **Visite fatte** (da `visits`, con esito, note e data). Voce di menu «Visite» inserita **prima di Vendita**.
> - **Azioni:** sulla bozza «Completa la visita» (apre `VisitaModal`) e «Nuova trattativa»; sulla visita fatta «Crea trattativa». Il bottone riusa il percorso già fatto per i Clienti: `/(app)/trattative?nuovoPer=<placeId>&nuovoNome=<nome>`, che apre il form col negozio già scelto.
> - **⚠️ «Cancella» = nascoste, non eliminate.** Le visite di un negozio che ha già una trattativa non compaiono più nell'elenco, ma i record di `visits` restano: alimentano Storico, Dashboard e Team. Eliminarli davvero cancellerebbe lo storico del lavoro sul campo.
> - **Effetto oggi, da sapere:** sui dati reali ci sono **12 visite, tutte su negozi che hanno già una trattativa** → la sezione «Visite fatte» risulta **vuota** e si vedono solo le 6 bozze. Non è un bug: è il filtro richiesto che agisce su tutti i dati esistenti.
> - **Limite del criterio:** si esclude se il negozio ha *una* trattativa, senza guardare le date. Un negozio con una trattativa vecchia chiusa e una visita nuova non comparirà mai. Confrontare le date non è affidabile perché `deals.created_at` è **NULL sulle 14 trattative preesistenti** (vedi migr. 0039).
>
> 🔑 **Chiavi delle altre app, gestite dall'admin da dentro Scout (26 lug 2026)** — richiesta utente: poter richiamare le altre app Deluxy via API key, con una schermata, e «**solo admin può inserire chiavi**». ⏳ **Il codice c'è ma NON è attivo: manca la migrazione** (vedi in fondo).
> - **Migrazione `0044_chiavi_app.sql`**: tabella `chiavi_app` (app, url_base, chiave, note) + vista `chiavi_app_stato` che dice **se** la chiave c'è, non qual è. **Non** si è usata la tabella `impostazioni` (0043) perché quella la leggono *tutti* gli autenticati (`select … using (true)`) e dichiara essa stessa che i segreti non ci vanno: una chiave di Anagrafiche in mano a ogni venditore varrebbe come dargli il registro. Qui **legge e scrive solo l'admin** (unica policy `for all`).
> - **⚠️ Trappola Postgres:** la vista ha `with (security_invoker = true)`. Senza, una vista gira coi privilegi di chi l'ha creata e **scavalca la RLS** della tabella sotto, rendendo l'elenco visibile a tutti gli autenticati.
> - **Schermata:** Profilo → Impostazioni → **App collegate** (visibile solo all'admin): le 7 app dell'ecosistema (`APP_DELUXY` in `lib/db.ts`) con stato *collegata / da collegare*, campo chiave in `secureTextEntry`, indirizzo opzionale, e «Scollega». La chiave **non torna mai al client**: la schermata legge solo la vista di stato.
> - **Verificato** che la schermata si apra e che l'errore, con la tabella assente, sia esplicito e azionabile («applica la migrazione 0044…») invece del messaggio tecnico di Supabase.
> - **⏳ Restano da fare (non ho potuto):** ① **applicare la migrazione** — serve `SUPABASE_PAT`, che qui non c'è: `SUPABASE_PAT=sbp_… node scripts/mgmt-query.mjs supabase/migrations/0044_chiavi_app.sql`; ② le **chiamate vere** alle app: vanno fatte da una Edge Function che legge la chiave con *service role* (come fa già `functions/anagrafiche` con `chiaveHub`), **mai dal client dei venditori**. Oggi la schermata custodisce le chiavi, non le usa ancora.
> - **Nota:** esiste già la **cassaforte dell'Hub** (`_shared/chiavi.ts` → `chiaveHub(nome)`, con fallback sulle env): per le Edge Function resta quella la strada. `chiavi_app` serve a gestirle da dentro Scout senza riga di comando.
>
> 🪜 **LIVELLI: il confine fra Selezionato e Prospect è il CONTATTO (26 lug 2026, decisione utente)** — «quelli con stellina da mappa o affiliazione devono andare in selezionati e non in prospect (che saranno solo quelli con un contatto che si apre)». Prima bastavano una visita, una trattativa aperta o uno stato "avviato" del registro: così i negozi scelti con la ⭐ finivano quasi tutti in Prospect e i Selezionati restavano vuoti.
> - **Regola nuova, in `lib/livelli.ts`:** dopo dormiente/cliente/perso, un negozio è **Prospect (`lead`) solo se ha un contatto** — una persona in rubrica (tabella `contacts`) oppure `hubspot_ha_contatto`. Tutto il resto resta **Selezionato (`prospect`)**, stella o non stella. Spariti dal criterio `stato === 'visitato'`, `hubspot_deal_aperta` e gli stati del registro.
> - **Come arriva il dato:** `fetchPlaceIdConContatto()` in `lib/db.ts` carica **una volta sola** gli id dei negozi con almeno un contatto (chiederlo riga per riga sarebbe una query per negozio); `usePlaces` lo espone come `conContatto`, e `lista.tsx` lo passa a `livelloDi(p, conContatto.has(p.id))` nei filtri, nei conteggi dei chip e nel badge della scheda. Se quella query fallisce restano tutti Selezionati (best-effort, meglio che rompere la lista).
> - **Verificato sui dati reali:** l'unico negozio della lista che era Prospect **solo perché risultava «visitato»** è tornato Selezionato; i negozi stellati che restano Prospect hanno davvero un contatto, quindi ci stanno per la nuova regola.
> - Aggiornati i testi delle viste: non dicono più «sentiti o visitati» ma «c'è un contatto in rubrica».
>
> 🚀 **Pubblicato tutto il lavoro del 26 lug — e il deploy va fatto SOLO con lo script (26 lug 2026)** — `deluxy-scout.vercel.app` era rimasto fermo al build delle 13:23 nonostante i push: il progetto **non builda da Git** (non ha build command valido né variabili d'ambiente; le `EXPO_PUBLIC_*` sono inlinate nel bundle al build locale). Ora è online: verificato che il bundle servito contenga «Mostra i filtri», «Nuova trattativa», «Account non assegnato», che le rotte profonde rispondano 200 e che il font Ionicons pesi **442.604 byte**.
> - **⚠️ Usare `bash scripts/deploy-web.sh`** (già esistente e catalogato in [scripts/README.md](../../../../scripts/README.md)). Facendo a mano `expo export` + `vercel deploy` si sbaglia in due modi, entrambi silenziosi — è successo:
>   1. **Icone come quadratini.** I font finiscono in `assets/node_modules/…` e **Vercel non carica i percorsi con `node_modules`**: il `.ttf` non arriva, e il rewrite SPA risponde con l'`index.html` al posto suo. Il file sembra esserci (**HTTP 200**) ma pesa **1,6 kB invece di 442 kB**: per questo l'errore non si nota dai codici di stato. Lo script rinomina la cartella in `assets/vendor` e aggiorna i riferimenti nel bundle.
>   2. **Deploy sul progetto sbagliato.** `vercel deploy dist-web` deduce il progetto dal **nome della cartella** e finisce su **`dist-web`** (`dist-web-beta-woad.vercel.app`), non su Scout. Lo script pinna il progetto per ID.
>   In più `expo export` **svuota `dist-web/`** e con essa il `vercel.json` col rewrite SPA: senza, ogni rotta diversa da `/` dà 404. Lo script lo riscrive.
>
> 💼 **Nuova trattativa dalla scheda cliente (26 lug 2026, ottavo giro)** — richiesta utente: «nei clienti dai possibilità con un bottone di aprire una nuova trattativa». Su un cliente acquisito è l'azione che serve più spesso e prima obbligava ad andare in Trattative e ricercare il negozio a mano.
> - **Come funziona:** quinto cerchietto (valigetta) nelle azioni della scheda in `clienti.tsx` → porta a `/(app)/trattative?nuovoPer=<placeId>&nuovoNome=<nome>`. La pagina Trattative legge i parametri, apre da sola il form e passa il negozio a `TrattativaModal` con la nuova prop **`placeIniziale`**: niente ricerca da rifare, e i **contatti del negozio vengono caricati** come nella selezione manuale. Il negozio resta cambiabile con l'icona di scambio.
> - **Attenzione:** chiudendo o salvando si fa `router.replace('/(app)/trattative')` per togliere i parametri dall'URL — senza, tornando su Trattative il form si riaprirebbe da solo sullo stesso cliente.
> - **Verificato** end-to-end sul locale: il form si apre con «ALPINUM SOLUTION» già scelto e il suo contatto HubSpot caricato. ⚠️ **Non** è stata premuta «Crea trattativa»: sono dati di produzione e avrebbe creato una deal vera. Il salvataggio vero e proprio resta da provare.
>
> 🗃️ **La scheda dei Prospect è la stessa dei Clienti (26 lug 2026, settimo giro)** — richiesta utente: «per questa cosa devi prendere lo stesso stile di una card in clienti». Non bastavano le azioni: era tutta la scheda a essere diversa (i Prospect avevano nome a piena larghezza, badge sotto, niente icona).
> - **`components/CardElenco.tsx`**: la forma della scheda (riquadro oro con icona a sinistra · blocco di testo · badge incolonnati a destra · azioni in fondo) estratta da `clienti.tsx` e usata da **entrambi**. Contenuto a slot (`nome`, `meta`, `account`, `tag`, `badge`, `extra`, `azioni`), così le due sezioni non possono più divergere. Verificato che le schede misurino gli stessi valori: larghezza 358, padding 16px, raggio 12px, stesso bordo, stesso riquadro icona 40×40.
> - **Effetto collaterale da sapere:** con l'icona a sinistra e i badge a destra la colonna del nome è più stretta, quindi i nomi lunghissimi arrivano al tetto delle 3 righe e vengono troncati (es. «Varesina Suite - Near Rho Fiera San Siro - Deluxe One-Bedroom Apartme…»). È il prezzo di avere la stessa identica forma di Clienti: per farceli stare interi bisognerebbe portare i badge sotto il nome, ma allora le due schede tornerebbero diverse.
>
> 🎛️ **Azioni identiche fra Prospect e Clienti, e nomi con tetto a 3 righe (26 lug 2026, sesto giro)**
> - **Stesso stile per le azioni** (richiesta utente): i Prospect avevano una pillola nera «Visita»/«Mail» più l'occhio in alto a destra, i Clienti quattro cerchietti in fondo. Estratto **`components/AzioniRiga.tsx`** (`AzioniRiga` + `IconaAzione`, prima copia locale in `clienti.tsx`) e usato da entrambi: due copie separate sarebbero tornate a divergere al primo ritocco. Verificato che i cerchietti misurino **gli stessi identici valori** nelle due sezioni (38×38, raggio 19px, stesso sfondo e bordo). ⚠️ `Place` **non ha telefono né email** (in Clienti arrivano dal primo contatto): sui Prospect restano quindi le azioni che avevano — Visita o Mail secondo la vista, più Nascondi — con lo stile dei Clienti. Per avere anche chiama/WhatsApp/email servirebbe portare i recapiti nella query di `usePlaces`.
> - **Nomi: tetto a 3 righe.** Toglierlo del tutto (terzo giro) faceva esplodere i dati sporchi: in Rubrica un contatto il cui «nome» è una nota («3407602202 lago di lugano però in italia…») occupava 5 righe. Rimesso `numberOfLines={3}` sugli stessi 35 punti: i nomi veri restano interi (anche «Varesina Suite - Near Rho Fiera San Siro - Deluxe One-Bedroom Apartment With City View» ci sta), i casi patologici si fermano.
> - **Ordine uniformato:** in `/lista` la ricerca ora viene prima del bottone dei filtri, come in Clienti, Affiliazioni, Trattative e Rubrica.
>
> 🎚️ **Filtri richiudibili: la lista si vede subito (26 lug 2026, quinto giro)** — segnalazione utente su `/clienti`: «il filtro così rende impossibile utilizzare la sezione». Mandando i filtri a capo (secondo giro) si erano risolti i tagli, ma il blocco occupava **più di una schermata** prima della prima riga.
> - **Nuovo componente `components/PannelloFiltri.tsx`**: i filtri stanno dietro un bottone «Filtri (n)». **Chiuso di default sul telefono, aperto da 900px in su** (dove c'è spazio e la sidebar è già permanente). Il numero di filtri attivi è sempre sul bottone e c'è «Azzera» a fianco: così una lista filtrata non sembra mai vuota «senza motivo» a pannello chiuso.
> - **Applicato a:** `clienti`, `components/Filters.tsx` (quindi tutte e tre le viste `/lista`), `affiliazioni`, `trattative`, `rubrica`. La **ricerca resta sempre fuori dal pannello**: è il comando più usato.
> - **Verifica a 390px:** il primo contenuto ora compare a y=270-329 (prima era sotto il bordo dello schermo) su Clienti, Prospect, Affiliazioni, Trattative e Rubrica. A 1280px i filtri risultano aperti di default. `tsc` pulito.
>
> 🖥️ **Su desktop il contenuto non occupa più tutta la larghezza (26 lug 2026, quarto giro)** — segnalazione utente su `/oggi`: «da desktop occupa tutto lo spazio». Misurato: con la sidebar da 264px, su un portatile a **1280** il contenuto arrivava a **928px** lasciando **49px** liberi — riempiva lo schermo, e nelle righe «nome … valore» i due estremi finivano lontanissimi (es. «Gucci» a sinistra e l'indirizzo all'altro capo).
> - **Correzione:** `CONTENUTO_MAX` in `lib/theme.ts` abbassato **da 960 a 760** (scelta dell'utente fra colonna più stretta / due colonne / valori ravvicinati). Vale per tutte le schermate che usano `contenutoCentrato`.
> - **Aggiunto `contenutoCentrato` dove mancava:** `da-completare`, `storico`, `task`, `script` — erano rimaste a 974-984px perché non applicavano il cap.
> - **Verifica a 1280px:** Oggi, Da fare, Storico, Task, Script, Clienti, Trattative ora sono tutte a **728px** uniformi (prima 928-984). Su mobile nessun effetto: sotto i 760px il contenuto resta a piena larghezza. `tsc` pulito.
>
> 🔤 **I nomi non si tagliano più, in tutte le schede (26 lug 2026, terzo giro)** — richiesta utente: «i nomi di tutti i clienti, prospect, negozi ecc si devono vedere sempre». Nelle schede il nome aveva `numberOfLines={1}`: quando non ci stava veniva troncato («Moncl…», «Church's Milano S…»), e dare più spazio non bastava perché il taglio era comunque a una riga.
> - **Correzione:** tolto `numberOfLines={1}` da **35 punti**, solo dove il testo è il **nome** di un negozio/cliente/contatto/lead/trattativa. Ora va a capo su più righe: es. «Moncler Milano Montenapoleone» si legge intero su due righe. File toccati: `affiliazioni`, `attivita/[id]`, `calendario`, `clienti`, `da-completare`, `invio/[scriptId]`, `lead`, `lista`, `mappa`, `nascosti`, `oggi`, `ordini`, `pagamenti`, `rubrica`, `storico`, `task`, `team`, `trattative`, `venditore/[ownerId]`, `RicercaAffiliazioni`, `TaskFormModal`, `VisitaModal`.
> - **Restano troncati di proposito** indirizzi, note, causali, anteprime, chip dei filtri e voci di menu: lì il taglio non fa perdere l'identità della riga. Se serve cambiare anche quelli, sono altri `numberOfLines` negli stessi file.
> - **Regola:** il **nome** non si tronca mai, va a capo. Il troncamento si usa solo per i campi di contorno.
> - **Verifica:** controllo automatico su Da fare, Clienti, Oggi, Storico e Trattative → fra i testi ancora tagliati restano **solo indirizzi/note/meta**, nessun nome. `tsc` pulito. ⚠️ ESLint non rieseguito in questo giro: manca la config (`.eslintrc.js` era stato generato da `expo lint` e poi rimosso perché non richiesto).
>
> 🧱 **Le testate non bloccano più lo scorrimento (26 lug 2026, secondo giro)** — segnalazione utente con screenshot della Mappa: «da mobile gli scrolling in tutta l'app non riescono perché la vista è bloccata dai filtri». Causa: intro, ricerca e filtri stavano **fuori** dalla `FlatList`/`SectionList`, quindi erano **fissi**: occupavano fino a metà schermo e ai contenuti restava una finestrella. Verificato in modo oggettivo: su `/affiliazioni` e `/clienti` l'intro **non aveva alcun antenato scrollabile**. ⚠️ Il giro precedente (filtri mandati a capo) **peggiorava** la cosa, perché alzava ancora la testata.
> - **Correzione:** la testata è passata dentro l'area che scorre — `ListHeaderComponent` in `lista.tsx`, `clienti.tsx`, `affiliazioni.tsx`, `rubrica.tsx`, `trattative.tsx` (`SectionList`), e dentro la `ScrollView` dell'elenco in `mappa.web.tsx`. Ora ogni schermata ha **una sola area scrollabile**: si scorre e sale tutto, filtri compresi.
> - **Restano fissi di proposito:** i tab Elenco/Ricerca in Affiliazioni e la ricerca dell'indirizzo nella Mappa (sono i comandi principali della schermata).
> - **Due trappole da ricordare:**
>   1. `ListHeaderComponent` va passato come **elemento** (`={<View>…</View>}`), **mai come funzione inline**: altrimenti a ogni lettera digitata l'header si rimonta e il campo di ricerca perde il fuoco.
>   2. La testata sta dentro il `contentContainerStyle` della lista, che ha già il suo `padding`: serve uno stile `headerScroll` con `marginHorizontal: -spacing.md` per non raddoppiare i margini e far restare la barra dei filtri da bordo a bordo.
> - **Verifica:** misurato che l'intro esce dallo schermo scorrendo (es. Clienti da y=72 a y=−328; Trattative a −336); sulla Mappa la testata risulta dentro un antenato con `overflow-y: auto`. `tsc` pulito, ESLint senza errori sui file toccati.
>
> 📱 **Leggibilità sul telefono: filtri a capo e nomi non più tagliati (26 lug 2026)** — segnalazione utente: «da mobile è impossibile da utilizzare». Cause misurate a 390px e corrette:
> - **Filtri invisibili.** Le righe di filtri erano `ScrollView horizontal` con `showsHorizontalScrollIndicator={false}`: venivano tagliate a metà parola e **niente faceva capire che si potesse scorrere**. Su `/lista` (Selezionati/Prospect/Dormienti) restavano fuori schermo 8 filtri (Stato, Città…, misurati fino a x=860 su schermo da 390). Ora **vanno a capo** (`flexWrap`), gruppi impilati: `components/Filters.tsx` (condiviso dalle 3 viste `/lista`), `trattative.tsx` (4 righe), `dashboard.tsx` (`FiltroRiga`, vale per tutte), `affiliazioni.tsx`, `rubrica.tsx` (+ `chips` del `GruppoFiltro`), `mappa.web.tsx` (`focusRow`/`subRow`: le linee di vendita visibili passano da 3 a 7).
> - **Clienti: nomi illeggibili.** Le 4 icone azione (38px l'una, ~182px) stavano **sulla stessa riga** del testo e al nome restavano ~88px → «ALPINU…», «Amir Ro…». Le azioni sono passate su una **riga propria** (`cardTop` + `azioniRiga`): ora si legge «ALPINUM SOLUTION», «Account: Eleonora Mannini».
> - **Da fare: nomi schiacciati.** Il badge «In ritardo» si spartiva la riga col nome → «Moncler…», «Church's Milano Sant'…». Ora `titoloRow` va a capo e `nome` ha `minWidth: 140`.
> - **Regola che ne esce:** in questa app **non usare `ScrollView horizontal` per i filtri**. Sul telefono lo scorrimento laterale non si vede e le voci oltre il bordo, per l'utente, non esistono: usare `flexWrap: 'wrap'` (modello di riferimento: `clienti.tsx`).
> - **Verifica:** controllo automatico su tutte e 22 le schermate a 390px con sessione reale → **zero contenuti oltre il bordo destro** (prima fallivano mappa, selezionati, prospect, inattivi); `tsc --noEmit` pulito; ESLint 0 errori sui file toccati. ⚠️ Verificato sulla **build web**: la mappa nativa (`mappa.tsx`, `react-native-maps`) non è stata provata su un dispositivo vero.

> 🧰 **Scheda negozio compatta + azioni per livello + città intera (24 lug 2026, sera)**:
> - **Scheda negozio** (`attivita/[id].tsx`): via lo stepper «Percorso verso cliente» e i bottoni giganti Naviga/Modifica; meta in una riga; subito sotto il nome una **riga di pill compatte**: Visita (primaria) · Chiama · WhatsApp · Email · Task · Contatto · Trattativa · Naviga · Modifica. Chiama/WhatsApp/Email si accendono col primo recapito dei contatti. Fix bug regex WhatsApp (`[^d]`→`[^0-9]`).
> - **Linguaggio livelli aggiornato (decisione utente)**: livello `prospect` si chiama **«Selezionato»** (⭐, mai contattato), livello `lead` si chiama **«Prospect»** (sentito o visitato). Menu Contatti: **Selezionati** (azione riga: Visita) · **Prospect** (azione riga: **Mail** → `ScegliScriptModal`: script dalla libreria o «Nuovo script» → `/invio/[scriptId]`) · **Clienti** (azioni rapide a icone sulla riga: chiama/whatsapp/email/visita — `fetchClienti` ora porta telefono+email dal primo contatto) · Dormienti e persi.
> - **Ricerca affiliazioni**: senza raggio si cerca su **tutta la città** (12 km, chip «Tutta la città»; l'edge `discover` accetta ora fino a 15 km — le query per tipo sono rankby=distance, il costo non cresce col raggio).
>
> > 🗂️ **Menu Contatti per livello + fix mappa vuota + Salva acceso (24 lug 2026)**:
> - **Contatti** ora ha 4 voci distinte, tutte su `/lista` con param `?vista=`: **Prospect** (`vista=prospect`), **Lead** (`vista=lead`), **Clienti** (`/clienti`), **Dormienti e persi** (`vista=inattivi` = dormiente+perso). `_layout.tsx`: `Voce` ha `route`+`params`, navigate li passa, focus via `voceAttiva` (confronta route+vista). `lista.tsx` legge `useLocalSearchParams().vista`, filtra per `LIVELLI_VISTA`, mostra i chip di livello solo se la vista ne ha più d'uno (inattivi → dormiente|perso).
> - **Fix «non compaiono più i negozi» sulla Mappa**: la cella di una zona già scansionata (<30gg) serviva dalla cache senza richiamare Google; se lì non c'erano places → 0 risultati. `discover` ora accetta `force` (bypassa la cache di tempo); `scopriNegozi(...force)` e il pulsante **«Cerca di nuovo qui»** (mappa web) + la Ricerca affiliazioni lo passano `true`: azione esplicita = risultati freschi. Deployato.
> - **Bottone «Salva» indirizzo**: ora è un toggle e si **accende** (pieno, «Salvato») se l'indirizzo scelto è già fra i preferiti (match per coordinate ~11 m). `usePreferiti` in `mappa.web.tsx`.
>
> 🪜 **LIVELLI COMMERCIALI (23 lug 2026, decisione utente)** — una sola scala derivata dai dati, in **`lib/livelli.ts`** (`livelloDi(place)`, `LIVELLI`, `LABEL_LIVELLO`, `coloreLivello`): **Prospect** (scelto con ⭐/+ ma mai contattato) → **Lead** (contatto avviato: visita, chiamata, richiesta web, trattativa aperta) → **Cliente** (ha chiuso una trattativa) · **Dormiente** (registro Anagrafiche = `dismesso`: ci ha già lavorato, da riattivare — NON è un perso) · **Perso**. Nessun campo nuovo nel DB: si deduce da `places.stato`, `anagrafiche_stato`, `hubspot_deal_aperta`. La pagina `/lista` (drawer «Contatti → Prospect e Lead») ha i chip di livello coi conteggi e mostra il badge livello su ogni riga al posto del vecchio stato di pipeline. Documentato in `docs/VISIONE-COMMERCIALE.md`.
>
> ⚙️ **Profilo → Impostazioni (24 lug 2026)** — nuova schermata `app/(app)/impostazioni.tsx` (card «IMPOSTAZIONI» nel Profilo): configura la **casella delle Richieste Web** e ha un bottone **«Prova il collegamento»** che esegue l'import vero e mostra l'esito. **Migr. 0043**: tabella `impostazioni` (chiave/valore, RLS: leggono tutti gli autenticati, scrive solo l'admin via `auth.jwt()->>'email'`), seed `mail.casella_richieste = commerciale@deluxy.it`. La Edge `mail` ora legge la casella **dalla tabella** (fallback al secret `MAIL_CASELLA_RICHIESTE`): cambiarla non richiede più la CLI. Helper `leggiImpostazione`/`salvaImpostazione` in `lib/db.ts`. **Regola**: in `impostazioni` niente segreti — chiavi e password restano nei secret delle Edge Function.
>
> > 📥 **Richieste Web dalla casella commerciale (23 lug 2026)** — AI Mail: `/api/v1/messaggi?casella=1` restituisce tutta la posta in arrivo dell'utente (SPAM/archiviate escluse) col `messageId`. Scout: **migr. 0042** (`leads.mail_id` unico) + azione **`richieste`** nella Edge `mail` (deployata): legge la casella del secret `MAIL_CASELLA_RICHIESTE` (default `commerciale@deluxy.it`) e importa come lead fonte `mail` solo le mail mai viste. Bottone «Importa da commerciale@deluxy.it» in Richieste Web. Secret `MAIL_CASELLA_RICHIESTE` impostato; `MAIL_API_TOKEN` già presente. L'azione `richieste` accetta anche `x-api-key: <COMMERCIALE_API_KEY>` (server-to-server, per cron/automazioni) e la funzione è deployata **--no-verify-jwt** — l'auth la fa il codice: utente loggato per `messaggi`, utente **o** chiave per `richieste`.
> - ⚠️ **VERIFICATO 24/07/2026: in AI Mail NON esiste una casella attiva `commerciale@deluxy.it`** → l'import risponde 404 con messaggio esplicito («Creala fra gli utenti di AI Mail o cambia il secret»). La catena tecnica funziona (chiave → Edge → AI Mail); manca solo la casella. Per attivarlo: creare l'utente in AI Mail (/utenti) con quell'email e collegarne l'IMAP, **oppure** puntare a una casella già esistente con `supabase secrets set MAIL_CASELLA_RICHIESTE=<email>`.
>
> 🐛 **Fix upsert su indici parziali (23 lug 2026)** — `assicuraPlace` e `creaOrdineDaDeal` usavano `upsert(onConflict)` su indici **unici parziali** (`where … is not null`): Postgres li rifiuta («no unique or exclusion constraint matching the ON CONFLICT specification») e la stella in Ricerca affiliazioni falliva. Ora: select → insert, con recupero sul 23505 in caso di corsa fra due venditori. **Regola**: in questo DB gli indici unici di `places.google_place_id`, `places.anagrafiche_id`, `ordini.deal_id` sono parziali → mai `onConflict` su quelli.
>
> > 🏁 **ROADMAP COMPLETATA: ordini, lead web, cadenze, motivi in Dashboard (23 lug 2026, sera)** — implementati i 4 punti della visione. **Migr. 0041** (applicata): tabelle `ordini` (registro delle vinte: cliente, descrizione, valore, canale, stato da_incassare|incassato|annullato, unique su deal_id) e `leads` (coda: nuovo|qualificato|scartato, fonte sito|mail|social|passaparola|altro). Novità:
> - **Ordini** (`ordini.tsx`, drawer «Vendita»): la trattativa **vinta genera l'ordine da sola** (`creaOrdineDaDeal` upsert su deal_id, chiamato dal TrattativaModal); pagina con chiuso anno / da incassare, azioni Segna incassato / Annulla / Riapri.
> - **Lead web** (`lead.tsx`, drawer «Canali»): coda di qualificazione; + a mano o **intake esterno via Edge `lead`** (POST, x-api-key = COMMERCIALE_API_KEY, deployata --no-verify-jwt, anti-doppione su nome+contatto). «Qualifica» → typeahead negozio → trattativa canale web (`qualificaLead`, follow-up +3g); «Scarta». Lead nuovo ≥2g = in ritardo (rosso).
> - **Cadenze** (`lib/cadenze.ts`): GIORNI_RISPOSTA_LEAD=2, GIORNI_FOLLOWUP_LEAD=3, GIORNI_FOLLOWUP_DEAL=7. `inserisciDeal` senza scadenza (undefined) → default **+7g**; null esplicito rispettato. Territorio/telefono restano su daRicontattare (3g/7g).
> - **Dashboard**: sezione **«Perché perdiamo»** — perse per `motivo_perso` con % e spaccato per canale.
> - **Home**: via la barra nera (feedback utente: «confonde») → testata sobria data+conteggio; nuova card **Web — lead da qualificare** con età e ritardo. **Menù riorganizzato sulla vendita**: Vendere oggi (Oggi, Da fare) · Canali (Territorio·Mappa, Chiamate·Affiliazioni, Richieste Web — ex «Lead web») · **Contatti** (Lead — ex «Target», Clienti) · Vendita (Trattative, Ordini, Pagamenti) · Strumenti (Rubrica, Script, Task, Calendario) · Andamento · Account.
> - Tutto deployato (web + Edge `lead`); tsc e 35 jest verdi. ⏳ Prossimi: aggancio ordine↔FINANCE (incasso da pro-forma), notifica push su lead nuovo, cadenze telefono con task automatici.
>
> 🧭 **VISIONE COMMERCIALE + Home cockpit (23 lug 2026)** — l'utente ha ridefinito l'app: «la home deve essere uno strumento per chi vende; le attività sono territorio, chiamata, lead da internet; ognuna genera trattativa e ordine; le perse vanno ricordate per essere riprese». Visione completa in **`docs/VISIONE-COMMERCIALE.md`** (3 canali → 1 funnel; perse = pipeline differita; roadmap: ordini, lead inbox web, cadenze, motivi in Dashboard). Implementato:
> - **Migr. 0040** (applicata): `deals.oggetto` (per cosa è), `canale` (territorio|telefono|web|altro), `motivo_perso` (prezzo|tempistica|concorrente|non_risponde|non_target|altro), `riprendere_il`, `chiusa_il`. Costanti `CANALI`/`MOTIVI_PERSO` in `types/index.ts`.
> - **Trattative**: il form ha Oggetto + Canale; scegliendo fase «Chiusa persa» chiede motivo + quando riprovarci (mai/1/3/6 mesi, default 90gg; «non target» → mai); riaprendo una persa la memoria si azzera. La riga mostra «Per: …» e «Persa per … · da riprendere il …».
> - **Home «Oggi» riscritta** (`oggi.tsx`): hero col conto delle azioni; striscia KPI settimana (visite, chiamate via `contaChiamateDal`, trattative aperte, valore pipeline); poi le 3 domande: **Territorio** (giro = starred da visitare), **Telefono** (richiami con ritardi), **Trattative da muovere** (follow-up ≤ oggi col valore), **Da riprendere** (perse con `riprendere_il` ≤ oggi), task di oggi.
> - **Edge `trattativa`** (rideployata): imposta `canale='web'` e `oggetto` (fallback nextAction) sulle trattative aperte dalle altre app.
> - Web deployato. ⏳ Roadmap non ancora fatta: oggetto "ordine", lead inbox web, cadenze per canale, report motivi di perdita in Dashboard.
>
> 🔌 **API «Apri trattativa» per le altre app (23 lug 2026)** — nuova Edge Function **`trattativa`**: `POST /functions/v1/trattativa` con header **`x-api-key: <COMMERCIALE_API_KEY>`** e body `{ azione:'apri', negozio, linea?, valoreAtteso?, fase?, scadenza?, nextAction? }`. La chiama **AI Mail** (azione `commerciale.trattativa` in `deluxy-mail/src/lib/appDeluxy.ts`, app "Commerciale" → `COMMERCIALE_URL` = `https://fdsziebgkljfsugqqbqd.supabase.co/functions/v1`), che finora mostrava «manca la chiave API sul server» perché **l'endpoint non esisteva affatto**. Comportamento: trova il negozio per nome normalizzato (404 con `candidati` se ambiguo o assente), **non duplica** se c'è già una trattativa aperta (200 con `gia_aperta`), altrimenti crea la deal (fase da alias italiani → enum `dealstage_t`, `owner: null` perché non c'è utente dietro la chiamata) e porta il negozio a `da_visitare`. Risponde con `link` alla scheda.
> - ✅ **ATTIVA (23 lug 2026)**: secret `COMMERCIALE_API_KEY` impostato e funzione **deployata con `--no-verify-jwt`** (obbligatorio: col gateway JWT attivo la chiamata muore prima di arrivare al codice, «Missing authorization header», e l'`x-api-key` non basta). Verificato in produzione: 401 senza chiave, 404 con `candidati` su nome ambiguo/assente. **Non** provata l'apertura vera (il classifier della sessione blocca la POST che scrive): primo test reale dall'azione di AI Mail.
> - **⏳ Resta da fare**: incollare la stessa chiave in AI Mail → Impostazioni App → **Commerciale** (`app.commerciale.key`, cifrata nel DB), altrimenti la card resta «manca la chiave API sul server».
>
> 📇 **Trattative e Clienti: data e account (23 lug 2026)** — Trattative: ogni riga mostra da quando è aperta (**migr. 0039** `deals.created_at`, aggiunta **senza default** così le trattative preesistenti restano a NULL = "data non registrata"; le righe da HubSpot/registro non hanno data per costruzione) e l'**account** che segue il cliente; nuovi filtri **Città** (bucket `lib/citta.ts`: Milano/Roma/Firenze/Altre/Tutte) e **Account**. Clienti: account sulla riga + filtro. Il dato è `places.anagrafiche_account` (dal registro), portato in `TrattativaConLuogo.place_account` e `Cliente.account` — in `fetchTutteTrattative` lo riempie anche `arricchisci()` dalla riga di registro. ✅ **migr. 0039 applicata (23 lug 2026)**: colonna `deals.created_at` creata; le 14 trattative preesistenti restano senza data, le nuove la prendono da sole.
>
> 🎯 **TARGET = solo i negozi aggiunti da una persona (23 lug 2026)** — decisione utente: la pagina Target deve essere **vuota** finché qualcuno non ci mette dentro un negozio. `app/(app)/lista.tsx` ora filtra su `creato_da` non nullo: la scoperta Google (Edge `discover`, service role) e gli import da terminale (`import-anagrafiche.mjs`, `import-places.mjs`) restano nel DB e sulla **Mappa**, ma **non** entrano in Target.
> - **Come un negozio entra in Target**: bottone **+** (`inserisciPlace`, `creato_da` = `auth.uid()` per default, migr. 0035) **oppure** la **stella ⭐ dalla Mappa** — `aggiornaStarred(id, true)` ora scrive `creato_da` se mancava (`lib/db.ts`), cioè "questo target lo prendo io". Non sovrascrive mai un creatore già presente.
> - Ogni riga mostra **`Inserito il <data> · da <nome>`** (oppure "dalla scoperta Google"/"da import iniziale" per i record storici).
> - **La scoperta Google non scrive più in `places` (23 lug)**: migrazione **0038** `google_negozi` (cache: google_place_id, dati Google, `nascosto`) + Edge `discover` riscritta — i negozi trovati vanno in cache e tornano al client con id **`g:<google_place_id>`**; la risposta è l'unione di `places_vicini` + cache non ancora presa. Il client li promuove a target veri alla prima azione: `assicuraPlace(p)` in `lib/db.ts` (upsert su `google_place_id`, `creato_da` = chi agisce) chiamato da stella / visita / apertura scheda in `mappa.web.tsx`, con `rimpiazzaId` che sostituisce l'id provvisorio nello stato locale. "Non interessante" su un negozio solo scoperto scrive `google_negozi.nascosto`, senza creare places. La mappa **nativa** legge da `usePlaces()` (solo `places`): non vede i `g:` e non richiede modifiche.
> - ✅ **APPLICATA E DEPLOYATA (23 lug 2026)**: migrazione 0038 eseguita (tabella `google_negozi` creata, 0 record: si popola al primo giro di Mappa) e `discover` rideployata. Da qui in avanti la scoperta non crea più `places`.
> - **Pulizia DB (facoltativa, NON ancora eseguita)**: `scripts/azzera-target-conteggio.sql` (prova a vuoto) e `scripts/azzera-target.sql` cancellano i places con `creato_da is null` + `stato='da_visitare'` + non starred + senza visite/trattative/contatti/chiamate/task/richieste di pagamento. Si lanciano con `SUPABASE_PAT=... node scripts/mgmt-query.mjs <file>`; catalogati in `scripts/README.md` del repo.

> 🤖 **ASSISTENTE AI su Trattative (20 lug 2026, commit `88bc017`)** — card "Assistente" in cima a Trattative che **riassume lo stato della pipeline** in linguaggio naturale (sintesi + azioni prioritarie + attenzione), rispettando i filtri attivi (fase/ricerca). Edge **`assistente-trattative`** (deployata): riusa il pattern Anthropic di `hubspot-match` (**Claude Haiku `claude-haiku-4-5-20251001`**); riceve dal client le trattative già filtrate+unificate (`fetchTutteTrattative`), calcola gli **aggregati deterministici** server-side (aperte/valore/vinte/perse/in ritardo) e chiede al modello solo il commento (JSON, parsing tollerante). `lib/assistente.ts` `riepilogoTrattative()`. **⏳ Richiede secret `ANTHROPIC_API_KEY`** (`npx supabase secrets set ANTHROPIC_API_KEY=sk-ant-... --project-ref fdsziebgkljfsugqqbqd`): senza, la card risponde "Assistente AI non ancora attivo" (inerte, nessun errore). È la **stessa chiave** che accenderebbe anche il match AI HubSpot (`hubspot-match`/`match_contacts`). CORS con apikey, 401 senza auth. Web deployato.
>
> 📧 **EMAIL PER-UTENTE ATTIVA (20 lug 2026, commit `77590d1`)** — SMTP finalmente configurabile, ma **per venditore** (scelta utente: "ogni utente metterà la propria mail e password"), non con una casella unica. Provider: **Register.it** (`authsmtp.register.it:465`, il codice attiva TLS da solo a porta 465).
> - Migr. **0031** `smtp_account` (owner, host, porta, utente, **password_cifrata**, mittente, verificato_il) — **RLS ATTIVO SENZA POLICY**: la legge solo il `service_role` (le Edge Function), mai il client. Applicata e verificata (RLS on, 0 policy).
> - Cifratura in **`supabase/functions/_shared/smtp.ts`** (AES-256-GCM Web Crypto, formato `iv.tag.dati` come deluxy-mail) con secret **`SMTP_ENC_KEY`** (32 byte base64, **già impostato** — generato in memoria, mai stampato). `credenzialiPerUtente(admin, ownerId)`: casella personale → fallback secret globali `SMTP_*` (non impostati) → null=inerte.
> - Edge **`smtp-config`** (deployata): azioni `stato`/`salva`/`verifica`/`rimuovi`; la password entra, si cifra, **non torna mai al client** (`stato` non la restituisce); `verifica` invia un'email di prova alla casella stessa e segna `verificato_il`.
> - **`notifica-task`** e **`promemoria`** rideployate: inviano dalla casella dell'utente (mittente = chi assegna il task / l'utente stesso). CORS esteso con `apikey`.
> - **UI**: schermata **`app/(app)/email-config.tsx`** (host precompilato, porta, email, password, mittente, "Salva", "Invia email di prova", "Scollega") + voce **EMAIL** nel Profilo. Web rideployato.
> - **⏳ Test end-to-end reale non fatto** (serve un utente loggato che inserisca una casella Register.it vera con SMTP autenticato abilitato): salvare → "Invia email di prova" → deve arrivare. Poi provare notifica task e "Inviami il riepilogo" da Oggi. Prerequisito Register.it: **SMTP autenticato abilitato** per la casella, altrimenti auth rifiutata. **Il vecchio piano "secret SMTP globali" è superato** da questo (i secret globali restano come fallback ma non sono impostati).

> 🆕 **Email per-utente + Sezione Script (20 lug 2026)**: 
> - **Casella email personale (Register.it)**: ogni venditore collega la PROPRIA casella da **Profilo → La mia email** (`app/(app)/email-config.tsx`). Password **cifrata** (AES-256-GCM, secret `SMTP_ENC_KEY`) in tabella `smtp_account` (migr. **0031**, RLS senza policy = solo service_role); funzione **`smtp-config`** (stato/salva/verifica/rimuovi); helper condiviso `supabase/functions/_shared/smtp.ts` (`credenzialiPerUtente`: casella utente → fallback secret globali SMTP_* → null). `notifica-task` e `promemoria` ora inviano dalla casella dell'utente (chi assegna / se stesso). **Tutto già applicato e deployato** (tabella+funzione+SMTP_ENC_KEY presenti, verificato). Manca solo che gli utenti inseriscano le credenziali. Host tipico `authsmtp.register.it:465`; su Register.it l'SMTP autenticato va abilitato per la casella.
> - **Sezione "Script" (voce drawer)** = libreria dei testi email + invio a più contatti. Migr. **0032** `script_email` (condivisa: tutti leggono, scrive/elimina creatore o admin; segnaposto `{nome}`/`{negozio}`). `app/(app)/script.tsx` (elenco+editor CRUD), `app/(app)/invio/[scriptId].tsx` (scegli contatti dalla Rubrica con email → revisione oggetto/testo+anteprima → **conferma esplicita** → esito per destinatario). Edge Function **`invio-email`** (invio sequenziale dalla casella personale, tetto 60, personalizza segnaposto). `lib/script.ts`. **Applicato e deployato**; web live. Serve la casella email collegata per inviare davvero.
>
> 🆕 **Pagamenti ↔ Deluxy Partner: pro-forma (20 lug 2026, commit `f2e6089` Scout + `1706b97` Partner)**:
> - **Partner** espone su `/api/proforma` (chiave `X-API-Key`, la stessa di /api/verifiche) anche **PATCH = conferma pagamento**: body `{ id | numero: "1/2026", fatturaNumero? }` → stato `fatturata` (idempotente; 422 se annullata). GET/POST già esistenti (dettaglio/elenco/creazione in bozza).
> - **Scout**: Edge Function **`proforma`** (proxy, secret `PARTNER_API_KEY` + opz. `PARTNER_URL`) con azioni `crea`/`conferma`; `lib/partner.ts` (`creaProformaDaRichiesta` — importo IVA inclusa, imponibile scorporato 22% così il **totale PF = importo richiesto**; `confermaPagamentoProforma`); nel **form richiesta** checkbox "Emetti anche la pro-forma su Deluxy Partner" (best-effort: se fallisce la richiesta resta, avviso con partner simili suggeriti, ritenta senza duplicare); **chip "PF n/anno"** sulla card (link al documento); **incasso completo** (stato pagata / rate tutte spuntate) → conferma automatica della PF collegata. Migr. **0029** (`proforma_numero`/`proforma_url`, salvataggio tollerante). `aggiornaRataPagata` ora ritorna lo stato ricalcolato.
> - **✅ ATTIVATO E VERIFICATO (20 lug 2026, sera)** — l'integrazione è **completamente live**:
>   - **Web Scout rideployato** su deluxy-scout.vercel.app (restyling DS + checkbox pro-forma). Verificato: il dominio serve Scout, login reso, 0 errori console.
>   - **Lato Partner LIVE**: chiave API valida e **PATCH `/api/proforma` deployato** in produzione (sonda senza id → 400 "Campo 'id' o 'numero' obbligatorio" = endpoint presente; nessun merge su `main` necessario).
>   - **Migrazione 0029 APPLICATA** (verificato: `proforma_numero` e `proforma_url` esistono su `richieste_pagamento`).
>   - **Secret `PARTNER_API_KEY` impostato** sul progetto (visibile in `supabase secrets list`).
>   - **Edge Function `proforma` DEPLOYATA** e verificata: OPTIONS → 200, POST senza auth → 401 (rifiuta i non loggati, come deve).
>   - **Pro-forma ATTIVA DI DEFAULT (richiesta utente, commit `dbae249`)**: migr. **0030** `profiles.proforma_default` (boolean, default **true**, applicata e verificata). La checkbox nel form nasce spuntata e segue la preferenza per utente; si disattiva da **Profilo → PAGAMENTI** (interruttore con salvataggio ottimistico). Resta comunque modificabile caso per caso senza toccare la preferenza. Letture/scritture tolleranti: senza migrazione vale `true`.
>   - **🐞 Fix CORS (commit `7e3a18e`)**: al primo tentativo reale dell'utente la pro-forma dava **"Failed to fetch"**. Causa: le Edge Function `proforma` **e `anagrafiche`** dichiaravano `Access-Control-Allow-Headers: authorization, content-type`, ma il client web invia anche **`apikey`** (e supabase-js aggiunge `x-client-info`) → **preflight bloccato dal browser** (falliva quindi anche la scheda "Registro Anagrafiche" da web; `geocode` era già corretta). Fix: header estesi a `authorization, content-type, apikey, x-client-info`, entrambe rideployate e **verificate** (preflight → 200 con gli header giusti). Aggiunto anche un messaggio leggibile al posto di "Failed to fetch". **Regola per le prossime Edge Function: elencare sempre `apikey` e `x-client-info` nei CORS.**
>   - ⏳ **Unico test non fatto**: il giro end-to-end reale (creare una pro-forma vera su Partner) — richiede un utente loggato e **crea un documento numerato reale**, quindi lo fa l'utente dalla UI: Pagamenti → nuova richiesta con la checkbox "Emetti anche la pro-forma".
> - ⚠️ **Trappola token (20 lug)**: un access token Supabase valido può appartenere a un **altro account/org** e dare 403 sulla Management API del progetto Scout. L'account giusto è quello che vede **`fdsziebgkljfsugqqbqd` (deluxy-scout)** e `zegbztfxisqeowngvgvh` (deluxy-partner); un altro account dell'utente vede solo `sxovckndpmdbqfrfkxhl` ("deluxy's"). Verifica rapida: `curl -H "Authorization: Bearer <pat>" https://api.supabase.com/v1/projects`.
>
> 🆕 **Fatto il 20 lug 2026** (branch `scout-ui`, commit `be96532`):
> - **Restyling UX completo al Deluxy Design System** su tutte le 20+ schermate. Nuovo **kit condiviso `components/ui.tsx`**: `PageIntro` (caption grigia che spiega ogni sezione), `EmptyState` (icona in quadratino gold-soft + frase d'aiuto + azione), `StatusBadge` (pillola dot + tinta 10% + testo colorato — il pattern badge DS), `Btn` a pillola, `tinta()`. **Da usare per ogni UI futura.**
> - Correzioni di conformità: **oro eliminato dai bottoni primari** (Nuova visita, Salva visita/contatto, Naviga, chip attivi → ink); badge a fondo pieno → dot+tinta ovunque; emoji → Ionicons; `PriorityBadge` ora "P1 · Alta" in stile DS; `Loader` sobrio (niente spinner giganti); nuovi token `coloreStato` e `coloreFase` in `lib/theme.ts`; colori hardcodati (#F0ECE2, #F3E9D6, #5A6274, rgba CRM) → token.
> - Comprensibilità: sigle sciolte ("+7g" → "+7 giorni", "Visite 7g" → per esteso, "Da richiam." → "Da richiamare", "Deal" → "Trattative"); editor rate Pagamenti etichettato (era "Rate/split" con toggle %/€ criptico e placeholder "30" che sembrava un valore → ora "Rate di pagamento", righe "Quanto"/"Scadenza", "% del totale"/"€", placeholder "es. 30", scadenze "entro 15 gg"); dealstage tradotti via `labelFase` anche nella scheda attività; filtri stato senza underscore; date GG/MM/AAAA; "da cache" → "ricerca recente salvata"; badge CRM "◑/◆" → "Contatto in CRM"/"Trattativa aperta"; legenda azioni card in Mappa web e legenda pin in Mappa nativa; Affiliazioni: selezione con **stella** (prima icona telefono ambigua, ora coerente con Mappa) e "Stato: … · tocca per cambiare"; Trattative: header gruppo chiari (via le barre navy), origine "da HubSpot/dal registro", "+ valore €" al posto della matita.
> - Verifica: `tsc` pulito, **35/35 jest**, `expo export --platform web` ok. **⏳ Web NON ancora rideployato** (serve `VERCEL_TOKEN`): `VERCEL_TOKEN=… bash scripts/deploy-web.sh`. Verifica visiva dell'utente raccomandata su: Pagamenti (editor rate), Mappa (legenda + badge), Trattative, Affiliazioni.

> 🆕 **Fatto il 17-18 lug 2026** (branch `scout-ui`):
> - **Sezione Affiliazioni** (`app/(app)/affiliazioni.tsx`): elenco negozi linea Re-seller con dati anagrafici, **numero di telefono visibile**, referente, ultima chiamata. Bottone **Chiama** (registra una chiamata in tabella `chiamate`), **step con i 7 stati** del registro Anagrafiche (`stato_affiliazione`), **icona telefono per selezionare** ("Selezionati" da contattare = flag `starred`, lo stesso della stella in Mappa). Filtri: `tutti / selezionati / <7 stati>`. Migrazione **0017** (enum `stato_affiliazione` + tabella `chiamate` + RLS).
> - **Mappa — scoperta arricchita** (`app/(app)/mappa.web.tsx`): card scoperta con **recensioni Google** (voto + n°), verifica se è già **partner** (da Anagrafiche) e lo **stato trattativa**; **stato editabile in linea** allineato agli stati mappa: click **stella** → `da_visitare` + aggiunge a "Selezionati"; click **cerchio** → `visitato` + apre il pop-up visita rapida. Migrazione **0018** (`google_rating`/`google_reviews`). Sottomenu **Fiori/Pasticcerie / Fiori / Pasticcerie / Tutti** (pill piccole) visibile **solo** quando la linea "Affiliazioni" è selezionata.
> - **"Selezionati"**: rinominata la ex sezione "Pianifica giro" → **Selezionati**; il **pallino conferma-visita** è presente anche in questa modalità (commit `53d50c1`).
> - **Trattative — valore + creazione** (`app/(app)/trattative.tsx`, commit da fare): il **valore** era già mostrato (riga deal + totale atteso in testata). Aggiunto **FAB "Nuova trattativa"** → modal con: **typeahead negozio** (`cercaPlaces`), **contatti del negozio** mostrati con nota "sincronizzati su HubSpot", **linea** (chip), **fase** (5 dealstage), **valore atteso €**, **prossima azione**. Salva → `inserisciDeal` (Supabase) + best-effort `syncTrattativa` (HubSpot).
>   - Backend: **nuova azione `sync_deal`** in `hubspot-sync/index.ts` (upsert Company + **tutti** i contatti del negozio, crea Deal con **`amount` = valore atteso** e fase). Aggiunto param `amount` a `createDeal`. `lib/db.ts`: `cercaPlaces` + `inserisciDeal`. `lib/hubspot.ts`: `syncTrattativa(dealId)`.
>   - Verifica: `npx tsc --noEmit` **pulito** + `npx expo export --platform web` **ok**.
>   - ✅ **DEPLOYATO il 18 lug** (web su deluxy-scout.vercel.app + Edge Function `hubspot-sync`). Token forniti al volo dall'utente e **non salvati** (poi da revocare).
>   - Nota HubSpot: `amount` è property standard del deal → nessuna property custom da creare.
> - **Pagamenti: split in rate (19 lug)**: migr. **0028** `rate_pagamento` (modo `valore|percentuale`, `importo`, `scadenza`, `pagata`, `ordine`) collegata a `richieste_pagamento` (RLS derivata dal padre). Form: editor rate (+aggiungi, toggle €/%, valore, scadenza per rata) con importo calcolato e residuo vs totale. Monitoraggio: spunta le rate incassate → rollup `importo_incassato`/`stato` (parziale/pagata) ricalcolato in `aggiornaRataPagata`; indicatore "N/M rate" sulla card. `fetch` con embed `rate_pagamento(*)`. Verificato: 30%/70% con scadenze + incasso acconto → parziale. Commit + deploy ok.
> - **Pagamenti = incasso da trattativa (19 lug, ridisegno)**: NON è una coda Finance interna, ma: **da una trattativa vinta si crea una richiesta di pagamento al cliente (anche parziale/acconto) e si monitora l'esito dell'incasso**. Migr. **0027**: ricreata `richieste_pagamento` collegata al **deal** (`deal_id`/`place_id`), campi `cliente`, `importo` (parziale ok), `importo_incassato`, `scadenza`, `stato` di incasso (`inviata/in_attesa/pagata/parziale/insoluta/annullata`), `nota`. Form: **crea da una trattativa** (typeahead, vinte prima, prefill cliente+importo dal valore deal, editabile). Monitoraggio: card espandibile con chip **esito** + campo **incassato** (deriva pagata/parziale); testata con totali *da incassare / incassati*. RLS: proprie; admin supervisione. `db.inserisci/aggiornaRichiestaPagamento` aggiornate. Verificato: crea + incasso parziale + RLS 2 utenti. Commit + deploy ok. (Sostituisce il precedente modello "richiesta a Finance".)
> - **🧭 Riorganizzazione "app del commerciale" (19 lug)**: visione utente = prospezione, visite, appuntamenti, automazioni mail/solleciti, assistente task, richieste di pagamento → Finance. Fatto: (1) **"Oggi"** (`oggi.tsx`) = nuova **home/assistente** (default redirect da index + post-login): saluto col nome, agenda del giorno (task+follow-up in scadenza oggi), sezione **In ritardo**, richiami, task aperti, azioni rapide (Task/Mappa/Trattative/Pagamento), bottone "Inviami il riepilogo via email". (2) **Pagamenti** (`pagamenti.tsx` + migr. **0025** `richieste_pagamento`): il commerciale apre una richiesta (beneficiario, importo, causale, IBAN opz., urgenza) → stati `inviata→in_lavorazione→pagata/rifiutata`; **RLS**: proprie; **Finance = admin** (stessa email) vede tutte e cambia stato in linea + nota. (3) **Edge Function `promemoria`**: riepilogo email dei task+follow-up in scadenza dell'utente (denomailer, **inerte senza SMTP** — verificato `{sent:false, task:1}`). (4) **Drawer riordinato per flusso**: Oggi/Task/Calendario/Da fare → Mappa/Target → Trattative/Affiliazioni/Rubrica → Pagamenti/Dashboard/Team/Profilo. Verificato: RLS pagamenti con 2 utenti (B non vede le richieste di A), promemoria 200. Commit + deploy ok. **⏳ Rimandato**: cron promemoria giornaliero (richiede verify_jwt=false o token dedicato — decidere), SMTP da configurare per attivare TUTTE le email (notifica-task + promemoria), collegamento richieste pagamento ↔ app Finance/Partner.
> - **Calendario: griglia mensile + sync iCal (19 lug)**: `calendario.tsx` ora ha una **vista mensile a griglia** (lun-first, navigazione mesi, pallino-conteggio sui giorni con eventi — rosso se scaduti — giorno selezionato con l'elenco). **Sync con calendari esterni**: Edge Function **`calendario-ics`** (`--no-verify-jwt`) genera un feed **.ics** dei task/follow-up in scadenza, protetto dal **`cal_token`** per-utente (migr. **0026** `profiles.cal_token`); modal "Sync calendario" mostra il link privato + istruzioni Google/Apple/Outlook (sola lettura, sottoscrizione). `db.fetchCalToken`/`urlFeedCalendario`. Verificato: feed ICS 200 `text/calendar` con evento, token errato → 404. ⚠️ Link segreto: chi ce l'ha vede gli appuntamenti (come i feed calendario standard). Commit + deploy ok.
> - **Calendario (18 lug)**: nuova schermata `calendario.tsx` (voce drawer, `calendar-outline`) che aggrega gli "appuntamenti" datati — **task con scadenza** (non completati) + **follow-up trattative con scadenza** — raggruppati per giorno (Oggi/Domani/data, ritardi in cima), con negozio, tipo (task/trattativa) e **CHI** (owner/assegnatario). Filtro per venditore. Riusa `fetchTask(false)` + `fetchTutteTrattative` (owner_nome già risolti). NB: i task rispettano la privacy RLS (propri+creati; admin tutti); i follow-up trattative sono visibili a tutti. Commit + deploy ok.
> - **Task = schermata di default (18 lug)**: i redirect d'ingresso (`app/index.tsx`) e post-login (`app/(auth)/_layout.tsx`) puntano a **`/(app)/task`** invece di `/(app)/mappa`. Commit `25d23a3` + deploy ok. (La voce "I miei task" nel drawer resta dov'è; si può spostare in cima se serve.)
> - **Trattative: linea multipla (18 lug)**: `deals.linee text[]` (migr. **0024**, backfill da `linea`); `linea` resta la primaria (display/HubSpot). `TrattativaModal` usa **`LineaSelector`** (multi) invece del chip singolo; salva `linee[]` + `linea = linee[0]`. `RigaDeal` mostra le linee unite. `Deal.linee?` opzionale. Verificato: array persiste. Commit + deploy ok.
> - **Mappa scoperta: dati live dal registro (18 lug)**: componente riusabile **`RegistroExpandable`** (toggle on-demand + `stopPropagation` per non navigare la card). Card scoperta (`mappa.web.tsx`) ristrutturata a **colonna**: `cardMain` (contenuto+azioni, row desktop / column mobile) + espandibile "Registro Anagrafiche" sotto. Commit `…` + deploy ok. ⚠️ Layout card modificato ma non testato live (login-gated) — verificare visivamente.
> - **Affiliazioni: dati live dal registro (18 lug)**: ogni card Affiliazioni ha un espandibile **"Registro Anagrafiche"** che carica live (via il proxy) stato/interessi/referenti **solo quando aperto** (niente N chiamate sulla lista). `AnagraficaRegistroCard` ha una variante **`compatta`** (senza chrome) per l'embed. Commit `14f04a0` + deploy ok.
> - **Anagrafiche: letture live dal registro + stato `dismesso` (18 lug)**: allineamento al README del registro. (A) stato **`dismesso`** (8° del registro) in `StatoAffiliazione`/`STATI_AFFILIAZIONE` + `labelAffiliazione`/`coloreAffiliazione` + migr. **0023** (`alter type stato_affiliazione_t add value 'dismesso'`). (B) Edge Function proxy **`anagrafiche`** (secret `ANAGRAFICHE_API_KEY` impostato) → letture LIVE `GET /api/v1/partners` server-side; `lib/anagrafiche.ts` `cercaAnagrafica(nome, citta)`. (C) **`AnagraficaRegistroCard`** nel dettaglio attività: mostra live **stato, INTERESSI (tipologia autorevole), recapiti, referenti** dal registro (non dalla copia locale). ⚠️ `interessi` valorizzati solo su ~10% dei partner (come `deluxy_linea` HubSpot). Verificato: proxy autenticato ritorna i dati (HTTP 200). Commit `1c139f8` + deploy ok. NB: la copia locale in `places` resta (import batch); la card è il primo passo verso il "leggi, non duplicare" del README.
> - **Notifica email assegnazione task (18 lug)**: Edge Function **`notifica-task`** (SMTP via denomailer): quando un task è assegnato a un ALTRO (owner≠creato_da), il `TaskFormModal` chiama best-effort `notificaAssegnazioneTask(taskId)` → email all'assegnatario. **Inerte finché non si impostano i secret SMTP** (`SMTP_HOST/PORT/USER/PASS/FROM`): senza, risponde `{sent:false, reason:'smtp_non_configurato'}` (verificato, nessun invio). Per attivare: `supabase secrets set SMTP_HOST=... SMTP_PORT=465 SMTP_USER=... SMTP_PASS=... SMTP_FROM=...` (usare le credenziali di Deluxy Mail o una app-password Gmail). Commit + deploy ok.
> - **Task: assegnazione + admin + modifica (18 lug)**: migr. **0022** — colonna `creato_da` (owner=assegnatario, creato_da=creatore) + **RLS granulari**: vedi/edita/elimina i task **assegnati a te o creati da te**, e se **admin** (`auth.jwt()->>'email' = 'nicolo.donato@deluxy.it'`) **tutti**. `inserisciTask(owner)` / `aggiornaTask(owner)` per assegnare/riassegnare; `fetchTask(soloMiei)` risolve nomi assegnatario/creatore. **`TaskFormModal`** (crea+modifica, selettore "Assegna a") usato da tasklist (FAB + tap=modifica) e scheda attività (rimosso `CreaTaskModal`). `task.tsx`: toggle **Assegnati a me / Tutti**. Verificato con 3 utenti: assegnazione, isolamento (C non vede), riassegnazione. ⚠️ Il ramo admin-vede-tutto non testato live (serve login admin) ma segue il pattern provato di 0014. Commit + deploy ok. **⏳ In sospeso**: notifica email all'assegnatario ("client di posta") — da chiarire con l'utente.
> - **Task collegati a un negozio (18 lug)**: scheda attività (`attivita/[id].tsx`) → bottone **Task** apre `components/CreaTaskModal.tsx` (titolo+priorità+scadenza) che crea un task con `place_id` del negozio. `fetchMieiTask` fa join `places(nome)` → la tasklist mostra il **negozio collegato** (chip oro) e vi **naviga** al tap. Embed FK verificato (200). Commit + deploy ok.
> - **Tasklist personale (18 lug)**: tabella **`tasks`** (migr. **0021**) **privata**, RLS **owner-only** (`using/with check owner = auth.uid()`). Campi: titolo, note, `priorita` (P1/P2/P3), `scadenza` (date), `completata`, `place_id` opz., `completata_at`. `lib/db.ts`: `fetchMieiTask`, `inserisciTask`, `aggiornaTask`, `completaTask`, `eliminaTask`. Schermata **`task.tsx`** ("I miei task", voce drawer con `checkbox-outline`): aggiunta rapida (titolo + priorità + scadenza a chip Oggi/Domani/+7g), sezioni Da fare/Completati, toggle check, elimina, badge scadenza + IN RITARDO. È distinta da "Da fare" (che è la coda derivata dai dati). Verificato: CRUD + isolamento RLS tra 2 utenti (A vede i propri, B no). Commit + deploy ok.
> - **Follow-up in "Da fare" con persona + scadenza (18 lug)**: nuovo campo **`deals.scadenza`** (migr. **0020**, date). Form trattativa: selettore scadenza (chip +7/+14/+30 giorni). Schermata **Da fare** (`da-completare.tsx`): nuova sezione **Follow-up affiliazioni** (da `followupAffiliazioni` sulle trattative unificate) con **owner (persona)** + **scadenza** (data + relativo, badge IN RITARDO se passata), ordinata per scadenza. Rimossa la lista follow-up dalla Dashboard. `Deal.scadenza` aggiunto (fixture test aggiornati). ⚠️ La scadenza persiste per deal **Scout**/registro-creati; sui deal solo-HubSpot non c'è una riga Scout dove salvarla. Commit + deploy ok (dopo un rate-limit Vercel free 100/die temporaneo). tsc + 35 test verdi.
> - **Dashboard: analisi trattative + filtri (18 lug)**: la Dashboard usa ora le **trattative unificate** (`fetchTutteTrattative`: Scout+HubSpot+registro, con valori). Nuove card: **pipeline aperto €, vinto €, win rate %, perso €**; grafici **trattative per stato** e **valore atteso per linea**; sezione attività sul campo (visite 7gg, da ricontattare, visite/settimana, copertura zone). **Barra filtri** in cima: **Zona, Venditore, Linea, Stato trattativa** (chip scrollabili + Azzera) che scopano trattative/visite/places. Aggiunto `place_zona` alle trattative unificate (per il filtro zona; visite filtrate via mappa place→zona). Metriche nuove in `metrics.ts`: `valoreTrattative`, `winRate`, `dealPerFase`, `valorePerLinea`; `chiusePerse`/`followupAffiliazioni` rese generiche. Nurturing (chiuse perse) solo per deal Scout (id reale). Commit `e3d003e`, deployato. ⚠️ Non testato live (login-gated).
> - **🐞 Fix sync HubSpot fallito in silenzio (18 lug)**: la sincronizzazione (visite E trattative) falliva per molti negozi e il `catch` best-effort la nascondeva. Due cause in `hubspot-sync`: (1) `upsertCompany` inviava il **settore IT** (es. `FIORISTA`) nella proprietà **`industry`** = enum HubSpot a valori fissi → **400 INVALID_OPTION**. **Rimosso `industry`** (classificazione già in `deluxy_linea`). (2) `upsertContact` non gestiva **409 "Contact already exists"** (email già presente) → ora estrae l'`Existing ID` dal messaggio e fa PATCH invece di POST. Verificato: sync_deal su "Armani Fiori" → **HTTP 200** (company `438444546247`, deal `511174041810`). Commit `87fc875`, deployato. ⚠️ I deal creati prima del fix (con `hubspot_deal_id` null) vanno ri-sincronizzati (riaprendo Trattative parte `refresh_deal_values`, ma quello aggiorna solo i valori; per crearli su HubSpot serve ri-salvarli o un batch di `sync_deal`).
> - **Modifica trattative (18 lug)**: tap su una riga in Trattative apre il modal in **modifica** (negozio bloccato, campi precompilati). Salvataggio **per origine**: **Scout** → `aggiornaDeal` (RLS `deals_write`: solo deal propri o `owner IS NULL`) + push HubSpot se già sincronizzata; **HubSpot** → `update_deal` (edge, service role → PATCH `/crm/v3/objects/deals/{id}` con `HubSpot.patchDeal` + mirror `hubspot_deals` + eventuale deal Scout collegato); **Registro** → crea una trattativa Scout gestibile per quel negozio (`inserisciDeal` + syncTrattativa). `TrattativaModal` unifica crea+modifica. **Testato**: update_deal HTTP 200 (patch vuoto, no scritture), PATCH deal Scout via RLS 200. Commit `209360c`, deployato. ⚠️ Modifica dei deal Scout altrui bloccata da RLS (owner); i deal HubSpot invece sempre modificabili (service role).
> - **Fix navigazione dopo salvataggio (18 lug)**: il layout è un **Drawer** (`expo-router/drawer`) → non ha uno stack lineare, quindi `router.back()` dopo un salvataggio riportava alla **Mappa** (schermata iniziale). Corretto in **contatto/[placeId]**, **modifica/[id]**, **visita/[placeId]** (3 punti): ora `router.replace('/(app)/attivita/<placeId>')` → si torna al **dettaglio del negozio**. Il back-arrow dell'header resta `router.back()` (azione esplicita, nessuna destinazione unica). Commit `94ceee7`, deployato. ⚠️ Non testato live (app login-gated).
> - **Registro Anagrafiche in Trattative (18 lug)**: `fetchTutteTrattative` ora unisce **3 fonti** — deal Scout + trattative aperte HubSpot + **partner registro `in_trattativa`** (19, senza deal Scout/HubSpot, con tipologia = `linea_ipotizzata`). Ogni riga è **arricchita** col negozio corrispondente (match negozio Scout → azienda HubSpot → **nome normalizzato**): riempie la tipologia mancante (+3 su HubSpot) e aggiunge **stato registro** + flag partner (badge dot+label via `coloreAffiliazione`/`labelAffiliazione`; origine "Registro"). Le righe registro usano fase interna `decisionmakerboughtin` per grouping/filtro ma mostrano lo **stato registro reale** in UI. **Verificato con utente reale**: 7 Scout + 43 HubSpot (36 con valore) + 19 registro = **69 righe**. ⚠️ Match per nome copre poco i deal HubSpot (il grosso non è nel registro). Commit `89667b8`, deployato.
> - **Tipologia di interesse (linea) in Trattative (18 lug)**: `hubspot_deals.linea` (migr. **0019**) popolata da `deluxy_linea` in `sync_crm` (hubspot-match, rideployata `--no-verify-jwt` — verify_jwt resta false, cron ok); `fetchTutteTrattative` la espone; `RigaDeal` mostra un **tag oro tipologia** quando distinta dal titolo. ⚠️ **Coverage bassa**: `deluxy_linea` è compilata solo su **1/44** deal aperti HubSpot (gli altri non la valorizzano su HubSpot); il tag compare man mano che si compila `deluxy_linea` su HubSpot. **Non** si inferisce la linea dal nome del deal (inaffidabile). Commit dopo `9cf8ec7`, deployato. Sync CRM rilanciata: 521 aziende / 1698 contatti / 133 deal.
> - **Pipeline HubSpot in Trattative + filtro stato (18 lug)**: la schermata mostrava solo i **7 deal Scout** (tabella `deals`, senza importo); il pipeline vero è nella copia CRM `hubspot_deals` (**133 deal, 111 con valore, 44 aperti**, sync notturno 04:00). `fetchTutteTrattative` ora **unisce** deal Scout + trattative **aperte** di `hubspot_deals` (dedup per id HubSpot; nome via negozio Scout→azienda HubSpot→nome deal). UI: **chip filtro per stato** (fasi presenti), totale sul filtrato, raggruppamento anche per deal senza negozio Scout, badge origine. RLS: `hubspot_deals`/`hubspot_companies` leggibili da `authenticated` (verificato con utente reale: 44 aperti leggibili). Commit `9cf8ec7`, deployato. Le fasi HubSpot coincidono con le nostre 5 dealstage.
> - **Importi anche per i deal da HubSpot (18 lug)**: le trattative nate da una visita (`sync_visit`) sono create su HubSpot **senza `amount`** e nella tabella `deals` hanno `valore_atteso=null`. Aggiunta azione **`refresh_deal_values`** in `hubspot-sync` (batch-read `amount/dealstage/deluxy_linea/next_action` da HubSpot via `HubSpot.readDeals`, blocchi da 100 → aggiorna `deals`); `lib/hubspot.ts` `aggiornaValoriTrattative()`; la schermata Trattative lo chiama **best-effort all'apertura** e ricarica se qualcosa cambia. **Testato end-to-end** (utente usa-e-getta creato+eliminato via Admin API, JWT reale): HTTP 200, `{"aggiornati":1}` (allineata 1 fase). ⚠️ **Nessuno dei 7 deal reali ha un `amount` su HubSpot** → nessun € mostrato finché non lo si imposta su HubSpot o non si crea una trattativa col valore dal form. (Il contatore `aggiornati` conta qualunque campo cambiato, non solo il valore.) Commit `ffb5c7a`, deployato.

> 🆕 **Fatto il 16 lug 2026** (branch `scout-ui`, tutto già **live**):
> - **Nomi venditori**: impostato `raw_user_meta_data.nome` sui 3 utenti (Nicolò Daniele Donato, Eleonora Mannini, Martina Calia) → la dashboard Team non mostra più "Utente xxxxxx". Migrazione **0015** (`profiles.ultimo_accesso`, backfill da `auth.last_sign_in_at`); l'app aggiorna il proprio ultimo accesso a ogni login (`lib/auth.tsx`). Commit `3bd6d23`.
> - **Team**: `attivitaPerVenditore` ora include **tutti** i venditori (anche a 0 visite, selezionabili); la card mostra *"attivo X"* se ha visite, altrimenti *"accesso X"*.
> - **🔑 Scoperta — bug importante risolto** (commit `edbf64f`): Google Nearby con `radius` ordina per **prominenza** e tronca a **60**; in zone dense (Quadrilatero) ci sono >60 esercizi anche entro **100 m**, quindi **le boutique piccole — il nostro target — restavano sempre fuori** (provato: MooRER, Corso Venezia 2, non usciva né con `type=clothing_store` né riducendo il raggio). Ora `discover` affianca alla generica una Nearby **`rankby=distance` per ogni tipo Deluxy** (parallele, stop appena fuori raggio, merge+dedup+filtro raggio). **Cache `google_aree` invalidata** (19 celle) per far ri-scansionare le zone.
> - **UI**: scegliere l'indirizzo **non avvia più** la scoperta → pulsante **"Cerca negozi qui"** (le chiamate Google costano: meglio esplicite).
> - ⚠️ Da sapere: esiste un doppione **"Moorer"** inserito a mano (source `manual`, indirizzo senza civico, posizione imprecisa, già `visitato`) che si affiancherà a "MooRER Boutique" da Google → va unito.
> - **📇 Registro Anagrafiche importato in Scout (commit `33cc3ac`)**: `deluxy-anagrafiche.vercel.app` è la **fonte di verità** dei partner/prospect B2B (572, nazionali). Importate **567 attività + 905 contatti** (5 senza indirizzo geocodificabile). Direzione scelta: **solo Anagrafiche → Scout** (chiave di **sola lettura** `deluxy-scout`, creata con `npm run chiave -- deluxy-scout` dal progetto Anagrafiche; salvata in `deluxy-scout/.env` come `ANAGRAFICHE_API_KEY`). **Il push Scout → Anagrafiche NON è stato fatto**: richiederebbe una chiave di *scrittura* (oggi ce l'ha solo la piattaforma consegne) — è una decisione di policy dell'utente.
>   - Migrazione **0016**: `places.anagrafiche_id` (unique parziale = legame + idempotenza), `anagrafiche_account`, `anagrafiche_stato` (i 7 stati del registro; Scout ne ha 4 e perderebbe in_attesa/in_contatto/da_ricontattare), `anagrafiche_ultima_visita`, indice trigram su `nome`.
>   - Ri-sync: `ANAGRAFICHE_API_KEY=… SUPABASE_PAT=… node scripts/import-anagrafiche.mjs` (`--limite=N` per provare, `--dry` per non scrivere). Non ri-geocodifica ciò che c'è già, non tocca i contatti aggiunti dai venditori.
>   - **Nota**: Anagrafiche **non ha entità "trattative"** — la trattativa è lo `stato` del Partner (`in_trattativa`) + `account` + `ultimaVisita`. Mappature: BOUTIQUE→moda, FIORISTA→fioraio, PASTICCERIA/CIOCCOLATERIA→pasticceria, RISTORANTE/ENOTECA→ristorante premium, GIFTING/MERCHANDISING→azienda corporate, CATERING/PARTY→event planner; `attivo`→cliente, `in_trattativa`→visitato, `non_interessato`→perso, resto→da_visitare.
>   - ⚠️ **~30+ doppioni noti** tra registro e negozi scoperti da Google (Gucci, Prada, Jimmy Choo, Dolce & Gabbana…, spesso a 0-40 m): **non fusi di proposito**. Lo script li elenca a fine run; serve una strategia di merge (decidere quale record vince e cosa fare di visite/contatti).

> ⚡ **COME RIPRENDERE IN UNA NUOVA FINESTRA (checklist)**
> 1. Apri una nuova sessione Claude Code nel progetto e scrivi **"continua deluxy-scout"** (la memoria si carica da sola e rimanda a questo file).
> 2. **Lavora nel worktree** `C:\Users\nicol\scoutwt` (branch `scout-ui`), **non** in `C:\Users\nicol\app`. Se il worktree non c'è più: `git -C C:\Users\nicol\app worktree add -b scout-ui C:\Users\nicol\scoutwt scout-ui` (o `deluxy-scout`), poi ricrea la junction `node_modules` (`cmd //c "mklink /J node_modules C:\Users\nicol\app\deluxy-scout\node_modules"`) e copia `.env`.
> 3. **Node**: `$env:Path = "$env:ProgramFiles\nodejs;$env:Path"`. Verifica con `npx tsc --noEmit` + `npx jest`.
> 4. **Token da rifornire** (io NON li salvo — chiedili all'utente quando servono): `EXPO_TOKEN` (build APK), `VERCEL_TOKEN` (deploy web), `SUPABASE_PAT`/`SUPABASE_ACCESS_TOKEN` (migrazioni + deploy Edge Functions), e opzionale `ANTHROPIC_API_KEY` (match AI). Google Maps/HubSpot vivono già come secret server.
> 5. **Deploy web**: usa **`VERCEL_TOKEN=<token> bash scripts/deploy-web.sh`** (fa build + **fix font** + deploy **pinnato per projectId** + **verifica post-deploy** sul `<title>`). ⚠️ Font: senza il fix Vercel esclude `assets/node_modules` (font @expo/vector-icons) e le icone diventano quadratini → lo script li sposta in `assets/vendor`. URL: deluxy-scout.vercel.app.
> ⚠️ **SOVRASCRITTURA RISOLTA (15 lug 2026)**: il progetto Vercel `deluxy-scout` aveva un'**integrazione Git** col repo condiviso `donatodnicolo-gif/search` (branch `main`, root = **app fiorai**): ogni push su `main` ripubblicava l'app fiorai su deluxy-scout.vercel.app, cancellando Scout. **Integrazione Git SCOLLEGATA** dal progetto (via API). Ora Scout si aggiorna **solo** via `deploy-web.sh` (pinnato per ID `prj_rnV0sqhZJ4GXiNXrT5OJXLb7Pjem`, org `team_vt9JRBhnxbY4spm5LzhNyxoY`). Se il dominio tornasse a mostrare "Trova Fiorai & Pasticcerie": qualcuno ha ricollegato il Git al progetto deluxy-scout → riscollegarlo e rideployare.
> 6. **Migrazioni — stato VERIFICATO sul DB il 16 lug 2026**: applicate fino a **0012**, più **0014 (`profiles`) ✅ applicata** e **0015 (`profiles.ultimo_accesso`) ✅ applicata**. **⚠️ Resta NON applicata la 0013 (`visits.concorrenti`)**: il campo "Concorrenti già presenti" esiste nella UI ma **il dato viene scartato in silenzio** (degrada con grazia). Per attivarlo: `node scripts/mgmt-query.mjs supabase/migrations/0013_concorrenti.sql` con `SUPABASE_PAT`, poi **rideploya `hubspot-sync`**.
> 6b. **Web deployato** con: campo Concorrenti + fix nomi mobile (card a colonna <560px) + **dashboard Team per admin** (riepilogo rete, scheda per venditore, feed) + **drill-down venditore giorno per giorno con KPI** + **editing nome** (Profilo per sé, dettaglio venditore per l'admin). URL live aggiornato.
> 7. `scout-ui` è **pushato su GitHub** (origin/scout-ui). Dopo ogni tappa: commit + `git push`. Per integrare: merge su `deluxy-scout`.

> ⚠️ **Segreti**: nessun valore segreto è in questo file. Le chiavi vere stanno in `deluxy-scout/.env` (gitignored) e nei secret della Edge Function. Gli identificatori qui sotto (project ref, portal id, URL) **non** sono segreti.

> 🧭 **AMBIENTE DI LAVORO (14 lug 2026)**: per sfuggire alla concorrenza con l'altra sessione (che continuava a spostare il branch `deluxy-scout` nella working tree condivisa `C:\Users\nicol\app`), lo sviluppo Scout si è spostato in un **git worktree isolato**: cartella **`C:\Users\nicol\scoutwt`**, branch **`scout-ui`** (creato da `deluxy-scout`). `node_modules` è una **junction** verso quello del main tree; `.env` copiato. Build/deploy si fanno da lì. **Per integrare il lavoro**: `git merge scout-ui` sul branch `deluxy-scout` (o PR). Il branch `deluxy-scout` è condiviso/rumoroso (ci committa anche l'altra sessione col progetto `deluxy-platform-next`); `scout-ui` è pulito e solo-Scout.

> 🧭 **Navigazione (14 lug 2026)**: da tab bar in basso a **drawer laterale a scomparsa** (`expo-router/drawer`, `_layout.tsx`): logo D in cima + voci con icona, attiva oro (sidebar DS), hamburger nell'header. Aggiunte dep `@react-navigation/drawer` + `react-native-gesture-handler` + `react-native-reanimated` (plugin in `babel.config.js`). Nota: reanimated è nativo → per l'APK Android servirà un **nuovo build EAS** (il web è già live).

---

## 1. Cos'è
App di prospezione commerciale sul territorio a Milano, scritta in React Native (Expo Router, TypeScript) ma **usata dal browser**: in produzione c'è **solo la build web** (deluxy-scout.vercel.app). ⚠️ «App mobile» è come è nata l'11/07, non come viene consegnata: l'APK Android è una **preview a distribuzione interna del 13/07/2026**, precedente al passaggio al drawer del 14/07 (che ha portato dipendenze native: `@react-navigation/drawer`, `gesture-handler`, `reanimated`) — quindi **non contiene nulla di quanto fatto dopo quella data**, e per aggiornarlo serve un nuovo build EAS (`EXPO_TOKEN`). Mai pubblicata sugli store, iOS mai configurato. Il codice però è davvero multipiattaforma: base Expo unica con due sole varianti dedicate al web (`mappa.web.tsx`, `RichTextEditor.web.tsx`); la **mappa nativa non è mai stata provata su un dispositivo vero**. Backend Supabase (DB/Auth/Storage). Integrazione HubSpot via Supabase Edge Function. 6 fasi: mappa+priorità+giro · nuova visita offline · sync HubSpot · dashboard · test · build.

- **Codice**: `deluxy-scout/` (sotto-cartella del repo `donatodnicolo-gif/search`, branch `main`).
- **Repo GitHub**: https://github.com/donatodnicolo-gif/search

## 2. ✅ Fatto e verificato
- **Codebase completa** delle 6 fasi + estensioni di campo: nuovo target da GPS, aggiungi contatto (flag decisore), navigazione Google Maps, **modifica attività** e **schermata Profilo/Impostazioni** (4° tab: utente, stato coda sync, integrazioni, logout). Validata: `tsc --noEmit` pulito, `jest` verde (2 suite), `expo export --platform android` bundle OK (**1267 moduli**).
- **Identità visiva**: icona, adaptive icon (Android), splash e favicon brandizzate navy/oro (pin di mappa oro) — generate da SVG con `scripts/gen-icons.mjs` (dep dev `sharp`), collegate in `app.config.ts`. In `assets/`.
- **Rifiniture**: ricerca testuale nella lista target (nome/indirizzo/zona/categoria); dettaglio visita con foto vetrina (tap su una visita nello storico della scheda); **export CSV** di attività e visite dalla schermata Profilo (BOM UTF-8, condivisione via `expo-sharing`). Bundle a 1276 moduli.
- **Build EAS Android**: progetto **@deluxyoff/deluxy-scout** (projectId `81ab09df-c772-4c2b-b860-c590df0ec789`, owner `deluxyoff`). Primo **APK preview** generato con successo. Variabili Supabase nel profilo `preview` di `eas.json`. Fix necessario per SDK 52: plugin `expo-build-properties` con `android.kotlinVersion: '1.9.25'` (il default 1.9.24 rompeva `expo-modules-core:compileReleaseKotlin` per mismatch col Compose Compiler 1.5.15). Owner impostato in `app.config.ts` (`owner: 'deluxyoff'`, `extra.eas.projectId`). La build si lancia con `EXPO_TOKEN` (personal access token Expo) in modo non-interattivo: `npx eas-cli build -p android --profile preview --non-interactive --no-wait`.
- **Supabase** — org "Deluxy" (piano Pro, verificato 18/08/2026: 7 giorni di backup giornalieri), progetto **deluxy-scout**:
  - Project ref: **`fdsziebgkljfsugqqbqd`** · URL: **`https://fdsziebgkljfsugqqbqd.supabase.co`**
  - Migrazioni applicate e verificate (schema + RLS + seed): 9 linee (3 standby con `attiva_bool=false`), 20 `category_rules`, RLS attivo su tutte le tabelle, storage bucket `vetrine`. Accenti UTF-8 corretti.
  - Anon (publishable) key già in `deluxy-scout/.env`.
- **HubSpot** — account "Deluxy Srl", portal **`147623810`**, regione **EU (app-eu1)**:
  - Service key "Deluxy Scout" (8 scope: companies/contacts/deals read+write + schemas companies/deals write). Token in `.env` come secret della Edge Function (non nel client).
  - 7 proprietà custom create (vedi ARCHITETTURA §mappature).
  - Edge Function `hubspot-sync` **deployata**: `https://fdsziebgkljfsugqqbqd.supabase.co/functions/v1/hubspot-sync` (in `.env`). Secret `HUBSPOT_TOKEN` impostato. Verificata: 401 senza auth.
- **Node.js** v24 installato (`C:\Program Files\nodejs`).
- **Google Maps (Android)** — chiave configurata (l'utente l'ha fornita il 13 lug). Salvata in `deluxy-scout/.env` (`EXPO_PUBLIC_GOOGLE_MAPS_ANDROID_KEY`, gitignored) **e** come **variabile d'ambiente EAS** (scope progetto, ambiente `preview`, visibilità *sensitive* → non committata): creata con `eas env:create`. Il profilo `preview` di `eas.json` ora ha `"environment": "preview"` così il build la preleva. iOS lasciato vuoto (app solo Android).
- **Build EAS con Maps (13 lug 2026)** — nuovo APK preview `finished` che include la chiave (log EAS: *"…loaded from the 'preview' environment: EXPO_PUBLIC_GOOGLE_MAPS_ANDROID_KEY"*). Artifact: `https://expo.dev/artifacts/eas/2HVixTwvQ6fV7RLXC9_IZ1GIwrqrmSAWxRXe2TQxVog.apk` (build `6ff9f020-8991-4452-8aa7-866064958b91`). Stesso keystore del build precedente (stessa SHA-1). Nota: sul tier gratuito la **coda** può durare ore.
- **Feature "giro navigabile" (13 lug)** — nuovo `lib/nav.ts` (URL Google Maps: singola destinazione + percorso multi-tappa con waypoint, cap 9 + troncamento segnalato) e pannello tappe ordinate in `mappa.tsx` con pulsante 🧭 Naviga. Logica del giro estratta in `lib/giro.ts` (condivisa). Test in `__tests__/nav.test.ts`. `tsc` pulito, **18/18 jest verdi**. Non ancora committato (vedi §git sotto).
- **Versione WEB live (14 lug 2026)** — l'app gira anche da browser: **https://deluxy-scout.vercel.app** (deploy statico su Vercel, account `donatodnicolo-gif`, team `deluxy`, progetto `deluxy-scout`). Verificata nel browser (login reso, 0 errori console). Come è fatta:
  - Dipendenze web: `react-dom`, `react-native-web`, `@expo/metro-runtime` (via `expo install`).
  - `app.config.ts` → `web: { bundler: 'metro', output: 'single' }` (SPA).
  - **`mappa.web.tsx`**: variante web della schermata Mappa (react-native-maps è solo-nativo). Niente mappa: lista target + pianificatore di giro + pulsante 🧭 Naviga (apre Google Maps in una scheda). Riusa `lib/giro.ts`.
  - **`metro.config.js`**: stub di `@opentelemetry/api` (dipendenza opzionale di supabase-js, non installata) e di `react-native-maps` su web (→ `{ type: 'empty' }`). Stub vuoto in `stubs/empty.js`.
  - Build: `npm run build:web` (= `expo export --platform web --output-dir dist-web`). Il bundle è **autoconsistente** (anon key Supabase già inclusa). La chiave Maps Android nel bundle è ininfluente sul web (ristretta all'app Android).
  - **Deploy**: `dist-web` (con `vercel.json` di rewrite SPA) copiata in una cartella `deluxy-scout/` e pubblicata con `npx vercel deploy --prod --yes --token <VERCEL_TOKEN>`. Nessuna integrazione Git (evita il branch condiviso). Per ripubblicare: `npm run build:web`, poi rideploy della cartella. **Serve login utente Supabase per usarla davvero** (vedi §3.1).
- **Sezioni nuove richieste dall'utente (14 lug 2026)** — funzionano su desktop e mobile:
  - **"Dove vai?"** — spostata nella **Mappa** (`mappa.tsx` nativa + `mappa.web.tsx`), non più in Target (scelta utente). Componente riutilizzabile `components/AddressSearch.tsx` con **suggerimenti Google (autocomplete)** a tendina mentre digiti; alla scelta ricentra la mappa (nativo) o riordina la lista per vicinanza (web) e imposta il punto di partenza del giro. Backend: `lib/geocode.ts` → **Edge Function `geocode`** con 3 azioni (`geocode`, `autocomplete`, `details`), Google **Geocoding + Places** lato server (chiavi come secret). ✅ **DEPLOYATA e funzionante (14 lug 2026)**: secret `GOOGLE_GEOCODING_KEY` impostato (si riusa la STESSA chiave Maps `AIza…qTH8`, scelta utente → resa senza restrizione app); Geocoding+Places API abilitate; testato: Google risponde, funzione raggiungibile (OPTIONS 200, 401 senza auth). Il flusso end-to-end autenticato lo verifica l'utente loggato dal sito.
  - **Rubrica** (`rubrica.tsx`, nuovo tab 📇): tutti i contatti registrati (query `fetchTuttiContatti` con join sul negozio), ricerca, badge "HubSpot ✓ / da sync", tap→scheda negozio, azioni tel/email. Dati da Supabase (già sincronizzati con HubSpot).
  - **Trattative** (`trattative.tsx`, nuovo tab 💼): tutte le deal raggruppate per negozio (`SectionList`, `fetchTutteTrattative`), fase leggibile (`labelFase` in `theme.ts`), valore atteso, totale in testata, tap→scheda negozio.
  - Tab totali ora 6: Mappa · Target · Rubrica · Trattative · Dashboard · Profilo. `tsc` pulito, web build OK, 18/18 jest.
- **🆕 Motore "Scoperta sul territorio" — TAPPA 1 (14 lug 2026)**: nuovo flusso richiesto dall'utente — digiti un indirizzo → l'app trova i negozi della zona da **Google Places Nearby**, li **classifica** per linea (category_rules), li salva in cache e li mostra; ⭐ = interessante (entra nel giro), badge **NOVITÀ** per i mai visti.
  - **Schema** (migrazione `0004_discovery.sql`, applicata): su `places` aggiunti `source`('manual'|'google'), `google_place_id`(unique), `google_types`, `starred`, `novita`, `da_completare`, `hubspot_deal_aperta`, `hubspot_sync_at`, `google_refresh_at`; tabella cache `google_aree` (griglia ~100m, refresh 30gg, RLS senza policy = solo service_role); funzione SQL `places_vicini(lat,lng,raggio)` (usa l'indice GIST).
  - **Edge Function `discover`** (deployata): azione `discover {lat,lng,radius}` → se la cella è fresca (<30gg) serve dal DB, altrimenti chiama Google Nearby, classifica, inserisce i nuovi (`novita=true`) senza toccare starred/stato dei noti, aggiorna `google_aree`, ritorna `{places, cached, nuovi}`. Riusa il secret `GOOGLE_GEOCODING_KEY` (serve **Places API** già abilitata). Google Nearby testato ✓.
  - **Client**: `lib/discover.ts` (`scopriNegozi`), `db.aggiornaStarred`, tipo `Place` esteso (campi opzionali). **Mappa web** (`mappa.web.tsx`) collegata: ricerca indirizzo → lista negozi scoperti con ⭐ e NOVITÀ, giro dai ⭐. `tsc`/web build/jest ok, deployata.
  - **TAPPA 1b fatta (14 lug 2026)**: **paginazione** Nearby (fino a ~60 risultati, 3 pagine con pagetoken+attesa 2s, dedup); **icone tipologia** per linea (`iconaLinea`/`lineaIcona` in `theme.ts`); **filtro per tipologia** a pillole nella Mappa web; **redesign stile Apple** della scheda scoperta (`mappa.web.tsx`: liste raggruppate, icona-tipologia per card, badge NOVITÀ, ⭐, dock flottante). tsc/build ok, deployata.
  - **TAPPA 1c fatta (14 lug 2026)**: **filtro priorità** ripristinato (era sparito nel redesign) + badge priorità sulle card, accanto al filtro tipologia (2 righe di pillole); **spunta "sono stato qui"** (☐/☑ sulla card) → `components/VisitaModal.tsx` (pop-up bottom-sheet: contatto opzionale + note; "Salva visita" = `registraVisitaRapida` crea contatto+visita e stato→visitato; "Compila dopo" = `segnaVisitatoDaCompletare` → `da_completare=true`, resta come attività); nuova **scheda "Da fare"** (`da-completare.tsx`, tab 📝) che elenca i `da_completare` e riapre il pop-up per completarli. Tab totali ora **7**. tsc/build/jest ok, deployata.
  - **TAPPA 1d fatta (14 lug 2026)**: **"Non interessante" 🚫** sulla card scoperta → `aggiornaNascosto(true)`: l'attività sparisce e non torna più nella scoperta (migrazione `0005_nascosti.sql`: colonna `nascosto` + `places_vicini` la esclude). Recuperabile da **Profilo → "Nascosti"** → `nascosti.tsx` (rotta drawer nascosta) con **Ripristina** (`aggiornaNascosto(false)`, `fetchNascosti`).
  - **TAPPA 1e — copia locale CRM HubSpot + match contatti (15 lug 2026)**: estratto TUTTO il CRM HubSpot (portal 147623810: **514 aziende, 1657 contatti**, 1365 con azienda) in tabelle locali Supabase `hubspot_companies`/`hubspot_contacts` (migrazione `0006_hubspot_crm.sql`, con **pg_trgm** + RPC `cerca_azienda_hubspot(nome, indirizzo)` per match fuzzy). Estrazione **server-side** via Edge Function `hubspot-match` azione **`sync_crm`** (deployata con `--no-verify-jwt`: sync_crm apribile senza sessione utente, `match_contacts` resta con auth). Client: `cercaContattiHubspot(nome, indirizzo)` (match locale istantaneo, **niente chiave AI**) usato dal pulsante **"Trova contatti su HubSpot"** nella scheda attività (mostra azienda + contatti importabili con "+ Aggiungi"). **Profilo → "Sincronizza contatti da HubSpot"** (`sincronizzaHubspot` → sync_crm) per riaggiornare periodicamente. La conciliazione **AI** (azione `match_contacts` + `trovaContattiAI`) resta pronta ma spenta finché non si imposta `ANTHROPIC_API_KEY` (secret).
  - ⏳ **TAPPE SUCCESSIVE (da fare)**: (2) wiring scoperta nella **Mappa nativa** (`mappa.tsx`, con pin) — ora ancora sul vecchio flusso DB; (3) **"Sincronizza HubSpot"** — azione batch che incrocia i negozi con HubSpot e salva `hubspot_deal_aperta` (ri-eseguibile ogni tot; il pallino "● trattativa" è già predisposto); (4) ✅ FATTO 15 lug — sync HubSpot automatico dopo `registraVisitaRapida` (vedi "Revisione commerciale"); (5) rifinire `TYPE_MAP` (Google types → categoria) + foto negozio da Google.
- **💼 Revisione da esperto commerciale (15 lug 2026)** — ottimizzato il ciclo visita→follow-up (commit `cb8faa1` + successivo, branch `scout-ui`):
  - **Esito obbligatorio nella visita rapida**: `VisitaModal` ora chiede "Com'è andata?" con i 4 chip (`EsitoButtons`); la nota è obbligatoria solo per interessato/da_richiamare. `registraVisitaRapida(placeId, {esito, note, contatto})` salva l'esito, deriva lo **stato del place** dalla regola unica `statoDaEsito` (spostata da `lib/syncQueue.ts` a **`@/types`**, re-export per compatibilità) e imposta un `next_step` commerciale suggerito (`nextStepDaEsito` in `db.ts`). Mostra anche l'**aggancio d'apertura** nel pop-up.
  - **Sync HubSpot immediato (best effort)** dopo la visita rapida: chiama `syncVisita` se configurato; **mai per i non_target** (non inquinare la pipeline); su errore la visita resta `hubspot_synced=false`.
  - **Coda richiami**: `daRicontattare(places, visits)` in `lib/metrics.ts` — ultima visita per negozio con esito interessato (soglia recap 3 gg) o da_richiamare (7 gg), esclusi cliente/perso; ordina i ritardi prima. 6 test in `__tests__/richiami.test.ts` (24/24 verdi). La scheda **"Da fare"** (`da-completare.tsx`) ha ora 2 sezioni: **Da ricontattare** (badge IN RITARDO, nota ultima visita, tap→scheda attività) + **Da completare** (come prima, tap→pop-up).
  - **Dashboard**: card "Visite ultimi 7 giorni" (ritmo, col totale come sub) e "Da ricontattare (N in ritardo)" al posto del solo storico; `carica()` con try/finally (spinner non resta più appeso su errore). Fix rollback ⭐ in `mappa.web.tsx` (ripristina anche `novita`).
  - ⚠️ **Non verificato nel browser**: serve un utente loggato (io non digito password). Verifica utente: segnare una visita rapida → controllare esito nella scheda, coda "Da fare", numeri dashboard, e la visita su HubSpot.
- **Tipologia di interesse = linea Deluxy (14 lug 2026)** — chiarito dall'utente: NON un campo nuovo, è la `linea_ipotizzata`. Resa **visibile** (tag oro nella lista Target, nella card web della Mappa, nella Rubrica) e **selezionabile** con `components/LineaSelector.tsx` (mostra le 6 linee attive, `LINEE_ATTIVE` in `types`): nel **nuovo target** (override dell'ipotesi automatica) e nella **modifica** attività (`aggiornaPlace` già accetta `linea_ipotizzata`). La query Rubrica ora include `place_linea`.

- **🎨 Adottato il Deluxy Design System v1.0 (14 lug 2026)**: il DS ufficiale vive in `deluxy-design-system/` (root del repo, committato sul branch `deluxy-scout` come `93f0429`). Scout lo adotta: `lib/theme.ts` **rimappato sui token DS** (sfondo `#F5F5F7`, testo `#1D1D1F`, azioni primarie **ink `#111318`**, accento **oro `#B8963E`** usato poco, hairline/fill neutri, ombre `shadow.card/float`). I nomi storici restano (`navy`→ink, `oro`→gold) per non rompere le schermate. **Login** ridisegnato stile DS (card chiara, logo D oro su quadrato ink, campi raggruppati, CTA pillola nera, footnote brand) — verificato live. Bottoni primari → ink in VisitaModal/nuovo-target/modifica. Commit `6c13b2f` + `79b26bc`. **Header/tab bar** ora chiari DS (surface bianco, testo ink, no ombra, icona attiva oro) e **FAB** ink — commit `2854be2`. ⏳ Resta (opzionale): dock "Naviga" ancora oro (accento tollerato), badge di stato secondo §Componenti del DS, eventuale blur (`expo-blur`) su barre.

- **🪜 Il funnel prende la sua forma: nasce il livello LEAD (27/07/2026)** — commit `c4c5a49`, deployato su https://deluxy-scout.vercel.app. Tre richieste dell'utente più una trappola trovata per strada:
  - **Filtri chiusi di default** (`components/PannelloFiltri.tsx`): restavano aperti da 900px in su e la lista cominciava a metà schermo. Il conteggio sul bottone dice comunque se c'è un filtro attivo.
  - **Azioni di contatto nei Selezionati** (`/lista`): avevano **un bottone solo** (visita o mail) — dalla riga non si poteva né chiamare né scrivere. Ora hanno le stesse cinque azioni dei Potenziali + «nascondi». ⚠️ Per non farle divergere una terza volta stanno in **un file solo**, `components/AzioniContatto.tsx`, usato sia da `lista.tsx` sia da `visite.tsx`: chi aggiunge un'azione la aggiunge lì. La mail parte **dall'app con gli script** (`ScegliScriptModal`), non con `mailto:`.
  - **Livello LEAD** (nuova voce di menu in Contatti). La scala ora è: **Selezionato** (scelto con la ⭐, mai toccato) → **Lead** (gli abbiamo scritto/telefonato/siamo passati) → **Prospect** (c'è una persona in rubrica) → Cliente. Il confine fra Lead e Prospect è **avere un nome con cui parlare**, non l'attività svolta.
  - **Perché il Lead non era calcolabile prima**: la mail partiva dall'app e **non lasciava traccia da nessuna parte** — né il client né la Edge Function `invio-email` scrivevano niente. Ora l'invio riuscito registra il negozio in **`contatti_avviati`** (migrazione **0046, DA APPLICARE**, vedi §3), che `fetchPlaceIdContattati()` unisce a `chiamate` e `visits`. Chiamate e visite popolano i Lead **già senza migrazione**.
  - **Rinominati gli identificatori dei livelli** in `lib/livelli.ts`: `prospect` si mostrava come "Selezionato" e `lead` come "Prospect"; con un Lead vero lo sfasamento diventava illeggibile. I livelli **non sono salvati nel database** (si ricalcolano), quindi nessuna migrazione. ⚠️ **`/lista?vista=prospect` ora mostra i Prospect veri, non più i Selezionati**: i vecchi link salvati portano a una lista diversa.
  - **Trappola del cap 1000, terza volta**: `fetchPlaceIdConContatto` leggeva `contacts` senza `range()` — oltre le mille righe i negozi in fondo risultavano «senza contatto» e **retrocedevano di livello da soli**. Ora tutte le letture di id passano dall'helper paginato `idPaginati()` in `lib/db.ts`.
  - Verificato: `tsc --noEmit` pulito, bundle Metro senza errori, deploy verificato dallo script. **Non verificato a schermo**: le schermate stanno dietro il login e io non digito password (vedi §3.1).


- **🧭 Giornata del 28/07/2026 — il funnel prende forma** (commit da `211336a` a `4460300c`):
  - **Scala dei livelli RIDEFINITA dall'utente** — la fonte è `lib/livelli.ts`: SELEZIONATO (scelto, mai toccato) → LEAD (**c'è un contatto**: persona in rubrica, messaggio partito, oppure è arrivato lui e lo si dichiara) → PROSPECT (**ha mostrato interesse**: trattativa aperta) → CLIENTE (trattativa vinta) → DORMIENTE (**cliente che ha smesso di fatturare**). ⚠️ Il confine Lead/Prospect era «una persona in rubrica»: ora è **la trattativa**. Un nome in rubrica è un recapito, non un interesse.
  - **Il dormiente arriva da FINANCE** (`lib/finance.ts` + Edge Function `finance`): `statoAnalisi.calcolato` di deluxy-partner, cioè il dato dai MOVIMENTI e non il codice scritto a mano nella scheda. Il ponte fra le app è `anagrafiche_id`, mai il nome.
  - **Sezioni nuove**: **Sequenze** (solleciti a scadenza, in Strumenti — si fermano da sé se il cliente risponde, e niente parte senza conferma), **Per interesse** (il funnel per linea di servizio), **Province · Copertura** (tutte e 107 le province: dove siamo, dove vendiamo, dove non c'è nessuno), **Segnalati · Fornitori** (i potenziali che l'app fornitori ha già messo nel registro).
  - **Filtri a scelta multipla** ovunque (`components/GruppoFiltro.tsx`, estratto da tre copie già divergenti) e **scelta multipla in Clienti** per scrivere a più negozi insieme.
  - **Indirizzi con ricerca Google** nei form: prima un negozio si poteva creare solo col GPS, stando davanti alla porta.
  - **«Gifting» unificato**: «Regali aziendali» era lo stesso interesse con due nomi — si filtrava per uno e si perdeva metà dei negozi. Canonizzazione al punto di lettura + migrazione 0049.
  - **Buco chiuso**: gli elenchi mostravano solo i negozi con `creato_da`/⭐ e buttavano via quelli con una **persona in rubrica**, che comparivano in Rubrica e sparivano dalle liste (`inLavorazione` in `lib/livelli.ts`).

- **📇 Anagrafiche, stessa giornata**: import del tracker Excel **che aggiunge invece di riscrivere** (`--solo-nuovi`, `--prova`) — 33 anagrafiche nuove, da 943 a 976; e nuova sezione **Riconciliazioni dati** (`/riconciliazioni`), dove si decide chi ha ragione fra tracker e registro. Le differenze di solo formato sono scartate in partenza: 26 su 30 erano «MI» contro «MILANO».

- **✉️ Giornata del 29/07/2026 — la mail del passo, e perché il deploy «falliva» senza dire niente**:
  - **🔎 TROVATO il motivo per cui l'errore del deploy non si leggeva: lo script guardava lo stream sbagliato.** `supabase functions deploy` scrive l'avanzamento su **stderr** e il **fallimento su stdout** (JSON: `{"_tag":"Error","error":{…}}`), esce con codice 1. `allinea-supabase.mjs` stampava le ultime 3 righe del **solo stderr** → mostrava «Uploading asset (health): …», cioè l'ultima cosa *riuscita*, e il motivo vero spariva. Ecco perché restava solo «4 passo/i non riuscito/i» (i 4 passi = le 4 funzioni). Ora lo script stampa **stdout + stderr per intero**, con tre traduzioni pronte (Docker · token/401-403 · npx fuori dal PATH).
  - **Verificato che la strada `--use-api` funziona**: con un token finto il CLI arriva fino all'upload e muore solo sull'autorizzazione (`unexpected deploy status 401`). Quindi **niente Docker, niente config.toml mancante**: il prossimo lancio con un token valido o deploya, o dice finalmente perché no. Serve un giro di `APPLICA-MIGRAZIONI.cmd` da parte dell'utente (io il token non ce l'ho).
  - **Un passo di sequenza può avere la SUA mail** (richiesta utente). Prima puntava solo a un modello della libreria Script: per un sollecito di due righe si era costretti a creare un modello che poi resta lì a sporcarla — ed è il motivo per cui il secondo passo non lo aggiungeva nessuno. Ora nel pannello «Aggiungi un passo» si sceglie **Testo dalla libreria** oppure **Scrivi la mail** (oggetto + editor rich text con il menu delle variabili). Migrazione **0052** (`sequenza_passi.oggetto`/`corpo` + vincolo «o l'uno o l'altro», `not valid` perché `script_id` è `on delete set null` e le righe orfane esistono già). Il testo lo risolve **`testoDelPasso()`** in `lib/sequenze.ts`: un punto solo, così coda, elenco e invio non possono divergere.
  - **Le variabili sono le stesse dei modelli** — `[nome]`, `[negozio]`, `[ruolo]`, `[email]`, `[telefono]`, `[zona]` le riempie `invio-email` per ogni destinatario. ⚠️ **Le variabili MANUALI (`[data]`…) in una sequenza non le compila nessuno**: nell'invio a mano c'è il modulo prima di partire, qui non c'è nessuno a cui chiederlo e la mail uscirebbe con «[data]» scritto dentro. Ora sono bloccate **due volte**: mentre si scrive il passo (avviso rosso) e prima di mandare (`variabiliManuali` in `mandaPasso`). Il secondo controllo copre anche i **modelli dalla libreria**, dove il buco c'era già.
  - **Trappola dell'editor**: `RichTextEditor` scrive il suo HTML iniziale **una volta sola** al montaggio (per non spostare il cursore a ogni battuta). Azzerare lo stato dopo «Aggiungi» non lo svuota: il passo dopo sarebbe nato col testo del passo prima. Si rimonta con una `key` che cambia.
  - **«Segnalati · Fornitori» si chiama «Segnalazioni CS»** (richiesta utente). Rinominata **solo l'etichetta**: la rotta resta `/segnalati` e il file `app/(app)/segnalati.tsx`, come già fatto per Visite→Potenziali. ⚠️ La **fonte non è cambiata**: legge sempre `FONTE = 'deluxy-suppliers'` dal registro, cioè quello che segnala l'app fornitori. Se «CS» vuol dire che devono arrivare da **Deluxy Customer Service** (`deluxy-messaging`), è un'altra riga da cambiare — dirlo.
  - **Titoli di testata mancanti**: `segnalati`, `province`, `interessi` e `sequenze` non avevano un `Drawer.Screen`, quindi la barra in alto mostrava il **nome della rotta** («segnalati»). Aggiunti tutti e quattro.
  - **✉️ Le mail scritte da Scout ora restano nella cartella «Inviata»** (richiesta utente: «ho bisogno di scrivere in questa app ma vederlo anche in mail»). Prima l'invio era **SMTP diretto**: la mail arrivava, ma nella casella di chi l'aveva scritta non ne restava traccia — SMTP consegna e basta, la copia in «Inviata» è un APPEND IMAP che nessuno faceva. Ora `invio-email` prova **prima AI Mail** (`POST /api/v1/invia`, header `x-api-key: MAIL_API_TOKEN` + `x-utente: <email dell'utente Scout>`), che spedisce **e** deposita la copia in «Inviata» oltre a registrarla fra le sue mail; l'SMTP resta come ripiego per chi in AI Mail non ha la casella collegata. **Mai tutte e due per lo stesso destinatario.**
    - Nuovo helper `supabase/functions/_shared/aimail.ts`. Il segnale `cassettaAssente` (404 o «nessuna casella») spegne AI Mail per **tutto** l'invio: senza, si ritentava una chiamata di rete per ognuno dei 60 destinatari.
    - **Serviva una modifica anche in AI Mail** (`deluxy-mail`): `/api/v1/invia` accettava solo testo semplice — `spedisci()` usa `text: corpo` e `html: corpoHtml`, e i corpi di Scout sono HTML, quindi sarebbero partiti **coi tag scritti dentro** («<p>Gentile…»). Ora la rotta accetta `corpoHtml` (alias `html`), riconosce l'HTML mandato in `corpo`, e ricava da sé il `text/plain` con `htmlAPlain`. Retro-compatibile: chi manda testo semplice non cambia niente.
    - L'esito dice da che strada è passata (`via`, `inInviata`) e il messaggio a schermo lo riporta (`doveLaTrovi()` in `lib/script.ts`): «La copia è nella tua cartella Inviata» oppure «sono partite in SMTP diretto». Aggiunta la spiegazione anche in Profilo → La mia email.
    - **L'ordine dei deploy non può fare danni**: Scout manda **tutt'e due le versioni** del corpo, `corpo` (testo) e `corpoHtml`. Una AI Mail non ancora aggiornata ignora il secondo e spedisce il testo semplice — mail leggibile, solo senza formattazione. Mandando il solo HTML, invece, sarebbe finito nel `text/plain` e al cliente sarebbe arrivato «&lt;p&gt;Gentile…».
    - ⏳ **Due deploy per averlo davvero**: ① **AI Mail** in produzione (⚠️ il push non pubblica: `vercel --prod` a mano) per riavere la formattazione; ② `supabase functions deploy invio-email` per far passare Scout da AI Mail. Se `MAIL_API_TOKEN` non c'è, tutto resta com'era prima (SMTP).
  - **🚶 La visita dice PERCHÉ ci vai, e si possono aprire task da lì** (richieste utente):
    - **Interessi già segnati**: in cima al form ci sono, in sola lettura, gli interessi del negozio (`places.linee_ipotizzate`, canonizzati). Prima si entrava senza sapere di cosa gli interessa parlare, o si tornava indietro a rileggere la scheda.
    - **«Linea proposta (primaria)» → «Motivo della visita — scegline uno o più»**: in un negozio si entra per più ragioni insieme, e sceglierne una faceva perdere l'altra metà. Migrazione **0053** (`visits.motivi text[]`). ⚠️ **`linea_proposta` NON sparisce**: resta il **primo** motivo, perché la leggono già storico, export CSV e `hubspot-sync` (che ci costruisce il nome della deal) — cambiare il campo sotto a tre lettori per guadagnarne uno era il modo sbagliato.
    - I motivi **partono da ciò che il negozio ha già segnato** (è l'ipotesi più probabile) e si tolgono. I chip sono l'**unione** fra le linee attive e i motivi già scelti: un interesse ereditato dal registro ma fuori catalogo, altrimenti, sarebbe rimasto selezionato **senza chip** — presente al salvataggio, invisibile sullo schermo.
    - `inserisciVisita` **degrada se la 0053 non è applicata** (come già per `concorrenti`): riprova senza `motivi`. Si perde l'elenco completo, non la visita — che è il lavoro di una persona uscita di casa.
    - **Task dalla visita**: riquadro in fondo al form con i task aperti del negozio e «Nuovo task» (riusa `TaskFormModal`). Il next step è una frase; il task è una cosa con una data e un nome sopra.
  - **🏷️ La scheda negozio dice il LIVELLO, non lo stato di pipeline** — segnalazione utente: «perché esce perso? dovrebbe uscire nuovo, l'ho appena creato». In cima a `/attivita/[id]` c'era `labelStato[place.stato]`, che ha **4 soli valori** (Da visitare · Visitato · Cliente · **Perso**) e nessuno che voglia dire «nuovo». Ora c'è il badge del **livello** (Selezionato · Lead · Prospect · Cliente · Dormiente · Perso), la stessa parola di tutte le liste: un negozio appena creato legge **«Selezionato»**. Lo stato di pipeline resta sotto come «Percorso: …».
    - **E quando il livello chiude il rapporto, la scheda dice da dove viene**: registro «non interessato»/«dismesso», oppure una visita con esito «non è un target», oppure lo stato scelto a mano in Modifica. Più un bottone **«Riportalo fra i Selezionati»**, perché la correzione richiedeva di aprire Modifica e sapere quale dei due campi guardare.
    - ⚠️ Nel calcolo, `contattato` è approssimato alle **visite**: chiamate e `contatti_avviati` non sono caricati in questa schermata. Al massimo un Lead si mostra come Selezionato, mai il contrario.
    - **Sul caso segnalato non posso dire di più senza il database**: nessun percorso di creazione scrive `perso` (`inserisciPlace`, `importaDalRegistro` e `assicuraPlace` nascono tutti `da_visitare`), quindi quel record ha davvero `stato='perso'` o un `anagrafiche_stato` che ci mappa. Ora la scheda lo dichiara da sola.
  - **🗑️ Un negozio lo cancella SOLO chi l'ha creato** (richiesta utente). Bottone «Elimina questo negozio» in fondo a `/attivita/[id]`, staccato dal resto, visibile solo se `places.creato_da` = utente loggato.
    - **La regola sta nel database, non nel bottone** (migrazione **0054**): `places` aveva **una sola policy**, `places_auth_all` = `for all using (true)` — chiunque fosse loggato poteva cancellare qualunque scheda, visite e trattative comprese. Ora select/insert/update restano aperti (il territorio è del team) e **solo la delete** chiede `creato_da = auth.uid()`. Un controllo che sta solo nell'app è un lucchetto disegnato sulla porta.
    - ⚠️ **Trappola RLS**: una DELETE che non trova righe **non è un errore**, torna zero righe. Senza guardarle, l'app direbbe «eliminato» anche quando non ha eliminato niente — e l'utente lo ritrova domani. `eliminaPlace()` fa `.delete().select('id')` e se non torna nulla lo dice a parole.
    - **I record storici non si cancellano da nessuno**: `creato_da` è NULL su import da terminale e scoperta Google (colonna arrivata con la 0035). Senza un proprietario, «solo il creatore» non è una regola ma un'ipotesi.
    - La conferma elenca cosa se ne va davvero con le FK `on delete cascade`: contatti, visite, trattative, chiamate, contatti/aziende scartati, duplicati, contatti avviati, bozze e iscrizioni alle sequenze. Restano senza collegamento (`set null`) task, pagamenti, ordini e lead. **Il registro Anagrafiche non viene toccato**: è un'altra app ed è lei la fonte di verità.
    - ⚠️ **Non fatto: cancellare un singolo referente** «solo se l'hai creato tu» — la tabella `contacts` **non ha nessuna colonna di creatore** (né `owner` né `creato_da`), quindi la regola non è esprimibile senza una migrazione che aggiunga il campo. Da decidere se serve.
  - **🔎 Ricerca globale in cima alla Home** (richiesta utente: «metti da subito possibilità di ricerca in prima pagina: trattative, contatti, lead, ogni possibile cosa in app»). `lib/ricerca.ts` + `components/RicercaGlobale.tsx`, montata in `oggi.tsx` **prima della testata**. Cerca in **nove fonti**: negozi, rubrica, trattative, richieste web, ordini, task, pagamenti, script, sequenze.
    - **Ogni fonte è best-effort**: se una tabella non c'è (migrazione non applicata) o la RLS la nega, quella torna vuota e le altre rispondono lo stesso. Una ricerca che si spegne tutta perché manca una tabella è peggio di una parziale.
    - **Trattative e ordini si cercano anche per negozio** — è così che uno li cerca davvero («la trattativa della Gucci») — ma il nome del negozio sta in un'altra tabella e PostgREST **non combina `or` con un filtro su una tabella agganciata**. Si riusano gli id dei negozi già trovati e si fa una `.in('place_id', …)`: due liste, unite deduplicando per id.
    - Tre accorgimenti che non si vedono: si cerca **dopo** che si è smesso di digitare (250 ms), le **risposte in ritardo si buttano** (una richiesta partita prima può tornare dopo e riscrivere i risultati con quelli di un testo che non c'è più), e **sotto i 2 caratteri non si cerca** (con una lettera torna mezza app).
    - ⚠️ **Il termine viene ripulito** da `, ( ) %` e backslash: dentro un `or(...)` di PostgREST la virgola separa le condizioni e le parentesi raggruppano — un termine che le contiene non romperebbe soltanto la query, la **cambierebbe**.
    - ⚠️ **I task che escono sono solo i propri**: `tasks` ha RLS `owner = auth.uid()`. Non è un buco della ricerca, è la regola della tabella.
    - Il tetto è di 6 righe **per fonte** e la schermata lo dichiara, invece di far credere che non ci sia altro.
  - **🐞 BUG RISOLTO: la visita non si salvava più — «Could not find the 'concorrenti' column»** (segnalato con screenshot). `inserisciVisita` aveva **due `if` separati** per le colonne che possono mancare: uno per `concorrenti` (migr. 0013) e uno per `motivi` (migr. 0053). Il secondo ricostruiva il payload **dall'originale** e quindi **rimetteva dentro `concorrenti`**: con entrambe le colonne assenti il salvataggio falliva comunque, e la visita finiva «in coda» per sempre. Ora è **un giro solo**: si legge il nome della colonna dal messaggio di PostgREST (`Could not find the 'xxx' column`), si toglie quella, si riprova — fino a 6 volte. Un elenco di `IRRINUNCIABILI` (place_id, data, esito, next_step, owner) impedisce di «aggirare» un guasto vero togliendo un campo che serve. Vale anche per la **coda offline**, che passa dalla stessa funzione.
  - **📅 Schermata visita, tre ritocchi chiesti dall'utente**: via la **barra nera «Ipotesi di interesse»** (`BoxIpotesi`, ormai ridondante: gli interessi si leggono nei chip sopra); **«Quando ci vai»** con l'icona calendario, che apre il `PianificaVisitaModal` già esistente (`places.visita_pianificata`, migr. 0047 — è la data che ci si dà, non quella della visita fatta); e le **risposte obbligatorie dichiarate**: `Label` accetta `obbligatorio` e stampa «* obbligatorio» in rosso su **Esito** e **Next step**. Prima l'Esito non lo diceva affatto e lo si scopriva al salvataggio, dopo aver compilato tutto il resto.
  - **✉️ La mail parte dallo stesso percorso ovunque, e si può scriverla al momento**:
    - Nella scheda negozio l'azione **Email** apriva `mailto:` — la mail usciva dal client di posta del telefono: **nessuna traccia in Scout**, il negozio non diventava Lead e la copia non finiva in «Inviata». Ora apre `ScegliScriptModal`, lo stesso di `/lista`.
    - **«Nuova mail»** in cima al modale (`/invio/nuovo`): oggetto e testo si scrivono al passo 2, e in libreria non ci finiscono. Il vecchio «Nuovo script» resta sotto, come cosa diversa: quello è un **modello da riusare**. ⚠️ `contatti_avviati.script_id` è una FK verso `script_email`: con la mail scritta al momento si passa **null**, se no la riga verrebbe rifiutata — ed è best-effort, quindi il contatto sparirebbe in silenzio e il negozio non diventerebbe Lead.
  - **❓ «I modelli mail si possono importare da AI Mail?» — no, e non è lì che stanno.** AI Mail **non possiede** i testi: li legge da **deluxy-scripts** (`GET /api/v1/script?app=<app>`, header `x-api-key`), che è l'app padrona dei testi aziendali. La strada giusta per Scout è la stessa — leggerli, non copiarli: una copia locale diventa la seconda versione di un testo aziendale che poi diverge, ed è esattamente ciò che l'app Scripts esiste per evitare. Da fare se l'utente conferma: registrare Scout fra le app di Scripts, prendere la chiave, e mostrare i testi accanto agli script locali in `ScegliScriptModal`.
  - **🪜 PERSO NON È PIÙ UN LIVELLO** (decisione utente: «dovrei averlo comunque in lead ma perso» + «in Dormienti e persi ci vanno solo quelli che Anagrafiche indica come dismessi»). Prima «perso» vinceva su tutto in `livelloDi()`: un lead con una persona in rubrica, chiuso con esito «non target», **spariva dai Lead** e ricompariva in «Dormienti e persi» — cioè spariva dal posto dove uno lo va a cercare.
    - Ora il livello è solo il funnel — `Livello` = selezionato · lead · prospect · cliente · **dormiente** — e il fatto di essere chiuso è **`ePerso(p)`**, un badge rosso sulla riga e sulla scheda. Un lead perso resta fra i Lead.
    - La vista `inattivi` si chiama **«Dormienti»** (menu e titolo) e contiene **solo `anagrafiche_stato = 'dismesso'`** (più, quando FINANCE sarà collegato, chi ha smesso di fatturare davvero: `nonFattura` resta nel calcolo perché è la **stessa** definizione, il dato buono invece del ripiego — oggi è sempre falso, la funzione `finance` non è deployata).
    - Togliere `'perso'` dal tipo `Livello` ha fatto trovare al compilatore tutti i punti da sistemare: `lista.tsx` (viste, titoli, badge) e `attivita/[id].tsx`. `interessi.tsx` non ha più il secchiello «perso».
  - **🎯 I bottoni dell'esito visita dicono cosa combinano.** «Non target» chiudeva il negozio come **Perso** e «Chiuso» lo promuoveva a **Cliente** (`statoDaEsito`), ma sui bottoni c'erano solo quelle due parole: due conseguenze grosse dietro quello che sembra un appunto. Ora sotto ogni etichetta c'è l'effetto («lo chiude come PERSO», «lo porta a CLIENTE», «resta da lavorare»). Nato dalla domanda dell'utente, fatta due volte, «perché è diventato perso?».
    - **Ed ecco la risposta a quella domanda**: non è automatico. Lo stato del negozio lo decide **l'esito della visita**, e l'applicazione avviene anche **in differita** — la coda offline (`flushCoda` → `aggiornaStatoPlace(…statoDaEsito[esito])`) lo scrive quando la visita rimasta in sospeso riesce finalmente a salvarsi. Con la visita bloccata dal bug delle colonne mancanti, l'effetto arrivava minuti o ore dopo, senza che nessuno avesse premuto niente: da qui la sensazione dell'«automatico».
  - **🏷️ Le liste mostravano UN interesse solo.** La riga passava `linea_ipotizzata` (la primaria) mentre la scheda legge `linee_ipotizzate`: un negozio con tre interessi ne mostrava uno, e sembrava che ne avesse uno. Corretto in `/lista` e nei Potenziali; la visita mostra i suoi `motivi`. `CardElenco` resta fermo a **tre** tag — oltre, la riga diventa illeggibile — ma ora scrive **«+N»** invece di tacere gli altri. ⚠️ Restano fuori gli interessi che stanno **solo nel registro**: la scheda li legge dal vivo da Anagrafiche e li porta su Scout solo col bottone «Salva e aggiorna Anagrafiche». La lista non interroga il registro riga per riga (sarebbe una richiesta per negozio su liste da centinaia): per allinearli tutti servirebbe una sync su `anagrafiche_id` — **non fatta, da decidere**.
  - **🔁 «Riportalo fra i Selezionati» prometteva una retrocessione che non avveniva** (segnalazione utente: «è un lead che ho creato io»). Con la scala nuova togliere il segno «Perso» **non declassa niente**: il livello si ricalcola dai dati, e un lead con una persona in rubrica resta un Lead. Il bottone ora si chiama **«Togli il segno Perso»** e la scheda lo dice esplicitamente.
    - **E non compare più quando non servirebbe a niente**: se la causa è `anagrafiche_stato` (`non_interessato`/`dismesso`), quel campo lo scrive **l'altra app** — `riapri()` cambia `places.stato` e il badge resterebbe lì, cioè un no-op con l'aria di un'azione riuscita. In quel caso al posto del bottone c'è la frase che dice di correggerlo nel registro.
  - **📮 «Nuova mail» era un vicolo cieco su un negozio senza contatti in rubrica** (segnalato: «sono arrivato qui scegliendo nuova mail ma non posso far nulla»). La schermata d'invio sa selezionare **solo contatti della rubrica**: su un lead appena creato non c'è niente da spuntare, il bottone «Rivedi il testo» resta spento e non si può fare nulla. Ora **si può scrivere direttamente un indirizzo**: se quello che si digita nella barra è un'email valida compare «Scrivi a …», e diventa un destinatario come gli altri (`rigaAdHoc` in `invio/[scriptId].tsx`).
    - Gli indirizzi ad-hoc **non entrano in rubrica** di proposito: qui si manda una mail, non si censisce una persona. Per salvarlo c'è «Nuovo contatto» sulla scheda — e l'empty state ora porta lì (`/contatto/[placeId]`) invece che alla Rubrica generica.
    - `place_id` dell'ad-hoc è il negozio di partenza **solo quando è uno solo**: serve a `registraContattoAvviato` per far diventare Lead il negozio a cui si è scritto. Con più negozi non è attribuibile e resta vuoto (quelle righe vengono già scartate dal `.filter(Boolean)`).
  - **🪟 «Scrivi in AI Mail»: la finestra di AI Mail aperta da Scout** (richiesta utente, con l'URL). `lib/aimail.ts` → `urlScriviAiMail()` compone `https://deluxy-mail.vercel.app/scrivi?a=…&oggetto=…&corpo=…&app=Deluxy Scout&rif=…` (parametri verificati sul codice di `deluxy-mail/src/app/scrivi/page.tsx`, non dedotti). Due punti d'ingresso: terza voce nel modale della mail, e «Apri in AI Mail» nel passo di revisione.
    - **Non sostituisce l'invio di Scout, ed è bene sapere cosa si perde**: niente invio a più negozi, **niente variabili** (di là non c'è un contatto a cui agganciarle — quindi si applicano PRIMA, qui), **niente formattazione** (AI Mail appiattisce l'HTML che arriva dall'URL: è una sua scelta esplicita, «chi manda formattato usa l'API») e **niente traccia in `contatti_avviati`**, quindi il negozio non diventa Lead da solo. Per questo è la terza scelta, non la prima.
    - Nel passo di revisione compare **solo con un destinatario**: con più contatti il testo uscirebbe personalizzato per il primo e mandato a tutti. Nel modale è spento se del negozio non abbiamo nessun indirizzo.
    - Il corpo è tagliato a **6000 caratteri**: oltre, l'URL non arriva a destinazione (AI Mail taglia comunque a 8000).
  - **🧮 NUOVA SEZIONE: Preventivi fornitori** (richiesta utente, primo giro). Un cliente chiede un lavoro fuori standard, si chiede il prezzo a due o tre fornitori, si confrontano e si sceglie. Prima quei numeri vivevano su WhatsApp: dopo una settimana nessuno sapeva più chi avesse offerto cosa e la richiesta si rifaceva da capo. Voce di menu in **Vendita, fra Trattative e Ordini** — è lì che serve: per fare un prezzo al cliente bisogna prima sapere quanto costa.
    - **Due tabelle** (migrazione **0055**): `lavori` (cosa ci hanno chiesto, cliente e trattativa facoltativi, linea, «serve entro», stato aperto/chiuso/annullato) e `preventivi` (fornitore, importo, tempi, note, allegato, stato richiesto/ricevuto/scelto/scartato). **Il lavoro è uno, i preventivi sono tanti**: tenerli in una riga sola vorrebbe dire non poterli confrontare, cioè togliere la ragione per cui si chiedono.
    - **`importo` NULL è uno stato, non un buco**: «gliel'ho chiesto e non ha risposto». Il preventivo resta «in attesa» e **non entra nel confronto** — un fornitore senza prezzo non è il più economico. Lo stato si deduce dalla presenza del prezzo, non si chiede.
    - **Un solo scelto per lavoro**, imposto dal database (indice unico parziale `preventivi_scelto_uix`). ⚠️ `scegliPreventivo()` toglie prima lo scelto al precedente e poi lo mette al nuovo: al contrario l'indice rifiuterebbe la scrittura e l'utente vedrebbe un errore di database al posto di un cambio di scelta.
    - **Il nome del fornitore resta scritto anche col collegamento** a `places`: un preventivo è un documento di un momento e deve restare leggibile se quel negozio viene rinominato o cancellato.
    - La scheda mostra **il più basso in evidenza e quanto costa in più ciascun altro**: è il numero che fa decidere, e farlo a mente su tre importi è il modo migliore per sbagliarlo. L'importo accetta la virgola decimale.
    - ⏳ **Non fatto, da decidere**: allegare il PDF del preventivo (la colonna `allegato_url` c'è, manca l'upload); il collegamento all'**ordine** quando il lavoro si vince; il margine (servirebbe anche il prezzo fatto al cliente); e i fornitori presi dal registro **Anagrafiche** invece che dai negozi di Scout.
  - **🔗 UNO STATO SOLO PER DUE APP + le due app si avvisano** (decisione utente del 29/07, dopo una ricerca sui CRM di mercato — HubSpot lifecycle, Salesforce Lead Status, framework di win-back).
    - **Vocabolario condiviso, 11 valori**: `selezionato · lead · prospect · in_contatto · in_attesa · in_trattativa · da_ricontattare · attivo · a_rischio · non_interessato · dismesso`, stesso ordine in `deluxy-scout/types/index.ts` e `deluxy-anagrafiche/src/lib/stati.ts`. Prima il registro non conosceva `selezionato` e `lead` (Scout glieli traduceva in `prospect`, perdendoli: `statoRegistroDaAffiliazione` ora è l'identità) e **`a_rischio` non ce l'aveva nessuno dei due**.
    - **`a_rischio`** viene dalla ricerca: fra «attivo» e «dismesso» non c'era niente, quindi un cliente usciva dal radar solo quando aveva già smesso. Come «perso» **non è un livello ma un segno** (`aRischio()` in `lib/livelli.ts`), badge giallo in lista e sulla scheda.
    - ⚠️ **TRAPPOLA CHIUSA: `prospect` voleva dire due cose opposte.** Nel registro è lo stato di *default* (azienda mai toccata), in Scout è il gradino più alto prima del cliente (trattativa aperta). La riga `if (stato_affiliazione === 'prospect') return 'prospect'` era rimasta da prima della ridefinizione della scala: **con la sincronizzazione accesa avrebbe fatto entrare ogni partner nuovo dritto fra i Prospect**. Tolta. E `in_contatto`/`in_attesa`/`da_ricontattare` ora valgono **Lead** — dicono solo che il contatto c'è stato; il dettaglio vive in `contatti_avviati`, `chiamate`, `visits`, ed è più preciso.
    - **Registro → Scout**: nuova Edge Function **`partner`** (`x-api-key: COMMERCIALE_API_KEY`, da deployare `--no-verify-jwt`) + `notificaCommerciale()` in `deluxy-anagrafiche/src/lib/commerciale.ts`, chiamata alla creazione di un partner. Il verso opposto (`sincronizzaPlaceRegistro` → `upsert_partner`) esisteva già. **Best-effort** (se l'altra app non risponde il salvataggio riesce lo stesso), **idempotente** (indice unico su `anagrafiche_id`; su un negozio esistente aggiorna stato e anagrafica ma **non tocca** stella, creatore, priorità e coordinate: è lavoro fatto in Scout), e **senza rimbalzo** (Scout non richiama il registro: due app che si avvisano a vicenda a ogni scrittura si rimbalzano all'infinito).
    - ⚠️ Il negozio creato dal registro nasce **`starred: true`** di proposito: `inLavorazione()` mostra solo i negozi scelti o stellati, e senza la stella entrerebbe nel database senza che lo veda nessuno — cioè il problema da cui siamo partiti.
    - ⏳ **Da fare**: ① deployare `partner` (è già nella lista del `.cmd`) e impostare `COMMERCIALE_API_KEY` **anche su Anagrafiche** (su Scout c'è già, la usano `lead` e `trattativa`); ② la notifica parte solo alla **creazione da UI**: il cambio di stato e le creazioni da API (`/api/v1/partners`) non avvisano ancora; ③ **il match dei dati attuali** — le 976 anagrafiche esistenti stanno sui vecchi stati e nessuna è collegata a un negozio di Scout: è il passo che l'utente ha esplicitamente rimandato.
  - **🔑 La chiave d'ingresso si genera dall'app** (richiesta utente, nata dal muro contro cui si è finiti: `COMMERCIALE_API_KEY` è un secret di Supabase e **un secret non si rilegge** — `secrets list` mostra un'impronta, non il valore. Per darla ad Anagrafiche bisognava averla annotata o rigenerarla, spegnendo in silenzio chi la usava già).
    - **Profilo → Impostazioni → «Chi può scrivere in Scout»** (solo admin): genera una chiave `dlxs_…`, la salva nella riga **`_ingresso`** di `chiavi_app` e la **mostra una volta sola**. Nessuna migrazione: la tabella c'era già (0045), e l'id con l'underscore non è un'app, quindi non compare nell'elenco delle app collegate.
    - ⚠️ Generata **nel client** con `crypto.getRandomValues`, e se l'ambiente non ce l'ha **fallisce dicendolo**: niente `Math.random()` per un segreto — una chiave debole è peggio di nessuna chiave, perché sembra una chiave. Il valore non viene mai riletto: la vista `chiavi_app_stato` espone solo `configurata`.
    - **Le Edge Function accettano ORA due chiavi** (`_shared/chiaveIn.ts`, usato da `partner`, `lead`, `trattativa`): prima quella di `chiavi_app`, poi il **secret storico**, che resta valido — una migrazione di segreti che spegne quello che già funziona non è una migrazione, è un guasto programmato. Confronto a tempo costante.
    - ⚠️ `lead` e `trattativa` sono state aggiunte a `FUNZIONI` **e a `SENZA_JWT`**: erano già pubblicate `--no-verify-jwt` e rideployarle senza la flag le romperebbe (col gateway JWT la chiamata muore prima del codice e l'`x-api-key` non serve a niente).
  - **📥 L'import dal registro porta dentro anche il LIVELLO** (decisione utente: attivo→Clienti, in_contatto+in_attesa→Lead, in_trattativa→Prospect, dismesso→Dormienti, più la sezione «A rischio»).
    - **Due bug nell'import che lo rendevano inutile**, trovati leggendolo: ① scriveva solo `anagrafiche_stato` e **non** `stato_affiliazione`, mentre `livelloDi()` leggeva solo il secondo → un `in_trattativa` del registro restava **Selezionato**; ② non metteva `starred`, e `inLavorazione()` mostra negli elenchi solo chi è stato scelto o stellato → **i partner importati finivano nel database senza comparire in nessuna lista**. Ora scrive entrambi, e `statoCommerciale(p)` in `lib/livelli.ts` legge indifferentemente dalle due colonne (vince `stato_affiliazione`, che è quella modificabile da qui).
    - **Nuova sezione «A rischio»** (`/lista?vista=a-rischio`, voce di menu dopo Clienti): è un **taglio dentro i Clienti**, non un livello — compra ancora, ed è il motivo per cui vale la pena occuparsene adesso. Migrazione **0056**: `a_rischio` nell'enum `stato_affiliazione_t` (senza, il registro manda uno stato che il database rifiuta). ⚠️ `alter type … add value` non sta in transazione: sta da sola, come 0023/0048/0051.
    - **«Dismesso» si legge «Dormiente»** nelle due app: cambiata l'**etichetta**, non il valore — quello è nel database di 976 anagrafiche e cambiarlo sarebbe una migrazione. Due nomi per la stessa cosa costringevano a tradurre a mente ogni volta.
    - ⏳ **L'import va lanciato**: `ANAGRAFICHE_API_KEY=… SUPABASE_PAT=… node scripts/import-anagrafiche.mjs` (c'è `--dry` per la prova a vuoto). Prima serve la migrazione 0056, se no le righe con `a_rischio` vengono rifiutate.
  - **🧭 31/07/2026 (versione finale) — DUE DIMENSIONI, allineate al registro.** **STATO** (6): `selezionato · lead · prospect · in_trattativa · attivo · dismesso`, dove `attivo` si legge **«Cliente»** e `dismesso` **«Dormiente»** (valori invariati, cambiano le etichette). **LIVELLO DEL RAPPORTO** (6, facoltativo): `in_contatto · in_attesa · da_ricontattare · attivo · a_rischio · non_interessato`, colonna `places.livello_rapporto` (migr. **0057**, che sposta anche i dati esistenti).
    - `a_rischio` e `non_interessato` sono **passati da stato a livello**: «a rischio» toglieva la parola *cliente* proprio a chi cliente lo è ancora, «non interessato» cancellava il fatto che restava un lead a cui avevamo parlato. La coppia che prima non si poteva scrivere — *Cliente · Da ricontattare* — adesso si scrive.
    - `ePerso()` e `aRischio()` guardano **il livello e anche il vecchio posto**, per i record non ancora migrati. Un livello impostato fa almeno un **Lead**.
    - ⚠️ `node --check` fallisce su `import-anagrafiche.mjs` **anche sulla versione committata**, nello stesso punto: è un limite suo, non una rottura. La verifica vera è il `--dry`.
  - **🧭 (storico) «in contatto / in attesa / da ricontattare» NON sono più stati** (decisione utente, presa prima nel registro e poi confermata qui). Non erano gradini del funnel ma il punto in cui è la conversazione, e stare nella stessa lista costringeva a scegliere fra due informazioni vere insieme — «è un prospect» **e** «sta aspettando una risposta». Con uno stato solo la seconda si perdeva.
    - Stati commerciali ora **8**: `selezionato · lead · prospect · in_trattativa · attivo · a_rischio · non_interessato · dismesso`. Nuova colonna `places.livello_contatto` (migrazione **0057**), con i tre valori vecchi.
    - ⚠️ **Il campo NON si chiama `livello`**, che nel registro è invece il suo nome: in Scout «livello» è già la scala del funnel (lib/livelli.ts). Chiamarli uguale avrebbe rifatto la trappola di `prospect` — stessa parola, due significati nelle due app, un giro a vuoto pagato ieri.
    - **Un momento impostato fa un Lead**, qualunque sia lo stato: senza quella riga in `livelloDi`, le 180 anagrafiche spostate su quella dimensione sarebbero retrocesse a Selezionato in silenzio.
    - La 0057 **sposta anche i dati esistenti** (stato → momento, stato = `lead`), su `stato_affiliazione` e su `anagrafiche_stato`. ⚠️ I tre valori restano dentro l'enum `stato_affiliazione_t`: da un enum Postgres **non si toglie** un valore. Nessuno li scriverà più.
    - Aggiornati di conseguenza: `import-anagrafiche.mjs` (legge `livello` dal registro, e se un registro non aggiornato manda ancora il vecchio stato lo interpreta come momento), la Edge Function `partner` (idem), il badge nelle liste e un selettore in Modifica — «premi di nuovo per toglierlo», perché *nessun momento* è una risposta valida.
  - **🔀 SEZIONE RICONCILIAZIONE + l'import non scarta più nessuno** (richiesta utente). Due schede in `/riconciliazione` (Canali, dopo Segnalazioni CS): **Senza posizione** e **Doppioni**. Stessa natura — un dato che c'è ma non basta, e una persona che deve guardarlo — azioni diverse, quindi schede separate.
    - **L'import prende anche chi non ha indirizzo**, a `0,0`: 196 anagrafiche su 1012 restavano fuori **in silenzio**, che è il modo peggiore di perdere dei dati. Lo 0,0 non è una posizione ed è riconoscibile come tale; la scheda «Senza posizione» le elenca e ci fa cercare l'indirizzo con AddressSearch — appena salvato, la riga sparisce. È la stessa scelta della Edge Function `partner`.
    - **Il verdetto sui doppioni sta nel database** (`coppie_duplicate`, migrazione **0058**), non nell'app: è un join di 1500 righe su sé stesse con trigram e distanza, e soprattutto così **un solo posto** decide cosa è un doppione — lo stesso calcolo era già stato scritto a mano in uno script per un'esportazione, e due copie divergono al primo ritocco.
    - ⚠️ **Decide la DISTANZA, non il nome**: «GUCCI» esiste a Milano, Roma, Firenze e Capri. E la distanza da sola non basta: due indirizzi diversi finiscono sullo stesso punto quando la geocodifica ripiega sulla via (caso reale LUCA FALONI, 0 m fra via Albricci e corso Matteotti). Un indirizzo discorde **fa scendere** il verdetto.
    - Unendo, il `google_place_id` passa al superstite **prima** dell'unione: `unisci_places` non lo copia, e senza quello la scoperta ricreerebbe il doppione appena chiuso. Le coppie scartate finiscono in `duplicati_ignorati`, la stessa tabella del bottone sulla scheda.
    - **Stato al 31/07**: 43 coppie già unite da riga di comando, ~294 ancora da guardare, 196 senza posizione.
  - **🔢 Le liste dicono «12 di 340»** (`ContoRighe` in `components/ui.tsx`): con un filtro attivo un elenco corto sembra un elenco che ha perso i dati. `/clienti` e `/trattative` il numero l'avevano già; Selezionati, Lead, Prospect, A rischio, Dormienti e Rubrica no. Componente unico: sei copie divergono al primo ritocco.
  - **✅ Interessi verificati**: confronto uno a uno registro↔Scout → **815 su 816 identici**. L'unico diverso è la regola di sicurezza che funziona (Scout ha «Consegne», il registro niente: non si cancella). ⚠️ Nel registro solo ~1 partner su 5 ha gli interessi compilati.
  - Verificato: `tsc --noEmit` pulito **su entrambe le app**, **35/35 jest**, `expo export --platform web` ok, `npm run build` di Anagrafiche ok. ⚠️ Le Edge Function sono **fuori dal `tsc`** (`tsconfig.json` esclude `supabase/functions`) e Deno qui non è installato: `_shared/aimail.ts` e `invio-email` sono verificati a occhio, non compilati. **Non verificato a schermo** (login) e **web non rideployato**: serve `VERCEL_TOKEN` per `bash scripts/deploy-web.sh`.

- **🌙 Sera del 01/08/2026 (commit `bd89b166`, `16a3627d`)** — non erano ancora in questo documento:
  - **`coppie_duplicate` andava in timeout** («canceling statement due to statement timeout»): `similarity(...) >= soglia` **non usa l'indice**, quindi Postgres calcolava la somiglianza su 1500 × 1500 coppie e la scheda Doppioni restava vuota senza dire perché. Migrazione **0059** la riscrive: `lower(a.nome) % lower(b.nome)` (indice GIN trigram `places_nome_trgm_ix`) + `st_dwithin(a.geo, b.geo, 2000)` (indice GIST `places_geo_gix`), e **taglio a 2 km** — oltre non sono doppioni: sparisce il verdetto «sedi diverse», che erano 125 righe di rumore. ⚠️ La soglia del `%` è quella di sistema (`pg_trgm.similarity_threshold` = 0.3), non quella passata: si rifiltra dopo con `similarity >= p_soglia`, ma ormai su poche coppie.
  - **L'import è diventato un workflow a orario**: `.github/workflows/deluxy-scout-import.yml` (cron `20 4 * * *` = 6:20 italiane d'estate, più `workflow_dispatch`) lancia `scripts/import-anagrafiche.mjs`. Segreti del repo richiesti: `SUPABASE_PAT` (o `SUPABASE_ACCESS_TOKEN`), `ANAGRAFICHE_API_KEY`, `GOOGLE_GEOCODING_KEY` — quest'ultima **costa**: geocodifica solo chi è a 0,0, costo calante. E **rigeocodifica chi è a 0,0**: prima «già importata» valeva «già a posto», quindi un indirizzo completato nel registro non sarebbe mai stato raccolto.
  - **L'import riprova quando la rete cade** (`conRiprova()` nello script): un `ECONNRESET` il 31/07 aveva ucciso un import intero dopo aver già scaricato 1012 anagrafiche. Si riprova **solo sugli errori di trasporto** (fetch failed / ECONNRESET / ETIMEDOUT / ENOTFOUND / socket hang up), 4 tentativi con attesa 1s·2s·4s; un 400 o 401 sono risposte e non si ripetono.
  - ⚠️ **VERIFICATO IL 21/08/2026: il cron dell'import NON è mai partito.** GitHub esegue gli `schedule` **solo dal branch di default** (`main`), e `main` **non ha nessun file in `.github/workflows/`**: il workflow d'import non risulta nemmeno registrato sul repo (API `…/actions/workflows/deluxy-scout-import.yml` → 404, zero run). `deluxy-scout-allinea.yml` invece funziona (4 run verdi, l'ultimo il 01/08 con la 0059) perché è a `push` e il push arriva su `scout-ui`. **Per far girare l'import da solo bisogna portare il file su `main`** (cherry-pick del solo workflow, o merge) **e** mettere i tre segreti nel repo. Finché non si fa, l'import si lancia a mano da `scoutwt/deluxy-scout`: `ANAGRAFICHE_API_KEY=… SUPABASE_PAT=… node scripts/import-anagrafiche.mjs` (`--dry` per la prova a vuoto).
  - **Stato di produzione al 21/08**: il web su deluxy-scout.vercel.app **è allineato** al commit `5baf3633` (il bundle servito contiene «Riconciliazione», «Senza posizione», «A rischio», «Preventivi fornitori», «Segnalazioni CS», `dlxs_`, `ContoRighe`, `coppie_duplicate`, `livello_rapporto`; i due commit successivi toccano solo script, migrazione e workflow, non l'app), e `/api/health` → `{"ok":true,"app":"deluxy-scout","database":true}`. ⚠️ Il PAT «deluxy-scout-setup» **scadeva il 10/08/2026**: se il secret `SUPABASE_PAT` del repo è quello, il prossimo run di `allinea` che tocca migrazioni/funzioni fallirà con 401 → rigenerare il PAT e aggiornare il secret (Settings → Secrets and variables → Actions).

## 3. ⏳ Cosa manca (richiede l'utente / suoi account)

0*. 🔓 **DA CORREGGERE NEL CODICE (21/08/2026) — non richiede te, ma il push da solo non basta.**
   - ① **Chiudere `hubspot-match`**: l'azione `sync_crm` risponde a chiunque, senza autenticazione (dettaglio e prova in cima al documento). Il controllo va spostato **sopra** lo smistamento delle azioni, in `supabase/functions/hubspot-match/index.ts:124`.
   - ② **Deployarla a mano** dopo la correzione — `hubspot-match` **non è** in `FUNZIONI` di `scripts/allinea-supabase.mjs`, quindi il workflow non la ripubblica: `SUPABASE_ACCESS_TOKEN=<pat> npx -y supabase@latest functions deploy hubspot-match --project-ref fdsziebgkljfsugqqbqd --use-api --no-verify-jwt` (la flag serve: la funzione fa l'auth nel proprio codice).
   - ③ **Decidere se allargare la lista `FUNZIONI`** alle 12 funzioni che oggi nessuno ripubblica, ricordando di aggiungere a `SENZA_JWT` quelle pubbliche al gateway (`mail`, `linee`, `hubspot-sync`, `hubspot-match`).
   - ④ **`ANTHROPIC_API_KEY`**: assente sul server (verificato). Finché manca, Assistente AI su Trattative e match AI dei contatti HubSpot restano spenti — è la stessa chiave per entrambi.

0⁰. 🤖 **LE MIGRAZIONI SI APPLICANO DA SOLE** (31/07/2026, richiesta utente «ok migrazioni che girano da sole»). `.github/workflows/deluxy-scout-allinea.yml` lancia `scripts/allinea-supabase.mjs` a ogni push su `scout-ui`/`main` che tocchi `deluxy-scout/supabase/migrations/**`, `…/functions/**` o lo script stesso. Applica le migrazioni **e** ripubblica le Edge Function.
   - **Prerequisito una volta sola**: il segreto **`SUPABASE_PAT`** in *Settings → Secrets and variables → Actions*. Senza, il workflow fallisce subito con un messaggio esplicito invece di provarci e mentire.
   - ⚠️ **Scrive in produzione**, quindi il filtro sui percorsi è stretto: il repo ospita quindici app e viene spinto in continuazione anche da altre sessioni. Un `concurrency` impedisce a due push ravvicinati di applicare SQL in parallelo sullo stesso database.
   - `APPLICA-MIGRAZIONI.cmd` resta e non fa danni (le migrazioni sono idempotenti), ma di norma non serve più.

0. ✅ **Migrazioni 0045–0051 APPLICATE** (28/07/2026, verificate sul database: tabelle `chiavi_app`, `contatti_avviati`, `bozze_visita`, `sequenze`, `sequenza_passi`, colonna `places.visita_pianificata`, valori `selezionato`/`lead` nell'enum). Si applicano con **`APPLICA-MIGRAZIONI.cmd`** (doppio clic, chiede il token) o `node scripts/allinea-supabase.mjs`.

0b. ✅ **TROVATA LA CAUSA VERA del deploy che falliva sempre (29/07/2026): `spawnSync npx.cmd EINVAL`.** Da Node 18.20.2 / 20.12.2 in poi (correzione della CVE-2024-27980) `spawn` su Windows **rifiuta di eseguire un `.cmd`** senza shell: `execFileSync('npx.cmd', …)` moriva prima ancora di far partire npx. Ecco perché fallivano **tutte e quattro** le funzioni, sempre, con qualunque token — e perché non usciva nessun messaggio dal CLI. Docker e il token, le due piste su cui si è perso un giorno, **non c'entravano niente**: l'unico indizio era `e.message`, che lo script non stampava. Corretto passando da `execSync` con la riga costruita a mano (con `shell:true` + array Node 24 avvisa DEP0190: gli argomenti verrebbero concatenati senza escape). Verificato: con un token finto il CLI ora arriva davvero all'upload e si ferma solo sul 401.
   ✅ **TUTTO PUBBLICATO (29/07/2026, dopo la correzione)**. Verificato dall'esterno: `health` → **200** `{"ok":true,"app":"deluxy-scout","database":true}` (pubblica, come deve) e anche `deluxy-scout.vercel.app/api/health` restituisce quel JSON, quindi **il Hub vede lo stato del database**; `ordini`, `finance` e `anagrafiche` → **401 «Missing authorization header»**, cioè esistono e rifiutano chi non è loggato (prima erano 404 «function was not found»). Migrazioni **0045–0055 tutte applicate** (`lavori`, `preventivi`, `sequenza_passi.corpo`, `visits.motivi` rispondono).
   ✅ **Anche `invio-email` rideployata** (29/07, secondo giro): da qui le mail di Scout provano prima AI Mail, che lascia la copia in «Inviata». AI Mail è stata a sua volta ripubblicata dalla cartella verificata (`vercel deploy --prod` da `scoutwt/deluxy-mail`) perché i deploy da riga di comando **non portano con sé il commit** e non era dimostrabile cosa contenesse quello di prima. ⚠️ Dall'esterno `invio-email` risponde 401 sia da vecchia sia da nuova: la sua versione **non è verificabile senza una sessione**, la prova è la riga «deployata» dello script.
   ⏳ **Ultimo miglio: le due chiavi in cassaforte Hub.** Le funzioni ci sono ma restano inerti finché non trovano `ORDERS_API_KEY` (il **venduto per provincia**) e `PARTNER_API_KEY` (i **dormienti dal fatturato vero** invece che dal codice del registro). Si mettono dalla pagina /chiavi del Hub; da fuori non sono verificabili — lo dicono le schermate Province e Dormienti. Al primo lancio sono fallite tutte perché il CLI Supabase impacchetta con **Docker**, assente su questa macchina: lo script ora passa `--use-api` (bundling sui server Supabase). **«4 passo/i non riuscito/i» = le 4 funzioni**, e il motivo non si leggeva perché lo script stampava il solo **stderr** mentre il CLI scrive il fallimento su **stdout** (corretto il 29/07). Provato con un token finto: con `--use-api` il CLI arriva all'upload e si ferma solo sull'autorizzazione → **la strada è buona, manca un lancio con token valido**: `APPLICA-MIGRAZIONI.cmd` (doppio clic) e questa volta l'errore, se c'è, si legge per intero. Cosa resta spento senza di esse:
   - `ordini` → la colonna **venduto** in Province · Copertura resta vuota (serve anche `ORDERS_API_KEY` in cassaforte Hub);
   - `finance` → i **dormienti** vengono dallo stato del registro invece che dal fatturato vero (serve anche `PARTNER_API_KEY`);
   - `health` → il Hub non vede lo stato del database di Scout (`/api/health` risponde 404 via il rewrite).
1. **Utente auth per il login** — pronto lo script `scripts/create-user.mjs`: crea/aggiorna l'utente in Supabase Auth via Admin API, con email+password forniti **dall'utente** via env var (l'agente non vede la password); crea l'utente già confermato. Serve la `service_role` key. Comando in §5. In alternativa: Supabase → Authentication → Users → *Add user*.
2. **Restringere la chiave Google Maps** (sicurezza, da fare) — la chiave Android finisce nell'APK ed è estraibile: la protezione vera è restringerla in Google Cloud Console → Credenziali → *App Android*: nome pacchetto `it.deluxy.scout` + **SHA-1** del keystore (presa da dashboard Expo → Credentials → Android), e restrizione API a *Maps SDK for Android*. Verificare che *Maps SDK for Android* sia abilitata e che il progetto Google Cloud abbia **billing attivo**.
3. **Test end-to-end HubSpot** — con un utente loggato (punto 1), registrare una visita e verificare company+contatto+deal con le proprietà su HubSpot.
4. **iOS / distribuzione store** — l'APK Android preview è fatto; per iOS serve setup Apple, per gli store la submission.
5. ✅ **FATTO (14 lug 2026)** — geocoding "Dove vai?" + suggerimenti Google **attivo** (funzione `geocode` deployata, secret `GOOGLE_GEOCODING_KEY` impostato con la chiave `AIza…qTH8`, Geocoding+Places abilitate, testato ok). Storico passaggi (per riferimento / rotazione chiave):
   a. Google Cloud → abilitare **Geocoding API** *e* **Places API** + **billing** attivo; creare una **chiave dedicata** con restrizione API = *Geocoding API + Places API* (NON restrizione app Android, altrimenti le REST non passano).
   b. Deploy della Edge Function e secret:
      ```powershell
      $env:SUPABASE_ACCESS_TOKEN = "<PAT Supabase>"
      npx -y supabase@latest functions deploy geocode --project-ref fdsziebgkljfsugqqbqd
      npx -y supabase@latest secrets set GOOGLE_GEOCODING_KEY=<chiave> --project-ref fdsziebgkljfsugqqbqd
      ```
   Finché non è fatto, la barra "Dove vai?" mostra un errore leggibile; il resto della scheda Target funziona normalmente. Il client deriva l'URL da solo (`<supabaseUrl>/functions/v1/geocode`), nessuna env var nuova.

> **Git** (branch `deluxy-scout`):
> - `0c5e0a9` — versione web (Vercel) + giro navigabile + config Maps.
> - `<commit 14 lug>` — sezioni Rubrica/Trattative + barra "Dove vai?" (geocode) + Edge Function `geocode`. File nuovi: `app/(app)/rubrica.tsx`, `app/(app)/trattative.tsx`, `lib/geocode.ts`, `supabase/functions/geocode/index.ts`. Modificati: `lib/db.ts`, `lib/theme.ts`, `app/(app)/_layout.tsx`, `app/(app)/lista.tsx`.
> - `.env` resta gitignored.
> **⚠️ Concorrenza**: un'altra sessione Claude lavora sull'app fiorai nella **stessa working directory** `C:\Users\nicol\app` e fa `git checkout` tra `main` e `deluxy-scout` → i file di deluxy-scout appaiono/spariscono ("flickering") e le modifiche non committate rischiano di perdersi. Non lanciare due sessioni sulla stessa cartella; se serve, committare spesso o usare un git worktree separato.

## 4. 🔐 Housekeeping sicurezza
- `deluxy-scout/.env` è gitignored: **non committarlo mai**.
- **Revocare** il Personal Access Token Supabase "deluxy-scout-setup" (`sbp_...`, scade 10 ago 2026) quando il setup è concluso: Dashboard → Account → Access Tokens → Elimina. Serviva solo per migrazioni e deploy da terminale.
- Il token HubSpot vive come secret della Edge Function; se va rigenerato: HubSpot → Sviluppo → Chiavi → Chiavi di servizio → "Deluxy Scout" → Ruota, poi `supabase secrets set HUBSPOT_TOKEN=...`.

## 5. Come riprendere (comandi esatti, PowerShell)
Prependi sempre Node al PATH: `$env:Path = "$env:ProgramFiles\nodejs;$env:Path"`.

```powershell
# 0. posizionati
# ⚠️ È il WORKTREE la cartella buona (branch scout-ui). `C:\Users\nicol\app\deluxy-scout`
#    è una copia ferma alla migrazione 0028 e non ha nemmeno gli script nuovi.
Set-Location 'C:\Users\nicol\scoutwt\deluxy-scout'
$env:Path = "$env:ProgramFiles\nodejs;$env:Path"

# 1. dipendenze (se node_modules manca)
npm install

# 2. validazione rapida
npx tsc --noEmit
npx jest
npx expo export --platform android --output-dir dist-check   # poi cancella dist-check

# 3. avvio dev server (per Expo Go / dev build)
npx expo start
```

**Eseguire SQL sul DB** (serve `SUPABASE_PAT` = Personal Access Token Supabase, dashboard → Account → Access Tokens):
```powershell
$env:SUPABASE_PAT = "<pat>"
node scripts/mgmt-query.mjs -e "select count(*) from places;"
node scripts/mgmt-query.mjs supabase/migrations/000X_file.sql
```

**Deploy Edge Function** (serve `SUPABASE_ACCESS_TOKEN` = un PAT Supabase):
```powershell
$env:SUPABASE_ACCESS_TOKEN = "<pat>"
npx -y supabase@latest functions deploy hubspot-sync --project-ref fdsziebgkljfsugqqbqd
npx -y supabase@latest secrets set HUBSPOT_TOKEN=<token> --project-ref fdsziebgkljfsugqqbqd
```
(Docker non serve: la CLI bundla via API.)

**Importare lead** (serve la service_role key, solo da terminale):
```powershell
$env:SUPABASE_URL = "https://fdsziebgkljfsugqqbqd.supabase.co"
$env:SUPABASE_SERVICE_ROLE_KEY = "<service_role>"
node scripts/import-places.mjs supabase/seed/lead.example.csv
```

## 6. Trappole d'ambiente (importante)
- **Node non nel PATH di sessione** dopo l'install: usa il prefisso `$env:Path` sopra, o richiama `C:\Program Files\nodejs\node.exe` col percorso completo.
- **Browser dashboard Supabase**: Google Translate rompe React (crash `removeChild`) e un gestore appunti sovrascrive la clipboard. → migrazioni **via `scripts/mgmt-query.mjs`**, non via SQL Editor. Se devi comunque usare il browser, chiedi all'utente di disattivare la traduzione per quel sito.
- **Regione HubSpot = EU**: la dashboard è `app-eu1.hubspot.com`; l'API resta `api.hubapi.com` (il token instrada da solo).

## 7. Script disponibili (in `deluxy-scout/scripts/`)
- `mgmt-query.mjs` — esegue un file .sql o `-e "SQL"` via Supabase Management API (no password DB, no clipboard).
- `hubspot-setup-properties.mjs` — crea/verifica le 7 proprietà custom HubSpot (idempotente).
- `import-places.mjs` — importa un CSV di lead nella tabella `places`.
- `create-user.mjs` — crea/aggiorna l'utente di login in Supabase Auth (Admin API). Email+password via env var (`SCOUT_EMAIL`/`SCOUT_PASSWORD`), utente già confermato. Idempotente.
- `gen-icons.mjs` — rigenera icona/splash/adaptive/favicon da SVG (richiede `sharp`).

## 8. Creare l'utente di login (comando)
```powershell
Set-Location 'C:\Users\nicol\scoutwt\deluxy-scout'
$env:Path = "$env:ProgramFiles\nodejs;$env:Path"
$env:SUPABASE_URL = "https://fdsziebgkljfsugqqbqd.supabase.co"
$env:SUPABASE_SERVICE_ROLE_KEY = "<service_role key>"   # Supabase → Project Settings → API
$env:SCOUT_EMAIL = "nome@deluxy.it"
$env:SCOUT_PASSWORD = "<password scelta dall'utente, min 6>"
node scripts/create-user.mjs
```

## 9. Chiave Maps come variabile EAS (già fatto — riferimento)
```powershell
$env:EXPO_TOKEN = "<personal access token Expo>"
npx eas-cli env:create --scope project --name EXPO_PUBLIC_GOOGLE_MAPS_ANDROID_KEY --value "<key>" --visibility sensitive --environment preview --non-interactive
npx eas-cli build -p android --profile preview --non-interactive --no-wait
```
