import type { NextConfig } from "next";

// Vuoto per standard (STANDARD-DELUXY.md §2.4). Gli header di sicurezza non
// stanno qui ma nel middleware, perché devono essere diversi fra UI e API.
const nextConfig: NextConfig = {};

export default nextConfig;
