<section class="plain">
  <p class="eyebrow">404</p>
  <h1 class="display plain__title">Pagina <em>non trovata</em></h1>
  <p class="rte">La pagina che cerchi non esiste o è stata spostata.</p>
  <a href="{{ routes.root_url }}" class="btn btn--dark">Torna alla home {% render 'b2b-icon', name: 'arrow' %}</a>
</section>

{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
